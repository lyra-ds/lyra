---
title: Lyra DS documentation
description: A CSS-first, white-label component system for thoughtful product interfaces.
---

<div className="lw-hero">
  <h1 className="lw-hero__title">Components that scale from prototype to production</h1>
  <div className="lw-hero__sub">
    Lyra is a CSS-first, white-label design system — the whole look lives in <code>.lyra-*</code>
    classes with light and dark themes, so the same core works across frameworks. This site is built
    with the very components it documents.
  </div>
  <HeroActions />
</div>

## Installation

Install the React package and the styles, then import the stylesheet once at your app root.

```bash
npm i @lyra-ds/react @lyra-ds/styles
```

```tsx
// app entry (main.tsx / layout.tsx)
import '@lyra-ds/styles/styles.css';
```

## Your first component

```tsx
import { Button } from '@lyra-ds/react';

export function App() {
  return <Button>Hello Lyra</Button>;
}
```

## Dark theme

Add <code>data-theme="dark"</code> to your `<html>` element. Every semantic token responds
automatically — no per-component work.

```html
<html data-theme="dark"></html>
```
