---
title: AppSidebar
description: Uma barra lateral de navegação da aplicação, com links compostos ou grupos de dados com destinos reais.
---

# AppSidebar

AppSidebar mantém os destinos de uma aplicação à vista. Escolha a porta de `children` quando os links
precisarem preservar o comportamento do router; escolha `groups` para uma interface rápida de seleção
com links ou ações.

## Exemplos

<Example id="composition" title="Links compostos e trilho de ícones" layout="block">
  Passe filhos SidebarGroup quando a aplicação for dona do routing. Ao recolher, a barra vira um
  trilho de 64px e fornece tooltips nativos e nomes acessíveis a seus descendentes de link a partir
  do texto.
</Example>

<Example id="groups" title="Grupos convenientes" layout="block">
  `groups` usa links para itens com destino e botão para ações e informa seu `id` por `onSelect`.
  Seu `label` é uma string de propósito, para que o trilho sempre possa nomear o item e mostrar um
  tooltip nativo.
</Example>

## Quando usar

Use AppSidebar para destinos agrupados e persistentes no shell de uma aplicação.

Prefira outro componente quando:

- **Uma linha curta de rotas no nível do site é suficiente** — use Navbar.
- **Os destinos são visualizações pares na página atual** — use Tabs.
- **Você precisa de apenas uma seção de navegação** — use SidebarGroup.

## Largura e propriedade do Shell

A marcação CSS-only de AppSidebar usa `260px` por padrão; adicione `lyra-appsidebar--rail` para o
padrão de trilho de `64px`. Defina `--appsidebar-width` quando a aplicação precisar de outra largura.
Quando AppSidebar for filho direto da barra lateral de um Shell com scroll de conteúdo, AppSidebar é
dona desse trilho; `Shell.sidebarWidth` continua dimensionando conteúdo comum de barra lateral e
trilhos de Shell com scroll de página.

```html
<!-- O padrão é 260px. -->
<nav class="lyra-appsidebar" aria-label="Navegação do espaço de trabalho"></nav>

<!-- O padrão é 64px; esta propriedade explícita também é suportada. -->
<nav
  class="lyra-appsidebar lyra-appsidebar--rail"
  style="--appsidebar-width: 64px"
  aria-label="Navegação do espaço de trabalho"
></nav>

<!-- Uma substituição do consumidor para a barra lateral expandida. -->
<nav
  class="lyra-appsidebar"
  style="--appsidebar-width: 296px"
  aria-label="Navegação do espaço de trabalho"
></nav>
```

## Acessibilidade

- AppSidebar renderiza um `<nav>` nativo. Passe `aria-label` para ele quando a página tiver mais de
  um landmark de navegação.
- No modo `groups`, `href` gera um link nativo, `asChild` preserva o link do router e itens de ação
  continuam botões. O item ativo recebe `aria-current="page"`. O callback do item roda antes de
  `onSelect(id, item)`. Use `target="_blank"` para abrir um link nativo em outra aba.
- No modo de composição, os filhos permanecem seus elementos originais. Use âncoras reais para rotas;
  no modo trilho, descendentes de link recebem `title` e `aria-label` de um rótulo explícito, título
  ou de seu conteúdo textual.
- O controle de recolhimento é um botão nativo cujo nome acessível e tooltip vêm de `labels` ou do
  padrão de `locale` (`en` ou `pt-BR`). No modo
  trilho, rótulos de grupo, texto de item e badges ficam visualmente ocultos, enquanto os nomes dos
  controles e itens continuam disponíveis.
- Um WorkspaceSwitcher no espaço da marca vira um gatilho compacto de avatar no modo trilho. O nome
  do workspace continua disponível para tecnologia assistiva; o popover mantém largura utilizável e
  fica dentro de um viewport estreito.

### Destinos com React Router e Next.js

```tsx
// React Router mantém a navegação no cliente.
<AppSidebar groups={[{ items: [{ id: 'relatorios', label: 'Relatórios', active: true,
  asChild: <Link to="/relatorios" /> }] }]} locale="pt-BR" />

// Next.js usa o mesmo contrato.
<AppSidebar groups={[{ items: [{ id: 'cobranca', label: 'Cobrança',
  asChild: <Link href="/cobranca" /> }] }]} locale="pt-BR" />
```

Para um link nativo, use `href: '/relatorios'`. Para nova aba, acrescente
`target: '_blank'` e `rel: 'noopener noreferrer'`. `asChild` substitui o conteúdo do Link
pelo ícone e rótulo do item. `locale="pt-BR"` localiza os rótulos padrão do controle;
`labels` ainda permite personalizá-los.

## API e código

<StackTabs>
  <StackPanel stack="react">
    <StackApi stack="react" />
  </StackPanel>

  <StackPanel stack="alpine">
    <StackApi stack="alpine" />

A porta de links compostos é uma marcação de navegação comum. Fora do React, implemente o
recolhimento, os rótulos do trilho e a seleção. Renderize o controle de recolhimento como um botão
nativo com `aria-label`; injete um chevron `<svg>` e inverta-o entre os estados expandido e trilho.
Se deixar o botão vazio, a folha de estilos fornece um chevron de reserva para o controle nunca
aparecer como uma caixa em branco, mas um ícone injetado permite refletir o estado de recolhimento:

```html
<nav class="lyra-appsidebar" aria-label="Navegação do espaço de trabalho">
  <div class="lyra-appsidebar__brand"><strong>Acme</strong></div>
  <div class="lyra-appsidebar__groups">
    <div class="lyra-sbgroup">
      <div class="lyra-sbgroup__label">Espaço de trabalho</div>
      <div class="lyra-sbgroup__items">
        <a
          class="lyra-sbgroup__item lyra-sbgroup__item--active"
          href="/overview"
          aria-current="page"
        >
          <span class="lyra-sbgroup__item-label">Visão geral</span>
        </a>
      </div>
    </div>
  </div>
  <button class="lyra-appsidebar__toggle" type="button" aria-label="Recolher barra lateral">
    <svg
      aria-hidden="true"
      width="15"
      height="15"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      stroke-width="2"
      stroke-linecap="round"
      stroke-linejoin="round"
    >
      <!-- expandido: chevron aponta para dentro; troque por m9 18 6-6-6-6 no trilho -->
      <path d="m15 18-6-6 6-6" />
    </svg>
  </button>
</nav>
```

  </StackPanel>

  <StackPanel stack="blade">
    <StackApi stack="blade" />
  </StackPanel>
</StackTabs>
