import { Container } from '@lyra-ds/react';

export function ContainerBasic() {
  return (
    <Container max={640}>
      <p>A focused reading measure keeps long decisions easier to scan.</p>
    </Container>
  );
}
