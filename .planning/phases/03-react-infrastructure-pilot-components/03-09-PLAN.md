---
phase: 03-react-infrastructure-pilot-components
plan: 09
type: execute
wave: 5
depends_on: ["03-08"]
files_modified:
  - tools/smoke/smoke.mjs
  - tools/smoke/vite-app/package.json
  - tools/smoke/vite-app/vite.config.mjs
  - tools/smoke/vite-app/index.html
  - tools/smoke/vite-app/tsconfig.json
  - tools/smoke/vite-app/src/main.tsx
  - tools/smoke/next-app/package.json
  - tools/smoke/next-app/next.config.mjs
  - tools/smoke/next-app/tsconfig.json
  - tools/smoke/next-app/app/layout.tsx
  - tools/smoke/next-app/app/page.tsx
  - tools/smoke/next-app/app/dialog-demo.tsx
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements: [RCT-03, RCT-04]

must_haves:
  truths:
    - "node tools/smoke/smoke.mjs packs @lyra-ds/react (and @lyra-ds/styles for CSS), verifies the react tarball allowlist (dist/README/LICENSE present; src, configs, tests, node_modules absent), and installs the tarball with a real npm install inside an OS temp dir — never a bare tar-extract (the react tarball has a real lucide-react dependency, Pitfall 6)"
    - "The Vite fixture package.json exact-pins its COMPLETE dependency set — react@19.2.7, react-dom@19.2.7, @types/react@19.2.17, @types/react-dom@19.2.3, typescript@5.9.3, @vitejs/plugin-react@6.0.3, vite@8.1.5 — never relying on npm peer auto-install for reproducibility (review fix: a TSX entry without pinned react/@types is not a reproducible install)"
    - "The Vite fixture runs tsc --noEmit against the installed tarball (full TypeScript support through the exports map — RCT-04) and a real vite PRODUCTION build whose JAVASCRIPT assets contain Button, Input, and Icon emissions but do NOT contain the Dialog runtime marker — the lyra-dialog-overlay className literal that only dialog.js embeds — with the Dialog-exclusion scan scoped to *.js assets ONLY (round-2 review fix: the fixture imports the full @lyra-ds/styles entry, whose emitted CSS asset legitimately contains the .lyra-dialog-overlay rule, so an all-assets scan is deterministically unsatisfiable; the CSS asset is validated separately); the JS assets also do NOT contain the Icon dev-warning literal (NODE_ENV guard proven stripped by a real bundler — the 03-05 production-silence proof); and all emitted assets pass the SEMANTIC CDN scan (known CDN hosts + lucide-static + non-allowlisted URL literals fail; two prefixes are allowlisted: W3C namespace URIs under http://www.w3.org/ because Lucide bakes xmlns http://www.w3.org/2000/svg into every bundled icon — round-2 review fix: a protocol-blind scan rejects a namespace URI, not a CDN — and https://react.dev/ because the fixture bundles react-dom's production build, which deterministically embeds minified-error decoder URLs under https://react.dev/errors/, an inert documentation string, not a network dependency — revision fix: without this prefix the scan is deterministically unsatisfiable against any real React production bundle)"
    - "A CommonJS consumer proof executes require() in anger against the installed package — the root barrel and at least one subpath — asserting the exports are functions (review fix: metadata tools alone don't prove the CJS runtime path; Vite and Next are both ESM-oriented)"
    - "The Next.js fixture (App Router: server page importing Button/Input/Icon + a client-boundary component exercising Dialog) completes next build — proving package resolution under Next's bundler AND that the dist client directive correctly marks the components as client boundaries (review fix: because every dist entry carries 'use client', Next treats the imports as client components — the direct renderToString proof is the ssr vitest project from the pilot plans, NOT this fixture)"
    - "Both fixtures are committed with exact-pinned package.json files and run reproducibly local and in CI as a build-job step"
    - "smoke.mjs uses mkdtempSync-unique temp dirs and the die() cleanup contract — interrupted or parallel runs never collide or leak temp state (concurrency guarantee); on install/build failure the captured npm/build output is printed BEFORE cleanup so failures are diagnosable (review fix)"
  artifacts:
    - "tools/smoke/smoke.mjs"
    - "tools/smoke/vite-app/ (committed fixture: package.json, vite.config.mjs, index.html, tsconfig.json, src/main.tsx)"
    - "tools/smoke/next-app/ (committed fixture: package.json, next.config.mjs, tsconfig.json, app/{layout,page,dialog-demo}.tsx)"
  key_links:
    - "fixtures import via BOTH the root barrel and the ./button subpath — exercising the exports-map resolution paths consumers use (D-13); the CJS proof exercises the require-condition paths for the root and ./icon"
    - "smoke.mjs ↔ tools/pack-smoke/pack-smoke.mjs — same die()/allowlist skeleton, extended per D-27 (binaries resolve from the fixture's own pinned installs)"
    - "vite production build ↔ 03-05 Icon dev-warn contract — the warning literal's absence from the built output is the production-silence proof"
    - "CI build job ↔ node tools/smoke/smoke.mjs as the final Phase 3 step"
  prohibitions:
    - { statement: "Fixtures must never import packages/react/src or resolve via workspace linking — only the packed tarball, or the smoke test proves nothing (Pitfall 6)", status: flagged-unverified }
---

<objective>
Ship the committed scratch-app validation (D-27): `tools/smoke/smoke.mjs` extending the Phase 2 pack-smoke pattern to pack the react package, npm-install the tarball into committed Vite and Next.js fixtures in an OS temp dir, typecheck and build both, execute a real CommonJS require() consumer, and assert the end-to-end RCT-03/RCT-04 proofs (subpath + barrel resolution, tree-shaken isolation, client-boundary + package resolution under Next, production NODE_ENV stripping, no CDN references). Wire it as the final build-job CI step.

Purpose: the phase's success criterion 1 — pilots install from a packed tarball into scratch Vite and Next.js apps and render with full TypeScript support.
Output: smoke script + two committed fixtures, green locally and in CI.

**Flagged assumption (edge-coverage, RCT-04 / unclassified probe):** the spec-less probe engine could not classify one RCT-04 edge ("unclassified — review manually"). Interpretation adopted here: the un-probed edge is *consumer-environment variance* (Vite vs Next module resolution). It is addressed by running BOTH fixtures rather than one; if verification later surfaces a different intended edge, extend the smoke matrix rather than weakening it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-infrastructure-pilot-components/03-CONTEXT.md
@.planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md
@.planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- `tools/smoke/smoke.mjs` — pack → allowlist → temp-dir npm install → tsc --noEmit → vite build + CJS require() proof + next build → output assertions (failure output preserved before cleanup)
- `tools/smoke/vite-app/` fixture (fully pinned: react@19.2.7, react-dom@19.2.7, @types/react@19.2.17, @types/react-dom@19.2.3, typescript@5.9.3, @vitejs/plugin-react@6.0.3, vite@8.1.5)
- `tools/smoke/next-app/` fixture (pinned: next@16.2.10, react@19.2.7, react-dom@19.2.7, @types/react@19.2.17, @types/react-dom@19.2.3, typescript@5.9.3)
- Root package.json script `smoke`; CI build-job step `node tools/smoke/smoke.mjs`

<tasks>

<task type="auto">
  <name>Task 1: Build smoke.mjs core + the Vite fixture (tarball install, tsc, build, isolation assertions)</name>
  <files>tools/smoke/smoke.mjs, tools/smoke/vite-app/package.json, tools/smoke/vite-app/vite.config.mjs, tools/smoke/vite-app/index.html, tools/smoke/vite-app/tsconfig.json, tools/smoke/vite-app/src/main.tsx</files>
  <read_first>
    - tools/pack-smoke/pack-smoke.mjs (the skeleton to extend per D-27: die() temp cleanup, REQUIRED/FORBIDDEN tarball allowlist, dist-walk assertions; NOTE its createRequire root-vite-binary trick is NOT reused here — this fixture pins and installs its own vite so the plugin's peer resolution is self-contained, review fix)
    - tools/pack-smoke/fixture/ (committed-fixture shape: purpose comments, vite config as a plain object avoiding a vite import)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 6 — the 6-step flow and the KEY DIVERGENCE: real npm install instead of tar-extract; Pitfall 6)
  </read_first>
  <action>
    Create tools/smoke/vite-app as a committed fixture: package.json with the COMPLETE exact-pinned dependency set (review fix — never rely on npm peer auto-install): react@19.2.7, react-dom@19.2.7, @types/react@19.2.17, @types/react-dom@19.2.3, typescript@5.9.3, @vitejs/plugin-react@6.0.3, and vite@8.1.5 (review fix — the plugin resolves vite as a peer; the fixture must resolve its own pinned copy rather than depending on the root binary being reachable from the temp dir), and NO dependency on the lyra packages (the smoke script installs the tarballs at runtime); vite.config.mjs importing the react plugin per the pack-smoke fixture style; tsconfig with moduleResolution bundler + jsx react-jsx; src/main.tsx importing `{ Button }` from the `@lyra-ds/react/button` subpath, `{ Input }` from the `@lyra-ds/react` root barrel (two resolution paths, D-13), AND `{ Icon }` from the root barrel rendering a registry icon (Icon inclusion makes the production build carry icon.js — enabling the production-silence assertion below), rendering into #root, plus the `@lyra-ds/styles` CSS import.

    Create tools/smoke/smoke.mjs copying the pack-smoke skeleton (doc-contract header; die() that removes the mkdtempSync temp dir before exit; spawnSync run() helper — capture stdout/stderr of installs and builds and PRINT the captured output on failure BEFORE cleanup runs, review fix: failures must be diagnosable after the temp dir is gone). Flow per RESEARCH Pattern 6: (1) `pnpm pack` packages/react and packages/styles into a temp pack dir; (2) tarball allowlist on the react tgz — REQUIRED: package/dist/, package/README.md, package/LICENSE, package/package.json; FORBIDDEN: package/src/, package/tsup.config.ts, package/vitest.config.ts, package/eslint.config.js, test files, package/node_modules/; (3) copy vite-app into the temp dir (outside the workspace root) and `npm install --no-audit --no-fund` the two tarballs plus the pinned fixture deps — a REAL install so lucide-react materializes (Pitfall 6, never tar-extract); (4) run the fixture's `tsc --noEmit` via the fixture-installed typescript (types resolve through the exports map — the RCT-04 TypeScript proof); (5) vite production build via the fixture-installed vite binary; (6) walk the emitted assets with TWO scoped scans (round-2 review fix — the fixture imports the full @lyra-ds/styles entry, so the emitted CSS asset always and legitimately contains the .lyra-dialog-overlay rule; an all-assets Dialog-exclusion scan can never pass): (6a) JS-ONLY scan over *.js assets asserting a Button class emission marker present, an Input marker present, an Icon svg emission present, the Dialog runtime marker ABSENT — use the lyra-dialog-overlay className string literal, which appears in JS only inside dialog.js's JSX output (barrel import was tree-shaken — the isolation proof), and the Icon dev-warning literal ABSENT (the production NODE_ENV guard was stripped by a real bundler — the 03-05 production-silence proof; the literal to grep for is the UI-SPEC Copywriting unknown-name warning string); (6b) CSS validated separately: assert an emitted .css asset exists and contains the .lyra-btn rule AND the .lyra-dialog-overlay rule (the full stylesheet ships by design — presence here is correct, not a leak); then run the SEMANTIC CDN scan over ALL emitted assets using the SAME rule set as the 03-08 dist scan (round-2 review fix — a protocol-blind https?:// rule rejects Lucide's baked-in xmlns http://www.w3.org/2000/svg namespace attribute): fail on known CDN hosts (unpkg.com, cdn.jsdelivr.net, cdnjs.cloudflare.com, esm.sh, skypack.dev), on lucide-static, and on any other https?:// URL literal not matching the allowlist pre-populated with TWO prefixes: the W3C/XML namespace prefix http://www.w3.org/ AND the React error-decoder prefix https://react.dev/ (revision fix — the fixture intentionally bundles react-dom's production build, which bakes minified-error decoder URLs under https://react.dev/errors/ into its output; that literal is an inert documentation string for humans decoding minified errors, never a runtime network dependency, and rejecting it makes the scan deterministically unsatisfiable; comment this rationale at the allowlist constant) — keep this host list + allowlist byte-identical with 03-08's, factored as constants with a sync-pointer comment; and add TWO positive present-yet-passing assertions proving the allowlist is alive rather than the scan dead: (i) the xmlns namespace URI (under the http://www.w3.org/ prefix) is present in the bundled assets and does not fail the scan, and (ii) the https://react.dev/ literal is present in the *.js assets (react-dom production build guarantees it) and does not fail the scan; (7) CJS consumer proof (review fix — execute require() in anger): from the installed fixture dir run node with a small inline CommonJS script that does require of `@lyra-ds/react` and of `@lyra-ds/react/icon`, asserting typeof Button === 'function' and typeof Icon === 'function' — the dual-runtime claim gets a real runtime data point, not only metadata-tool approval.
  </action>
  <verify>
    <automated>node tools/smoke/smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
    - smoke.mjs exits 0 with the Vite path green from a clean checkout state
    - tools/smoke/vite-app/package.json pins react, react-dom, @types/react, @types/react-dom, typescript, @vitejs/plugin-react, and vite exactly (no ^ or ~ ranges, no missing peer)
    - Forced-failure proof: temporarily adding `package/src/` content to the FORBIDDEN check inputs (or asserting against a bogus REQUIRED entry) makes it exit 1 with a named diagnostic; reverted afterwards
    - The *.js assets contain `lyra-btn`, `lyra-input`, and an Icon emission, and do NOT contain the `lyra-dialog-overlay` literal (Dialog-exclusion scan scoped to JS assets only); the emitted .css asset exists and DOES contain both the .lyra-btn and .lyra-dialog-overlay rules (full stylesheet ships by design); the *.js assets do NOT contain the Icon unknown-name warning literal
    - The semantic CDN scan over all fixture assets finds zero CDN-host references, zero lucide-static occurrences, and zero non-allowlisted URL literals — while BOTH allowlisted literals are present yet passing: the bundled Lucide xmlns namespace URI (under the http://www.w3.org/ prefix) AND the react-dom error-decoder URL (under the https://react.dev/ prefix) are each asserted present in the emitted assets and do NOT fail the scan (proving the allowlist works, not that the scan is dead)
    - The scan's host list + allowlist constants are byte-identical to 03-08's dist-scan rule set (sync-pointer comment present in both)
    - The CJS proof step executes require() of the root barrel and the ./icon subpath and exits 0
    - The install step is `npm install` of the .tgz files (no tar -x into node_modules anywhere in smoke.mjs)
    - Temp dir is created with mkdtempSync and removed on both success and die() paths; on failure the captured npm/vite output is printed before removal
  </acceptance_criteria>
  <done>Vite scratch-app proof green: tarball installs reproducibly, types resolve, build tree-shakes to the imported pilots only, CJS require() works, production strips the dev warning.</done>
</task>

<task type="auto">
  <name>Task 2: Build the Next.js fixture and its smoke path (client-boundary + package-resolution proof)</name>
  <files>tools/smoke/next-app/package.json, tools/smoke/next-app/next.config.mjs, tools/smoke/next-app/tsconfig.json, tools/smoke/next-app/app/layout.tsx, tools/smoke/next-app/app/page.tsx, tools/smoke/next-app/app/dialog-demo.tsx, tools/smoke/smoke.mjs</files>
  <read_first>
    - tools/smoke/smoke.mjs (Task 1 state — extend, keep the die()/temp contract)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 6 step 6 — App Router server page + client boundary; Pitfall 3 — what a missing directive looks like; Assumption A1/A4)
  </read_first>
  <action>
    Create tools/smoke/next-app (minimal App Router shape, Claude's-discretion area resolved as): package.json pinning next@16.2.10, react@19.2.7, react-dom@19.2.7, @types/react@19.2.17, @types/react-dom@19.2.3, typescript@5.9.3 (complete pin set — same reproducibility rule as the Vite fixture) with a `build: next build` script; next.config.mjs minimal; tsconfig per next defaults with strict; app/layout.tsx importing the @lyra-ds/styles CSS; app/page.tsx importing Button, Input, and Icon from @lyra-ds/react and rendering dialog-demo; app/dialog-demo.tsx as a client-boundary component (its own client directive at top) with a trigger button and a Dialog.

    PROOF FRAMING (review fix — do not overclaim): because every dist entry carries the client directive banner, Next treats the imported Lyra components as CLIENT boundaries; `next build` succeeding therefore proves (a) package/exports-map resolution under Next's bundler, (b) the directive banner survives into dist and is honored (components with hooks imported from a server page compile without the client-hook-in-server-component error precisely because the directive marks them), and (c) the prerendered client-boundary output emits the expected markup. It is NOT the direct renderToString proof — that lives in the pilots' ssr vitest projects (D-26), which remain the authoritative SSR evidence.

    Extend smoke.mjs: copy next-app to the temp dir, npm install the tarballs + pinned fixture deps (registry access per A4), run `next build` via the fixture's installed binary (captured output printed on failure before cleanup); assert exit 0 and that the prerendered page output contains the Button class emission. Keep the single-temp-dir-per-fixture + cleanup discipline.
  </action>
  <verify>
    <automated>node tools/smoke/smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
    - smoke.mjs exits 0 running BOTH fixtures end to end
    - tools/smoke/next-app/package.json pins next, react, react-dom, @types/react, @types/react-dom, and typescript exactly (no ranges)
    - next build succeeds importing the Lyra components from a server page (no client-hook-in-server-component error — the dist client directive is present and honored; framed as client-boundary + resolution proof, not a renderToString substitute)
    - The next-app fixture never appears inside pnpm-workspace globs (it lives under tools/smoke/ as data, installed only in the temp copy; `pnpm install` at repo root stays clean)
    - Prerendered output contains `lyra-btn`
  </acceptance_criteria>
  <done>Next.js scratch-app proof green: SSR prerender + client boundary + directive survival, from the packed tarball.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the smoke step into CI and run the full phase gate suite</name>
  <files>.github/workflows/ci.yml, package.json</files>
  <read_first>
    - .github/workflows/ci.yml (post-03-08 state: build job step order — smoke goes last)
    - package.json (root scripts block — parity/pack-smoke naming convention to mirror)
  </read_first>
  <action>
    Add root script `"smoke": "node tools/smoke/smoke.mjs"` beside the existing pack-smoke script. Append the build-job step `- run: node tools/smoke/smoke.mjs` after the size-limit step with a `# Phase 3, RCT-04:` comment in the Phase 2 convention (frozen four jobs — a step, never a job). Then run the complete phase gate suite locally as the phase exit check: `pnpm run lint`, `pnpm run typecheck`, `pnpm run test`, `pnpm run parity`, `node tools/icon-registry/generate.mjs --check`, `pnpm run build`, `pnpm exec publint packages/react`, `pnpm --filter @lyra-ds/react exec attw --pack .` (with the 03-08 profile flag if documented), `pnpm --filter @lyra-ds/react exec size-limit`, `node tools/smoke/smoke.mjs`.
  </action>
  <verify>
    <automated>pnpm run lint && pnpm run typecheck && pnpm run test && pnpm run parity && node tools/icon-registry/generate.mjs --check && pnpm run build && pnpm exec publint packages/react && pnpm --filter @lyra-ds/react exec size-limit && node tools/smoke/smoke.mjs</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml still has exactly four jobs; the smoke step is the last build-job step with a Phase 3 comment
    - Every command in the verify chain exits 0 locally
    - actionlint passes on the final workflow
  </acceptance_criteria>
  <done>All Phase 3 machinery green end to end — batch conversion (Phase 4) is now safe repetition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → temp fixture installs | fixture installs pull registry deps at CI time |
| packed tarball → consumer projects | the tarball is exactly what future consumers receive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Elevation of privilege | malicious build-time scripts in fixture installs | medium | mitigate | fixtures committed with exact pins; `npm install --no-audit --no-fund` of a local tarball + pinned registry deps only; no postinstall in the approved dep set (RESEARCH legitimacy audit) |
| T-03-18 | Tampering | tarball leaking source/config/test files | medium | mitigate | REQUIRED/FORBIDDEN allowlist check before install; files allowlist in package.json (03-01) |
| T-03-16 | Tampering | CDN reference reaching consumers | high | mitigate | fixture-output CDN grep on top of the 03-08 dist grep — two independent detection points |
| T-03-SC | Tampering | npm installs | low | accept | fixture deps (next, @vitejs/plugin-react) were covered by the 03-01 grouped legitimacy checkpoint |
</threat_model>

<verification>
- `node tools/smoke/smoke.mjs` green on both fixtures; full phase gate chain green; ci.yml four-job contract intact
</verification>

<success_criteria>
- Success criterion 1 holds: pilot components install from a packed tarball into scratch Vite and Next.js apps and render with full TypeScript support, reproducibly, in CI (D-27)
</success_criteria>

<output>
Create `.planning/phases/03-react-infrastructure-pilot-components/03-09-SUMMARY.md` when done
</output>
