---
phase: 03-react-infrastructure-pilot-components
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/icon-registry/generate.mjs
  - tools/icon-registry/fixtures/self-test.jsx
  - packages/react/src/icon/icon-registry.ts
autonomous: true
requirements: [RCT-05]

must_haves:
  truths:
    - "tools/icon-registry/generate.mjs scans handoff/components/** + handoff/ui_kits/** (.jsx/.html/.tsx/.ts) with a FOUR-pass extraction — literal <Icon name= props, conditional branch literals inside name={…} expressions, manifest-declared indirect sources (icon array, resolver functions, icon: data properties), and an unresolved-dynamic detector — and emits exactly 68 unique names (review fix: the literal-only regex yields 50, not the 54 D-01 assumed; dynamic handoff uses push the true union to 68)"
    - "The 68-name canonical inventory supersedes D-01's '54' count while preserving D-01's intent verbatim: registry = exactly the icons the handoff uses, smallest possible surface, Phase 4 never hits a missing icon — 54 was a planning-time undercount from an incomplete scan"
    - "Any <Icon name={…}> occurrence that is neither branch-literal-resolvable nor covered by the SOURCE_MANIFEST / ALLOWED_PASSTHROUGH lists makes the generator fail listing file:line and the expression (review fix: dynamic uses can never be silently excluded)"
    - "The generated, committed icon-registry.ts statically imports 67 PascalCase lucide-react icons and vendors github via createLucideIcon with the ISC path data from lucide-static 0.469.0 — Github does not exist in lucide-react 1.x"
    - "IconName is exported as the literal union of the 68 registry keys per D-04"
    - "generate.mjs --check regenerates in-memory and byte-diffs against the committed file, exiting non-zero on drift — the D-02 CI drift guard; when lucide-react is installed it additionally validates every generated import name against the real lucide-react exports (review fix: the registry validates its own imports, not just a later typecheck)"
    - "Registry entries are emitted in sorted name order so regeneration is byte-stable (equal inputs always produce identical output — deterministic ordering)"
    - "Duplicate icon-name occurrences across handoff files and extraction passes dedupe to a single registry entry — registry keys are unique (equal names merge, never collide)"
    - "The generator asserts the exact 68-count boundary: 67 lucide-resolvable + 1 vendored; any count drift fails with a named error listing the delta"
    - "generate.mjs --check is read-only and re-runnable: parallel or interrupted runs never mutate the committed registry file"
    - "generate.mjs --self-test exercises the committed fixture covering literal, conditional, array-driven, resolver-driven, data-prop, and disallowed-dynamic cases — the extraction rules are themselves regression-tested (review fix)"
  artifacts:
    - "tools/icon-registry/generate.mjs"
    - "tools/icon-registry/fixtures/self-test.jsx (extraction-rule test fixture)"
    - "packages/react/src/icon/icon-registry.ts (GENERATED + COMMITTED)"
  key_links:
    - "handoff scan set ↔ committed registry — --check makes drift a CI failure (same contract as tools/parity)"
    - "icon-registry.ts named imports ↔ lucide-react 1.25.0 exports (validated by --check when installed; github vendored)"
    - "68-count ↔ every dependent assertion in 03-05 (68-icon render matrix) and 03-08 (Icon size-limit calibration text)"
  prohibitions:
    - { statement: "Never import the whole lucide-react namespace or build a dynamic name-to-module lookup — the registry must be exactly 68 static named imports (67 + vendored github), or the ~1,400-icon set leaks into consumer bundles", status: flagged-unverified }
    - { statement: "No CDN fetch of icon data anywhere — path data for the vendored icon is embedded in the generator source, not downloaded", status: flagged-unverified }
---

<objective>
Build the curated icon registry pipeline (D-01/D-02): a zero-dep Node generator in `tools/icon-registry/` that derives the complete 68-icon handoff inventory via a four-pass, dynamic-use-aware scan, emits the committed `icon-registry.ts` (67 static lucide-react imports + vendored `github` via createLucideIcon + the `IconName` literal union, D-04), and provides the `--check` drift mode CI will run — the same pattern as the Phase 2 parity script.

**Review incorporation (Codex, HIGH — inventory recount):** the previously specified literal-only `<Icon name="…">` regex yields exactly 50 unique names (verified twice at planning time), not 54, and the handoff additionally feeds Icon through dynamic sources the regex misses: the `names` array in `handoff/components/icons/icons.card.html` (adds e.g. calendar, heart, eye, filter, code, zap, sun, moon, book-open, upload), the `uploadIconFor`/`fmIconFor` resolver functions in `handoff/components/files/FileUpload.jsx` / `FileManager.jsx` (add image, file-text, file-spreadsheet, file-archive, film, file, music), conditional literal branches (`sun`/`moon` theme toggles in `handoff/ui_kits/website/sections.jsx` + `handoff/ui_kits/dashboard/shell.jsx`, `minus`/`check` in `sections-marketing.jsx`, `circle-alert` in `FileUpload.jsx`), and `icon: "…"` nav-data properties in `handoff/ui_kits/dashboard/shell.jsx` feeding `<Icon name={n.icon}>`. The recomputed canonical union is **exactly 68 names**. RESEARCH.md's "54 scoped" claim and Pitfall 2's counts are superseded by this plan; RESEARCH Pattern 3's vendored-github mechanics remain valid.

**Canonical 68-name inventory (planning-time approved; the generator must reproduce it):**
archive, arrow-left, arrow-right, arrow-up-right, bell, book-open, calendar, chart-line, check, chevron-down, chevron-right, chevrons-up-down, circle-alert, circle-check, circle-x, cloud-upload, code, copy, credit-card, download, ellipsis, external-link, eye, file, file-archive, file-spreadsheet, file-text, film, filter, folder, folder-open, github, hard-drive, heart, house, image, inbox, info, layout-dashboard, layout-grid, link, list, lock, log-out, mail, minus, moon, music, pencil, plus, rocket, scale, search, send, settings, shield, sparkles, star, sun, terminal, trash-2, triangle-alert, upload, user, user-plus, users, x, zap

Purpose: RCT-05's foundation — a statically-analyzable, tree-shakable, no-CDN icon surface that Phase 4 never has to extend ad hoc.
Output: generator script + self-test fixture + generated committed registry, drift-guarded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-infrastructure-pilot-components/03-CONTEXT.md
@.planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md
@.planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- `tools/icon-registry/generate.mjs` — four-pass scan + SOURCE_MANIFEST + emit + `--check` drift/import-validation gate + `--self-test` (zero-dep Node ESM)
- `tools/icon-registry/fixtures/self-test.jsx` — committed extraction-rule fixture
- `packages/react/src/icon/icon-registry.ts` — generated module: `iconRegistry` const (68 entries, `as const satisfies Record<string, LucideIcon>`), vendored `Github` via `createLucideIcon`, `export type IconName`

<tasks>

<task type="auto">
  <name>Task 1: Write the registry generator with four-pass dynamic-aware scan, vendored github, --check, and --self-test</name>
  <files>tools/icon-registry/generate.mjs, tools/icon-registry/fixtures/self-test.jsx</files>
  <read_first>
    - tools/parity/parity.mjs (the skeleton to copy wholesale per D-02 "same pattern": shebang + doc-contract header, zero-dep ESM imports, repo-relative paths, pinned EXPECTED count, error-accumulation + exit contract, recursive file walk `listCss` shape; also the MASK_DIVERGENCE named-allowlist discipline the SOURCE_MANIFEST mirrors)
    - handoff/components/icons/icons.card.html (the `const names = [...]` literal array — manifest source 1)
    - handoff/components/files/FileUpload.jsx and handoff/components/files/FileManager.jsx (the `uploadIconFor`/`fmIconFor` resolvers returning icon-name string literals — manifest sources 2 and 3; also the conditional `circle-alert` branch at the FileUpload call site)
    - handoff/ui_kits/dashboard/shell.jsx (the nav-data `icon:` string properties feeding Icon — manifest source 4; plus its sun/moon ternaries)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 3 — vendored github ISC path data from lucide-static@0.469.0, emit-shape rules; NOTE: its 54-count and "scoped scan returns 54" claims are superseded by this plan's 68-name inventory)
    - .planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md (section "tools/icon-registry/generate.mjs" — pattern excerpts with line references)
  </read_first>
  <action>
    Create tools/icon-registry/generate.mjs copying the parity.mjs skeleton (doc-contract header stating WHAT is proven, WHY, and the failure contract; `EXPECTED_ICONS = 68`; error accumulation; exit non-zero listing every failure). Behavior:

    1. Recursively walk `handoff/components/**` and `handoff/ui_kits/**` for `.jsx`, `.html`, `.tsx`, and `.ts` files (review fix: include TS extensions so later handoff additions are never silently excluded). Extract icon names in four passes, collecting into one Set (dedupe) then sorting alphabetically (byte-stable regeneration):
       - **Pass A — literal props:** occurrences of an Icon element opening tag with a double-quoted literal name attribute (scoped to the Icon element context — a bare name-attribute grep picks up meta-viewport and form-input name attributes; scope the match so neither `viewport` nor `plan` can enter the set).
       - **Pass B — conditional branch literals:** for every `name={expression}` occurrence, extract only the kebab-case string literals that immediately follow a `?` or `:` token inside the expression (ternary RESULT branches). This captures the theme-toggle and status ternaries while never capturing comparison operands (the FileUpload status-equality string must NOT be extracted — it follows `===`, not `?`/`:`).
       - **Pass C — SOURCE_MANIFEST indirection:** a named, commented constant (MASK_DIVERGENCE discipline) enumerating the four known indirect sources with file path + extraction rule: (1) the literal `names` array in handoff/components/icons/icons.card.html; (2) `return "…"` literals inside the `uploadIconFor` function in FileUpload.jsx; (3) `return "…"` literals inside the `fmIconFor` function in FileManager.jsx; (4) `icon: "…"` string data properties in handoff/ui_kits/dashboard/shell.jsx. If a manifest file is missing or an entry extracts zero names, fail with a named manifest-drift error.
       - **Pass D — unresolved-dynamic detector:** every `name={expression}` occurrence must be accounted for — it contributed Pass-B literals, OR its resolver/identifier is covered by a manifest entry, OR its file matches an ALLOWED_PASSTHROUGH list (documented pass-through prop/iterator sites whose feeding literals are captured elsewhere: the icons.card.html array iterator, the shell.jsx nav-data reference, the screens.jsx PlaceholderScreen prop pass-through, the FileUpload/FileManager resolver call sites). Anything else FAILS listing file:line and the expression text (review fix: unresolvable dynamic uses are hard errors with source locations, never silent exclusions).
    2. Assert the deduped set size is exactly EXPECTED_ICONS (68); fail with a named error listing the delta (added/missing names) otherwise.
    3. Emit `packages/react/src/icon/icon-registry.ts` per RESEARCH Pattern 3 emit rules. Assemble the emitted file content from an array of line strings (`lines.push(...)` joined with `\n`) rather than one column-0 template literal — the generator's own top-level `import` statements must be the only lines in generate.mjs matching `^import `, so the zero-dep verify can distinguish the script's imports from emitted template text. Emitted shape: header banner marking it GENERATED (source-of-truth = handoff scan; regenerate via `node tools/icon-registry/generate.mjs`); one named import statement of the 67 PascalCase icons that exist in lucide-react 1.x plus `createLucideIcon` and `type LucideIcon`; kebab-to-Pascal conversion by splitting on `-` and capitalizing segments (trash-2 becomes Trash2, chevrons-up-down becomes ChevronsUpDown, circle-alert becomes CircleAlert); special-case `github` (inventory member, removed from lucide in v1.0) as a vendored `const Github = createLucideIcon('github', [...])` block whose two-path ISC node data is embedded verbatim in the generator source, copied from RESEARCH Pattern 3 (origin lucide-static@0.469.0 — pixel-identical to the prototype's pinned CDN source); an `export const iconRegistry = { ... } as const satisfies Record<string, LucideIcon>` in sorted key order mapping kebab name to component; and `export type IconName = keyof typeof iconRegistry` (D-04).
    4. `--check` mode: regenerate in-memory only, byte-diff against the committed file, print a drift diagnostic naming the canonical source, exit 1 on drift or missing file, exit 0 otherwise — never writes in this mode (same contract as `pnpm run parity`). Additionally (review fix — validate imports against the installed package): if `lucide-react` is resolvable from packages/react (createRequire against packages/react/package.json), load its export names and assert every generated PascalCase import (except the vendored Github) exists as a real export — hard fail listing missing names; if lucide-react is not installed yet (wave-1 local run before 03-01 completes), print a skip notice and continue — CI runs --check after pnpm install, so the validation always executes there.
    5. `--self-test` mode (review fix — the extraction rules are themselves tested): run the four passes against the committed fixture directory only and assert the exact expected outcome. Create tools/icon-registry/fixtures/self-test.jsx containing one instance of each category: a literal Icon prop, a ternary with two branch literals plus a comparison-operand decoy string that must NOT be extracted, a literal names array matching the manifest-1 shape, a resolver-style function returning two icon literals, an `icon: "…"` data property, and one deliberately-unresolvable `name={someUnknownFn(x)}` use. Self-test passes iff the resolvable names are all extracted, the decoy is absent, and the unresolvable use is reported with its file:line.
  </action>
  <verify>
    <automated>node tools/icon-registry/generate.mjs --self-test && node -e "const s=require('fs').readFileSync('tools/icon-registry/generate.mjs','utf8'); const own=[...s.matchAll(/^import[^\n]*?from\s+['\"]([^'\"]+)['\"]/gm)].map(m=>m[1]); const zeroDep=own.length>0 && own.every(p=>p.startsWith('node:')); process.exit(s.includes('createLucideIcon') && s.includes('EXPECTED_ICONS') && s.includes('SOURCE_MANIFEST') && s.includes('ALLOWED_PASSTHROUGH') && s.includes('--check') && zeroDep ? 0 : 1)"</automated>
  </verify>
  <acceptance_criteria>
    - generate.mjs's own top-level import statements (column-0 `import … from`) reference only node: builtins (zero dependencies); emitted-file content is built via a joined lines array, so no emitted import text starts at column 0 of the generator source
    - The four passes derive exactly 68 unique names from the handoff, matching the canonical inventory in this plan's objective; none of `viewport`, `plan`, or `error` appears in the set
    - SOURCE_MANIFEST and ALLOWED_PASSTHROUGH are named, commented constants enumerating exact files (no wildcards); removing a manifest file or adding a new unlisted `name={…}` dynamic use makes the generator exit non-zero with file:line diagnostics
    - `node tools/icon-registry/generate.mjs --self-test` exits 0 against the committed fixture and exercises literal, conditional (with decoy), array, resolver, data-prop, and unresolvable-dynamic cases
    - The vendored block's icon node contains two path entries whose `d` attributes match RESEARCH Pattern 3 byte-for-byte
    - `node tools/icon-registry/generate.mjs --check` with a missing or modified committed file exits 1, and never writes to packages/react/src/icon/icon-registry.ts in check mode
  </acceptance_criteria>
  <done>Generator exists: zero-dep, four-pass dynamic-aware scan, manifest-guarded, vendored github, deterministic sorted output, --check drift + import-validation contract, self-tested extraction rules.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and commit icon-registry.ts; prove drift-guard and count invariants</name>
  <files>packages/react/src/icon/icon-registry.ts</files>
  <read_first>
    - tools/icon-registry/generate.mjs (Task 1 output — the tool being exercised)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 3 expected file shape — counts superseded to 68)
  </read_first>
  <action>
    Run `node tools/icon-registry/generate.mjs` to emit packages/react/src/icon/icon-registry.ts (create the src/icon/ directory if 03-01 has not landed yet — wave-1 plans run in parallel). Then run the --check mode and confirm exit 0. Spot-verify the generated file: 68 registry keys in sorted order matching the canonical inventory in the objective; dynamic-source-only names present (e.g. music, circle-alert, book-open, image, sun, moon — none of these appear as literal props anywhere); `github` maps to the vendored `Github` const (not a lucide-react import); every other value is a named import (67 total); `IconName` union exported; GENERATED banner present. Never hand-edit the generated file — any fix goes into the generator and is regenerated (per D-02 the file is generated + committed + CI-guarded). Note: full TypeScript/lucide-exports validation of this file is exercised once plan 03-01's toolchain and lucide-react install land (same wave) and on every CI --check run thereafter — this plan's own gate is the generator contract.
  </action>
  <verify>
    <automated>node tools/icon-registry/generate.mjs --check && node -e "const s=require('fs').readFileSync('packages/react/src/icon/icon-registry.ts','utf8'); const keys=[...s.matchAll(/^\s*'([a-z0-9-]+)':/gm)].map(m=>m[1]); const sorted=[...keys].sort(); const dyn=['music','circle-alert','book-open','image','sun','moon']; const ok = keys.length===68 && JSON.stringify(keys)===JSON.stringify(sorted) && dyn.every(k=>keys.includes(k)) && s.includes('createLucideIcon(') && s.includes('IconName') && !keys.includes('viewport') && !keys.includes('plan') && !keys.includes('error'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/icon-registry/generate.mjs --check` exits 0 against the committed file
    - The registry object has exactly 68 keys, alphabetically sorted, deduped (no repeated key), equal as a set to the canonical inventory listed in this plan's objective
    - Dynamic-source-only names (music, circle-alert, book-open, image, sun, moon at minimum) are present — proof the scan is not literal-only
    - Key `github` resolves to the vendored const; `Github` is NOT in the lucide-react import list; the import list has exactly 67 icon names
    - `export type IconName = keyof typeof iconRegistry` present
    - Negative drift proof: temporarily appending a character to the committed file makes --check exit 1; restored afterwards
  </acceptance_criteria>
  <done>Registry committed, 68/68, drift guard demonstrably red on modification and green on match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handoff/ scan → generated source file | generator output becomes shipped library code |
| lucide-react package → registry imports | third-party icon components enter the bundle surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering | generated registry drift (hand-edits, stale scan, new unlisted dynamic uses) | medium | mitigate | --check byte-diff + lucide-exports validation in CI (wired plan 03-08); Pass-D hard failure on unaccounted dynamic uses; GENERATED banner; exact-count assertion |
| T-03-05 | Tampering | vendored SVG path data corruption | low | mitigate | path data pinned as a literal in the generator, compared against RESEARCH Pattern 3 in acceptance criteria; regeneration deterministic |
| T-03-SC | Tampering | npm/pip/cargo installs | low | accept | this plan installs nothing; lucide-react install is gated by plan 03-01's legitimacy checkpoint |
</threat_model>

<verification>
- `node tools/icon-registry/generate.mjs --self-test` green; `node tools/icon-registry/generate.mjs` then `--check` exits 0; registry has 68 sorted unique keys matching the canonical inventory; vendored github present
</verification>

<success_criteria>
- The D-01/D-02 curated registry exists as generated + committed + drift-guarded code covering the COMPLETE handoff icon usage (literal + dynamic), ready for the Icon pilot (plan 03-05) and the CI step (plan 03-08)
</success_criteria>

<output>
Create `.planning/phases/03-react-infrastructure-pilot-components/03-03-SUMMARY.md` when done
</output>
