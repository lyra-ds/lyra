---
phase: 03-react-infrastructure-pilot-components
plan: 07
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-04"]
files_modified:
  - packages/react/src/dialog/dialog.tsx
  - packages/react/src/dialog/index.ts
  - packages/react/src/dialog/dialog.browser.test.tsx
  - packages/react/src/dialog/dialog.ssr.test.ts
autonomous: true
requirements: [RCT-04]

must_haves:
  truths:
    - "Dialog preserves the prototype DOM (.lyra-dialog-overlay > .lyra-dialog with __header > __title + __close, __body, __footer?) using div + role=dialog + aria-modal=true (D-15)"
    - "Dialog renders through Portal with an optional container prop defaulting to document.body, SSR-guarded (D-21) — renderToString with open=true yields no crash and no dialog markup server-side"
    - "All three close paths work — Esc, overlay click (target identity), and the x button — each individually disableable via closeOnEsc / closeOnOverlayClick (default true) (D-16)"
    - "Focus is trapped while open (Tab and Shift+Tab wrap inside the panel), initial focus lands on the first focusable element with fallback to the panel itself via tabIndex -1, and focus RESTORES to the opener element on every close path (D-20 + APG)"
    - "The title is wired via aria-labelledby + useId (D-20)"
    - "Close button renders className lyra-dialog__close with aria-label Close (EN canonical) and an inline 14px X svg — no borrowed Tag class, no inline visual styles (D-19)"
    - "On close the component stays mounted while .lyra-dialog--closing (and the overlay counterpart) play, unmounting after animation end or the presence timeout (D-17/D-18 via usePresence)"
    - "Body scroll is locked while open with scrollbar-width compensation, restored on close (D-22)"
    - "open=false renders null (after the exit transition); omitted footer omits .lyra-dialog__footer entirely — no empty chrome (UI-SPEC)"
    - "Focus-visible ring, trap, Esc, and restore are covered by real chromium keyboard events; axe passes light + dark (UI-SPEC cross-cutting focus/keyboard row)"
    - { statement: "Browser-mode test renders body content taller than the viewport; the panel stays within max-width 440px + overlay --space-6 padding and the page behind is scroll-locked", verification: backstop }
    - { statement: "Long-title fixture: the title wraps within the header while .lyra-dialog__close keeps its 28px hit area", verification: backstop }
  artifacts:
    - "packages/react/src/dialog/dialog.tsx"
    - "packages/react/src/dialog/index.ts"
    - "packages/react/src/dialog/dialog.browser.test.tsx"
    - "packages/react/src/dialog/dialog.ssr.test.ts"
  key_links:
    - "dialog.tsx ↔ internal/{portal,use-focus-trap,use-presence,use-scroll-lock} (03-04) — the overlay behavior stack"
    - "dialog.tsx closing state ↔ .lyra-dialog--closing / .lyra-dialog-overlay--closing CSS shipped in 03-02"
    - "focus trap operates on the panel ref INSIDE the portal subtree — never the React-tree ancestor (Pitfall 8)"
  prohibitions:
    - { statement: "Dialog must not import the Icon component (or any sibling component) — that would pull lucide-react into every Dialog consumer and break the one-import-pulls-nothing-else rule; the close glyph is a self-contained inline svg", status: flagged-unverified }
    - { statement: "No native dialog element — rejected by D-15 (fights the handoff overlay CSS, inconsistent focus behavior)", status: flagged-unverified }
---

<objective>
Convert the Dialog pilot — the overlay contract that locks portal + focus trap + presence + scroll lock for every Phase 4 overlay (Drawer, CommandPalette, Toast). Implements D-15 through D-22 on top of the 03-04 internals and the 03-02 additive CSS, with the full keyboard/focus browser suite that becomes the overlay test template (D-25).

Purpose: the hardest conversion case, proven once, repeated 36 times cheaply.
Output: Dialog pilot + the overlay keyboard/axe/ssr test template, all green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-infrastructure-pilot-components/03-CONTEXT.md
@.planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md
@.planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md
@.planning/phases/03-react-infrastructure-pilot-components/03-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan's share)

- `Dialog` component (`DialogProps`: open, onClose, title, footer, closeOnEsc?, closeOnOverlayClick?, container?, className, children, ...rest)
- `packages/react/src/dialog/index.ts` entry for the ./dialog subpath (D-13/D-28)
- The overlay test template instance: `dialog.browser.test.tsx` (keyboard + focus + presence + axe) and `dialog.ssr.test.ts` (portal-null server proof)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert the Dialog pilot integrating portal, trap, presence, and scroll lock</name>
  <files>packages/react/src/dialog/dialog.tsx, packages/react/src/dialog/index.ts</files>
  <read_first>
    - handoff/components/feedback/Dialog.jsx and handoff/components/feedback/Dialog.d.ts (canonical DOM to preserve; the borrowed close-button pattern being replaced per D-19; overlay-click target-identity check)
    - handoff/components/feedback/Dialog.prompt.md (usage fixtures)
    - packages/react/src/internal/portal.tsx, use-focus-trap.ts, use-presence.ts, use-scroll-lock.ts (03-04 contracts to consume)
    - packages/styles/components/feedback/feedback.css (03-02 state: .lyra-dialog__close, .lyra-dialog--closing, .lyra-dialog-overlay--closing class names to apply)
    - .planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md (section "dialog.tsx" — canonical DOM excerpt + conversion deltas D-15..D-22)
  </read_first>
  <behavior>
    - open=true mounts overlay + panel into container ?? document.body via Portal; open=false (steady state) renders nothing
    - Esc keydown calls onClose unless closeOnEsc=false; overlay click calls onClose only when the click target IS the overlay (not panel content) and closeOnOverlayClick is not false; the x button always calls onClose
    - While closing, the panel carries lyra-dialog--closing and the overlay its counterpart; the subtree unmounts after animationend or the presence timeout
    - On open, focus moves to the first focusable in the panel (or the panel itself via tabIndex -1); on every close path focus returns to the element that was active at open time
    - Body scroll locked while open with paddingRight compensation
  </behavior>
  <action>
    Convert per PATTERNS "dialog.tsx" deltas: keep the canonical DOM byte-identical for classes/structure; replace the early-return-on-closed with `usePresence` (D-17) driving mount + the closing classes from 03-02 (D-18); wrap output in `Portal` with the optional `container` prop defaulting to document.body (D-21); `role="dialog"` + `aria-modal="true"` on the panel div (D-15); `aria-labelledby` generated with `useId` and set on the h2 title (D-20); `useFocusTrap(panelRef, open)` where panelRef is the ref INTO the portal subtree (Pitfall 8); initial-focus effect targeting the first focusable else the panel with tabIndex -1 (D-20); capture `document.activeElement` when open flips true and restore it on every close path (Esc, overlay, x); `useScrollLock(open)` (D-22); close paths per D-16 with `closeOnEsc`/`closeOnOverlayClick` defaulting true and the overlay click using the target-identity check from the prototype. The x button: `type="button"`, `className="lyra-dialog__close"` (D-19 — never the borrowed Tag class, no inline width/height styles), `aria-label="Close"` (EN canonical per UI-SPEC), containing the prototype's self-contained 14px X svg path (stroke currentColor, strokeWidth 2.5) — deliberately NOT the Icon component so importing Dialog pulls zero sibling code. forwardRef to the panel div (D-08); cx for class assembly with consumer className last (D-09); rest-spread on the panel (D-10); named export only (D-12); JSDoc EN. Create dialog/index.ts re-exporting Dialog + DialogProps.
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react run typecheck && pnpm --filter @lyra-ds/react run lint</automated>
  </verify>
  <acceptance_criteria>
    - typecheck + lint green; dialog.tsx imports Portal/useFocusTrap/usePresence/useScrollLock from ../internal and nothing from ../icon or any sibling component folder
    - The close button carries className lyra-dialog__close and aria-label Close, with no style attribute
    - closeOnEsc and closeOnOverlayClick appear in DialogProps with default true semantics
    - dialog.tsx has no import ending in .css
  </acceptance_criteria>
  <done>Dialog implemented over the shared internals with all D-15..D-22 contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the overlay keyboard/focus/presence browser suite and the SSR portal proof</name>
  <files>packages/react/src/dialog/dialog.browser.test.tsx, packages/react/src/dialog/dialog.ssr.test.ts</files>
  <read_first>
    - packages/react/src/dialog/dialog.tsx (component under test)
    - packages/styles/tests/brand-theme.test.ts (Phase 2 harness patterns)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md ("SSR test shape" example; Pattern 5 axe pattern; Pitfalls 7 and 8 — the failure modes these tests exist to catch)
    - .planning/phases/03-react-infrastructure-pilot-components/03-UI-SPEC.md (Dialog interaction contract + UI Considerations Dialog rows)
  </read_first>
  <behavior>
    - Trap: with the dialog open over a page containing focusable background elements, Tab from the last panel focusable lands on the first, Shift+Tab from the first lands on the last; focus never reaches the background
    - Close paths: Esc closes; Esc with closeOnEsc=false does not; overlay click closes; overlay click with closeOnOverlayClick=false does not; click INSIDE the panel never closes; x click closes
    - Restore: after each of the three close paths, document.activeElement is the trigger button that opened the dialog
    - Presence: immediately after triggering close the panel is still in the DOM bearing the closing class; within 500ms it is unmounted (event or timeout fallback — Pitfall 7 wedge guard)
    - Scroll lock: while open, body overflow is hidden with paddingRight compensation; restored after close
    - Tall-body fixture: body content taller than the viewport keeps the panel within its 440px max-width and the page behind scroll-locked (backstop); long-title fixture wraps while the close button keeps its 28px box (backstop)
    - axe zero violations, light and dark; SSR: renderToString(<Dialog open ...>) returns a string containing no dialog overlay markup (portal mount-guard renders null server-side)
  </behavior>
  <action>
    Write dialog.browser.test.tsx as the OVERLAY template (D-25): styles CSS imported in-test; a trigger-button harness that opens the dialog so focus-restore is observable; real chromium keyboard events for Tab/Shift+Tab/Esc via the browser locators/userEvent; assertions per the behavior list, including getComputedStyle reads for the scroll lock and the 28px close-button box, and axe.run in both themes. Write dialog.ssr.test.ts per the RESEARCH SSR shape: renderToString with open=true, assert typeof string and absence of the overlay class in the server HTML (D-21/D-26 proof).
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react run test -- --project browser src/dialog && pnpm --filter @lyra-ds/react run test -- --project ssr src/dialog</automated>
  </verify>
  <acceptance_criteria>
    - Browser project green for src/dialog covering trap both directions, all three close paths plus both opt-out flags, focus restore on every path, presence closing-class-then-unmount, scroll lock with compensation, tall-body and long-title fixtures, axe light + dark
    - SSR project green: server HTML contains no overlay markup and no exception is thrown
  </acceptance_criteria>
  <done>The overlay test template is green — Drawer/CommandPalette/Toast in Phase 4 copy this suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer app → Dialog props/children | consumer nodes render inside a focus-trapped modal region |
| page ↔ modal | focus and scroll containment between background page and dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Elevation of privilege | focus escape / clickjack-adjacent interaction with the background | medium | mitigate | trap on the portal-subtree panel ref (Pitfall 8) + aria-modal; keyboard suite asserts no background focus |
| T-03-06 | Denial of service | stuck-mounted closing dialog blocking the page | medium | mitigate | presence timeout fallback (Pitfall 7); test asserts unmount within 500ms |
| T-03-13 | Tampering | children content injection | low | mitigate | children render as React nodes (auto-escaped); no raw-HTML API |
| T-03-SC | Tampering | npm installs | low | accept | no installs in this plan |
</threat_model>

<verification>
- Browser + ssr projects green for src/dialog with the full D-15..D-22 assertion matrix
</verification>

<success_criteria>
- The overlay conversion recipe (portal + trap + presence + scroll lock + close-path flags) is locked on green code with the reusable keyboard/axe/ssr template
</success_criteria>

<output>
Create `.planning/phases/03-react-infrastructure-pilot-components/03-07-SUMMARY.md` when done
</output>
