---
phase: 03-react-infrastructure-pilot-components
plan: 08
type: execute
wave: 4
depends_on: ["03-05", "03-06", "03-07"]
files_modified:
  - packages/react/src/index.ts
  - packages/react/package.json
  - packages/react/CONVENTIONS.md
  - CONTRIBUTING.md
  - .github/workflows/ci.yml
  - .changeset/phase-03-react-pilots.md
autonomous: true
requirements: [RCT-03, RCT-04, RCT-05]

must_haves:
  truths:
    - "pnpm --filter @lyra-ds/react run build emits dist/{index,button,input,dialog,icon}.{js,cjs,d.ts,d.cts} with NO chunk-*.js files (splitting: false from 03-01 — the one-file-per-component contract is exact) and every dist .js/.cjs file starts with the use client directive banner"
    - "publint and attw --pack both pass on packages/react (dual ESM+CJS with per-condition types — no FalseCJS/FalseESM masquerading)"
    - "size-limit passes with per-minimal-import budgets: a Button-only entry and an Icon entry that deliberately measures lucide-react (D-07); limits are calibrated at execution as measured size + ~20% headroom against the corrected 68-icon registry (review fix: never calibrate against the stale 54 count)"
    - "The Button budget is an order of magnitude below the Icon budget — proof Button pulls zero lucide code (RCT-03) and the Icon bundle carries only the 68-icon registry, not the ~1,400-icon set (RCT-05)"
    - "src/index.ts is the final barrel: named exports of Button, Input, Dialog, Icon + their prop types + IconName — no default exports (D-12)"
    - "packages/react/CONVENTIONS.md exists in EN with the D-24 conversion checklist, the locked-decision table, the D-25 component-type-to-pilot-test map, and the documented deviations/extensions list (.lyra-icon class hook, useId id generation, value-callback vs native-onChange wiring); CONTRIBUTING.md links to it (D-23)"
    - "A phase changeset exists (.changeset/phase-03-react-pilots.md, minor bump for the lockstep pair) — CONTRIBUTING.md requires a changeset for any change to a published package (review fix)"
    - "CI: lint job runs the react eslint step, test job runs the icon-registry --check step, build job runs publint(react) + attw + size-limit + the dist directive/CDN scans — all as steps inside the four frozen jobs, never new jobs"
    - "The dist CDN scan is host-list + protocol based: it fails on ANY https?:// URL literal and on lucide-static in dist .js/.cjs/.d.ts — not merely one hardcoded host (review fix: unpkg-only checking does not establish the no-CDN policy)"
    - { statement: "size-limit budgets calibrated with ~20% headroom absorb minifier/compression variance, and the Icon budget sits far below the full-icon-set cost so measurement rounding can never mask a full-barrel regression", verification: backstop }
  artifacts:
    - "packages/react/src/index.ts (final barrel)"
    - "packages/react/package.json (size-limit config block)"
    - "packages/react/CONVENTIONS.md"
    - ".github/workflows/ci.yml (Phase 3 steps in the four frozen jobs)"
  key_links:
    - "size-limit entries ↔ dist/button.js + dist/icon.js emitted by the 03-01 tsup entries — budgets die if entries rename"
    - "CONVENTIONS.md checklist ↔ the four pilots' real tests (D-25: the tests ARE the template; the doc maps component type → pilot suite to copy)"
    - "CI steps ↔ frozen job names lint/typecheck/test/build (required-check contexts — never new jobs)"
  prohibitions:
    - { statement: "lucide-react must never be added to the size-limit ignore list for the Icon or Button entries — ignoring it hollows out the entire RCT-05/RCT-03 proof (only react and react-dom are ignorable)", status: flagged-unverified }
    - { statement: "Budgets must never be inflated to make CI pass — a regression to full-barrel icon bundling overshoots the calibrated Icon budget by >10x and MUST fail", status: flagged-unverified }
---

<objective>
Assemble and gate the package: finalize the root barrel (D-12/D-13), prove the dual-format build green end-to-end (tsup → publint → attw), add the calibrated per-minimal-import size-limit budgets (D-07 — the RCT-03 isolation and RCT-05 no-1,400-icons proofs), write `packages/react/CONVENTIONS.md` (D-23/D-24/D-25) linking the pilots' tests as the Phase 4 template, and wire every Phase 3 quality gate as steps inside the four frozen CI jobs.

Purpose: this is where RCT-03/04/05 become machine-enforced instead of aspirational.
Output: green build + gates locally and in CI; the conventions note Phase 4 executes as a recipe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-infrastructure-pilot-components/03-CONTEXT.md
@.planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md
@.planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md
</context>

<assumption_delta_decision>
Signal: pluralization ("alongside") on the phase-goal sentence "internal/ utilities ... alongside a conventions note and test template".
Identity question: does the conventions note / test template become the primary noun (promote) or sit beside the utilities (add-alongside)?
Decision: **no-change** — the conventions note and test template are co-equal companion deliverables of the pilot conversion, not a promotion of any existing artifact; nothing in the codebase changes identity. No accepted debt.
</assumption_delta_decision>

## Artifacts this phase produces (this plan's share)

- Final `src/index.ts` barrel: `Button`, `ButtonProps`, `Input`, `InputProps`, `Dialog`, `DialogProps`, `Icon`, `IconProps`, `IconName`
- `size-limit` config block in packages/react/package.json (Button + Icon entries, calibrated limits)
- `packages/react/CONVENTIONS.md` — the Phase 4 conversion recipe + documented deviations/extensions list
- `.changeset/phase-03-react-pilots.md` — the phase changeset (minor, lockstep pair)
- CONTRIBUTING.md link to the conventions note
- CI steps: react eslint (lint job), icon-registry `--check` (test job), publint(react) + `attw --pack` + size-limit + dist directive/CDN scans (build job)

<tasks>

<task type="auto">
  <name>Task 1: Finalize the barrel and prove build + publint + attw green</name>
  <files>packages/react/src/index.ts</files>
  <read_first>
    - packages/react/src/index.ts (03-01 stub being finalized)
    - packages/react/src/{button,input,dialog,icon}/index.ts (the per-folder entries to re-export)
    - packages/react/package.json and packages/react/tsup.config.ts (exports map + entries the dist output must satisfy)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 2 notes on attw profiles; Assumption A3 — node16 profile fallback rule)
  </read_first>
  <action>
    Rewrite src/index.ts as the final barrel: named re-exports of Button/Input/Dialog/Icon plus their Props types and IconName from the four folder entries (D-12 named-only, D-13 root surface). Run `pnpm --filter @lyra-ds/react run build`; verify dist contains the 5 entries in all four formats (.js/.cjs/.d.ts/.d.cts), contains NO other .js/.cjs files (no chunk-*.js — splitting: false from 03-01; review fix: the one-file-per-component contract is asserted, not assumed), and that every dist .js and .cjs begins with the directive banner from the 03-01 tsup config. Run `pnpm exec publint packages/react` and `pnpm --filter @lyra-ds/react exec attw --pack .` with the DEFAULT profile first and capture its output; only if the node10 column ALONE flags, re-run with `--profile node16`, record the default-profile failure output in the SUMMARY, and document the rationale in a comment where the CI step lands (RESEARCH A3; review note: the fallback must never mask a default-profile problem silently — the rationale is written at the moment the fallback is adopted, with the captured evidence). Fix any other attw problem kind properly (they indicate a real exports-map bug, per Pitfall 4).
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react run build && pnpm exec publint packages/react && (pnpm --filter @lyra-ds/react exec attw --pack . || pnpm --filter @lyra-ds/react exec attw --pack . --profile node16)</automated>
  </verify>
  <acceptance_criteria>
    - `ls packages/react/dist` shows index/button/input/dialog/icon in .js, .cjs, .d.ts, and .d.cts forms and NO additional .js/.cjs files (zero chunk files)
    - `head -c 40` of every dist .js/.cjs file contains the client directive string
    - publint exits 0; attw exits 0 (default profile; node16 only with the default-profile output recorded in the SUMMARY and a written rationale)
    - src/index.ts contains no `default` keyword and exports IconName
  </acceptance_criteria>
  <done>Dual ESM+CJS build with valid types proven by the two packaging gates (RCT-04 build half).</done>
</task>

<task type="auto">
  <name>Task 2: Add and calibrate the size-limit per-minimal-import budgets</name>
  <files>packages/react/package.json</files>
  <read_first>
    - packages/react/package.json (current state — size-limit block being added)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 4 — the two load-bearing ignore rules; Pitfall 5; Assumption A2 expected magnitudes)
  </read_first>
  <action>
    Add the `size-limit` array to packages/react/package.json per RESEARCH Pattern 4: entry 1 named for the Button subpath import — `path: dist/button.js`, `import: "{ Button }"`, `ignore: ["react","react-dom"]`; entry 2 named for the Icon import (68-icon registry, lucide measured) — `path: dist/icon.js`, `import: "{ Icon }"`, same ignore list (lucide-react deliberately NOT ignored — that measurement IS the RCT-05 proof, and its absence from the Button measurement IS the RCT-03 proof). Calibrate per D-07 against the CORRECTED 68-icon registry from plan 03-03 (review fix: calibrating against the stale 54 inventory would bless an incomplete bundle): run `pnpm --filter @lyra-ds/react exec size-limit` once with generous provisional limits, read the measured sizes, then set each limit to measured × 1.2 rounded up to a clean value. Sanity-check against RESEARCH A2 magnitudes scaled for 68 icons (Button ≈ low single-digit kB; Icon ≈ roughly 12–40 kB min — still an order of magnitude under the ~500 kB+ full set); if Icon measures in the hundreds of kB, the registry is leaking the full set — stop and fix, never raise the budget.
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react exec size-limit</automated>
  </verify>
  <acceptance_criteria>
    - size-limit exits 0 with both entries under their limits
    - The Icon entry's ignore array does not contain lucide-react; neither does the Button entry's
    - The Button limit is at least 5x smaller than the Icon limit (isolation sanity)
    - Limits in package.json equal measured size + ~20% (recorded in the SUMMARY with the measured numbers)
  </acceptance_criteria>
  <done>D-07 budgets calibrated and green — the machine proof of RCT-03 isolation and RCT-05 registry containment.</done>
</task>

<task type="auto">
  <name>Task 3: Write CONVENTIONS.md, add the phase changeset, link from CONTRIBUTING, and wire all Phase 3 CI steps</name>
  <files>packages/react/CONVENTIONS.md, CONTRIBUTING.md, .github/workflows/ci.yml, .changeset/phase-03-react-pilots.md</files>
  <read_first>
    - .github/workflows/ci.yml (the four frozen jobs + Phase 2 step-comment convention + the future-hooks comment in the build job)
    - CONTRIBUTING.md (where the D-23 link lands; the Changesets section requiring one per published-package change; the pre-1.0 minor-bump convention)
    - .changeset/config.json (fixed lockstep pair + changelog-github config the changeset file must be valid against)
    - .planning/phases/03-react-infrastructure-pilot-components/03-CONTEXT.md (D-23/D-24/D-25 — format: actionable checklist + locked-decision table + pilot-test map)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md ("CI step additions" block — exact step commands; Open Question 3 — React 18 pre-publish note)
  </read_first>
  <action>
    Write packages/react/CONVENTIONS.md (EN, D-23) as the D-24 actionable conversion checklist: read the handoff .d.ts → forwardRef on the primary element (D-08) → cx + className/style merge with consumer values last (D-09) → extract known props, rest-spread onto the root the .d.ts extends (D-10) → Omit conflicting native attribute keys BEFORE redefining them (the Input size / Dialog title lesson — review fix, now a Phase 4 rule) → translate JSDoc to canonical EN → controlled state via useControllableState when the contract is value-shaped; when the contract is a native DOM event handler, compose (setter + forward the original event) — never pass an event handler as the hook's value callback (D-14 wiring decision from 03-06) → ids via the id prop else useId, never content-derived → overlays compose Portal/useFocusTrap/usePresence/useScrollLock with DOM-dependent effects INSIDE the portal subtree → test matrix (smoke light+dark, keyboard where interactive, axe both themes against document.body for portaled components, renderToString) → run the gates (typecheck, lint, size-limit, publint/attw). Add the locked-decision table (D-08..D-22 one-liners with rationale), the D-25 map (simple → copy src/button tests; form/controlled → src/input; overlay → src/dialog; registry → src/icon — no dead template file), a "documented deviations & extensions" section (review fix): the always-emitted .lyra-icon class hook (D-06 extension over the prototype, no shipped CSS rule), the useId id generation replacing label-derived slugs, and the native-onChange composition wiring. Also the icon-only-Button-requires-aria-label rule (UI-SPEC checker flag; consumer-enforced — no API guarantee), the no-raw-HTML-injection rule (never render consumer strings as HTML — security convention, threat T-03-08 breadcrumb), and a pre-publish note: run a React 18 test leg before 0.1.0 (RESEARCH Open Question 3, deferred to Phase 7). Add one line to CONTRIBUTING.md linking the note.

    Create .changeset/phase-03-react-pilots.md (review fix — CONTRIBUTING requires a changeset for any published-package change; @lyra-ds/react loses private:true in 03-01): frontmatter bumping `"@lyra-ds/react": minor` (the lockstep fixed group carries @lyra-ds/styles with it; pre-1.0 convention makes minor the ceiling), body summarizing the phase in EN (react package scaffold + Button/Input/Dialog/Icon pilots + curated icon registry + Dialog additive CSS). Confirm `pnpm changeset status` accepts it.

    Append CI steps per the RESEARCH "CI step additions" block, each with a `# Phase 3, <REQ-ID>:` comment in the Phase 2 convention, tools via pinned lockfile deps: lint job += `pnpm --filter @lyra-ds/react run lint` (RCT-03 CSS-ban rule); test job += `node tools/icon-registry/generate.mjs --check` (RCT-05 drift guard, D-02); build job (after the existing styles publint/pack-smoke steps) += `pnpm exec publint packages/react`, `pnpm --filter @lyra-ds/react exec attw --pack .` (with the profile flag only if Task 1 documented it), `pnpm --filter @lyra-ds/react exec size-limit`, plus two verification scans over packages/react/dist: one asserting every .js/.cjs first line carries the client directive (Pitfall 3 suspenders), and the no-CDN scan (review fix — one hardcoded host proves nothing): fail on ANY `https?://` URL literal in dist .js/.cjs/.d.ts files and on the string lucide-static, with an explicitly-commented allowlist mechanism for future legitimate literals (currently empty — no URL belongs in shipped code). Typecheck job needs no yml change (recursion; the line-58 comment already reserves it — update that comment to note Phase 3 delivered it).
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); const i=y.search(/^jobs:/m); const keys=[...y.slice(i).matchAll(/^  ([a-z-]+):/gm)].map(m=>m[1]); const ok=keys.length===4 && ['lint','typecheck','test','build'].every(j=>keys.includes(j)); process.exit(ok && y.includes('icon-registry') && y.includes('attw') && y.includes('size-limit') ? 0 : 1)" && grep -c "CONVENTIONS.md" CONTRIBUTING.md | awk '{exit ($1>=1)?0:1}' && test -f .changeset/phase-03-react-pilots.md && pnpm changeset status</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml still defines exactly the four job keys lint/typecheck/test/build (frozen contract — zero new jobs)
    - New steps present: react eslint in lint; icon-registry --check in test; react publint, attw, size-limit, the directive scan, and the protocol-based no-CDN scan in build — each with a Phase 3 REQ-ID comment
    - Local run of the no-CDN scan over packages/react/dist finds zero https?:// literals and zero lucide-static occurrences
    - CONVENTIONS.md contains the checklist (including the Omit rule, the composition wiring rule, the useId rule, and the portal-effect rule), the locked-decision table, the four-pilot test map, the deviations/extensions section, the icon-only aria-label rule, and the React 18 pre-publish note; CONTRIBUTING.md links to it
    - .changeset/phase-03-react-pilots.md exists with a minor bump and `pnpm changeset status` exits 0
    - `actionlint` passes on the modified workflow (the lint job runs it in CI)
  </acceptance_criteria>
  <done>All Phase 3 gates are CI steps inside the frozen jobs; the phase changeset exists; the Phase 4 recipe is written and linked (D-23/D-24/D-25).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dist artifacts → consumers | published output is the supply-chain surface consumers trust |
| CI config → release gates | gate steps are the enforcement point; weakening them silently is the threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-14 | Tampering | full icon set silently entering consumer bundles | high | mitigate | size-limit Icon entry measures lucide-react (never ignored); calibrated budget with >10x margin to the full-set cost; prohibition recorded |
| T-03-15 | Repudiation | gate steps removed/renamed breaking the required-check contract | medium | mitigate | steps live inside the four frozen job names; actionlint in CI; REQ-ID comments make removals visible in review |
| T-03-16 | Tampering | runtime CDN reference reintroduced | high | mitigate | dist-wide CDN-host grep as a build-job step; smoke fixtures re-assert (03-09) |
| T-03-SC | Tampering | npm installs | low | accept | no new installs; all tools were pinned in 03-01 behind the legitimacy checkpoint |
</threat_model>

<verification>
- Local: build + publint + attw + size-limit + both dist greps green; `pnpm run lint`, `pnpm run typecheck`, `pnpm run test`, `pnpm run parity`, and `node tools/icon-registry/generate.mjs --check` all green at repo root
- ci.yml four-job contract intact with the new steps
</verification>

<success_criteria>
- RCT-03 (isolation rules CI-enforced), RCT-04 (dual build validated by publint+attw), and RCT-05 (registry containment proven by size-limit + drift guard) are all machine-checked; success criterion 4's conventions note + test template map exist for Phase 4
</success_criteria>

<output>
Create `.planning/phases/03-react-infrastructure-pilot-components/03-08-SUMMARY.md` when done
</output>
