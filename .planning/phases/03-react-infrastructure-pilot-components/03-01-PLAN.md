---
phase: 03-react-infrastructure-pilot-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - packages/react/package.json
  - packages/react/tsconfig.json
  - packages/react/tsup.config.ts
  - packages/react/vitest.config.ts
  - packages/react/eslint.config.js
  - packages/react/LICENSE
  - packages/react/README.md
  - packages/react/src/index.ts
  - packages/react/src/button/index.ts
  - packages/react/src/input/index.ts
  - packages/react/src/dialog/index.ts
  - packages/react/src/icon/index.ts
autonomous: false
requirements: [RCT-03, RCT-04]
user_setup: []

must_haves:
  truths:
    - "packages/react has real package.json: name @lyra-ds/react, type module, sideEffects false, peerDependencies react >=18 <20 and react-dom >=18 <20, dependencies lucide-react only"
    - "Exports map exposes root '.' plus ./button, ./input, ./dialog, ./icon subpaths, each with per-condition types (.d.ts for import, .d.cts for require) and NO ./internal export"
    - "pnpm --filter @lyra-ds/react run typecheck exits 0 and pnpm --filter @lyra-ds/react run lint exits 0"
    - "ESLint no-restricted-imports rule rejects any *.css import inside packages/react/src (the RCT-03 CI-enforced rule)"
    - "vitest.config.ts defines two projects: browser (chromium via @vitest/browser-playwright, include src/**/*.browser.test.tsx) and ssr (environment node, include src/**/*.ssr.test.ts) per D-26"
    - "All new devDependencies and lucide-react are exact-pinned versions from 03-RESEARCH.md Standard Stack"
    - "pnpm --filter @lyra-ds/react run build exits 0 from this plan onward — documented placeholder folder entries (export {}) exist for button/input/dialog/icon so the 5-entry tsup build is never red across waves (review fix: no intentionally-broken CI window)"
    - "tsup config sets splitting: false — one dist file per entry, no shared chunk-*.js files, so the exports map ↔ dist filename contract is exact (review fix: chunk-splitting decision)"
  artifacts:
    - "packages/react/package.json"
    - "packages/react/tsconfig.json"
    - "packages/react/tsup.config.ts"
    - "packages/react/vitest.config.ts"
    - "packages/react/eslint.config.js"
    - "packages/react/src/index.ts"
  key_links:
    - "exports map subpath keys (./button etc.) ↔ tsup entry object keys (button: src/button/index.ts) — dist filenames must match exports paths"
    - "package typecheck/test/lint/build scripts ↔ root `pnpm -r --if-present run` recursion (frozen CI jobs pick them up with zero yml edits for typecheck)"
  prohibitions:
    - { statement: "No CSS imports anywhere under packages/react/src — appearance flows only through .lyra-* classes from @lyra-ds/styles", status: flagged-unverified }
    - { statement: "No Tailwind anywhere in packages (locked project constraint)", status: flagged-unverified }
    - { statement: "Named exports only across the package; default exports are forbidden (D-12)", status: flagged-unverified }
---

<objective>
Turn the placeholder `packages/react` into a real, configured dual-format library workspace: exact-pinned toolchain installs (with the mandatory package-legitimacy checkpoint), the attw-safe exports map with per-component subpaths (D-13), tsup dual ESM+CJS config with `'use client'` banner and `splitting: false`, the two-project Vitest harness (browser + ssr, D-26), and the ESLint flat config that enforces the zero-CSS-imports rule (RCT-03). Zero runtime deps beyond lucide-react (D-11); named exports only (D-12); folder-per-component layout scaffolded with documented placeholder entries (D-28) so the build is green from wave 1 — the pilots replace placeholder contents in wave 3 (review fix: the earlier "build stays red until wave 4" design guaranteed CI failure across waves and weakened per-plan verification/bisecting; rejected in favor of buildable placeholders).

Purpose: every later plan (internals, pilots, gates, smoke) builds on this scaffold; getting packaging metadata right at authoring time is the whole Phase 1-3 doctrine.
Output: installable workspace package skeleton that typechecks, lints, AND builds green from this plan onward.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-infrastructure-pilot-components/03-CONTEXT.md
@.planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md
@.planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- `packages/react/package.json` — real manifest: exports map (root + 4 subpaths + ./package.json), `sideEffects: false`, peers, scripts (`build`, `test`, `typecheck`, `lint`)
- `packages/react/tsconfig.json` — extends `tsconfig.base.json`, adds `jsx: react-jsx`
- `packages/react/tsup.config.ts` — 5 object-form entries, `format: ['esm','cjs']`, `dts: true`, `splitting: false`, banner `"use client";`
- `packages/react/vitest.config.ts` — projects `browser` + `ssr`
- `packages/react/eslint.config.js` — flat config: typescript-eslint + jsx-a11y + react-hooks + CSS-import ban
- `packages/react/LICENSE`, `packages/react/README.md` — mirroring @lyra-ds/styles
- `packages/react/src/index.ts` — barrel stub (finalized in plan 03-08)
- `packages/react/src/{button,input,dialog,icon}/index.ts` — documented placeholder entries (`export {};` + scaffold banner) that keep the 5-entry tsup build green until the wave-3 pilots replace them
- Root devDependency pins: tsup, @arethetypeswrong/cli, size-limit (+preset), eslint stack, vitest-browser-react, axe-core, react/react-dom + @types
- `packages/react` dependencies: `lucide-react` (exact pin); devDependency `@lyra-ds/styles: workspace:*`

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify legitimacy of the six too-new-flagged npm packages before install</name>
  <read_first>
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (section "Package Legitimacy Audit")
  </read_first>
  <what-built>Nothing yet — this is the pre-install supply-chain gate. The Phase 3 research legitimacy audit flagged six packages as [SUS] solely on the "too-new release" signal (all are canonical org repos with 440K–134M weekly downloads, no postinstall scripts): @arethetypeswrong/cli@0.18.5, lucide-react@1.25.0, next@16.2.10, @vitejs/plugin-react@6.0.3, eslint@10.7.0, typescript-eslint@8.64.0.</what-built>
  <action>Present the six [SUS]-flagged package name/version/repo triples from the RESEARCH.md Package Legitimacy Audit table to the human and BLOCK until each is confirmed against npmjs.com (grouped single checkpoint per the RESEARCH recommendation). Do not run any install command before approval. This checkpoint is never auto-approvable regardless of workflow mode.</action>
  <how-to-verify>
    1. For each of the six, open `https://www.npmjs.com/package/<name>` and confirm: (a) the repository link points at the canonical org shown in the RESEARCH audit table (e.g. lucide-react → github.com/lucide-icons/lucide, next → github.com/vercel/next.js), (b) weekly downloads are in the hundreds-of-thousands-or-more range, (c) the exact version pinned above exists.
    2. Confirm none shows a deprecation banner or a suspicious brand-new maintainer takeover notice.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all six package name/version/repo triples match the RESEARCH.md audit table
  </acceptance_criteria>
  <resume-signal>Type "approved" to unblock the install task, or name any package that failed verification</resume-signal>
</task>

<!-- Review deferral (Codex, LOW): machine-verifiable registry provenance/integrity recording alongside the
     human legitimacy check is DEFERRED to Phase 7 (release infrastructure), where npm trusted publishing /
     provenance attestation lands anyway. Phase 3 risk is covered by: exact pins + save-exact, the blocking
     human checkpoint above, pnpm --frozen-lockfile in CI, and the no-postinstall property of the approved set. -->


<task type="auto">
  <name>Task 2: Install pinned toolchain and rewrite packages/react/package.json with the attw-safe exports map</name>
  <files>package.json, pnpm-lock.yaml, packages/react/package.json, packages/react/tsconfig.json, packages/react/LICENSE, packages/react/README.md</files>
  <read_first>
    - packages/react/package.json (current 6-line placeholder being replaced)
    - packages/styles/package.json (repo conventions to mirror: license, type, files allowlist, publishConfig, script names)
    - package.json (root — existing devDependencies block style, exact pins, alphabetized)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 2 exports-map shape; Installation block)
    - tsconfig.base.json (base compiler options — extend, don't restate)
  </read_first>
  <action>
    Install root devDeps exactly per RESEARCH Installation block (save-exact is already on): `pnpm add -D -w @arethetypeswrong/cli@0.18.5 size-limit@12.1.0 @size-limit/preset-small-lib@12.1.0 eslint@10.7.0 typescript-eslint@8.64.0 eslint-plugin-jsx-a11y@6.10.2 eslint-plugin-react-hooks@7.1.1 vitest-browser-react@2.2.0 axe-core@4.12.1 react@19.2.7 react-dom@19.2.7 @types/react@19.2.17 @types/react-dom@19.2.3 tsup@8.5.1`. Then `pnpm --filter @lyra-ds/react add lucide-react@1.25.0` (runtime dep — the ONLY one, D-11; 1.25.0 per RESEARCH assumption A6, superseding the UI-SPEC 1.24.0 note) and `pnpm --filter @lyra-ds/react add -D @lyra-ds/styles@workspace:*` (browser tests import the CSS in-test, never in src).

    Rewrite packages/react/package.json following RESEARCH Pattern 2: name @lyra-ds/react, version 0.0.0, description in EN, `"type": "module"`, `"sideEffects": false` (safe here ONLY because zero CSS imports in src — the CI-enforced RCT-03 rule; never the styles-package CSS-glob form), `"files": ["dist", "README.md", "LICENSE"]`, top-level `main: ./dist/index.cjs` / `module: ./dist/index.js` / `types: ./dist/index.d.cts` (node10 fallbacks), exports map with entries for `.`, `./button`, `./input`, `./dialog`, `./icon`, `./package.json` — each subpath using per-condition blocks `import: { types: ./dist/<name>.d.ts, default: ./dist/<name>.js }` and `require: { types: ./dist/<name>.d.cts, default: ./dist/<name>.cjs }` with types FIRST in each block (publint rule; split types avoid attw FalseCJS/FalseESM). NO ./internal export — internal utilities stay private by construction (D-13). peerDependencies `react: >=18 <20`, `react-dom: >=18 <20`. Drop `"private": true` (mirror the @lyra-ds/styles precedent — pack/publint must behave as they will at publish; changesets lockstep `fixed` group already covers this package). Add `publishConfig.access: public`, `license: MIT`, scripts: `build: tsup`, `test: vitest run`, `typecheck: tsc --noEmit`, `lint: eslint .`.

    Create packages/react/tsconfig.json: `extends ../../tsconfig.base.json`, compilerOptions add `"jsx": "react-jsx"`, `include: ["src"]`. Copy packages/styles/LICENSE verbatim to packages/react/LICENSE. Create a minimal packages/react/README.md (EN: one-paragraph pitch, install snippet `npm i @lyra-ds/react @lyra-ds/styles`, note that all styling comes from @lyra-ds/styles).
  </action>
  <verify>
    <automated>node -e "const p=require('./packages/react/package.json'); const ok = p.sideEffects===false && p.exports['./button'].import.types==='./dist/button.d.ts' && p.exports['./button'].require.types==='./dist/button.d.cts' && !p.exports['./internal'] && p.peerDependencies.react==='>=18 <20' && p.dependencies['lucide-react'] && !p.private; process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm install` exits 0 and pnpm-lock.yaml contains lucide-react 1.25.0, tsup 8.5.1, @arethetypeswrong/cli 0.18.5 (exact, no ^ ranges in manifests)
    - packages/react/package.json has exactly one runtime dependency (lucide-react) and exports for ., ./button, ./input, ./dialog, ./icon, ./package.json — and no ./internal key
    - Each exports condition block lists `types` as its first key
    - `Object.keys(dependencies).length === 1` for packages/react (D-11)
  </acceptance_criteria>
  <done>packages/react is a real workspace package with pinned deps, attw-safe exports map, and no private flag; root toolchain installed.</done>
</task>

<task type="auto">
  <name>Task 3: Create tsup, vitest (browser+ssr projects), ESLint flat config with the CSS-import ban, and buildable placeholder entries</name>
  <files>packages/react/tsup.config.ts, packages/react/vitest.config.ts, packages/react/eslint.config.js, packages/react/src/index.ts, packages/react/src/button/index.ts, packages/react/src/input/index.ts, packages/react/src/dialog/index.ts, packages/react/src/icon/index.ts</files>
  <read_first>
    - packages/styles/vitest.config.ts (Phase 2 browser block to copy verbatim, incl. its comment discipline about chromium install ordering)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 1 tsup config; Pattern 5 vitest projects; Open Question 2 ESLint recommendation)
    - .planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md (sections on vitest.config.ts and tsup.config.ts)
  </read_first>
  <action>
    Create packages/react/tsup.config.ts per RESEARCH Pattern 1: object-form `entry` keyed `{ index: src/index.ts, button: src/button/index.ts, input: src/input/index.ts, dialog: src/dialog/index.ts, icon: src/icon/index.ts }` (keys control dist filenames matching the exports map, D-13), `format: ['esm','cjs']`, `dts: true`, `splitting: false` (review decision: the "one dist file per component" contract is exact — tsup's default ESM code-splitting would emit shared chunk-*.js files that break the exports-map ↔ filename mapping and the 03-08 dist assertions; the only shared internal code is tiny cx-scale utilities, so duplication cost is negligible), `target: 'es2022'`, `sourcemap: true`, `clean: true`, `treeshake: true`, `external: ['react','react-dom','react/jsx-runtime']` (lucide-react auto-externalized as a dependency — never bundle it), `banner: { js: '"use client";' }` (deterministic directive across all outputs; the dist grep + next-app fixture verify it later). Add a doc-contract header comment (repo convention).

    Create the four placeholder folder entries packages/react/src/{button,input,dialog,icon}/index.ts, each containing a short scaffold banner comment (stating it is a Phase 3 scaffold replaced by the pilot plan for that folder) and `export {};` — this keeps the 5-entry tsup build green from wave 1 (review fix: never ship an intentionally-red build across waves; root `pnpm -r run build` recursion stays green after every plan).

    Create packages/react/vitest.config.ts per D-26/RESEARCH Pattern 5: `test.projects` with (a) project `browser` — include `src/**/*.browser.test.tsx`, browser enabled, `provider: playwright()` from @vitest/browser-playwright, headless, instances `[{ browser: 'chromium' }]` (copy the styles config browser block + comment discipline); (b) project `ssr` — include `src/**/*.ssr.test.ts`, `environment: 'node'`. Package-level config keeps the Phase 2 styles harness and root `pnpm -r --if-present run test` recursion untouched (RESEARCH Open Question 1 recommendation).

    Create packages/react/eslint.config.js (flat config, ESLint 10): typescript-eslint recommended for src, eslint-plugin-react-hooks recommended, eslint-plugin-jsx-a11y flat/recommended, and the RCT-03 rule: `no-restricted-imports` with patterns `['*.css','**/*.css']` and a message explaining CSS lives only in @lyra-ds/styles and would be silently dropped under sideEffects:false. Ignore `dist/`.

    Create packages/react/src/index.ts as a documented stub barrel (a header comment explaining it is finalized in plan 03-08 as components land, named exports only per D-12; body `export {};` so tsc has an input). Folder-per-component layout (D-28) is created by the pilot plans.
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react run typecheck && pnpm --filter @lyra-ds/react run lint && pnpm --filter @lyra-ds/react run build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @lyra-ds/react run typecheck` exits 0
    - `pnpm --filter @lyra-ds/react run lint` exits 0
    - `pnpm --filter @lyra-ds/react run build` exits 0 against the placeholder entries, emitting all five entries in .js/.cjs/.d.ts/.d.cts with no chunk-*.js files in dist (splitting: false)
    - Lint smoke-proof of the CSS ban: creating a throwaway src file containing an import of './x.css' makes `pnpm --filter @lyra-ds/react run lint` exit non-zero; file removed afterwards
    - tsup.config.ts entry keys are exactly index, button, input, dialog, icon and it sets `splitting: false`
    - vitest.config.ts defines exactly two projects named browser and ssr with the include globs above
  </acceptance_criteria>
  <done>Toolchain configs exist; typecheck, lint, and build pass from wave 1; the CSS-import ban demonstrably fires.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → workspace | third-party packages enter the build/dev environment at install time |
| consumer app → library API | untrusted props cross into components (later plans) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (root devDeps + lucide-react) | high | mitigate | Task 1 blocking-human legitimacy checkpoint for the six [SUS] packages; exact pins + save-exact; `--frozen-lockfile` in CI; no postinstall scripts in the approved set |
| T-03-01 | Tampering | exports map / packaging metadata | medium | mitigate | publint + attw gates (plan 03-08) prove no masquerading paths; `files` allowlist limits tarball contents |
| T-03-02 | Info disclosure | source maps in published dist | low | accept | sourcemaps aid debugging of an MIT OSS library; no secrets exist in source |
</threat_model>

<verification>
- `pnpm install` clean; `pnpm --filter @lyra-ds/react run typecheck`, `run lint`, and `run build` green
- packages/react/package.json passes the node assertion in Task 2 verify
- No `.css` import can pass lint inside packages/react/src
</verification>

<success_criteria>
- packages/react is a configured, installable workspace package with the D-13 exports surface, D-26 test projects, and the RCT-03 lint rule active
- All installs are exact-pinned and legitimacy-checked
</success_criteria>

<output>
Create `.planning/phases/03-react-infrastructure-pilot-components/03-01-SUMMARY.md` when done
</output>
