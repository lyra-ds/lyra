---
phase: 03-react-infrastructure-pilot-components
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/react/src/internal/cx.ts
  - packages/react/src/internal/use-controllable-state.ts
  - packages/react/src/internal/portal.tsx
  - packages/react/src/internal/use-focus-trap.ts
  - packages/react/src/internal/use-presence.ts
  - packages/react/src/internal/use-scroll-lock.ts
  - packages/react/src/internal/internal.browser.test.tsx
autonomous: true
requirements: [RCT-04]

must_haves:
  truths:
    - "src/internal/ contains cx, useControllableState, Portal, useFocusTrap, usePresence, useScrollLock — the shared utilities success criterion 4 requires, each ~50 lines or less, APG-faithful, JSDoc in canonical English"
    - "cx filters falsy args and joins with a space — the handoff's own filter(Boolean).join idiom extracted once (D-11, no clsx)"
    - "useControllableState is controlled iff value !== undefined; setter mutates internal state only when uncontrolled and always calls onChange; dev-warns on controlled/uncontrolled switches (D-14); its onChange param is a VALUE callback (next: T) => void — components with native DOM event contracts (Input) never pass their native ChangeEventHandler into it, they compose (review fix: value-callback vs event-handler conflation)"
    - "Portal renders null until a useEffect sets mounted, then createPortal into container ?? document.body — no document/window access at module scope (D-21 SSR guard)"
    - "usePresence(open) returns { mounted, closing, onAnimationEnd } — the caller attaches onAnimationEnd to the animated element; it ignores bubbled child animations (event.target === event.currentTarget) and pairs with a ~250ms setTimeout fallback so reduced-motion or display-none environments never wedge the closing state (D-17, Pitfall 7; review fix: the old usePresence(open) signature had no way to observe the panel node)"
    - "useScrollLock compensates scrollbar width by ADDING the delta to the body's existing computed padding-right (never replacing it) and is reference-counted so nested overlays never unlock the body early; the last release restores prior inline styles (D-22; review fixes)"
    - "useFocusTrap queries focusables on each keydown from the panel ref (portal subtree), never a cached list or the React-tree ancestor (Pitfall 8), and filters out disabled, hidden, inert, aria-hidden, and invisible candidates so they can never become wrap targets (review fix)"
    - "Focused hook tests exist in internal.browser.test.tsx covering reopen-during-close, controlledness-switch warning, and nested scroll locks (review fix: utility contracts are unit-proven, not only pilot-integration-proven)"
    - "pnpm --filter @lyra-ds/react run typecheck and run lint both exit 0; the browser project passes for src/internal"
  artifacts:
    - "packages/react/src/internal/cx.ts"
    - "packages/react/src/internal/use-controllable-state.ts"
    - "packages/react/src/internal/portal.tsx"
    - "packages/react/src/internal/use-focus-trap.ts"
    - "packages/react/src/internal/use-presence.ts"
    - "packages/react/src/internal/use-scroll-lock.ts"
    - "packages/react/src/internal/internal.browser.test.tsx"
  key_links:
    - "usePresence closing flag ↔ .lyra-dialog--closing class shipped by plan 03-02 (Dialog applies it while closing)"
    - "internal/ modules ↔ NOT in the exports map (03-01) — private by construction; consumed only by pilots via relative imports"
    - "useControllableState ↔ Input pilot (03-06) locking the RCT-09 pattern for Phase 4"
  prohibitions:
    - { statement: "No new runtime dependencies for these utilities — no clsx, no focus-trap libs, no Radix/Ark/Zag; all hand-rolled per D-11/D-15 locked decisions", status: flagged-unverified }
    - { statement: "No document/window/localStorage access at module scope in any internal module — SSR safety is the machine-checked convention (ssr vitest project)", status: flagged-unverified }
---

<objective>
Implement the six shared `internal/` utilities (success criterion 4) that the pilots — and all 36 Phase 4 components — depend on: `cx` (D-11), `useControllableState` (D-14), SSR-guarded `Portal` (D-21), `useFocusTrap` (APG Dialog pattern, for D-15), `usePresence` (keep-mounted-while-closing, D-17), and `useScrollLock` with scrollbar compensation (D-22). These are the one-place-to-fix primitives; their behavior contracts are locked by RESEARCH Pattern 7 and verified end-to-end by the Dialog/Input pilot test suites in wave 3.

Purpose: one shared, private implementation per behavior concern before any pilot duplicates logic.
Output: six typed, linted, EN-JSDoc'd modules under packages/react/src/internal/.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-infrastructure-pilot-components/03-CONTEXT.md
@.planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md
@.planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- `cx(...args)` — class joiner (string | false | null | undefined varargs → string)
- `useControllableState({ value, defaultValue, onChange })` → `[value, setValue]` — `onChange: (next: T) => void` is a VALUE callback by contract
- `Portal` component — `{ container?: HTMLElement; children }`, SSR-safe mount guard
- `useFocusTrap(panelRef, active)` — Tab/Shift+Tab wrap on live, visibility-filtered focusable query
- `usePresence(open)` → `{ mounted, closing, onAnimationEnd }` — exit-animation state machine; caller attaches onAnimationEnd to the animated element
- `useScrollLock(active)` — reference-counted body lock + additive scrollbar-width compensation
- `internal.browser.test.tsx` — focused hook tests (browser project)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement cx, useControllableState, and SSR-guarded Portal</name>
  <files>packages/react/src/internal/cx.ts, packages/react/src/internal/use-controllable-state.ts, packages/react/src/internal/portal.tsx</files>
  <read_first>
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 7 rows: cx, useControllableState, Portal — the locked contracts)
    - handoff/components/buttons/Button.jsx (lines ~19-28: the filter(Boolean).join idiom cx extracts)
    - packages/react/tsconfig.json and packages/react/eslint.config.js (constraints the modules must satisfy)
  </read_first>
  <behavior>
    - cx('a', false, null, undefined, 'b') returns 'a b'; cx() returns ''
    - useControllableState: with value provided (controlled) the setter never changes rendered value by itself but always invokes onChange; with only defaultValue (uncontrolled) the setter updates state and invokes onChange; controlled-to-uncontrolled switch triggers a dev-only console warning
    - Portal: renders null before effects run (server pass), then portals children into container ?? document.body after mount
  </behavior>
  <action>
    Write the three modules per RESEARCH Pattern 7, JSDoc in canonical English:
    - cx.ts: `export function cx(...args: Array<string | false | null | undefined>): string` returning `args.filter(Boolean).join(' ')` (D-11 — the handoff's own idiom, extracted once; consumer className is always passed LAST by callers per D-09).
    - use-controllable-state.ts: `useControllableState<T>({ value, defaultValue, onChange })` returning `[T, (next: T) => void]`; controlled iff `value !== undefined` (D-14); internal `useState(defaultValue)`; setter updates internal state only when uncontrolled, always calls onChange; dev-only warning (guarded by `process.env.NODE_ENV !== 'production'`) when controlled-ness flips between renders (track with a ref). CONTRACT DECISION (review fix, addresses the value-callback vs native-event conflation): `onChange` here is a VALUE callback `(next: T) => void` — this is the internal RCT-09 pattern contract for Phase 4 components whose public API is value-shaped (Tabs, Accordion, …). Components whose public contract is a native DOM event handler (Input's `onChange: ChangeEventHandler`) must NOT pass that handler into this hook; they call the returned setter with the extracted value inside their own DOM event handler and forward the original event to the consumer separately (see plan 03-06 for the Input wiring). State this rule explicitly in the JSDoc.
    - portal.tsx: `Portal({ container, children })` per D-21 — `mounted` boolean set in `useEffect`; return null until mounted; then `createPortal(children, container ?? document.body)`. No DOM globals at module scope; named export only (D-12). JSDoc must note the consumer-visible timing consequence: children render one effect-tick after the owner mounts, so any effect that needs the portaled DOM must live INSIDE the portaled subtree (plan 03-07 relies on this for Dialog initial focus — review fix for the focus race).
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react run typecheck && pnpm --filter @lyra-ds/react run lint</automated>
  </verify>
  <acceptance_criteria>
    - All three modules typecheck strict and pass lint (hooks rules included)
    - use-controllable-state.ts contains the `value !== undefined` controlled check and a NODE_ENV-guarded warning
    - portal.tsx contains no top-level (module-scope) reference to document or window — DOM access only inside the render/effect path after the mount guard
    - `grep -rn "from 'clsx'" packages/react/src` returns no matches
  </acceptance_criteria>
  <done>Three foundation utilities exist with locked contracts; full behavior proof lands with the Input/Dialog pilot suites (D-25).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement useFocusTrap, usePresence, and useScrollLock, plus the focused hook test suite</name>
  <files>packages/react/src/internal/use-focus-trap.ts, packages/react/src/internal/use-presence.ts, packages/react/src/internal/use-scroll-lock.ts, packages/react/src/internal/internal.browser.test.tsx</files>
  <read_first>
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 7 rows: useFocusTrap, focus restore, usePresence, useScrollLock; Pitfall 7 — presence wedging; Pitfall 8 — trapping the portal subtree; NOTE: Pattern 7's `usePresence(open) → { mounted, closing }` row is superseded by this plan's onAnimationEnd-returning API)
    - packages/styles/components/feedback/feedback.css (post-03-02: the closing classes, the lyra-pop-out/lyra-fade-out keyframes, and their 180ms duration usePresence must outlast)
    - packages/styles/tests/brand-theme.test.ts (Phase 2 Browser Mode harness patterns for the test file)
  </read_first>
  <behavior>
    - useFocusTrap(panelRef, active): while active, Tab on the last focusable wraps to the first and Shift+Tab on the first wraps to the last; the focusable list is queried from panelRef.current at each keydown (content changes must be seen); disabled, hidden, inert, aria-hidden, and invisible elements are never wrap targets
    - usePresence(open): open=true → mounted true, closing false; open flips false → closing true while still mounted; after the caller-attached onAnimationEnd fires on the animated element itself (bubbled child animation events ignored) OR a ~250ms timeout (whichever first) → mounted false; re-open during closing cancels the close and clears the timeout
    - useScrollLock(active): first activation sets body overflow hidden plus paddingRight equal to existing computed padding-right + the scrollbar-width delta; nested activations increment a counter without touching styles; only the final release restores the exact prior inline style values
  </behavior>
  <action>
    Write the three modules per RESEARCH Pattern 7 (as amended below), JSDoc in EN:
    - use-focus-trap.ts: keydown listener while active; on Tab/Shift+Tab compute focusables via `panelRef.current.querySelectorAll` with a FOCUSABLE_SELECTOR constant (anchors with href, button/input/select/textarea not disabled, tabindex >= 0), then apply a post-query candidate filter (review fix — the raw selector can select an invisible/inert wrap target): drop elements that are disabled, have the hidden attribute, are inside inert or aria-hidden="true" subtrees, or are not rendered (zero client rects / offsetParent null for non-fixed elements). Wrap focus at the edges of the FILTERED list. Operate strictly on the ref into the portal subtree — never a React-tree ancestor (Pitfall 8). Supports D-15's hand-rolled trap.
    - use-presence.ts (review fix — the old `usePresence(open)` signature had no panel ref and could not listen to animation events on the panel node; concrete API adopted): `usePresence(open: boolean) → { mounted: boolean; closing: boolean; onAnimationEnd: React.AnimationEventHandler }`. State machine open → closing → unmounted per D-17; on close, expose `closing: true` (caller applies the .lyra-dialog--closing class from plan 03-02 and attaches `onAnimationEnd` to that SAME animated element); onAnimationEnd finalizes the unmount ONLY when `event.target === event.currentTarget` (ignore bubbled child animations — review-specified identity check); additionally arm a setTimeout fallback of ~250ms (above the 180ms --duration-base) when closing starts so reduced-motion/display-none environments can't wedge it (Pitfall 7); whichever fires first wins and clears the other; re-open during closing cancels the close and the timeout. No refs, no direct addEventListener — the caller-attached React handler is the event path.
    - use-scroll-lock.ts: per D-22 compute scrollbar width as `window.innerWidth - document.documentElement.clientWidth`; set `document.body.style.overflow = 'hidden'` and paddingRight = EXISTING computed body padding-right + delta (review fix: replacing instead of adding shifts layout for pages that already pad the body). Reference-count via a module-level lock counter (review fix: nested Dialog/Drawer/CommandPalette must not unlock the body while another overlay is open) — only the 0→1 transition applies styles and captures prior inline values; only the 1→0 transition restores them. All DOM access inside effects only.

    Write internal.browser.test.tsx (browser project — review fix: the utility contracts get focused tests instead of relying solely on wave-3 integration): (a) usePresence harness component asserting the full open→closing→unmounted walk via a real CSS animation, the bubbled-child-animation ignore (child animation ending must NOT unmount), the timeout fallback with animations disabled, and reopen-during-close cancelling the unmount; (b) useScrollLock nested harness asserting a second lock keeps the body locked after the first releases, padding-right equals prior padding + delta while locked, and exact restoration after the last release; (c) useControllableState harness asserting the dev warning fires exactly once on a controlled→uncontrolled switch.
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react run typecheck && pnpm --filter @lyra-ds/react run lint && pnpm --filter @lyra-ds/react run test -- --project browser src/internal</automated>
  </verify>
  <acceptance_criteria>
    - All modules typecheck strict and pass lint; the browser project passes for src/internal
    - use-presence.ts returns an onAnimationEnd handler that checks event.target === event.currentTarget, and contains a setTimeout fallback (~250ms constant with a comment citing Pitfall 7); it contains no addEventListener call
    - use-focus-trap.ts queries focusables inside the keydown handler (no useMemo/cached NodeList) and applies the visibility/inert/disabled filter after querySelectorAll
    - use-scroll-lock.ts adds the delta to the existing computed padding-right and maintains a module-level reference count
    - internal.browser.test.tsx covers reopen-during-close, bubbled-animation ignore, timeout fallback, nested locks, and the controlledness-switch warning
    - No module-scope DOM globals in any of the modules
  </acceptance_criteria>
  <done>Behavior hooks exist with the locked D-15/D-17/D-22 contracts and focused unit proof; the Dialog pilot suite (03-07) remains their end-to-end proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer app → hooks | consumer-provided container elements and open-state flow into DOM manipulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Denial of service | usePresence wedge (dialog stuck mounted) | medium | mitigate | setTimeout fallback alongside animation events (Pitfall 7); browser test asserts eventual unmount (03-07) |
| T-03-07 | Elevation of privilege | focus escaping the modal to the page behind | medium | mitigate | live focusable query on portal subtree per keydown (Pitfall 8); keyboard tests in 03-07 assert wrap on both directions |
| T-03-SC | Tampering | npm installs | low | accept | no installs in this plan (deps landed in 03-01 behind the legitimacy checkpoint) |
</threat_model>

<verification>
- typecheck + lint green across packages/react with all six internal modules present; browser project green for src/internal
- Contracts spot-checked per acceptance criteria; end-to-end behavior verified by pilot suites in wave 3 (D-25: the pilots' tests ARE the template)
</verification>

<success_criteria>
- Success criterion 4's "shared internal/ utilities exist" is satisfied: focus trap, portal, controllable state, SSR guards (+ presence, scroll lock, cx) in place, private by construction
</success_criteria>

<output>
Create `.planning/phases/03-react-infrastructure-pilot-components/03-04-SUMMARY.md` when done
</output>
