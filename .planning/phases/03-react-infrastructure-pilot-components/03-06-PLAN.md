---
phase: 03-react-infrastructure-pilot-components
plan: 06
type: execute
wave: 3
depends_on: ["03-01", "03-04"]
files_modified:
  - packages/react/src/input/input.tsx
  - packages/react/src/input/index.ts
  - packages/react/src/input/input.browser.test.tsx
  - packages/react/src/input/input.ssr.test.ts
autonomous: true
requirements: [RCT-04]

must_haves:
  truths:
    - "Input emits the exact handoff DOM: .lyra-field > .lyra-label? + (.lyra-input-wrap? > .lyra-input-wrap__icon? + .lyra-input) + .lyra-hint?, with size/error modifiers .lyra-input--{sm|lg|error}"
    - "Controlled and uncontrolled value paths both work via useControllableState — controlled iff value !== undefined (D-14, the RCT-09 pattern locked now)"
    - "error prop replaces hint, switches to .lyra-hint--error + .lyra-input--error, and wires aria-invalid=true plus aria-describedby to the message node (UI-SPEC error contract)"
    - "label wires to the input via htmlFor with precedence: consumer id, else the handoff label-derived id, else useId (observable API preserved)"
    - "Each optional prop (label, hint, icon) is independently omittable — the DOM contains no empty wrapper nodes for absent props; a bare Input renders the input element without .lyra-field chrome"
    - "Long values use the native single-line horizontal scroll of the input element (frozen CSS does not alter it) — overflow contract"
    - "ref points at the input element, not the wrapper (D-08)"
    - "Input renders via renderToString with no module-scope DOM access (ssr project, D-26); axe passes light + dark"
    - { statement: "~120-char label + hint fixture: both wrap at 1.5 line-height without overlapping the control", verification: backstop }
  artifacts:
    - "packages/react/src/input/input.tsx"
    - "packages/react/src/input/index.ts"
    - "packages/react/src/input/input.browser.test.tsx"
    - "packages/react/src/input/input.ssr.test.ts"
  key_links:
    - "input.tsx ↔ internal/use-controllable-state.ts (03-04): the controlled/uncontrolled contract Phase 4 repeats for every stateful component"
    - "input.tsx ↔ internal/cx.ts: modifier class assembly with consumer className last (D-09)"
  prohibitions:
    - { statement: "No CSS imports and no visual inline styles in input.tsx — the wrapper adds zero visual decisions", status: flagged-unverified }
---

<objective>
Convert the Input pilot — the form/controlled case that locks the D-14 controlled/uncontrolled contract (`value`/`defaultValue` + `onChange` via `useControllableState`) and the field-chrome a11y wiring (label htmlFor, hint/error aria-describedby, aria-invalid) Phase 4 will repeat for every stateful component. Wrapper conventions per D-08/D-09/D-10/D-12; co-located tests per D-25/D-26/D-28.

Purpose: the form-component conversion recipe, proven on green code.
Output: Input pilot + template-grade browser/ssr tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-infrastructure-pilot-components/03-CONTEXT.md
@.planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md
@.planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md
@.planning/phases/03-react-infrastructure-pilot-components/03-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan's share)

- `Input` component (forwardRef<HTMLInputElement>, `InputProps`: label, hint, error, size, iconLeft, value/defaultValue/onChange passthrough, id, className, ...rest)
- `packages/react/src/input/index.ts` entry for the ./input subpath (D-13/D-28)
- The form-component test template instance: `input.browser.test.tsx` + `input.ssr.test.ts` (D-25)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert the Input pilot with controlled/uncontrolled and a11y wiring</name>
  <files>packages/react/src/input/input.tsx, packages/react/src/input/index.ts</files>
  <read_first>
    - handoff/components/forms/Input.jsx and handoff/components/forms/Input.d.ts (canonical DOM, class assembly, label-derived id fallback, props contract)
    - handoff/components/forms/Input.prompt.md (usage examples doubling as test fixtures)
    - packages/react/src/internal/use-controllable-state.ts and packages/react/src/internal/cx.ts (03-04 utilities to consume)
    - .planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md (section "input.tsx" — conversion deltas, including the flagged id-precedence decision)
    - .planning/phases/03-react-infrastructure-pilot-components/03-UI-SPEC.md (Input interaction contract)
  </read_first>
  <behavior>
    - Uncontrolled: defaultValue + typing updates the DOM value and fires onChange; Controlled: value prop drives the DOM value, typing fires onChange but value only changes when the parent re-renders with it (D-14)
    - error="msg" renders span.lyra-hint.lyra-hint--error, adds lyra-input--error to the input, sets aria-invalid=true and aria-describedby pointing at the message node id; hint renders plain .lyra-hint when no error
    - label present → label.lyra-label[htmlFor] resolves to the input id; id precedence: id prop wins, else label-derived (handoff behavior preserved), else useId
    - No label/hint/error → the bare control renders WITHOUT the .lyra-field wrapper (handoff early-return preserved); iconLeft absent → no .lyra-input-wrap
  </behavior>
  <action>
    Convert per PATTERNS "input.tsx": preserve the handoff DOM and class assembly byte-identically (field > label + wrap > icon + input + hint; modifiers lyra-input--sm/--lg/--error). Conversion deltas: `forwardRef<HTMLInputElement, InputProps>` with the ref on the input element (D-08); `cx()` with consumer className last (D-09); known props extracted, `...rest` spread onto the input (D-10 — the element the .d.ts extends); wire `value`/`defaultValue`/`onChange` through `useControllableState` (D-14) so the controlled contract is explicit; id precedence per the PATTERNS flagged decision resolved as: `id` prop, else the handoff label-derived slug, else `useId()` (additive fallback; keeps the observable label-derived API). Add a11y wiring per UI-SPEC: generate a describedby id for the hint/error node, set `aria-describedby` when hint or error present, `aria-invalid` when error. Named export only (D-12); JSDoc EN. Create input/index.ts re-exporting Input + InputProps.
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react run typecheck && pnpm --filter @lyra-ds/react run lint</automated>
  </verify>
  <acceptance_criteria>
    - typecheck + lint green with input.tsx consuming useControllableState and cx from internal/
    - input.tsx has no import ending in .css; class strings match the handoff assembly exactly
    - The early-return path (no label/hint/error → bare control, no .lyra-field div) exists
  </acceptance_criteria>
  <done>Input pilot implemented with the locked D-14 contract and full a11y wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the Input browser + ssr test template</name>
  <files>packages/react/src/input/input.browser.test.tsx, packages/react/src/input/input.ssr.test.ts</files>
  <read_first>
    - packages/react/src/input/input.tsx (the component under test)
    - packages/styles/tests/brand-theme.test.ts (Phase 2 harness: CSS-in-test import, light/dark toggling)
    - packages/react/src/button/button.browser.test.tsx if already present (template consistency across pilots; wave-3 plans run in parallel so absence is fine)
  </read_first>
  <behavior>
    - Controlled and uncontrolled typing paths assert the D-14 contract with real keyboard input in chromium
    - error fixture asserts class swap + aria-invalid + aria-describedby resolution to the visible message
    - Partial-prop fixtures assert no empty wrapper nodes (no .lyra-field without chrome props, no .lyra-input-wrap without iconLeft, no .lyra-hint without hint/error)
    - Long-value fixture confirms the native single-line behavior (value longer than the visible box; input scrolls, no wrap)
    - ~120-char label + hint fixture renders with both texts wrapping without overlapping the control (backstop)
    - axe.run zero violations in light and [data-theme=dark]; renderToString succeeds in the node project
  </behavior>
  <action>
    Write input.browser.test.tsx following the Phase 2 harness (styles CSS imported in-test; light/dark permutation helper; vitest-browser-react render + real typing via the browser locators): fixtures and assertions per the behavior list above, axe.run on each fixture in both themes. Write input.ssr.test.ts in the node project: renderToString of bare, labeled+hint, and error variants; assert string output, no throw (D-26).
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react run test -- --project browser src/input && pnpm --filter @lyra-ds/react run test -- --project ssr src/input</automated>
  </verify>
  <acceptance_criteria>
    - Browser project green for src/input including the controlled/uncontrolled, error-a11y, partial-prop, and long-text fixtures
    - SSR project green for src/input
    - axe assertions run in both themes with violations toEqual([])
  </acceptance_criteria>
  <done>The form-component test template exists and is green — Phase 4 copies it for every stateful component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer app → Input props | consumer strings (label, hint, error, value) flow into the DOM as React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Tampering | consumer-string rendering | low | mitigate | all strings render as React children (auto-escaped); no raw-HTML API on the component |
| T-03-12 | Information disclosure | error text exposure | low | accept | error copy is consumer-owned and consumer-rendered by design (UI-SPEC) |
| T-03-SC | Tampering | npm installs | low | accept | no installs in this plan |
</threat_model>

<verification>
- Browser + ssr projects green for src/input; D-14 contract and a11y wiring assertions pass
</verification>

<success_criteria>
- The form/controlled conversion recipe (RCT-09 pattern, piloted here for RCT-04's pilot set) is locked on green code with template-grade tests
</success_criteria>

<output>
Create `.planning/phases/03-react-infrastructure-pilot-components/03-06-SUMMARY.md` when done
</output>
