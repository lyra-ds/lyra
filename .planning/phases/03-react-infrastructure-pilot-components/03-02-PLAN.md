---
phase: 03-react-infrastructure-pilot-components
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/styles/components/feedback/feedback.css
  - tools/parity/parity.mjs
autonomous: true
requirements: [RCT-04]

must_haves:
  truths:
    - "packages/styles ships .lyra-dialog__close (28px hit area, 14px icon, token-driven, no inline styles) replacing the prototype's borrowed .lyra-tag__remove + inline style per D-19"
    - ".lyra-dialog--closing and .lyra-dialog-overlay--closing play the reverse of the entry animations at --duration-base/--ease-out per D-17/D-18"
    - "Entry keyframes remain untouched: lyra-fade-in still starts at 0.6 opacity and lyra-pop-in animates transform only (locked animation constraint); only EXIT keyframes may fade out"
    - "pnpm run parity exits 0 with the additive classes present — the ADDITIVE_EXTENSIONS allowlist enumerates exactly the new class names and keyframes, and handoff declarations still match exactly (handoff ⊆ package)"
    - "EXPECTED_CLASSES stays 248 (a handoff-side count, unchanged by additions)"
  artifacts:
    - "packages/styles/components/feedback/feedback.css (additive .lyra-dialog__close, .lyra-dialog--closing, .lyra-dialog-overlay--closing + exit keyframes)"
    - "tools/parity/parity.mjs (ADDITIVE_EXTENSIONS allowlist)"
  key_links:
    - "ADDITIVE_EXTENSIONS entries ↔ exact new selectors/keyframes in feedback.css — any un-enumerated addition must still FAIL parity"
    - ".lyra-dialog--closing ↔ usePresence hook (plan 03-04) and Dialog pilot (plan 03-07) which apply/wait on it"
  prohibitions:
    - { statement: "Entry keyframes must never start fully transparent — the frozen entry constraint (transform-only entries; lyra-fade-in starts at 0.6) is untouchable; only exit animations may fade to transparent", status: flagged-unverified }
    - { statement: "No modification of any existing handoff-derived declaration in feedback.css — additions only (handoff remains byte-matched by parity)", status: flagged-unverified }
---

<objective>
Ship the two documented additive CSS extensions the Dialog pilot needs — the exit-animation classes (D-17/D-18) and the dedicated close-button class (D-19) — inside `@lyra-ds/styles`, and teach the Phase 2 parity script an explicit, auditable ADDITIVE_EXTENSIONS allowlist (D-18) so parity stays a hard gate: handoff declarations still match exactly, and every addition is enumerated by name.

Purpose: the react package must add zero visual decisions; all Dialog exit/close visuals live in the CSS package, parity-audited.
Output: updated feedback.css + parity.mjs, both green under `pnpm run parity` and stylelint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-infrastructure-pilot-components/03-CONTEXT.md
@.planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

- `.lyra-dialog__close` class (new, additive) in `packages/styles/components/feedback/feedback.css`
- `.lyra-dialog--closing` + `.lyra-dialog-overlay--closing` classes and their exit keyframes (new, additive)
- `ADDITIVE_EXTENSIONS` allowlist in `tools/parity/parity.mjs`

<tasks>

<task type="auto">
  <name>Task 1: Add the additive Dialog exit-animation and close-button CSS to feedback.css</name>
  <files>packages/styles/components/feedback/feedback.css</files>
  <read_first>
    - packages/styles/components/feedback/feedback.css (existing dialog block ~lines 66-119: .lyra-dialog-overlay, .lyra-dialog, keyframes lyra-fade-in, lyra-pop-in — the entry animations to reverse; also the file's one-EN-banner comment policy)
    - packages/styles/components/display/display.css (~lines 76-88: .lyra-tag__remove — the visual source for .lyra-dialog__close)
    - handoff/components/feedback/Dialog.jsx (line ~17: the inline 28px override on the borrowed close button — the exact visual to reproduce)
    - .planning/phases/03-react-infrastructure-pilot-components/03-UI-SPEC.md (Motion table: exit mirrors entry at 180ms --ease-out; Spacing exceptions: 28px hit area / 14px icon)
  </read_first>
  <action>
    Append (never modify existing declarations) to feedback.css, following the file's declaration style (token shorthands, one EN banner already present — do not add new banners):

    1. `.lyra-dialog__close` per D-19 — merge `.lyra-tag__remove`'s declarations with the prototype's inline override: `display: inline-flex; align-items: center; justify-content: center; width: 28px; height: 28px; border: 0; padding: 0; border-radius: var(--radius-xs); background: transparent; color: var(--text-faint); cursor: pointer;` plus the hover rule `background: var(--surface-sunken); color: var(--text-primary);`. The 14px icon size is carried by the SVG the Dialog wrapper renders, not by this class.

    2. Exit-animation classes per D-17/D-18: `.lyra-dialog--closing` playing a new keyframe that reverses lyra-pop-in (identity → scale(0.97) translateY(4px), and it MAY also fade to transparent — the transform-only constraint applies to ENTRY frames only), and `.lyra-dialog-overlay--closing` playing the reverse of lyra-fade-in (1 → 0.6 or to transparent). Both use `var(--duration-base) var(--ease-out)` and `animation-fill-mode: forwards` so the final exit frame holds until unmount (usePresence in plan 03-04 waits on animationend). Name the keyframes with the established `lyra-` prefix (e.g. lyra-pop-out, lyra-fade-out). Do NOT touch lyra-fade-in or lyra-pop-in.
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/styles run lint:css && grep -c "lyra-dialog__close" packages/styles/components/feedback/feedback.css | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - feedback.css contains selectors `.lyra-dialog__close`, `.lyra-dialog__close:hover`, `.lyra-dialog--closing`, `.lyra-dialog-overlay--closing` and two new `lyra-`-prefixed exit keyframes
    - `git diff packages/styles/components/feedback/feedback.css` shows additions only — zero removed/changed existing lines
    - `.lyra-dialog__close` declares width and height of 28px and uses var(--radius-xs), var(--text-faint), var(--surface-sunken), var(--text-primary) — no hex literals
    - stylelint (`pnpm --filter @lyra-ds/styles run lint:css`) exits 0
  </acceptance_criteria>
  <done>Additive close-button and exit-animation CSS exists, token-driven, entry animations untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add the ADDITIVE_EXTENSIONS allowlist to the parity script</name>
  <files>tools/parity/parity.mjs</files>
  <read_first>
    - tools/parity/parity.mjs (its MASK_DIVERGENCE allowlist ~lines 80-95 — the exact-payload allowlist discipline to replicate; classCheck() ~lines 483-500 — the "present in package but not in canonical handoff" branch; diffFile()/placementCheck() ~lines 418-469 — the "Extra declaration" branch)
    - packages/styles/components/feedback/feedback.css (post-Task-1 state: the exact new selector and keyframe names to enumerate)
  </read_first>
  <action>
    Following the MASK_DIVERGENCE discipline (named, commented, exact-payload constant at the top of the file), add an `ADDITIVE_EXTENSIONS` structure enumerating EXACTLY the Task-1 additions: the new class names (`lyra-dialog__close`, `lyra-dialog--closing`, `lyra-dialog-overlay--closing`) and the new keyframe names, with a comment citing D-18/D-19 (documented additive extensions: handoff ⊆ package; handoff declarations still match exactly). Wire two check sites:
    1. `classCheck()` — the package-class-not-in-handoff failure branch skips allowlisted class names; `EXPECTED_CLASSES = 248` stays untouched (it is a handoff-side count).
    2. `diffFile()`/`placementCheck()` — the extra-declaration failure branch skips declarations whose selector chain is rooted in an allowlisted class or keyframe name; everything else still fails.
    The allowlist must be exact-match on names (no prefixes/wildcards) so any future un-enumerated addition still fails parity.
  </action>
  <verify>
    <automated>pnpm run parity</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run parity` exits 0 with Task 1's additions present in feedback.css
    - Negative proof: temporarily appending a dummy class (e.g. a throwaway `.lyra-zzz-test` rule) to feedback.css makes `pnpm run parity` exit non-zero; dummy removed afterwards
    - parity.mjs contains an ADDITIVE_EXTENSIONS constant with a comment referencing D-18 and D-19
    - The 209-token and 248-class expected counts in parity.mjs are unchanged
  </acceptance_criteria>
  <done>Parity is green with the additions enumerated, and still fails on any un-listed extension.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handoff/ (canonical design) → packages/styles | drift between canonical CSS and shipped CSS is the integrity surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | parity allowlist scope creep | medium | mitigate | exact-name enumeration only (no wildcards); negative-proof test in Task 2 acceptance; allowlist visible + commented with decision IDs |
| T-03-SC | Tampering | (no installs in this plan) | low | accept | plan touches only in-repo CSS and a zero-dep node script |
</threat_model>

<verification>
- `pnpm run parity` green; stylelint green; git diff of feedback.css is additive-only
</verification>

<success_criteria>
- The Dialog pilot's exit/close visuals exist in @lyra-ds/styles as enumerated, parity-audited additive extensions (D-18/D-19)
</success_criteria>

<output>
Create `.planning/phases/03-react-infrastructure-pilot-components/03-02-SUMMARY.md` when done
</output>
