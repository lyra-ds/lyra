---
phase: 03-react-infrastructure-pilot-components
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-03", "03-04"]
files_modified:
  - packages/react/src/button/button.tsx
  - packages/react/src/button/index.ts
  - packages/react/src/button/button.browser.test.tsx
  - packages/react/src/button/button.ssr.test.ts
  - packages/react/src/icon/icon.tsx
  - packages/react/src/icon/index.ts
  - packages/react/src/icon/icon.browser.test.tsx
  - packages/react/src/icon/icon.ssr.test.ts
autonomous: true
requirements: [RCT-03, RCT-05]

must_haves:
  truths:
    - "Button emits byte-identical .lyra-* class strings to handoff Button.jsx (lyra-btn, lyra-btn--{variant}, lyra-btn--{size}, conditional --loading/--full) with consumer className appended last per D-09"
    - "Button loading renders .lyra-btn__spinner, keeps the label visible (no layout shift), sets aria-busy, and blocks interaction via disabled semantics (UI-SPEC loading contract)"
    - "Icon-only Button usage (--icon) requires aria-label — demonstrated in fixtures and asserted by the axe suite"
    - "Button smoke matrix renders all 5 variants x 3 sizes in light AND [data-theme=dark] with zero axe violations"
    - "Button iconLeft/iconRight are independently omittable with no empty nodes emitted for absent props"
    - "Icon resolves component = icon prop ?? iconRegistry[name] (D-03 escape hatch); renders inline svg via lucide-react with class .lyra-icon, size default 20, currentColor (D-06)"
    - "The always-emitted .lyra-icon class is a DELIBERATE class-API extension over the prototype (which only forwards consumer className): D-06 names the class explicitly; no CSS rule ships for it in @lyra-ds/styles — it is a stable consumer/test hook, recorded in CONVENTIONS.md (review fix: extension documented, not implicit)"
    - "Unknown Icon name at runtime dev-warns with the exact UI-SPEC message pointing at the icon escape hatch and renders null; production is silent (D-05) — the empty/null-input contract: no name and no icon also renders null without crashing; production silence in REAL bundler builds is statically proven by plan 03-09's packed Vite production fixture (warning literal absent from the built output — review fix: NODE_ENV replacement is proven, not assumed)"
    - "Icon title prop yields role=img + aria-label; absent title yields aria-hidden=true (handoff contract)"
    - "All 68 registry icons render in the browser smoke test at default 20px, light + dark, zero axe violations (68 = the corrected canonical inventory from plan 03-03, superseding the 54 count)"
    - "Both pilots renderToString without module-scope DOM access (ssr vitest project, D-26)"
    - { statement: "A held-out ~120-char Button label fixture documents nowrap truncation at the container edge (frozen white-space nowrap) — overflow behavior", verification: backstop }
    - { statement: "The same held-out ~120-char label fixture serves the long-text probe in the Button pilot suite", verification: backstop }
  artifacts:
    - "packages/react/src/button/button.tsx"
    - "packages/react/src/button/index.ts"
    - "packages/react/src/button/button.browser.test.tsx"
    - "packages/react/src/button/button.ssr.test.ts"
    - "packages/react/src/icon/icon.tsx"
    - "packages/react/src/icon/index.ts"
    - "packages/react/src/icon/icon.browser.test.tsx"
    - "packages/react/src/icon/icon.ssr.test.ts"
  key_links:
    - "icon.tsx ↔ icon-registry.ts (03-03): registry lookup + IconName typing (D-04)"
    - "button.tsx/icon.tsx ↔ internal/cx.ts (03-04): class merge with consumer className last"
    - "browser tests ↔ @lyra-ds/styles CSS imported IN THE TEST (never in src) — real rendering for axe color-contrast"
  prohibitions:
    - { statement: "Icon must never dynamically import or namespace-import lucide-react — resolution is a static registry key lookup plus the icon prop; anything else bundles the ~1,400-icon set", status: flagged-unverified }
    - { statement: "Neither pilot imports any CSS or sets font/color/spacing inline — appearance is 100% .lyra-* classes (CSS-first lock)", status: flagged-unverified }
---

<objective>
Convert the first two pilots: Button (the simple case that locks the wrapper conventions D-08/D-09/D-10/D-12/D-28) and Icon (the registry case that delivers RCT-05's consumer surface per D-03/D-04/D-05/D-06). Their co-located browser + ssr tests are written as the reusable Phase 4 template (D-25): smoke matrix light/dark, axe.run per fixture, renderToString.

Purpose: lock the simple-component and registry-component conversion recipes on real code.
Output: two production pilots + four test files, all green under the browser and ssr vitest projects.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-react-infrastructure-pilot-components/03-CONTEXT.md
@.planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md
@.planning/phases/03-react-infrastructure-pilot-components/03-PATTERNS.md
@.planning/phases/03-react-infrastructure-pilot-components/03-UI-SPEC.md
</context>

## Artifacts this phase produces (this plan's share)

- `Button` component (forwardRef<HTMLButtonElement>, `ButtonProps` extending ButtonHTMLAttributes: variant, size, iconLeft, iconRight, loading, full)
- `Icon` component (forwardRef<SVGSVGElement>, `IconProps`: name?: IconName, icon?: LucideIcon, size?, color?, title?, className?, style?) + re-exported `IconName`
- Per-folder `index.ts` entries matching the ./button and ./icon subpath exports (D-13/D-28)
- Test template instances: `*.browser.test.tsx` (smoke + axe light/dark) and `*.ssr.test.ts` for the simple and registry component types (D-25)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert the Button pilot with its browser + ssr test template</name>
  <files>packages/react/src/button/button.tsx, packages/react/src/button/index.ts, packages/react/src/button/button.browser.test.tsx, packages/react/src/button/button.ssr.test.ts</files>
  <read_first>
    - handoff/components/buttons/Button.jsx and handoff/components/buttons/Button.d.ts (canonical DOM, class strings, and props contract — byte-identical class emission required)
    - handoff/components/buttons/Button.prompt.md (usage examples doubling as test fixtures)
    - packages/react/src/internal/cx.ts (the class joiner to use)
    - packages/styles/tests/brand-theme.test.ts (Phase 2 Browser Mode harness patterns: CSS-import-in-test, light/dark permutation toggling, style-recalc forcing)
    - .planning/phases/03-react-infrastructure-pilot-components/03-UI-SPEC.md (Button interaction contract + Copywriting loading-state row)
  </read_first>
  <behavior>
    - Default render emits exactly `lyra-btn lyra-btn--primary lyra-btn--md`; variant/size props map to `lyra-btn--{primary|secondary|soft|ghost|danger}` / `--{sm|md|lg}`; full adds `--full`; consumer className appends after all .lyra-* classes
    - loading: spinner span with class lyra-btn__spinner + aria-hidden present, label still in the DOM, button disabled, aria-busy true
    - iconLeft/iconRight: absent props emit no wrapper nodes; children null emits no label span
    - ref points at the underlying button element (D-08)
    - renderToString of every variant succeeds in the node project
  </behavior>
  <action>
    Convert per PATTERNS "button.tsx" section: keep the handoff class assembly and DOM byte-identical, apply the conversion deltas — `forwardRef<HTMLButtonElement, ButtonProps>` (D-08), `cx()` with consumer className last (D-09), known props extracted and `...rest` spread onto the root button (D-10), named export only (D-12), JSDoc translated to canonical English. `ButtonProps` extends `React.ButtonHTMLAttributes<HTMLButtonElement>` with variant/size/iconLeft/iconRight/loading/full per Button.d.ts. Add `aria-busy` when loading (UI-SPEC contract). Rewrite button/index.ts (replacing the 03-01 placeholder contents) re-exporting `Button` and `ButtonProps` (entry for the ./button subpath, D-13/D-28).

    button.browser.test.tsx (the SIMPLE-component template, D-25): import the styles CSS in-test (via the @lyra-ds/styles devDep, per the Phase 2 pattern — never in src); render via vitest-browser-react; smoke matrix all 5 variants x 3 sizes in light and [data-theme="dark"] asserting exact class strings and no console errors; loading fixture asserting spinner presence + label persistence + disabled + aria-busy; icon-only fixture (`lyra-btn--icon` class via className plus aria-label) — axe must pass; the held-out ~120-char label fixture (backstop rows: documents nowrap truncation, no wrapping); axe.run per fixture in BOTH themes with `expect(results.violations).toEqual([])`.

    button.ssr.test.ts (node project, D-26): renderToString for a representative variant set; assert string output and no throw.
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react run test -- --project browser src/button && pnpm --filter @lyra-ds/react run test -- --project ssr src/button</automated>
  </verify>
  <acceptance_criteria>
    - Browser project passes for src/button: class-string assertions, loading contract, icon-only + aria-label, axe zero violations light and dark
    - SSR project passes for src/button
    - `grep -c "forwardRef" packages/react/src/button/button.tsx` >= 1 and index.ts contains no `default` keyword
    - button.tsx contains no import ending in .css and no inline style literals for visual styling
  </acceptance_criteria>
  <done>Button pilot green in both projects; its tests are the simple-component template Phase 4 copies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert the Icon pilot over the curated registry with its tests</name>
  <files>packages/react/src/icon/icon.tsx, packages/react/src/icon/index.ts, packages/react/src/icon/icon.browser.test.tsx, packages/react/src/icon/icon.ssr.test.ts</files>
  <read_first>
    - handoff/components/icons/Icon.d.ts (canonical prop surface/defaults) and handoff/components/icons/Icon.jsx (ONLY its a11y contract lines ~13-16 — the span+CSS-mask+CDN rendering is deleted per D-06)
    - packages/react/src/icon/icon-registry.ts (generated in 03-03 — iconRegistry + IconName)
    - .planning/phases/03-react-infrastructure-pilot-components/03-RESEARCH.md (Pattern 3 icon.tsx contract sketch; Security Domain V5 row — registry lookup hygiene)
    - .planning/phases/03-react-infrastructure-pilot-components/03-UI-SPEC.md (Icon interaction contract; Copywriting: exact unknown-name warning string)
  </read_first>
  <behavior>
    - <Icon name="check" /> renders an inline svg with class lyra-icon, width/height 20, stroke currentColor
    - <Icon icon={SomeLucideComponent} /> renders that component (escape hatch wins over name when both provided)
    - Unknown name: console.warn fires in dev with the exact UI-SPEC message and the component returns null; with NODE_ENV=production no warn; no name and no icon → null without crashing
    - title="Settings" → role img + aria-label Settings; no title → aria-hidden true
    - All 68 IconName values render successfully
  </behavior>
  <action>
    Create icon.tsx per D-03/D-04/D-05/D-06 and RESEARCH Pattern 3: `IconProps` with `name?: IconName`, `icon?: LucideIcon` (D-03 escape hatch), `size?` (default 20), `color?`, `title?`, `className?`, `style?`; resolve `Comp = icon ?? (name !== undefined && Object.hasOwn(iconRegistry, name) ? iconRegistry[name] : undefined)` — plain frozen-map key access, no dynamic imports (V5 hygiene); when unresolved, dev-warn (guarded by `process.env.NODE_ENV !== 'production'`) with the exact UI-SPEC Copywriting string and return null (D-05); render `<Comp className={cx('lyra-icon', className)} size={size} color={color} aria-hidden={title ? undefined : true} role={title ? 'img' : undefined} aria-label={title} style={style} ref={ref} />` — lucide spreads extras onto the svg, currentColor is its native default (D-06). The always-present `.lyra-icon` class is a deliberate extension over the prototype (which forwards only consumer className): document it in the component JSDoc as a stable class-API hook with no shipped CSS rule, and record it in the CONVENTIONS.md class-emission expectations (03-08) — review fix. forwardRef to the svg (D-08); named exports only (D-12); JSDoc EN. Also JSDoc the icon-only-usage a11y rule: accessible naming for icon-only interactive parents is CONSUMER-enforced (title prop or parent aria-label) — Icon has no API guarantee (review fix: documented as guidance, not implied enforcement). icon/index.ts re-exports Icon, IconProps, and IconName (type) — entry for ./icon (D-13); replace the 03-01 placeholder contents.

    icon.browser.test.tsx (the REGISTRY-component template, D-25): CSS-in-test import; render every one of the 68 registry names (iterate Object.keys(iconRegistry) — never a hardcoded list, so a registry recount can't desync this test) at default size in light + dark, assert an svg with class lyra-icon appears for each and that the key count is exactly 68; unknown-name fixture asserting console.warn spy called once with a message containing the icon-prop hint and null output (empty container); escape-hatch fixture passing a lucide component directly; title/no-title a11y attribute assertions; axe.run light + dark. (Production-silence of the warn branch under real bundler NODE_ENV replacement is proven statically in plan 03-09's Vite production fixture.)

    icon.ssr.test.ts: renderToString of a registry icon, the escape hatch, and the unknown-name null path.
  </action>
  <verify>
    <automated>pnpm --filter @lyra-ds/react run test -- --project browser src/icon && pnpm --filter @lyra-ds/react run test -- --project ssr src/icon</automated>
  </verify>
  <acceptance_criteria>
    - Browser project passes for src/icon: 68/68 icons render in both themes, unknown-name warn+null contract, escape hatch, title a11y wiring, axe zero violations
    - SSR project passes for src/icon
    - icon.tsx contains no `import *` from lucide-react and no dynamic import() call
    - The literal warning text in icon.tsx matches the UI-SPEC Copywriting row (starts with `[lyra-ds] Icon: unknown name`)
  </acceptance_criteria>
  <done>Icon pilot green in both projects; the curated-registry consumer surface (RCT-05) works end to end with no CDN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer app → component props | arbitrary strings (name, title, className) and nodes (icon, children) cross into rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | icon markup injection | medium | mitigate | icons are React elements from lucide-react/createLucideIcon; no raw-HTML injection API exists in either pilot (canon rule recorded in CONVENTIONS.md, plan 03-08 — security breadcrumb) |
| T-03-09 | Information disclosure | dev warning echoing consumer strings | low | mitigate | warn goes to console only (never rendered) and is stripped from production via the NODE_ENV guard (D-05) |
| T-03-10 | Spoofing | prototype-chain lookup on registry (e.g. name="constructor") | low | mitigate | Object.hasOwn guard before key access (RESEARCH Security Domain V5) |
| T-03-SC | Tampering | npm installs | low | accept | no installs in this plan |
</threat_model>

<verification>
- `pnpm --filter @lyra-ds/react run test -- --project browser src/button src/icon` and `--project ssr` green
- Class-emission, loading, registry, and a11y contracts hold per acceptance criteria
</verification>

<success_criteria>
- The simple (Button) and registry (Icon) conversion recipes are locked on green code with template-grade tests (D-25), covering RCT-03's isolation precedent and RCT-05's runtime surface
</success_criteria>

<output>
Create `.planning/phases/03-react-infrastructure-pilot-components/03-05-SUMMARY.md` when done
</output>
