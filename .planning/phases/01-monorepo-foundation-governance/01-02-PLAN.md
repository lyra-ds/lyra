---
phase: 01-monorepo-foundation-governance
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - pnpm-lock.yaml
  - tsconfig.base.json
  - .npmrc
  - .nvmrc
  - .gitignore
  - .prettierrc.json
  - .prettierignore
  - .changeset/config.json
  - .changeset/README.md
  - packages/styles/package.json
  - packages/react/package.json
autonomous: false
requirements: [OSS-03, OSS-05]

must_haves:
  truths:
    - "pnpm install --frozen-lockfile succeeds from the repo root (lockfile committed and in sync with the workspace)"
    - "pnpm exec changeset status exits 0 with the lockstep fixed group covering @lyra-ds/styles and @lyra-ds/react (D-06)"
    - "Root scripts lint, typecheck, test, and build all exit 0 (typecheck/test/build are --if-present no-ops until Phases 2-3 — RESERVED CI CONTEXTS that give the ruleset stable check names, explicitly NOT claimed as working validation gates; OSS-03's package gates activate in Phases 2-4)"
    - "package.json devDependencies are exact pins with no range prefixes: @changesets/cli 2.31.1, @changesets/changelog-github 0.7.0, prettier 3.9.5, typescript 5.9.3 (OSS-03 precision edge: version resolution has zero float)"
    - statement: "Interrupted or parallel changeset flows are safe: pending changesets accumulate as independent files under .changeset/, version state is only ever mutated by the deliberate Version Packages flow (Phase 7), and the placeholder packages stay at 0.0.0 private in the meantime (OSS-05 concurrency edge)"
      verification: backstop
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace globs anticipating packages/*, apps/* (Phase 6 docs), tools/* (Phase 5 docgen)"
      contains: "packages/*"
    - path: "package.json"
      provides: "Root manifest: private, packageManager pin, engines, fan-out scripts, exact-pinned devDependencies"
      contains: "pnpm@11.13.1"
    - path: ".changeset/config.json"
      provides: "Lockstep versioning scaffold (D-06) with GitHub changelog"
      contains: "fixed"
    - path: "packages/styles/package.json"
      provides: "Placeholder manifest that makes the changesets fixed group validate (research Pattern 1)"
      contains: "@lyra-ds/styles"
    - path: "packages/react/package.json"
      provides: "Placeholder manifest that makes the changesets fixed group validate (research Pattern 1)"
      contains: "@lyra-ds/react"
    - path: "tsconfig.base.json"
      provides: "Strict shared TS base (no project references) that packages extend from Phase 2+"
      contains: '"strict": true'
  key_links:
    - from: ".changeset/config.json"
      to: "packages/styles/package.json"
      via: "fixed-group package names must match existing workspace packages or every changesets command throws ValidationError (verified in research)"
      pattern: "@lyra-ds/(styles|react)"
    - from: "package.json"
      to: "packages/*/package.json"
      via: "root scripts fan out with pnpm -r --if-present run <script>"
      pattern: "pnpm -r --if-present"
    - from: ".npmrc + .nvmrc + engines"
      to: "clean-clone contributors"
      via: "engine-strict=true makes Node/pnpm version mismatches fail fast (research Pitfall 6)"
      pattern: "engine-strict=true"
  prohibitions:
    - statement: "Placeholder package manifests must contain ONLY the four keys name, version, private, description — no export maps, no tree-shaking flags, no publish file lists; that packaging metadata belongs to Phases 2-3 and half-authoring it now recreates the pitfalls those phases exist to prevent (research anti-pattern)"
      status: flagged
      verification: unverified
    - statement: "No dependency may be installed at a floating version or dist-tag; in particular typescript must resolve to 5.9.3 — the registry latest is the 7.x tsgo compiler line, a different compiler (research Pitfall 4, CLAUDE.md lock)"
      status: flagged
      verification: unverified
    - statement: "No Tailwind package may be added anywhere in the workspace (locked project constraint: CSS-first core, no Tailwind)"
      status: flagged
      verification: unverified
---

<objective>
Scaffold the pnpm workspace: root manifest with pinned toolchain and fan-out scripts, workspace globs, placeholder package manifests for `@lyra-ds/styles` and `@lyra-ds/react`, the changesets lockstep config (D-06), the strict shared tsconfig base, and repo hygiene files (.npmrc/.nvmrc/.gitignore/prettier config).

Purpose: This is the substrate for phase success criterion 5 (`pnpm install` + workspace commands from a clean clone) and encodes D-06 in tooling. The placeholder stubs are load-bearing: the changesets `fixed` group hard-fails validation without them (verified by experiment in research Pattern 1).
Output: Installable workspace where `pnpm install --frozen-lockfile`, all four root scripts, and `pnpm exec changeset status` succeed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

New file paths: `pnpm-workspace.yaml`, `package.json` (root), `pnpm-lock.yaml`, `tsconfig.base.json`, `.npmrc`, `.nvmrc`, `.prettierrc.json`, `.prettierignore`, `.changeset/config.json`, `.changeset/README.md`, `packages/styles/package.json`, `packages/react/package.json`. Modified: `.gitignore`.
New package names (placeholders): `@lyra-ds/styles` 0.0.0 private, `@lyra-ds/react` 0.0.0 private.
New root npm scripts: `lint`, `format`, `typecheck`, `test`, `build`, `changeset`.

## Review findings addressed (Codex review, 01-REVIEWS.md)

- **MEDIUM — Task 3 verification omits promised typecheck + scoped prettier:** INCORPORATED — the `<automated>` command now runs `pnpm run typecheck` and the exact scoped prettier check the action promises.
- **MEDIUM — no-op scripts overstated as validation:** INCORPORATED — the must_haves truth and Task 2/3 wording now describe typecheck/test/build as "reserved CI contexts" (stable check names for the ruleset), never as working validation; OSS-03's real gates land in Phases 2-4 (see the OSS-03 reconciliation in 01-04/01-05 and REQUIREMENTS.md).
- **LOW — Node engines `>=24` is not a pin:** INCORPORATED — engines narrowed to `>=24 <25` (Node 24 LTS is the supported line per research; engine-strict makes this fail fast, matching the "pinned Node 24 toolchain" language).
- **LOW — package legitimacy checkpoint disproportionate:** REJECTED — the GSD supply-chain gate mandates a blocking, never-auto-approvable human checkpoint for every [SUS]-flagged package before install, regardless of how canonical the packages look; the research audit flagged all four [SUS: too-new], so Task 1 stays. The checkpoint is cheap (~1 minute) and doubles as the recorded provenance check the reviewer asked for.

**Round 2 (2026-07-17):**

- **LOW — pinned pnpm runtime never asserted:** INCORPORATED — Task 2 gains step 8 and a verify clause asserting `pnpm --version` prints exactly `11.13.1` after the `packageManager` field exists (the machine runs 11.13.0 pre-scaffold; pnpm's self-managed version switching / corepack must honor the pin, so the assert proves the switch actually happened). The reserved-contexts vs. real-gates distinction the reviewer asked to preserve is untouched.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification for [SUS]-flagged installs</name>
  <files>(none — verification-only gate before any install)</files>
  <read_first>.planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (section "Package Legitimacy Audit")</read_first>
  <action>Pause BEFORE running any pnpm add command and present the four npmjs.com links plus the researcher's context (recency-heuristic false positives on canonical packages) from how-to-verify. This gate is never auto-approvable regardless of yolo/auto_advance settings. On approval, Task 2 installs the exact pins only.</action>
  <what-built>Nothing yet. The research legitimacy audit flagged four packages [SUS: too-new] because their latest versions were published within days of the research run. The researcher's analysis: this is a recency heuristic firing on canonical, decade-old packages (official source repos, 656K-216M weekly downloads), mitigated by installing exact registry-verified pins instead of floating latest. Per the supply-chain gate, [SUS] packages still require human sign-off before install.</what-built>
  <how-to-verify>
    Spend ~1 minute confirming each package page is the canonical project (publisher, repo link, weekly downloads):
    1. https://www.npmjs.com/package/@changesets/cli — repo github.com/changesets/changesets, install pin 2.31.1
    2. https://www.npmjs.com/package/@changesets/changelog-github — same repo, install pin 0.7.0
    3. https://www.npmjs.com/package/prettier — repo github.com/prettier/prettier, install pin 3.9.5
    4. https://www.npmjs.com/package/typescript — repo github.com/microsoft/TypeScript, install pin 5.9.3 (NOT the 7.x latest)
  </how-to-verify>
  <acceptance_criteria>
    - User confirms all four npmjs.com pages match the canonical projects and the exact pins above
    - The pinned versions exist on the registry: `npm view @changesets/cli@2.31.1 version`, `npm view @changesets/changelog-github@0.7.0 version`, `npm view prettier@3.9.5 version`, `npm view typescript@5.9.3 version` each print the requested version
  </acceptance_criteria>
  <verify>
    <automated>npm view @changesets/cli@2.31.1 version && npm view @changesets/changelog-github@0.7.0 version && npm view prettier@3.9.5 version && npm view typescript@5.9.3 version</automated>
  </verify>
  <done>User approved all four packages; the exact pins are confirmed present on the registry; installs may proceed.</done>
  <resume-signal>Type "approved" to allow the pinned installs, or name any package that looks wrong</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Root workspace config, toolchain pins, and hygiene files</name>
  <files>pnpm-workspace.yaml, package.json, pnpm-lock.yaml, tsconfig.base.json, .npmrc, .nvmrc, .gitignore, .prettierrc.json, .prettierignore</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md (sections "pnpm-workspace.yaml", "Root package.json", ".gitignore (MODIFY existing)", and the discretion anchors for .npmrc/.nvmrc/prettier/tsconfig)
    - .gitignore (current single-line content must be preserved)
    - .planning/research/ARCHITECTURE.md (tsconfig.base: strict, no TS project references)
  </read_first>
  <action>
    Create the workspace substrate exactly per the verified excerpts in 01-PATTERNS.md:

    1. `pnpm-workspace.yaml` with the three globs `packages/*`, `apps/*`, `tools/*` (unmatched globs verified harmless; apps arrives Phase 6, tools Phase 5).
    2. Root `package.json` copied from the PATTERNS.md excerpt with ONE deviation: name `lyra-ds-monorepo`, `private: true`, `packageManager: "pnpm@11.13.1"`, `engines: { "node": ">=24 <25" }` (deviation from the excerpt's `>=24` — Node 24 LTS is the supported line and `engine-strict` should fail fast on untested majors, per review), scripts `lint` (`prettier --check .`), `format` (`prettier --write .`), `typecheck`/`test`/`build` (each `pnpm -r --if-present run <name>` — use the SIMPLE typecheck form, PATTERNS.md explicitly rejects the convoluted variant), and `changeset`. devDependencies as exact pins with no caret/tilde: `@changesets/cli` `2.31.1`, `@changesets/changelog-github` `0.7.0`, `prettier` `3.9.5`, `typescript` `5.9.3`.
    3. `.nvmrc` containing `24`. `.npmrc` containing `engine-strict=true` (research Pitfall 6).
    4. `.gitignore`: keep the existing line `.planning/research/.cache/` and append `node_modules/`, `dist/`, `*.tsbuildinfo`, `.env*`.
    5. `.prettierrc.json`: minimal explicit config (Claude's discretion; e.g. printWidth 100, singleQuote true — pick and stay consistent). `.prettierignore` MUST list: `handoff/` (read-only prototype bundle — never format), `.planning/` and `.claude/` (GSD-generated docs would fail `prettier --check` on every planning PR and break required CI checks), `pnpm-lock.yaml`, `CHANGELOG.md`, and `.changeset/*.md`.
    6. `tsconfig.base.json`: strict shared base with NO project references (per ARCHITECTURE.md). Concrete compilerOptions: `"strict": true`, `"target": "ES2022"`, `"module": "ESNext"`, `"moduleResolution": "bundler"`, `"skipLibCheck": true`, `"isolatedModules": true`, `"forceConsistentCasingInFileNames": true`, `"noEmit": true`. Packages extend and override from Phase 2+.
    7. Install with exact pins (Task 1 approved): `pnpm add -D -w @changesets/cli@2.31.1 @changesets/changelog-github@0.7.0 prettier@3.9.5 typescript@5.9.3`. Commit the generated `pnpm-lock.yaml`.
    8. Assert the executing pnpm matches the pin now that the `packageManager` field exists (round-2 review — the machine ran 11.13.0 before this scaffold): `pnpm --version` must print exactly `11.13.1`. pnpm's self-managed version switching honors the `packageManager` pin from inside the project; if the assert fails, run `corepack enable` (or install pnpm 11.13.1 directly) and re-run — do not weaken the pin.

    Do NOT create the packages/ stubs or .changeset/ here (Task 3). Do not add any linter other than prettier — ESLint/stylelint arrive with code in Phases 2-3 (research: installing linters with nothing to lint produces fake-green CI).
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile && pnpm --version | grep -qx '11.13.1' && pnpm exec tsc --version | grep -q '5\.9\.3' && node -e "const p=require('./package.json'); const d=p.devDependencies; for (const [k,v] of Object.entries(d)) { if (/^[\^~]/.test(v)) { console.error('range prefix on '+k); process.exit(1);} } if(d.typescript!=='5.9.3'||d.prettier!=='3.9.5'||d['@changesets/cli']!=='2.31.1'||d['@changesets/changelog-github']!=='0.7.0'){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` contains the globs `packages/*`, `apps/*`, and `tools/*`
    - `package.json` contains `"packageManager": "pnpm@11.13.1"` and `"node": ">=24 <25"` under engines; `engine-strict=true` appears in `.npmrc`; `.nvmrc` content is `24`
    - `pnpm exec tsc --version` prints `Version 5.9.3` (not a 7.x line)
    - `pnpm --version | grep -qx '11.13.1'` exits 0 from the repo root (the executing package manager matches the `packageManager` pin — round-2 review)
    - `node -e` pin check above exits 0 (all four devDependencies exact, no `^`/`~`)
    - `.prettierignore` contains the lines `handoff/`, `.planning/`, `.claude/`, and `pnpm-lock.yaml`
    - `.gitignore` still contains `.planning/research/.cache/` and now contains `node_modules/`
    - `tsconfig.base.json` contains `"strict": true` and does not contain a `references` array
  </acceptance_criteria>
  <done>Workspace installs reproducibly with the pinned toolchain; prettier lint scope excludes reference/GSD content; TS base config ready for Phase 2+ packages.</done>
</task>

<task type="auto">
  <name>Task 3: Placeholder package stubs + changesets lockstep config (D-06)</name>
  <files>packages/styles/package.json, packages/react/package.json, .changeset/config.json, .changeset/README.md</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md (sections "packages/styles/package.json + packages/react/package.json" and ".changeset/config.json" — both verified excerpts)
  </read_first>
  <action>
    Encode D-06 (lockstep versioning) in the changesets scaffold, unblocked by placeholder stubs (research Pattern 1 — the fixed group throws ValidationError without matching workspace packages; both exact names and globs fail).

    1. Create `packages/styles/package.json` with exactly four keys: `"name": "@lyra-ds/styles"`, `"version": "0.0.0"`, `"private": true`, and a `"description"` noting it is a placeholder until Phase 2. Create `packages/react/package.json` identically shaped with `"@lyra-ds/react"` and a Phase-3 note. Nothing else — no packaging metadata of any kind (see prohibitions).
    2. Run `pnpm exec changeset init`, then overwrite `.changeset/config.json` with the verified content from PATTERNS.md: `$schema` for @changesets/config 3.1.1, `changelog: ["@changesets/changelog-github", { "repo": "lyra-ds/lyra" }]`, `commit: false`, `fixed: [["@lyra-ds/styles", "@lyra-ds/react"]]`, `linked: []`, `access: "public"`, `baseBranch: "main"`, `updateInternalDependencies: "patch"`, `ignore: []`.
    3. Re-run `pnpm install` so the lockfile picks up the two new workspace packages; commit the lockfile delta.
    4. Scoped verification (do NOT run whole-tree prettier here — plans 01-03/01-04 may be writing files in parallel; whole-tree lint is plan 01-05's job): `pnpm exec changeset status` (exits 0 = fixed group validates), `pnpm run build`, `pnpm run test`, `pnpm run typecheck` (all --if-present no-ops, all exit 0), and `pnpm exec prettier --check package.json pnpm-workspace.yaml tsconfig.base.json .changeset/config.json packages/`.
  </action>
  <verify>
    <automated>pnpm exec changeset status && pnpm run build && pnpm run test && pnpm run typecheck && pnpm exec prettier --check package.json pnpm-workspace.yaml tsconfig.base.json .changeset/config.json packages/ && node -e "for (const p of ['styles','react']) { const m=require('./packages/'+p+'/package.json'); const keys=Object.keys(m).sort().join(','); if(keys!=='description,name,private,version'){console.error(p+' has extra keys: '+keys);process.exit(1);} if(m.version!=='0.0.0'||m.private!==true)process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec changeset status` exits 0
    - `.changeset/config.json` contains `"fixed"` with both `@lyra-ds/styles` and `@lyra-ds/react` in one group, and `"repo": "lyra-ds/lyra"`
    - `node -e` stub-shape check above exits 0 (each stub has exactly the keys description,name,private,version)
    - `pnpm run build`, `pnpm run test`, and `pnpm run typecheck` exit 0 from the repo root (verify runs everything the action promises — review verify-drift fix)
    - `pnpm exec prettier --check package.json pnpm-workspace.yaml tsconfig.base.json .changeset/config.json packages/` exits 0 (scoped — whole-tree lint is plan 01-05's job)
  </acceptance_criteria>
  <done>changesets lockstep scaffold is live and validated; workspace fan-out scripts are green; stubs carry zero packaging decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> node_modules | Third-party code enters the repo toolchain at install time |
| lockfile -> future clean clones | pnpm-lock.yaml pins the transitive graph for every contributor and CI run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-04 | Spoofing | npm devDependencies (typosquat/hijack) | high | mitigate | Exact registry-verified pins (no floating versions), committed lockfile, CONTRIBUTING dependency policy (plan 01-03) |
| T-1-06 | Tampering | pnpm-lock.yaml integrity | medium | mitigate | Lockfile committed; CI installs with --frozen-lockfile so lockfile drift fails loudly (research Pitfall 6) |
| T-1-09 | Denial of Service | wrong compiler line (typescript 7.x/tsgo) breaking the toolchain | medium | mitigate | Exact 5.9.3 pin + pin-check assertion in Task 2 verify |
| T-1-SC | Tampering | npm installs | high | mitigate | slopcheck audit done in research + blocking human checkpoint (Task 1) for all [SUS]-flagged packages before any install |
</threat_model>

<verification>
- `pnpm install --frozen-lockfile` from repo root exits 0
- `pnpm exec changeset status` exits 0 (D-06 fixed group functional)
- All four root scripts exit 0; devDependency pins exact; stubs are four-keys-only
</verification>

<success_criteria>
- Phase success criterion 5 is locally true (clean-clone proof runs in plan 01-05)
- D-06 encoded in `.changeset/config.json`; all three prohibitions hold
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-foundation-governance/01-02-SUMMARY.md` when done (committed on the phase branch).
</output>
