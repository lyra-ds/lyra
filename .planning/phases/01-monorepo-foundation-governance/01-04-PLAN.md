---
phase: 01-monorepo-foundation-governance
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .github/workflows/ci.yml
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - .github/pull_request_template.md
  - .github/CODEOWNERS
autonomous: true
requirements: [OSS-02, OSS-03]

must_haves:
  truths:
    - "ci.yml defines exactly four jobs named lint, typecheck, test, build — the frozen contract the ruleset's required checks reference (research Pitfall 5)"
    - "ci.yml triggers on every pull request with no file-based filtering, so a docs-only GSD planning PR still reports all four checks (OSS-03 boundary edge: the minimal-diff PR is the boundary case and it must still report)"
    - "The workflow token is read-only at the top level and EVERY action reference is pinned to a full 40-char commit SHA with a trailing version comment — no mutable tag refs anywhere, first-party included (OSS-03 precision edge; review supply-chain finding)"
    - "The actionlint binary is fetched from a version-pinned release URL and verified against a sha256 checksum committed in the workflow before it executes — no remote script is ever piped into a shell (review supply-chain finding)"
    - "Issue intake uses YAML issue forms with blank issues disabled, and the PR template carries the changeset checkbox reinforcing breaking = minor while 0.x (OSS-02, research Pitfall 7)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Visible CI on every PR: lint/typecheck/test/build with commented future-gate hooks (publint/attw/size-limit/parity)"
      contains: "pnpm install --frozen-lockfile"
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Structured bug intake form"
      contains: "name:"
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "Structured feature intake form"
      contains: "name:"
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "Blank issues disabled + security contact link"
      contains: "blank_issues_enabled: false"
    - path: ".github/pull_request_template.md"
      provides: "PR checklist incl. changeset convention checkbox"
      contains: "changeset"
    - path: ".github/CODEOWNERS"
      provides: "Default ownership enabling require_code_owner_review later"
      contains: "@franciscpd"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "each job runs a root fan-out script (pnpm run lint|typecheck|test|build)"
      pattern: "pnpm run (lint|typecheck|test|build)"
    - from: ".github/workflows/ci.yml job names"
      to: "GitHub ruleset required checks (added in plan 01-05)"
      via: "job names ARE the required-check context names — frozen forever"
      pattern: "^  (lint|typecheck|test|build):"
    - from: ".github/ISSUE_TEMPLATE/config.yml"
      to: "SECURITY.md policy (plan 01-03)"
      via: "contact link routes vulnerability reports away from public issues"
      pattern: "security"
  prohibitions:
    - statement: "ci.yml must not filter workflow runs by which files changed — required checks from a skipped workflow never report, and docs-only GSD planning PRs would be permanently unmergeable (research Pitfall 2)"
      status: flagged
      verification: unverified
    - statement: "Future quality gates (publint, attw, size-limit, token/class parity) must land as steps inside the existing four jobs, never as new job names — a new required job name orphans the ruleset contract (research Pitfall 5)"
      status: flagged
      verification: unverified
    - statement: "The workflow must never use the privileged pull-request trigger variant nor interpolate GitHub event strings into run: steps (GitHub Actions injection hardening; actionlint enforces in CI)"
      status: flagged
      verification: unverified
---

<objective>
Author the `.github/` surface: the CI workflow (the base-CI half of OSS-03) with the four frozen job names and commented hooks for the Phase 2-4 gates, plus the community templates (OSS-02): YAML issue forms, PR template with the changeset checkbox, and CODEOWNERS.

OSS-03 scope statement (review reconciliation): OSS-03's full wording requires publint/attw, size-limit, and parity scripts on every PR. Those gates CANNOT run before the packages they gate exist — this is a dependency constraint, not a scope cut. Phase 1 delivers the base-CI infrastructure (visible lint/typecheck/test/build on every PR + frozen check contexts + commented hooks); the package gates activate in Phases 2-4 as steps inside these same jobs. OSS-03 stays only PARTIALLY satisfied at Phase-1 end (see REQUIREMENTS.md traceability note and plan 01-05's success criteria — no plan may claim it fully TRUE).

Purpose: The job names authored here become the ruleset's required-check contexts in plan 01-05 — they are a frozen contract.
Output: Six files under `.github/` ready to run on the scaffold PR (the first CI proof).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md
</context>

## Artifacts this phase produces (this plan's share)

New file paths: `.github/workflows/ci.yml`, `.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml`, `.github/ISSUE_TEMPLATE/config.yml`, `.github/pull_request_template.md`, `.github/CODEOWNERS`.
New CI job names (frozen contract): `lint`, `typecheck`, `test`, `build`.

## Review findings addressed (Codex review, 01-REVIEWS.md)

- **HIGH — OSS-03 not actually satisfied by comments-only gates:** INCORPORATED — the objective now carries an explicit OSS-03 scope statement: Phase 1 = base-CI infrastructure only; publint/attw/size-limit/parity activate in Phases 2-4 (dependency constraint — the packages they gate do not exist yet). REQUIREMENTS.md traceability annotated to match; plan 01-05 no longer claims OSS-03 fully TRUE.
- **HIGH — mutable remote code execution (actionlint installer script from a movable tag, piped to a shell):** INCORPORATED — Task 1 now downloads the actionlint release tarball from the version-pinned URL and verifies it against a sha256 checksum committed in the workflow before executing; the negative grep in acceptance forbids any curl-pipe pattern.
- **MEDIUM — first-party actions on mutable major tags:** INCORPORATED — every `uses:` reference (actions/checkout, actions/setup-node, pnpm/action-setup) is pinned to a full commit SHA with a trailing version comment; acceptance asserts uses-line count == SHA-pinned count.
- **LOW — issue-form YAML parsing not in automated verifier:** INCORPORATED — Task 2's `<automated>` now parses all three YAML files with `pnpm dlx js-yaml@4.1.0` (canonical nodeca/js-yaml, exact pin, dev-time-only ephemeral fetch — legitimacy basis: repo github.com/nodeca/js-yaml, 4.1.0 published 2021, tens of millions of weekly downloads).

<tasks>

<task type="auto">
  <name>Task 1: CI workflow with frozen job names and future-gate hooks (OSS-03)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (Code Examples: ".github/workflows/ci.yml (skeleton)"; Security Domain: known threat patterns; State of the Art: action versions verified 2026-07-16)
    - .planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md (section ".github/workflows/ci.yml" — setup block, hardening block, future-hook placement rule)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` from the verified RESEARCH.md skeleton:

    - `name: CI`; triggers: the standard PR event (bare `pull_request:`) and pushes to `main`. Keep triggers unfiltered by changed files — a header comment must state "runs on every PR regardless of which files changed; job names are required-check contexts, never rename them". Do not use the privileged PR-trigger variant anywhere, and do not name it in comments either (the acceptance criteria negative-grep the workflow file for it).
    - Top-level hardening: `permissions:` with `contents: read`; `concurrency:` group `ci-${{ github.ref }}` with `cancel-in-progress: true`.
    - Exactly four jobs named `lint`, `typecheck`, `test`, `build` (frozen contract). Each job on `ubuntu-latest` with the same setup block: actions/checkout v7.0.0, pnpm/action-setup v6.0.9, and actions/setup-node v7.0.0 (with `node-version: 24` and `cache: pnpm`), then `pnpm install --frozen-lockfile`.
    - ALL action references — first-party included — are pinned to the full 40-char commit SHA of the release tag, followed by a trailing comment naming the version (e.g. a comment reading `# v7.0.0` after the SHA). Resolve each SHA at execution time via `gh api repos/OWNER/REPO/git/ref/tags/TAG --jq '.object.sha'`; if `--jq '.object.type'` says `tag` (annotated tag), dereference once more via `gh api repos/OWNER/REPO/git/tags/THAT_SHA --jq '.object.sha'` to get the commit SHA. Never write a tag or branch name after the @ in a `uses:` line (review supply-chain finding: a full commit SHA is the only immutable action ref).
    - actionlint step in the `lint` job — checksum-verified binary, never a remote script executed blind: BEFORE authoring the workflow, fetch the official checksum once via `gh release download v1.7.12 --repo rhysd/actionlint --pattern 'actionlint_1.7.12_checksums.txt' --output -` and copy the sha256 for `actionlint_1.7.12_linux_amd64.tar.gz`. The CI step then runs, as separate lines: `curl -sSfLO https://github.com/rhysd/actionlint/releases/download/v1.7.12/actionlint_1.7.12_linux_amd64.tar.gz`, then a `sha256sum -c` verification of that file against the checksum literal committed in the workflow (echo the "CHECKSUM  filename" line into `sha256sum -c -`), then `tar xzf actionlint_1.7.12_linux_amd64.tar.gz actionlint`, then `./actionlint -color`. The committed checksum makes the download tamper-evident even though release URLs live under a tag. Do not pipe any downloaded content into a shell interpreter anywhere in the workflow.
    - Job bodies: `lint` runs `pnpm run lint` then the actionlint step above; `typecheck` runs `pnpm run typecheck`; `test` runs `pnpm run test`; `build` runs `pnpm run build`.
    - Commented future-gate hooks INSIDE existing jobs (base-CI half of OSS-03 — see objective scope statement): in `lint` a comment for stylelint on packages/styles (Phase 2); in `test` a comment for the token parity script (Phase 2, STY-06); in `build` comments for publint, attw --pack, and size-limit (Phases 2-4). These are comments only — no fake-green steps — and each comment names the phase that activates it.
    - No GitHub event strings (titles, branch names, body text) interpolated into any `run:` step.
  </action>
  <verify>
    <automated>grep -Ec '^  (lint|typecheck|test|build):' .github/workflows/ci.yml | grep -qx 4 && grep -q 'contents: read' .github/workflows/ci.yml && grep -q 'frozen-lockfile' .github/workflows/ci.yml && test "$(grep -c 'pull_request_target' .github/workflows/ci.yml)" = 0 && test "$(grep -Ec '^[[:space:]]+(paths|paths-ignore):' .github/workflows/ci.yml)" = 0 && test "$(grep -c 'uses:' .github/workflows/ci.yml)" = "$(grep -Ec 'uses:[^@]+@[0-9a-f]{40}' .github/workflows/ci.yml)" && test "$(grep -Ec 'curl[^|]*\|' .github/workflows/ci.yml)" = 0 && grep -q 'sha256sum -c' .github/workflows/ci.yml && grep -q 'actionlint' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec '^  (lint|typecheck|test|build):' .github/workflows/ci.yml` returns exactly 4
    - `grep -c 'pull_request_target' .github/workflows/ci.yml` returns 0
    - `grep -Ec '^[[:space:]]+(paths|paths-ignore):' .github/workflows/ci.yml` returns 0 (no file-based trigger filters)
    - Every `uses:` line references a 40-char commit SHA: `grep -c 'uses:'` equals `grep -Ec 'uses:[^@]+@[0-9a-f]{40}'` on the workflow file, and each SHA line carries a trailing version comment
    - `grep -Ec 'curl[^|]*\|' .github/workflows/ci.yml` returns 0 (no download is piped into an interpreter) and `sha256sum -c` appears in the actionlint step with a 64-hex checksum literal present in the file
    - Workflow contains `permissions:` -> `contents: read`, a `concurrency:` block, and the actionlint 1.7.12 release download URL
    - Comments mentioning publint, attw, size-limit, and parity exist inside the build/test/lint jobs (grep -qi publint / attw / size-limit / parity all match)
  </acceptance_criteria>
  <done>CI workflow is authored, hardened (all refs SHA-pinned, checksum-verified linter binary), and ready to produce the four required-check contexts on the scaffold PR.</done>
</task>

<task type="auto">
  <name>Task 2: Issue forms, PR template, CODEOWNERS (OSS-02)</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_request.yml, .github/ISSUE_TEMPLATE/config.yml, .github/pull_request_template.md, .github/CODEOWNERS</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md (section ".github/ISSUE_TEMPLATE/* + pull_request_template.md" and the CODEOWNERS anchor)
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (Pitfall 7 — the PR-template checkbox is the human enforcement point for the 0.x convention)
  </read_first>
  <action>
    All files in English (D-04):

    1. `bug_report.yml` — YAML issue form (not markdown): fields for description (required textarea), reproduction steps (required), expected behavior, package dropdown (`@lyra-ds/styles`, `@lyra-ds/react`, docs, other), version input, environment (browser/OS) input. Labels: `bug`.
    2. `feature_request.yml` — YAML issue form: problem statement (required), proposed solution, alternatives considered, scope dropdown (styles / react / docs / tooling). Labels: `enhancement`.
    3. `config.yml` — `blank_issues_enabled: false` plus one contact link routing security reports to `https://github.com/lyra-ds/lyra/security/policy` (pairs with SECURITY.md from plan 01-03).
    4. `pull_request_template.md` — summary section plus a checklist: conventional-commit PR title; changeset added if packages are touched, with the explicit reminder that while 0.x a breaking change uses a `minor` changeset (Pitfall 7 reinforcement); docs updated if behavior changed; CI green.
    5. `CODEOWNERS` — single line `* @franciscpd` (cheap now; enables require_code_owner_review later without a new file — research Open Question 2).
  </action>
  <verify>
    <automated>test -f .github/ISSUE_TEMPLATE/bug_report.yml && test -f .github/ISSUE_TEMPLATE/feature_request.yml && pnpm dlx js-yaml@4.1.0 .github/ISSUE_TEMPLATE/bug_report.yml > /dev/null && pnpm dlx js-yaml@4.1.0 .github/ISSUE_TEMPLATE/feature_request.yml > /dev/null && pnpm dlx js-yaml@4.1.0 .github/ISSUE_TEMPLATE/config.yml > /dev/null && grep -q 'blank_issues_enabled: false' .github/ISSUE_TEMPLATE/config.yml && grep -qi 'changeset' .github/pull_request_template.md && grep -qi 'minor' .github/pull_request_template.md && grep -q '@franciscpd' .github/CODEOWNERS</automated>
  </verify>
  <acceptance_criteria>
    - All three YAML files parse without error via `pnpm dlx js-yaml@4.1.0 <file>` (exact-pinned canonical parser; the parse is IN the automated verify, per review) — the local machine has no PyYAML, so do not substitute a python3 one-liner
    - `config.yml` contains `blank_issues_enabled: false` and a `security/policy` contact link
    - `pull_request_template.md` contains a checklist with a changeset item mentioning `minor` for breaking changes while 0.x
    - `.github/CODEOWNERS` content is `* @franciscpd`
  </acceptance_criteria>
  <done>Structured issue intake, PR checklist reinforcing the 0.x convention, and default code ownership are in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR authors (anyone) -> CI runner | Untrusted forks trigger workflow runs; event payload is attacker-controlled data |
| Action marketplace -> workflow | Third-party action code executes with repo checkout access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Tampering | mutable action refs and remote linter code | high | mitigate | ALL actions (first-party included) pinned to full commit SHAs with version comments; actionlint 1.7.12 binary verified against a committed sha256 checksum before execution; nothing downloaded is piped into a shell |
| T-1-02 | Elevation | over-privileged GITHUB_TOKEN | medium | mitigate | Top-level `permissions: contents: read`; widen per-job only when Phase 7 release automation needs it |
| T-1-05 | Tampering/Elevation | script injection via event data in run: steps | high | mitigate | No event-string interpolation in any run: step; no privileged PR-trigger variant; actionlint flags violations in CI |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new installs in this plan (CI uses --frozen-lockfile only); legitimacy checkpoint enforced in plan 01-02 |
</threat_model>

<verification>
- ci.yml has exactly the four frozen job names, no file-based trigger filters, read-only token, ALL actions SHA-pinned, a checksum-verified actionlint step, and commented future-gate hooks
- Issue forms parse as YAML (automated); blank issues disabled; PR template carries the changeset/minor checkbox; CODEOWNERS present
</verification>

<success_criteria>
- The base-CI workflow half of OSS-03 is done (the ruleset half lands in plan 01-05; the publint/attw/size-limit/parity gates activate in Phases 2-4 — OSS-03 is NOT fully satisfied by Phase 1, per the objective's scope statement); OSS-02's template half is done (governance docs are plan 01-03)
- All three prohibitions hold (no file filters, no new job names planned for gates, no privileged trigger/injection surface)
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-foundation-governance/01-04-SUMMARY.md` when done (committed on the phase branch).
</output>