---
phase: 01-monorepo-foundation-governance
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - README.md
  - README.pt-BR.md
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - SECURITY.md
  - VERSIONING.md
  - .github/workflows/ci.yml
autonomous: true
requirements: [OSS-03]

must_haves:
  truths:
    - "The scaffold PR ran visible CI with all four checks (lint, typecheck, test, build) green before merge (OSS-03: 'every PR runs visible CI')"
    - "The main ruleset now requires the four status checks lint, typecheck, test, build in addition to requiring a PR (research Pattern 3 step 6)"
    - "A clean clone passes pnpm install --frozen-lockfile, pnpm run build, pnpm run test, and pnpm exec changeset status (phase success criterion 5)"
    - "main contains the merged scaffold and the phase branch remains usable for the remaining phase docs (merge commit, not squash, so the branch stays contained in main)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Proven-on-a-real-PR CI (may receive formatting-only touch-ups here)"
      contains: "frozen-lockfile"
  key_links:
    - from: "GitHub ruleset required_status_checks"
      to: ".github/workflows/ci.yml job names"
      via: "the four contexts exactly match the four job names — the frozen contract"
      pattern: "lint|typecheck|test|build"
    - from: "phase branch gsd/phase-1-monorepo-foundation-governance"
      to: "origin/main"
      via: "gh pr create -> checks green -> gh pr merge --merge (D-07 flow exercised end to end)"
      pattern: "gsd/phase-1"
  prohibitions:
    - statement: "Never push directly to main after this plan — every change, including the remaining Phase-1 planning docs, rides a PR with green CI (D-07)"
      status: flagged
      verification: unverified
    - statement: "The required-check contexts added to the ruleset must exactly equal the four CI job names — no renamed, extra, or missing contexts, or merges block on checks that never report (research Pitfall 5)"
      status: flagged
      verification: unverified
---

<objective>
Integrate the wave-2 scaffold: reconcile formatting repo-wide, prove the workspace from a literal clean clone, land everything as the repo's first PR with green CI, merge it, and then complete branch protection by adding the four required status checks to the ruleset (research Pattern 3 steps 5-6).

Purpose: This is where OSS-03 becomes TRUE rather than authored — CI is proven on a real PR, and the protection loop closes so main only accepts PR-merged, CI-green changes (D-07).
Output: Merged scaffold on main; ruleset with four required checks; clean-clone proof recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md
</context>

## Artifacts this phase produces (this plan's share)

- The scaffold PR itself (first PR on lyra-ds/lyra) and its merge commit on `main`
- Ruleset update: `required_status_checks` rule with the contexts `lint`, `typecheck`, `test`, `build`
- Possible formatting-only commits to wave-2 files (no content changes)
- No new code symbols or file paths beyond formatting deltas

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide format reconciliation + clean-clone proof (success criterion 5)</name>
  <files>README.md, README.pt-BR.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md, VERSIONING.md, .github/workflows/ci.yml</files>
  <read_first>
    - .prettierignore (confirm handoff/, .planning/, .claude/, pnpm-lock.yaml are excluded before running whole-tree prettier)
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (Validation Architecture: the clean-clone check and phase-gate commands)
  </read_first>
  <action>
    Wave-2 plans ran in parallel and deliberately deferred whole-tree lint to this plan.

    1. Run `pnpm run lint` (prettier --check .). If it fails, run `pnpm run format`, review the diff is formatting-only (no content changes), and commit as `style(01): prettier reconciliation across scaffold`.
    2. Run the local phase gate: `pnpm install --frozen-lockfile && pnpm run build && pnpm run test && pnpm run typecheck && pnpm exec changeset status` — all must exit 0.
    3. Clean-clone proof (research Validation Architecture, SC-5): in a scratch dir, `git clone --branch gsd/phase-1-monorepo-foundation-governance "file://$PWD" <tmpdir>/lyra-clone`, then inside the clone run `pnpm install --frozen-lockfile && pnpm run build && pnpm run test && pnpm exec changeset status`. All must exit 0. If the frozen-lockfile install fails, the lockfile is out of sync — fix in the working repo (pnpm install, commit lockfile) and repeat.
  </action>
  <verify>
    <automated>pnpm run lint && pnpm exec changeset status && C=$(mktemp -d) && git clone -q --branch gsd/phase-1-monorepo-foundation-governance "file://$PWD" "$C/lyra-clone" && cd "$C/lyra-clone" && pnpm install --frozen-lockfile --silent && pnpm run build && pnpm run test && pnpm exec changeset status</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run lint` exits 0 in the working repo
    - In a fresh `git clone` of the phase branch: `pnpm install --frozen-lockfile`, `pnpm run build`, `pnpm run test`, and `pnpm exec changeset status` all exit 0
    - `git status --porcelain` is empty after any formatting commit (nothing uncommitted)
  </acceptance_criteria>
  <done>Phase success criterion 5 is proven from a literal clean clone; the tree is lint-clean for the PR's CI run.</done>
</task>

<task type="auto">
  <name>Task 2: Open the scaffold PR, watch CI green, merge (first CI proof)</name>
  <files>(remote platform state only — no local files)</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (Pattern 3 step 5 and Assumption A2: same-repo PR-branch workflows run on the pull_request event)
  </read_first>
  <action>
    1. Push the phase branch: `git push -u origin gsd/phase-1-monorepo-foundation-governance`.
    2. Open the PR: `gh pr create --base main --head gsd/phase-1-monorepo-foundation-governance --title "chore(01): monorepo foundation and governance scaffold" --body` summarizing: workspace + changesets lockstep (D-06), governance docs (OSS-01/02/05), CI with frozen job names (OSS-03), and noting required checks are added post-merge (two-step bootstrap).
    3. Watch checks: `gh pr checks --watch` (poll fallback: `gh pr checks` every 30s). All four contexts lint/typecheck/test/build must report success. If a check fails, fix on the branch, push, and re-watch — do not merge red (D-07). If NO workflow runs at all, research Assumption A2 was wrong for this repo state: push a trivial workflow-touching commit to the branch and re-check; if still nothing, surface the blocker instead of merging.
    4. Merge with a merge commit (NOT squash — squash would strand the still-active phase branch's history): `gh pr merge --merge`. Do not delete the branch — the remaining phase docs (summaries, verification) continue on it and land via the phase-end /gsd-ship PR.
    5. `git fetch origin main` so the local view of main is current.
  </action>
  <verify>
    <automated>PR=$(gh pr list --repo lyra-ds/lyra --state merged --head gsd/phase-1-monorepo-foundation-governance --json number --jq '.[0].number') && test -n "$PR" && gh pr checks "$PR" --repo lyra-ds/lyra | grep -Ec '^(lint|typecheck|test|build).*(pass|success)' | grep -qx 4</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --state merged --head gsd/phase-1-monorepo-foundation-governance` returns the scaffold PR number
    - `gh pr checks <PR>` shows lint, typecheck, test, and build all passing
    - `git ls-remote origin main` SHA differs from the pre-PR main SHA (merge landed) and `gh api repos/lyra-ds/lyra/contents/pnpm-workspace.yaml --jq .name` returns `pnpm-workspace.yaml` (scaffold visible on main)
  </acceptance_criteria>
  <done>The first PR exercised the full D-07 flow: branch -> PR -> visible green CI -> merge.</done>
</task>

<task type="auto">
  <name>Task 3: Add the four required status checks to the ruleset (protection complete)</name>
  <files>(remote platform state only — no local files)</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (Code Examples: "Ruleset via gh api" — full payload including the required_status_checks rule; Assumption A4 on payload shape)
  </read_first>
  <action>
    Research Pattern 3 step 6 — now that CI has reported once, the checks can be required without deadlocking PRs:

    1. Get the ruleset id: `gh api repos/lyra-ds/lyra/rulesets --jq '.[0].id'`.
    2. Update it with the FULL payload (GET the current ruleset, add the required_status_checks rule to its rules array, PUT it back): rule type `required_status_checks` with `strict_required_status_checks_policy: false` and contexts exactly `lint`, `typecheck`, `test`, `build` — matching the CI job names character-for-character (Pitfall 5). Keep the existing pull_request/deletion/non_fast_forward rules intact. If the PUT rejects the payload shape (Assumption A4), adjust per the live error or apply the same change in the web UI (Settings -> Rules) and verify via API afterwards.
    3. Verify end-state: `gh api "repos/lyra-ds/lyra/rules/branches/main"` must now list all four rule types including required_status_checks with the four contexts.
    4. Live-blocking note for the phase record: the next PR from this phase branch (the /gsd-ship docs PR at phase end) is the natural live proof that merges block until the four checks pass — record this expectation in the SUMMARY so /gsd-verify-work checks it.
  </action>
  <verify>
    <automated>gh api "repos/lyra-ds/lyra/rules/branches/main" --jq '[.[] | select(.type=="required_status_checks")] | .[0].parameters.required_status_checks | [.[].context] | sort | join(",")' | grep -qx 'build,lint,test,typecheck'</automated>
  </verify>
  <acceptance_criteria>
    - `gh api "repos/lyra-ds/lyra/rules/branches/main"` lists rule types including `pull_request` and `required_status_checks`
    - The required contexts, sorted, are exactly `build,lint,test,typecheck` — no more, no fewer
  </acceptance_criteria>
  <done>OSS-03 end-state: every future PR must run visible CI and cannot merge until lint, typecheck, test, and build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase branch -> main | The merge is the moment scaffold content becomes the protected public truth |
| gh CLI (authenticated) -> repo settings | Ruleset mutation is a privileged platform operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-03 | Tampering | main branch integrity | high | mitigate | Ruleset completed with the four required checks; force-push/deletion already blocked (plan 01-01) |
| T-1-13 | Denial of Service | required checks referencing wrong context names (self-inflicted merge deadlock) | medium | mitigate | Contexts copied verbatim from job names; Task 3 verify asserts the exact sorted set; recovery path = ruleset edit via gh api/UI |
| T-1-14 | Repudiation | unreviewable merge history | low | accept | Solo maintainer, 0 required approvals by design; PR history itself is the public build log (D-07) |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs from the committed lockfile only (--frozen-lockfile); no new packages; legitimacy gate lives in plan 01-02 |
</threat_model>

<verification>
- Scaffold PR merged with four green checks; main shows the scaffold files
- Ruleset requires exactly build,lint,test,typecheck; PR rule still active
- Clean-clone gate passed (install/build/test/changeset status)
</verification>

<success_criteria>
- OSS-03 fully TRUE: visible CI on every PR, enforced by required checks, with future-gate hooks in place
- Phase success criteria 3 and 5 satisfied and evidenced; both prohibitions hold
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-foundation-governance/01-05-SUMMARY.md` when done (committed on the phase branch — it rides the phase-end /gsd-ship PR, which doubles as the live proof that required checks block merges).
</output>
