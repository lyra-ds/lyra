---
phase: 01-monorepo-foundation-governance
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - README.md
  - README.pt-BR.md
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - SECURITY.md
  - VERSIONING.md
  - LICENSE
  - .github/workflows/ci.yml
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - .github/pull_request_template.md
  - .github/CODEOWNERS
  - package.json
  - pnpm-workspace.yaml
  - tsconfig.base.json
  - .prettierrc.json
  - .changeset/config.json
  - .changeset/README.md
  - packages/styles/package.json
  - packages/react/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [OSS-03]

must_haves:
  truths:
    - "The scaffold PR ran visible CI with all four checks (lint, typecheck, test, build) green before merge (OSS-03: 'every PR runs visible CI')"
    - "The main ruleset now requires the four status checks lint, typecheck, test, build in addition to requiring a PR (research Pattern 3 step 6)"
    - "A clean clone passes pnpm install --frozen-lockfile, pnpm run build, pnpm run test, and pnpm exec changeset status (phase success criterion 5)"
    - "main contains the merged scaffold and the phase branch remains usable for the remaining phase docs (merge commit, not squash, so the branch stays contained in main)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Proven-on-a-real-PR CI (may receive formatting-only touch-ups here)"
      contains: "frozen-lockfile"
  key_links:
    - from: "GitHub ruleset required_status_checks"
      to: ".github/workflows/ci.yml job names"
      via: "the four contexts exactly match the four job names — the frozen contract"
      pattern: "lint|typecheck|test|build"
    - from: "phase branch gsd/phase-1-monorepo-foundation-governance"
      to: "origin/main"
      via: "gh pr create -> checks green -> gh pr merge --merge (D-07 flow exercised end to end)"
      pattern: "gsd/phase-1"
  prohibitions:
    - statement: "Never push directly to main after this plan — every change, including the remaining Phase-1 planning docs, rides a PR with green CI (D-07)"
      status: flagged
      verification: unverified
    - statement: "The required-check contexts added to the ruleset must exactly equal the four CI job names — no renamed, extra, or missing contexts, or merges block on checks that never report (research Pitfall 5)"
      status: flagged
      verification: unverified
---

<objective>
Integrate the wave-2 scaffold: reconcile formatting repo-wide, prove the workspace from a literal clean clone, land everything as the repo's first PR with green CI, merge it, and then complete branch protection by adding the four required status checks to the ruleset (research Pattern 3 steps 5-6).

Purpose: This is where the BASE-CI half of OSS-03 becomes TRUE rather than authored — CI is proven on a real PR, and the protection loop closes so main only accepts PR-merged, CI-green changes (D-07). OSS-03's package gates (publint/attw, size-limit, parity) activate in Phases 2-4; this plan must NOT declare OSS-03 fully satisfied (review reconciliation — see the scope statement in 01-04-PLAN.md and the REQUIREMENTS.md traceability note).
Output: Merged scaffold on main; ruleset with four required checks; clean-clone proof recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md
</context>

## Artifacts this phase produces (this plan's share)

- The scaffold PR itself (first PR on lyra-ds/lyra) and its merge commit on `main`
- Ruleset update: `required_status_checks` rule with the contexts `lint`, `typecheck`, `test`, `build` (applied to the ruleset named `main-protection`, selected by name)
- Possible formatting-only commits to wave-2 files (no content changes; scoped to files prettier itself reports as different — all candidates are declared in files_modified)
- Possible corrected `pnpm-lock.yaml` commit if the clean-clone gate exposes lockfile drift (declared ownership — round-2 review fix)
- No new code symbols or file paths beyond formatting deltas

## Review findings addressed (Codex review, 01-REVIEWS.md)

- **HIGH — declared OSS-03 "fully TRUE" despite missing gates:** INCORPORATED — objective and success criteria now claim only the base-CI half (visible CI + four required checks); the publint/attw/size-limit/parity gates activate in Phases 2-4, and the SUMMARY must record OSS-03 as PARTIAL. REQUIREMENTS.md traceability carries the same split.
- **HIGH — 01-VALIDATION.md was an unfilled template:** INCORPORATED at the phase level — the validation contract has been filled (test infrastructure = shell-command verification, per-task verification map for all 13 tasks, manual-only register for the two human checkpoints) during the reviews-mode replan.
- **MEDIUM — repo-wide `pnpm run format` exceeds declared file ownership:** INCORPORATED — Task 1 formats ONLY the files `prettier --list-different .` reports, and every legitimate candidate (all wave-2 outputs) is now declared in files_modified.
- **MEDIUM — clean-clone verification omits typecheck:** INCORPORATED — `pnpm run typecheck` added to both the local phase gate and the automated clean-clone command.
- **LOW — ruleset selected positionally (first element):** INCORPORATED — Task 3 selects the ruleset by its exact name `main-protection` (guaranteed by plan 01-01) and fails loudly if that name is absent.

**Round 2 (2026-07-17):**

- **MEDIUM — "GET, modify, PUT it back" is not a safe ruleset recipe (GET responses carry read-only fields like `id`, `source`, `_links`, timestamps; a blind PUT likely 422s):** INCORPORATED — Task 3 step 2 now constructs the PUT body explicitly with ONLY the writable schema fields (`name`, `target`, `enforcement`, `bypass_actors`, `conditions`, `rules`), appending the required_status_checks rule via a jq expression, and never round-trips the GET object.
- **MEDIUM — lockfile recovery path exceeded declared ownership (`pnpm-lock.yaml` was committed by the failure path but absent from `<files>`/frontmatter):** INCORPORATED — `pnpm-lock.yaml` is now declared in this plan's frontmatter `files_modified` and Task 1 `<files>`; the recovery path stays inline (an out-of-sync lockfile surfacing at the clean-clone gate is a plan 01-02 defect, but the repair is a single deterministic `pnpm install` + commit, now within declared ownership).
- **LOW — PR-check verification grepped human-formatted CLI text:** INCORPORATED — Task 2's verify now uses `gh pr checks --json name,bucket` and asserts the exact `{name, bucket: "pass"}` set `build,lint,test,typecheck` structurally.
- **(from 01-04 round-2) issue-forms rendering proof:** INCORPORATED here — Task 2 step 6 records the post-merge UI spot-check (both forms visible at /issues/new/choose, no blank-issue option) in the SUMMARY for /gsd-verify-work; rendering is only observable after the templates land on the default branch.

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide format reconciliation + clean-clone proof (success criterion 5)</name>
  <files>README.md, README.pt-BR.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md, VERSIONING.md, .github/workflows/ci.yml, pnpm-lock.yaml</files>
  <read_first>
    - .prettierignore (confirm handoff/, .planning/, .claude/, pnpm-lock.yaml are excluded before running whole-tree prettier)
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (Validation Architecture: the clean-clone check and phase-gate commands)
  </read_first>
  <action>
    Wave-2 plans ran in parallel and deliberately deferred whole-tree lint to this plan.

    1. Run `pnpm run lint` (prettier --check .). If it fails, get the exact offender list with `pnpm exec prettier --list-different .`, verify every listed file appears in this plan's files_modified frontmatter (if a file outside that list shows up, stop and surface it — do not silently rewrite undeclared files), then run `pnpm exec prettier --write` on EXACTLY those listed files (never a bare repo-wide format — review scope finding), review the diff is formatting-only (no content changes), and commit as `style(01): prettier reconciliation across scaffold`.
    2. Run the local phase gate: `pnpm install --frozen-lockfile && pnpm run build && pnpm run test && pnpm run typecheck && pnpm exec changeset status` — all must exit 0.
    3. Clean-clone proof (research Validation Architecture, SC-5): in a scratch dir, `git clone --branch gsd/phase-1-monorepo-foundation-governance "file://$PWD" <tmpdir>/lyra-clone`, then inside the clone run `pnpm install --frozen-lockfile && pnpm run build && pnpm run test && pnpm run typecheck && pnpm exec changeset status`. All must exit 0 (typecheck included — the clean-clone command must run everything the action promises, review verify-drift finding). If the frozen-lockfile install fails, the lockfile is out of sync — a plan 01-02 defect surfacing here: run `pnpm install` in the working repo, commit the corrected `pnpm-lock.yaml` (declared in this plan's `<files>` and frontmatter — round-2 ownership fix), note the 01-02 defect in the SUMMARY, and repeat the clean-clone gate.
  </action>
  <verify>
    <automated>pnpm run lint && pnpm exec changeset status && C=$(mktemp -d) && git clone -q --branch gsd/phase-1-monorepo-foundation-governance "file://$PWD" "$C/lyra-clone" && cd "$C/lyra-clone" && pnpm install --frozen-lockfile --silent && pnpm run build && pnpm run test && pnpm run typecheck && pnpm exec changeset status</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm run lint` exits 0 in the working repo
    - Any formatting commit touched only files reported by `pnpm exec prettier --list-different .`, all of which are declared in this plan's files_modified
    - In a fresh `git clone` of the phase branch: `pnpm install --frozen-lockfile`, `pnpm run build`, `pnpm run test`, `pnpm run typecheck`, and `pnpm exec changeset status` all exit 0
    - `git status --porcelain` is empty after any formatting commit (nothing uncommitted)
  </acceptance_criteria>
  <done>Phase success criterion 5 is proven from a literal clean clone; the tree is lint-clean for the PR's CI run.</done>
</task>

<task type="auto">
  <name>Task 2: Open the scaffold PR, watch CI green, merge (first CI proof)</name>
  <files>(remote platform state only — no local files)</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (Pattern 3 step 5 and Assumption A2: same-repo PR-branch workflows run on the pull_request event)
  </read_first>
  <action>
    1. Push the phase branch: `git push -u origin gsd/phase-1-monorepo-foundation-governance`.
    2. Open the PR: `gh pr create --base main --head gsd/phase-1-monorepo-foundation-governance --title "chore(01): monorepo foundation and governance scaffold" --body` summarizing: workspace + changesets lockstep (D-06), governance docs (OSS-01/02/05), CI with frozen job names (OSS-03), and noting required checks are added post-merge (two-step bootstrap).
    3. Watch checks: `gh pr checks --watch` (poll fallback: `gh pr checks` every 30s). All four contexts lint/typecheck/test/build must report success. If a check fails, fix on the branch, push, and re-watch — do not merge red (D-07). If NO workflow runs at all, research Assumption A2 was wrong for this repo state: push a trivial workflow-touching commit to the branch and re-check; if still nothing, surface the blocker instead of merging.
    4. Merge with a merge commit (NOT squash — squash would strand the still-active phase branch's history): `gh pr merge --merge`. Do not delete the branch — the remaining phase docs (summaries, verification) continue on it and land via the phase-end /gsd-ship PR.
    5. `git fetch origin main` so the local view of main is current.
    6. Post-merge UI spot-check for the phase record (round-2 review — closes the 01-04 issue-forms rendering loop): record in the SUMMARY that /gsd-verify-work must visit https://github.com/lyra-ds/lyra/issues/new/choose and confirm both issue forms (bug report, feature request) render and the blank-issue option is absent. Rendering is only provable in the GitHub UI once the templates exist on the default branch — no API exposes it, so this is a listed manual-only verification (01-VALIDATION.md).
  </action>
  <verify>
    <automated>PR=$(gh pr list --repo lyra-ds/lyra --state merged --head gsd/phase-1-monorepo-foundation-governance --json number --jq '.[0].number') && test -n "$PR" && gh pr checks "$PR" --repo lyra-ds/lyra --json name,bucket --jq '[.[] | select(.bucket=="pass") | .name] | sort | join(",")' | grep -qx 'build,lint,test,typecheck'</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr list --state merged --head gsd/phase-1-monorepo-foundation-governance` returns the scaffold PR number
    - `gh pr checks <PR> --json name,bucket` reports the exact pass set: the names with `bucket == "pass"`, sorted, are `build,lint,test,typecheck` (structural JSON assertion, not a grep of human-formatted CLI text — round-2 review)
    - The SUMMARY records the /gsd-verify-work manual spot-check: both issue forms render at https://github.com/lyra-ds/lyra/issues/new/choose with no blank-issue option
    - `git ls-remote origin main` SHA differs from the pre-PR main SHA (merge landed) and `gh api repos/lyra-ds/lyra/contents/pnpm-workspace.yaml --jq .name` returns `pnpm-workspace.yaml` (scaffold visible on main)
  </acceptance_criteria>
  <done>The first PR exercised the full D-07 flow: branch -> PR -> visible green CI -> merge.</done>
</task>

<task type="auto">
  <name>Task 3: Add the four required status checks to the ruleset (protection complete)</name>
  <files>(remote platform state only — no local files)</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (Code Examples: "Ruleset via gh api" — full payload including the required_status_checks rule; Assumption A4 on payload shape)
  </read_first>
  <action>
    Research Pattern 3 step 6 — now that CI has reported once, the checks can be required without deadlocking PRs:

    1. Get the ruleset id BY NAME (never positionally — review finding): `gh api repos/lyra-ds/lyra/rulesets --jq '.[] | select(.name=="main-protection") | .id'`. Plan 01-01 guarantees the name `main-protection`; if the result is empty, stop and surface the mismatch instead of guessing.
    2. Update it with an explicitly constructed request body — never round-trip the raw GET response, which carries read-only fields (`id`, `source`, `_links`, timestamps) that make a blind PUT likely to 422 (round-2 review). Build the body from ONLY the writable schema fields, appending the new rule, using gh's built-in jq: `BODY=$(mktemp) && gh api repos/lyra-ds/lyra/rulesets/$ID --jq '{name, target, enforcement, bypass_actors: (.bypass_actors // []), conditions, rules: (.rules + [{type: "required_status_checks", parameters: {strict_required_status_checks_policy: false, required_status_checks: [{context: "lint"}, {context: "typecheck"}, {context: "test"}, {context: "build"}]}}])}' > "$BODY"`. Inspect `$BODY` before sending: it must contain the four contexts AND the pre-existing pull_request/deletion/non_fast_forward rules, and no `id`/`source`/`_links` keys. Then `gh api -X PUT repos/lyra-ds/lyra/rulesets/$ID --input "$BODY"`. Contexts match the CI job names character-for-character (Pitfall 5). If the PUT still rejects the payload shape (Assumption A4), adjust per the live error or apply the same change in the web UI (Settings -> Rules) and verify via API afterwards.
    3. Verify end-state: `gh api "repos/lyra-ds/lyra/rules/branches/main"` must now list all four rule types including required_status_checks with the four contexts.
    4. Live-blocking note for the phase record: the next PR from this phase branch (the /gsd-ship docs PR at phase end) is the natural live proof that merges block until the four checks pass — record this expectation in the SUMMARY so /gsd-verify-work checks it.
  </action>
  <verify>
    <automated>gh api "repos/lyra-ds/lyra/rules/branches/main" --jq '[.[] | select(.type=="required_status_checks")] | .[0].parameters.required_status_checks | [.[].context] | sort | join(",")' | grep -qx 'build,lint,test,typecheck'</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/lyra-ds/lyra/rulesets --jq '.[] | select(.name=="main-protection") | .id'` returns a non-empty id (selection by name, not position)
    - The PUT body was constructed with only the writable schema fields (name, target, enforcement, bypass_actors, conditions, rules) — no read-only GET fields round-tripped (round-2 review)
    - `gh api "repos/lyra-ds/lyra/rules/branches/main"` lists rule types including `pull_request` and `required_status_checks`
    - The required contexts, sorted, are exactly `build,lint,test,typecheck` — no more, no fewer
  </acceptance_criteria>
  <done>Base-CI end-state of OSS-03: every future PR must run visible CI and cannot merge until lint, typecheck, test, and build pass. The publint/attw/size-limit/parity contexts are intentionally absent — they activate as steps inside these jobs in Phases 2-4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase branch -> main | The merge is the moment scaffold content becomes the protected public truth |
| gh CLI (authenticated) -> repo settings | Ruleset mutation is a privileged platform operation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-03 | Tampering | main branch integrity | high | mitigate | Ruleset completed with the four required checks; force-push/deletion already blocked (plan 01-01) |
| T-1-13 | Denial of Service | required checks referencing wrong context names (self-inflicted merge deadlock) | medium | mitigate | Contexts copied verbatim from job names; Task 3 verify asserts the exact sorted set; recovery path = ruleset edit via gh api/UI |
| T-1-14 | Repudiation | unreviewable merge history | low | accept | Solo maintainer, 0 required approvals by design; PR history itself is the public build log (D-07) |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs from the committed lockfile only (--frozen-lockfile); no new packages; legitimacy gate lives in plan 01-02 |
</threat_model>

<verification>
- Scaffold PR merged with four green checks; main shows the scaffold files
- Ruleset requires exactly build,lint,test,typecheck; PR rule still active
- Clean-clone gate passed (install/build/test/changeset status)
</verification>

<success_criteria>
- Base-CI half of OSS-03 TRUE: visible CI on every PR, enforced by the four required checks, with future-gate hooks in place. OSS-03 as a whole remains PARTIAL until Phases 2-4 activate publint/attw, size-limit, and parity — the SUMMARY and any requirement checkoff MUST say "partial", never "complete" (review reconciliation)
- Phase success criteria 3 and 5 satisfied and evidenced; both prohibitions hold
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-foundation-governance/01-05-SUMMARY.md` when done (committed on the phase branch — it rides the phase-end /gsd-ship PR, which doubles as the live proof that required checks block merges).
</output>
