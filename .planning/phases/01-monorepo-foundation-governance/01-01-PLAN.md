---
phase: 01-monorepo-foundation-governance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/config.json
autonomous: false
requirements: [OSS-03]
user_setup:
  - service: github
    why: "GitHub organization creation has no self-serve API — the user must create the org in the web UI (D-01)"
    dashboard_config:
      - task: "Create free organization named 'lyra-ds'"
        location: "github.com -> top-right + menu -> New organization -> Free plan"
      - task: "Set org short description (copy from handoff/assets/github/org-profile.md)"
        location: "github.com/lyra-ds -> Settings -> Profile"

must_haves:
  truths:
    - "Remote repository lyra-ds/lyra exists on GitHub and contains the full local main history (git ls-remote --heads origin main returns the local main SHA)"
    - "A ruleset on main requires a pull request and blocks force-push and deletion, with NO required status checks yet (two-step bootstrap, research Pattern 3)"
    - "Local work continues on branch gsd/phase-1-monorepo-foundation-governance and .planning/config.json declares the phase branching strategy (D-08)"
    - "Private vulnerability reporting is enabled on lyra-ds/lyra (gh api repos/lyra-ds/lyra/private-vulnerability-reporting returns enabled: true), so the advisory URL published in SECURITY.md by plan 01-03 is actually usable"
    - "The pre-publication disclosure audit ran clean before the first public push (no sensitive filenames tracked anywhere in history, no secret-pattern hits outside .planning/)"
  artifacts:
    - path: ".planning/config.json"
      provides: "GSD branching strategy reconciled with branch protection (D-08)"
      contains: '"branching_strategy": "phase"'
  key_links:
    - from: "local git repo"
      to: "github.com/lyra-ds/lyra"
      via: "git remote 'origin' pointing at the new repo, main pushed with -u"
      pattern: "lyra-ds/lyra"
    - from: "GitHub ruleset on main"
      to: "all future phase work"
      via: "pull_request rule forces branch -> PR flow (D-07)"
      pattern: "pull_request"
  prohibitions:
    - statement: "Never enable required status checks in this plan — a required check that has never reported leaves every PR stuck at 'Expected'; checks become required only in plan 01-05 after the first CI run (research Pattern 3 / Assumption A3)"
      status: flagged
      verification: unverified
    - statement: "No commit may land on local main after the ruleset is active — all subsequent phase commits go to the phase branch (D-07, D-08, research Pitfall 3)"
      status: flagged
      verification: unverified
---

<objective>
Bootstrap the GitHub remote: user creates the `lyra-ds` org (D-01, guided), Claude creates the `lyra-ds/lyra` repo (D-02), pushes the existing main history, enables PR-required branch protection WITHOUT required status checks (D-07, two-step bootstrap), and flips GSD to phase-branch mode (D-08) so no commit is ever stranded on protected main.

Org branding (avatar, profile README, social preview) is explicitly NOT done here — that is Phase 7 scope per D-03. Only the org, its short description, the repo, the push, and the ruleset.

Purpose: Everything after this plan must ride the branch -> PR -> CI flow ("PRs from day one", D-07). This plan creates the platform state that makes that flow possible and safe, before any scaffold files exist, so the entire Phase-1 scaffold lands as the repo's first PR (research Pattern 3).
Output: Live `lyra-ds/lyra` repo with protected main, local phase branch checked out, GSD config flipped to `phase` branching.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md
</context>

## Artifacts this phase produces (this plan's share)

- GitHub org `lyra-ds` (user-created, D-01)
- GitHub repo `lyra-ds/lyra` (public, empty at creation, then receives main history)
- GitHub ruleset named exactly `main-protection` on the default branch (pull_request + deletion + non_fast_forward rules; required checks arrive in plan 01-05, which selects this ruleset BY NAME)
- Repo setting: private vulnerability reporting enabled (consumed by plan 01-03's SECURITY.md)
- Git branch `gsd/phase-1-monorepo-foundation-governance` (local + later pushed by plan 01-05)
- Modified file: `.planning/config.json` (git.branching_strategy: "none" -> "phase")
- Recorded decisions in `01-01-SUMMARY.md`: confirmed LICENSE copyright holder line + public enforcement/security contact email (consumed by plan 01-03)

## Review findings addressed (Codex review, 01-REVIEWS.md)

- **HIGH — clean-tree precondition fails (untracked 01-PATTERNS.md):** RESOLVED — the pattern map was committed in 75fa729; Task 2's precondition additionally hardened: uncommitted GSD planning artifacts are committed (they are intentionally public history), not treated as a hard abort.
- **MEDIUM — no pre-publication disclosure audit:** INCORPORATED — Task 2 step 4 adds a history-wide sensitive-filename scan and secret-pattern scan that must run clean BEFORE the first push; any hit stops execution and raises a checkpoint.
- **MEDIUM — LICENSE copyright holder inferred / private vulnerability reporting never enabled:** INCORPORATED — Task 1's checkpoint now also confirms the legal copyright line and enforcement contact (recorded in 01-01-SUMMARY.md for plan 01-03); Task 2 step 7 enables private vulnerability reporting via the API and verifies it.
- **LOW — ambiguous commit-count criterion in Task 3:** INCORPORATED — replaced with a path-based assertion (only `.planning/config.json` may differ from main outside `.planning/phases/`).

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User creates the GitHub org lyra-ds (D-01)</name>
  <files>(none — human web-UI step; GitHub org creation has no self-serve API)</files>
  <read_first>handoff/assets/github/org-profile.md</read_first>
  <action>Pause execution and present the numbered steps in how-to-verify to the user verbatim, including the short description source file. Do NOT attempt to create the org via gh/API (research Assumption A1: web-UI-only). Resume only on the user's signal, then hand off to Task 2 which verifies the org via API before proceeding.</action>
  <what-built>Nothing yet — this step has no API. GitHub organization creation is web-UI-only (research Assumption A1), and D-01 locks the org handle to `lyra-ds` (availability verified 2026-07-16; `lyra` and `lyra-ui` are taken).</what-built>
  <how-to-verify>
    Do these steps in the browser (2-3 minutes):
    1. Go to https://github.com/account/organizations/new — choose the Free plan.
    2. Organization account name: exactly `lyra-ds` (matches the npm scope 1:1 per D-01).
    3. Contact email: your email. Belongs to: personal account.
    4. After creation, open https://github.com/organizations/lyra-ds/settings/profile and paste the short description from `handoff/assets/github/org-profile.md` (the one-line org description — D-03 allows reusing the short description now; avatar/profile README/social preview stay in Phase 7).
    5. Recommended (research security domain, ASVS V2): in org Settings -> Authentication security, enable "Require two-factor authentication".
    Do NOT create any repository yet — Claude does that in Task 2.
    6. Confirm two legal/contact values that plan 01-03 will write into public governance files (legal identity must not be inferred — your git author name and your legal name may differ):
       a. The exact LICENSE copyright line. Proposed default: `Copyright (c) 2026 Francisco Soares de Oliveira and Lyra DS contributors` — reply with the corrected legal name if this is wrong.
       b. The public enforcement/security contact email for CODE_OF_CONDUCT.md and SECURITY.md. Proposed default: `franciscpd@gmail.com`.
  </how-to-verify>
  <acceptance_criteria>
    - `gh api orgs/lyra-ds --jq .login` prints `lyra-ds` (checked by Task 2 before proceeding)
    - The user explicitly confirmed (or corrected) the copyright line and contact email; the confirmed values are recorded under a "Recorded decisions" heading in 01-01-SUMMARY.md so plan 01-03 can consume them without guessing
  </acceptance_criteria>
  <verify>
    <automated>gh api orgs/lyra-ds --jq .login | grep -qx 'lyra-ds'</automated>
  </verify>
  <done>The GitHub org `lyra-ds` exists with its short description set; copyright line and enforcement contact are confirmed and will be recorded in 01-01-SUMMARY.md; user has resumed execution.</done>
  <resume-signal>Type "done" to accept the proposed copyright line and contact email as-is, or paste corrections (and any error you hit)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create lyra-ds/lyra, push main history, enable PR-only ruleset (D-02, D-07)</name>
  <files>(remote platform state only — no local files)</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (Pattern 3 sequencing, ruleset JSON at "Ruleset via gh api")
    - handoff/assets/github/org-profile.md (repo description copy)
  </read_first>
  <action>
    Implements D-02 (repo is `lyra-ds/lyra`) and D-07 (PRs from day one) via research Pattern 3 steps 2-3.

    1. Verify the org exists: `gh api orgs/lyra-ds --jq .login` must print `lyra-ds`. If it 404s, stop and re-raise the Task 1 checkpoint.
    2. Confirm the working tree is clean: `git status --porcelain` must be empty. If uncommitted or untracked GSD planning artifacts exist (anything under `.planning/`), commit them first with a `docs(01):` conventional commit — they are part of the intentionally public history (CONTEXT.md) — then re-check. Only non-planning untracked files are a hard stop. Note `git rev-parse main` as the expected clean SHA.
    3. Create the repo EMPTY — no auto-init, no license flag (LICENSE is authored by plan 01-03): `gh repo create lyra-ds/lyra --public --description "<one-line description adapted from org-profile.md>"`. Do not pass any flag that creates a remote commit (no --add-readme, no --license, no --gitignore).
    4. Pre-publication disclosure audit — the entire local history becomes public on push, so this gate runs BEFORE the first push (review finding, 01-REVIEWS.md):
       a. Sensitive filename scan across all history: `git log --all --diff-filter=A --name-only --pretty=format: | sort -u | grep -Ei '\.(pem|key|p12|pfx)$|id_rsa|id_ed25519|\.env$|\.env\.|credentials|secret'` must print nothing (exit 1).
       b. Secret-pattern scan across all revisions, excluding `.planning/` (planning docs mention token *formats*, which would self-match): `git grep -IE '(AKIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{36}|github_pat_[A-Za-z0-9_]{22,}|xox[baprs]-[A-Za-z0-9-]{10,}|-----BEGIN (RSA |EC |OPENSSH |PGP )?PRIVATE KEY|sk-[A-Za-z0-9]{32,})' $(git rev-list --all) -- ':(exclude).planning' ':(exclude)node_modules'` must print nothing (exit 1). If `gitleaks` happens to be installed, `gitleaks detect --log-opts=--all` is an acceptable stronger substitute.
       c. If EITHER scan hits: STOP — do not push. Raise a blocking checkpoint listing the hits so the user decides (scrub history vs. accept).
    5. Add the remote and push the existing history: `git remote add origin git@github.com:lyra-ds/lyra.git` (fall back to the https URL if ssh fails), then `git push -u origin main`. This publishes the pre-existing `.planning/` + `handoff/` + `.claude/` history, which CONTEXT.md confirms is intended.
    6. Create the ruleset via `gh api repos/lyra-ds/lyra/rulesets -X POST` using the JSON from RESEARCH.md "Ruleset via gh api" but WITHOUT the required_status_checks rule (that rule is added by plan 01-05 after CI has reported once). The ruleset `name` MUST be exactly `main-protection` — plan 01-05 selects it by this name, never positionally. Include: target branch `~DEFAULT_BRANCH`, enforcement active, rules: deletion, non_fast_forward, pull_request with required_approving_review_count 0 (solo maintainer merges own PRs after green CI; raise later). The endpoint payload shape is research Assumption A4 — if the POST 400s, adjust field names per the live error message or fall back to creating the same ruleset in the web UI (Settings -> Rules -> Rulesets) and verify via API afterwards.
    7. Enable private vulnerability reporting so the advisory URL plan 01-03 writes into SECURITY.md actually works (review finding: the feature is off by default and the `/security/advisories/new` path is unusable until the owner enables it): `gh api -X PUT repos/lyra-ds/lyra/private-vulnerability-reporting`. Verify with the GET below; if the endpoint 404s on this plan/repo type (research Assumption A6), enable it in the web UI (Settings -> Advanced Security / Code security -> Private vulnerability reporting) and re-verify.
  </action>
  <verify>
    <automated>gh api orgs/lyra-ds --jq .login | grep -qx 'lyra-ds' && git ls-remote --heads origin main | grep -q "$(git rev-parse main)" && gh api repos/lyra-ds/lyra/rulesets --jq '.[].name' | grep -qx 'main-protection' && gh api repos/lyra-ds/lyra/private-vulnerability-reporting --jq .enabled | grep -qx 'true'</automated>
  </verify>
  <acceptance_criteria>
    - `gh repo view lyra-ds/lyra --json visibility --jq .visibility` prints `PUBLIC`
    - `git ls-remote --heads origin main` returns the same SHA as `git rev-parse main`
    - Both disclosure-audit scans (step 4a filename scan, step 4b secret-pattern scan) exited non-zero (no matches) before the push, and this is stated in the SUMMARY
    - `gh api repos/lyra-ds/lyra/rulesets --jq '.[].name'` output includes exactly `main-protection`
    - `gh api "repos/lyra-ds/lyra/rules/branches/main" --jq '[.[].type] | sort | join(",")'` output includes `deletion`, `non_fast_forward`, and `pull_request` and does NOT include `required_status_checks`
    - `gh api repos/lyra-ds/lyra/private-vulnerability-reporting --jq .enabled` prints `true`
  </acceptance_criteria>
  <done>Repo lyra-ds/lyra is public with the full main history pushed after a clean disclosure audit; main accepts changes only via PR; private vulnerability reporting is enabled; no required status checks exist yet.</done>
</task>

<task type="auto">
  <name>Task 3: Create phase branch and flip GSD branching to "phase" (D-08)</name>
  <files>.planning/config.json</files>
  <read_first>
    - .planning/config.json (current state — git block is around lines 13-19; only the branching_strategy value changes)
    - .planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md (section ".planning/config.json (MODIFY existing) — D-08" shows the exact current block)
  </read_first>
  <action>
    Implements D-08 (reconcile GSD commit flow with branch protection) per research Pattern 4. Timing is load-bearing: this happens AFTER the push (Task 2) and BEFORE any further file-creating commits, or GSD strands commits on protected local main (research Pitfall 3).

    1. Create and switch to the phase branch: `git checkout -b gsd/phase-1-monorepo-foundation-governance` (matches the already-configured template `gsd/phase-{phase}-{slug}`).
    2. Edit `.planning/config.json`: change the value of `git.branching_strategy` from `"none"` to `"phase"`. Change nothing else — `phase_branch_template` is already correct.
    3. Commit this single-file change on the phase branch with a conventional message, e.g. `chore(01): switch GSD branching to phase for protected main`.
    All remaining Phase-1 work (plans 01-02 .. 01-05, summaries, verification docs) accumulates on this branch and lands via PR.
  </action>
  <verify>
    <automated>test "$(git branch --show-current)" = "gsd/phase-1-monorepo-foundation-governance" && grep -q '"branching_strategy": "phase"' .planning/config.json</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` prints `gsd/phase-1-monorepo-foundation-governance`
    - `.planning/config.json` contains `"branching_strategy": "phase"`
    - Path-based branch-content check (replaces the ambiguous commit-count criterion per review): `git diff main..HEAD --name-only -- ':(exclude).planning/phases'` prints exactly one path, `.planning/config.json` — i.e. besides GSD phase docs (summaries/metadata under .planning/phases/), only the config flip differs from main
  </acceptance_criteria>
  <done>Phase branch is the active branch; GSD config commits to phase branches from now on; local main is untouched after the push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> GitHub remote | Repo history becomes public; platform rules now gate what lands on main |
| GitHub UI (human) -> org settings | Org creation/config done by the user outside Claude's control |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-03 | Tampering | main branch on lyra-ds/lyra | high | mitigate | Ruleset: require PR, block force-push (non_fast_forward) and deletion; no bypass actors configured in Phase 1 |
| T-1-07 | Spoofing | org handle `lyra-ds` | low | accept | Handle availability verified 2026-07-16 and claimed immediately by the user in Task 1; npm scope squat risk handled at Phase 7 publish |
| T-1-08 | Elevation | org member permissions | medium | mitigate | Solo-maintainer org; guided step recommends enabling the org-wide 2FA requirement (ASVS V2) |
| T-1-15 | Information Disclosure | full local history becomes public on first push (secrets, private data, unintended assets) | high | mitigate | Task 2 step 4 pre-publication disclosure audit: history-wide sensitive-filename scan + secret-pattern scan, hard stop + checkpoint on any hit; CONTEXT.md confirms `.planning/`+`handoff/` publication intent |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this plan; slopcheck + blocking human checkpoint enforced in plan 01-02 where installs occur |
</threat_model>

<verification>
- Org exists and repo `lyra-ds/lyra` is public with main history pushed after a clean disclosure audit
- `gh api "repos/lyra-ds/lyra/rules/branches/main"` shows pull_request + deletion + non_fast_forward active, and NO required_status_checks rule; the ruleset is named exactly `main-protection`
- `gh api repos/lyra-ds/lyra/private-vulnerability-reporting --jq .enabled` prints `true`
- Current branch is `gsd/phase-1-monorepo-foundation-governance`; `.planning/config.json` has branching_strategy `phase`
- 01-01-SUMMARY.md records the confirmed copyright line and enforcement contact for plan 01-03
</verification>

<success_criteria>
- All three tasks done; main is protected PR-only; the two prohibitions hold (no required checks yet, nothing new on local main)
- Wave 2 plans can start committing scaffold files to the phase branch immediately
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-foundation-governance/01-01-SUMMARY.md` when done (committed on the phase branch).
</output>
