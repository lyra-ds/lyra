---
phase: 01-monorepo-foundation-governance
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - LICENSE
  - CODE_OF_CONDUCT.md
  - SECURITY.md
  - CONTRIBUTING.md
  - VERSIONING.md
  - README.md
  - README.pt-BR.md
autonomous: true
requirements: [OSS-01, OSS-02, OSS-05]

must_haves:
  truths:
    - "README.md is non-empty and leads with the CSS-first pitch, the white-label-in-4-tokens story, and the adapter/registry roadmap, plus install instructions and a minimal usage snippet (OSS-01; covers the empty-input edge: required sections are grep-asserted, not assumed)"
    - "README.pt-BR.md is a valid UTF-8 pt-BR mirror of README.md and the two files cross-link at the top — 'Português' badge/link in README.md, 'English' link in README.pt-BR.md (D-05; covers the encoding edge: byte-level UTF-8 validity is asserted with iconv)"
    - "VERSIONING.md states that 0.MINOR = breaking and declares the API surface as component props, .lyra-* class names, design token names, and package export paths (OSS-05)"
    - "LICENSE is verbatim MIT with a filled copyright line and CODE_OF_CONDUCT.md is Contributor Covenant 3.0 with a filled enforcement contact (OSS-02)"
    - "CONTRIBUTING.md documents setup (Node 24, pnpm), the pre-1.0 changeset convention (breaking = minor), the locked-decisions list, the dependency policy, and conventional commits"
  artifacts:
    - path: "LICENSE"
      provides: "Canonical MIT text (GitHub license detection requires verbatim text)"
      contains: "MIT License"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant 3.0 with real enforcement contact"
      contains: "Contributor Covenant"
    - path: "CONTRIBUTING.md"
      provides: "Setup, changeset flow, locked decisions, dependency policy"
      contains: "changeset"
    - path: "VERSIONING.md"
      provides: "Written 0.x policy with the declared API surface (OSS-05)"
      contains: "0.MINOR"
    - path: "README.md"
      provides: "Repo landing page: pitch + install + snippet (OSS-01)"
      contains: "@lyra-ds/styles"
    - path: "README.pt-BR.md"
      provides: "1:1 pt-BR mirror of README (D-05)"
      contains: "@lyra-ds/styles"
    - path: "SECURITY.md"
      provides: "Vulnerability reporting policy (private reporting + email fallback)"
      contains: "vulnerability"
  key_links:
    - from: "README.md"
      to: "README.pt-BR.md"
      via: "prominent language link at the top of each file (D-05)"
      pattern: "README\\.pt-BR\\.md"
    - from: "README.md"
      to: "VERSIONING.md"
      via: "versioning-policy link so consumers (not just contributors) find the 0.x rules"
      pattern: "VERSIONING\\.md"
    - from: "CONTRIBUTING.md"
      to: "VERSIONING.md"
      via: "pre-1.0 changeset convention references the written policy (research anti-pattern: policy must not live only in CONTRIBUTING)"
      pattern: "VERSIONING\\.md"
  prohibitions:
    - statement: "README must not present the npm install instructions as currently working without the pre-release status warning — the packages are unpublished until Phase 7 and misleading install docs would burn exactly the audience this phase targets"
      status: flagged
      verification: unverified
    - statement: "No governance file may ship with an unfilled bracketed template field (copyright holder in LICENSE, enforcement contact in the Code of Conduct) — empty placeholders read as negligence (research anti-pattern)"
      status: flagged
      verification: unverified
    - statement: "No governance doc other than README gets a pt-BR mirror — CONTRIBUTING, Code of Conduct, SECURITY, VERSIONING, and templates are EN-only (D-04, D-05)"
      status: flagged
      verification: unverified
---

<objective>
Author the complete governance and policy surface in English (D-04): MIT LICENSE, Contributor Covenant 3.0, SECURITY.md, CONTRIBUTING.md, VERSIONING.md (the OSS-05 0.x policy), and the README pair (EN canonical + pt-BR mirror per D-05) carrying the OSS-01 pitch.

Purpose: OSS-01, OSS-02, and OSS-05 are satisfied almost entirely by this plan; these files are what a dev landing on github.com/lyra-ds/lyra actually reads to judge credibility.
Output: Seven governance/policy files, all with filled fields, canonical texts, and cross-links.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md
@handoff/assets/github/org-profile.md
</context>

## Artifacts this phase produces (this plan's share)

New file paths: `LICENSE`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `CONTRIBUTING.md`, `VERSIONING.md`, `README.md`, `README.pt-BR.md`. No code symbols — all human-read governance documents.

**Flagged assumption (spec-less edge probe, OSS-02/unclassified):** the OSS-02 edge probe could not be auto-classified. This plan interprets OSS-02 acceptance as: file presence + canonical external texts (never paraphrased) + zero unfilled template fields. If the verifier needs a different interpretation, it must escalate rather than silently pass.

<tasks>

<task type="auto">
  <name>Task 1: LICENSE, Code of Conduct, and SECURITY.md (canonical texts, filled fields)</name>
  <files>LICENSE, CODE_OF_CONDUCT.md, SECURITY.md</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md (section "LICENSE + CODE_OF_CONDUCT.md" and the SECURITY.md discretion anchor)
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (section "Don't Hand-Roll" — canonical sources; Assumption A6 on private vulnerability reporting)
  </read_first>
  <action>
    Never paraphrase canonical texts (research Don't Hand-Roll):

    1. `LICENSE`: verbatim MIT text (SPDX), copyright line filled as `Copyright (c) 2026 Francisco Soares de Oliveira and Lyra DS contributors`. GitHub license detection requires the canonical wording.
    2. `CODE_OF_CONDUCT.md`: Contributor Covenant 3.0 verbatim (current since 2025-07-28, adopted by changesets itself — do NOT use 2.1). Fill the enforcement/reporting contact with `franciscpd@gmail.com`. Replace every bracketed template field — none may survive into the committed file.
    3. `SECURITY.md`: short policy — supported versions (0.x, latest minor only), report privately via GitHub private vulnerability reporting (`https://github.com/lyra-ds/lyra/security/advisories/new`) with `franciscpd@gmail.com` as email fallback (research Assumption A6: if private reporting is unavailable on the repo plan, the email path still works), expected response window (e.g. 7 days), and a no-public-disclosure-before-fix request.
    All three files in English (D-04). Write prettier-friendly markdown (plan 01-05 runs the whole-tree format reconciliation).
  </action>
  <verify>
    <automated>test -f LICENSE && test -f CODE_OF_CONDUCT.md && test -f SECURITY.md && grep -q "Copyright (c) 2026" LICENSE && grep -qi "Contributor Covenant" CODE_OF_CONDUCT.md && grep -q "franciscpd@gmail.com" CODE_OF_CONDUCT.md && grep -Eic '\[(INSERT|year|fullname|email)' LICENSE CODE_OF_CONDUCT.md | grep -qv ':[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Permission is hereby granted, free of charge" LICENSE` returns 1 (canonical MIT body)
    - `CODE_OF_CONDUCT.md` contains `Contributor Covenant` and `franciscpd@gmail.com`
    - `grep -Ec '\[(INSERT|year|fullname|email)' LICENSE` returns 0 and the same grep on `CODE_OF_CONDUCT.md` returns 0 (no surviving bracketed template fields)
    - `SECURITY.md` contains `security/advisories/new` and `franciscpd@gmail.com`
  </acceptance_criteria>
  <done>All three files exist with canonical texts and zero unfilled fields.</done>
</task>

<task type="auto">
  <name>Task 2: CONTRIBUTING.md and VERSIONING.md (OSS-05 policy)</name>
  <files>CONTRIBUTING.md, VERSIONING.md</files>
  <read_first>
    - .planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md (sections "VERSIONING.md" — ready content outline — and "CONTRIBUTING.md")
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (Pitfall 7 — changesets has no 0.x mode; a major changeset on 0.1.0 produces 1.0.0)
    - .claude/CLAUDE.md (Constraints section — source for the locked-decisions list)
  </read_first>
  <action>
    1. `VERSIONING.md` from the PATTERNS.md outline (OSS-05, D-06). Must state: (a) `@lyra-ds/styles` and `@lyra-ds/react` version in lockstep — one "Lyra 0.x" version (D-06); (b) while 0.x, **0.MINOR = breaking** and 0.x.PATCH = additive or fix, with `major` changesets reserved for the deliberate 1.0 release (Pitfall 7: this is convention, not tooling — changesets applies plain semver); (c) the declared public API surface: 1. component props (published .d.ts contracts), 2. `.lyra-*` class names, 3. design token names (CSS custom properties), 4. package export paths (`@lyra-ds/styles`, `./tokens/*`, component entry points); (d) explicitly NOT API: internal DOM structure, undocumented classes, dist file layout; (e) every breaking change ships a migration note in the changelog.
    2. `CONTRIBUTING.md` per the PATTERNS.md five-point content plan: setup (Node 24 via `.nvmrc`, pnpm >= 11.13 via `corepack enable` OR standalone install — corepack no longer ships with newer Node lines, so document both paths; `pnpm install`; the root scripts); changeset required on package-touching PRs with the pre-1.0 convention (breaking = `minor`) linking to VERSIONING.md; a **Locked decisions** section that prevents re-litigation (CSS-first architecture, no Tailwind in core, Lyra naming canonical with compat-shadcn.css opt-in outside the default entry, entry keyframes animate transform only, no runtime CDN dependencies); dependency policy (new runtime deps need maintainer approval; `@lyra-ds/styles` stays zero-dependency); conventional commits (existing repo convention).
    Both files EN-only (D-04). Both link to each other where the changeset convention is described.
  </action>
  <verify>
    <automated>grep -q "0.MINOR" VERSIONING.md && grep -q "lyra-" VERSIONING.md && grep -qi "token" VERSIONING.md && grep -qi "lockstep" VERSIONING.md && grep -qi "changeset" CONTRIBUTING.md && grep -q "VERSIONING.md" CONTRIBUTING.md && grep -qi "tailwind" CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    - `VERSIONING.md` contains `0.MINOR`, `.lyra-`, a token-names mention, and a lockstep statement covering both package names
    - `VERSIONING.md` states that `major` changesets are reserved for the 1.0 release
    - `CONTRIBUTING.md` contains a "Locked decisions" heading, mentions the no-Tailwind rule, links to `VERSIONING.md`, and documents `pnpm install` + Node 24 setup
  </acceptance_criteria>
  <done>The 0.x policy is a written, consumer-facing document declaring the broad API surface; CONTRIBUTING wires contributors into it.</done>
</task>

<task type="auto">
  <name>Task 3: README.md (EN, OSS-01) + README.pt-BR.md mirror (D-05)</name>
  <files>README.md, README.pt-BR.md</files>
  <read_first>
    - handoff/assets/github/org-profile.md (canonical pitch copy: tagline, pitch bullets, package table)
    - .planning/phases/01-monorepo-foundation-governance/01-PATTERNS.md (sections "README.md" and "README.pt-BR.md" — structure, copywriting rules: sentence case, no emoji, drop the optional emoji bullets)
    - .planning/phases/01-monorepo-foundation-governance/01-RESEARCH.md (README.md skeleton in Code Examples)
  </read_first>
  <action>
    1. `README.md` (EN, canonical per D-04) following the RESEARCH.md skeleton with copy adapted from org-profile.md:
       - Top line: title, then a prominent `[Português (pt-BR)](./README.pt-BR.md)` link (D-05), MIT badge, and CI badge (`https://github.com/lyra-ds/lyra/actions/workflows/ci.yml/badge.svg` — turns green once plan 01-05 merges the scaffold PR).
       - Tagline: "Open source, CSS-first design system. Semantic tokens, white-label theming, thin React wrappers — Vue, Svelte & Web Components on the same CSS core."
       - Pitch bullets adapted from org-profile.md WITHOUT emoji (Lyra copywriting voice is emoji-free): 209 semantic tokens with built-in light/dark; white-label via the four brand tokens `--brand`, `--brand-contrast`, `--brand-radius`, `--brand-font` with everything derived via color-mix; `@lyra-ds/react` as 40 thin wrappers with Vue/Svelte/Web Components adapters (Zag.js) and a shadcn-style registry on the roadmap; opt-in shadcn compat layer; llms.txt generated from the real .d.ts sources.
       - Package table: `@lyra-ds/styles` (tokens + component CSS, zero JS) and `@lyra-ds/react` (React 18+ components).
       - A clearly marked pre-release status section stating packages are not yet on npm (removed at Phase 7).
       - Installation as documented target state: `npm i @lyra-ds/styles @lyra-ds/react`, then `import '@lyra-ds/styles'` once at app root.
       - Minimal usage snippet: a `<Button variant="primary">` example.
       - Links: Versioning -> VERSIONING.md, Contributing -> CONTRIBUTING.md, License MIT.
    2. `README.pt-BR.md`: faithful 1:1 pt-BR translation of README.md, with an `[English](./README.md)` link at the top. Brazilian Portuguese copywriting: sentence case, no emoji, Brazilian number formats where numbers appear. This is the ONLY mirrored file (D-05).
  </action>
  <verify>
    <automated>test -s README.md && test -s README.pt-BR.md && grep -qi "css-first" README.md && grep -q "@lyra-ds/styles" README.md && grep -q "README.pt-BR.md" README.md && grep -q "README.md" README.pt-BR.md && grep -q "VERSIONING.md" README.md && grep -qi "pre-release" README.md && iconv -f UTF-8 -t UTF-8 README.pt-BR.md > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` is non-empty and contains: `css-first` (case-insensitive), `--brand` (white-label story), an adapters/registry roadmap mention, `npm i @lyra-ds/styles @lyra-ds/react`, and a `<Button` usage snippet
    - `README.md` links to `README.pt-BR.md` in its first 5 lines; `README.pt-BR.md` links back to `README.md`
    - `README.md` contains a pre-release warning section (grep -i "pre-release" matches)
    - `iconv -f UTF-8 -t UTF-8 README.pt-BR.md` exits 0 (valid UTF-8) and the file contains pt-BR content (e.g. grep -q "Instala" README.pt-BR.md)
    - `README.md` links to `VERSIONING.md` and `CONTRIBUTING.md`
  </acceptance_criteria>
  <done>OSS-01 satisfied: pitch, install, snippet, roadmap in EN with a prominent pt-BR mirror.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs -> public readers | Governance docs are the public trust surface; wrong/misleading content damages credibility and invites misuse |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-10 | Repudiation | vulnerability reports lost (no channel) | medium | mitigate | SECURITY.md with private-reporting URL + email fallback and response window |
| T-1-11 | Spoofing | install instructions for unpublished packages (name-squat window until Phase 7 publish) | medium | mitigate | Pre-release warning section in README; Phase 7 publishes under the reserved @lyra-ds scope |
| T-1-12 | Information Disclosure | personal email in public CoC/SECURITY | low | accept | User's email is already public on the GitHub profile; standard OSS practice |
| T-1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs in this plan; enforced in plan 01-02 (legitimacy checkpoint) |
</threat_model>

<verification>
- All seven files exist; canonical texts verbatim; zero bracketed template fields
- README pair cross-linked and UTF-8 valid; VERSIONING.md declares 0.MINOR = breaking + the four-part API surface
</verification>

<success_criteria>
- OSS-01 and OSS-05 fully satisfied; OSS-02 satisfied except issue/PR templates (plan 01-04)
- All three prohibitions hold (pre-release warning present, no unfilled fields, only README mirrored)
</success_criteria>

<output>
Create `.planning/phases/01-monorepo-foundation-governance/01-03-SUMMARY.md` when done (committed on the phase branch).
</output>
