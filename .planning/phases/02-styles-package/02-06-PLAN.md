---
phase: 02-styles-package
plan: 06
type: execute
wave: 4
depends_on: [02-03, 02-04, 02-05]
files_modified:
  - .github/workflows/ci.yml
  - package.json
  - tools/pack-smoke/pack-smoke.mjs
  - tools/pack-smoke/fixture/
autonomous: true
requirements: [STY-01, STY-02, STY-06, STY-07]
user_setup: []
must_haves:
  truths:
    - "The pre-placed CI hooks are filled as STEPS inside the four FROZEN jobs (never new jobs): stylelint in `lint`, Browser Mode test + token/class parity in `test`, publint + packed-artifact smoke test in `build` (02-PATTERNS.md CI section)."
    - "publint passes green against packages/styles, confirming a valid exports map (incl. `./styles.css`) and that `\"sideEffects\": [\"**/*.css\"]` protects the CSS from consumer tree-shaking (STY-07)."
    - "The parity script runs in CI on every PR and fails the build on any token/class drift, selector/file relocation, or external `url(...http...)` (STY-06)."
    - "The Browser Mode test runs in CI AFTER `pnpm exec playwright install chromium --with-deps`, and that install step is ordered BEFORE the root `pnpm run test` command (which recurses into packages/styles via `pnpm -r`) so a fresh runner cannot fail for a missing browser (addresses review concern: Browser-setup-before-tests ordering)."
    - "CI tools are invoked via lockfile-installed `pnpm exec` binaries (publint, playwright), NOT floating `pnpm dlx`/`npx` downloads, backed by exact root devDependencies (addresses review concern: floating tool resolution)."
    - "A packed-artifact smoke test runs a REAL consumer bundler build, not a bare path resolve (addresses review concern: `require.resolve` resolves a `.css` path without loading/bundling it, proving nothing): `pnpm pack` → install the tarball into a throwaway fixture whose entry CSS does `@import \"@lyra-ds/styles\";` + `@import \"@lyra-ds/styles/styles.css\";` + one `@import \"@lyra-ds/styles/tokens/…\";` subpath → run a minimal pinned Vite (`vite@8.1.5`) build → assert the EMITTED/bundled CSS contains a known `.lyra-*` class AND a known token (e.g. `--brand` or a color token). This exercises the packed tarball, the root `@lyra-ds/styles` export, the literal `./styles.css` export, and the relative `@import` chain in one gate (STY-01, STY-02; PITFALLS.md:43 requires proving a bundler accepts the CSS root export)."
    - { statement: "import-safety: publint-green exports map + sideEffects:['**/*.css'] + the packed-artifact smoke test running a REAL Vite consumer build (importing the root `@lyra-ds/styles`, the literal `./styles.css`, and a token subpath, then asserting a `.lyra-*` class + a token in the emitted CSS) together prove the CSS import survives consumer tree-shaking, actually bundles through a real bundler from a real install, and compat stays out of the entry — this CI wiring is the evidence the backstop item required.", verification: covered }
  prohibitions:
    - "MUST NOT add or rename CI jobs — the four job names (lint, typecheck, test, build) are required-status-check contexts; new gates land only as STEPS (02-PATTERNS.md FROZEN CONTRACT)."
    - "MUST NOT wire publint against a `\"sideEffects\": false` package (would defeat STY-07)."
    - "MUST NOT run the Browser Mode test (root `pnpm run test`) before the Chromium install step — the install must precede it."
    - "MUST NOT invoke CI tools via floating `pnpm dlx`/`npx` — use lockfile-installed `pnpm exec` binaries."
  artifacts:
    - .github/workflows/ci.yml
    - tools/pack-smoke/pack-smoke.mjs
    - tools/pack-smoke/fixture/
  key_links:
    - "ci.yml steps → invoke `pnpm --filter @lyra-ds/styles run lint:css` / `pnpm exec playwright install …` / `pnpm run test` / `pnpm run parity` / `pnpm exec publint packages/styles` / `node tools/pack-smoke/pack-smoke.mjs` inside the four frozen jobs; the phase's live quality gates."
---

<objective>
Wire all Phase-2 quality gates into the existing CI workflow as STEPS inside the four frozen jobs, filling the pre-placed hooks: stylelint (lint); Chromium install → Browser Mode test → parity (test, in that order); publint + packed-artifact smoke test (build). Install/pin publint; invoke tools via `pnpm exec` (not `dlx`/`npx`). Confirm every gate runs green on the current package, closing STY-06 (parity in CI), STY-07 (publint green with `sideEffects: ["**/*.css"]`), and validating STY-01/STY-02 resolution from a packed tarball.

Purpose: The gates authored in plans 02-04/02-05 only guard the phase once CI runs them on every PR. This is the integration step that makes the phase's guarantees continuous — and it must order Chromium install before tests, or a clean runner fails deterministically.
Output: updated .github/workflows/ci.yml, publint devDep pinned, tools/pack-smoke/pack-smoke.mjs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-styles-package/02-CONTEXT.md
@.planning/phases/02-styles-package/02-PATTERNS.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire stylelint, parity, publint, and Browser Mode test into the four frozen CI jobs</name>
  <read_first>
    - .github/workflows/ci.yml (the FROZEN header lines 1-8; the three hook comments at the lint job "future hook (Phase 2): stylelint", the test job "future hook (Phase 2, STY-06): token/class parity script", the build job "future hooks (Phases 2-4, OSS-03): publint …"; the existing step style `- run: pnpm run <script>`)
    - 02-PATTERNS.md ".github/workflows/ci.yml" section (add STEPS never jobs; wire as pnpm scripts) + "CI gate wiring convention"
    - packages/styles/package.json scripts (`lint:css`, `test`) + root package.json script (`parity`)
  </read_first>
  <action>Edit .github/workflows/ci.yml IN PLACE, adding steps to the existing four jobs only (no new/renamed jobs). In the `lint` job at the stylelint hook: add `- run: pnpm --filter @lyra-ds/styles run lint:css`. In the `test` job: because root `pnpm run test` is `pnpm -r --if-present run test` and now recurses into packages/styles' Browser Mode `test` script, the Chromium install MUST come FIRST — insert `- run: pnpm exec playwright install chromium --with-deps` BEFORE the existing `- run: pnpm run test` step (do NOT append it after), then add `- run: pnpm run parity` after the test step at the parity hook. In the `build` job at the publint hook: add `- run: pnpm exec publint packages/styles` (lockfile-installed devDep, NOT `pnpm dlx`), then `- run: node tools/pack-smoke/pack-smoke.mjs` (the packed-artifact smoke test authored in Task 2). Add `publint@0.3.21` (EXACT pin — addresses review concern: previously "latest stable"; legitimacy-cleared in 02-04) and `vite@8.1.5` (the pack-smoke consumer-build engine; legitimacy-cleared in 02-04) to root devDependencies (save-exact); use `pnpm exec` for playwright too (playwright is a root devDep from 02-05). Keep the frozen job names and the actionlint step intact.</action>
  <verify>
    <automated>grep -q 'lint:css' .github/workflows/ci.yml && grep -q 'run parity' .github/workflows/ci.yml && grep -q 'pnpm exec publint' .github/workflows/ci.yml && grep -q 'pnpm exec playwright install chromium' .github/workflows/ci.yml && grep -q 'pack-smoke' .github/workflows/ci.yml && ! grep -q 'dlx publint' .github/workflows/ci.yml && awk '/^  test:/{t=1} t&&/playwright install chromium/{pw=NR} t&&/run: pnpm run test/{if(!pw||NR<pw){print "ORDER-FAIL"; exit 1}} /^  build:/{t=0}' .github/workflows/ci.yml && test $(grep -cE '^  (lint|typecheck|test|build):' .github/workflows/ci.yml) -eq 4</automated>
  </verify>
  <done>ci.yml still declares exactly the four jobs lint/typecheck/test/build; stylelint is a step in lint; in `test` the `pnpm exec playwright install chromium --with-deps` step precedes `pnpm run test`, and parity follows; in `build` publint (via `pnpm exec`) and the pack-smoke step are present; `publint` pinned in root devDependencies; no `pnpm dlx`/`npx` invocations remain.</done>
  <acceptance_criteria>
    - exactly 4 top-level jobs remain (`grep -cE '^  (lint|typecheck|test|build):'` equals 4); no new job names
    - in the `test` job the `pnpm exec playwright install chromium --with-deps` line appears BEFORE the `pnpm run test` line (deterministic-fresh-runner ordering) — addresses review concern
    - ci.yml uses `pnpm exec publint` and `pnpm exec playwright` (NO `pnpm dlx`/`npx`) — addresses review concern
    - ci.yml contains a `lint:css`, a `parity`, a `pack-smoke`, and a `publint` invocation
    - `actionlint` passes on the edited workflow (run the checksum-verified binary already in the lint job)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author the packed-artifact smoke test and prove all gates run green locally</name>
  <read_first>
    - packages/styles/package.json (exports incl. `./styles.css`/sideEffects/files — the publint + pack target) + .planning/research/PITFALLS.md publint expectations
    - tools/parity/parity.mjs (02-04) + packages/styles/.stylelintrc.json (02-04) + packages/styles/vitest.config.ts (02-05)
  </read_first>
  <action>Create tools/pack-smoke/pack-smoke.mjs (plain Node, ESM) that drives a REAL consumer bundler build (addresses review concern: `require.resolve` resolves a `.css` path without loading/bundling it — it cannot prove the root export, nested `@import`s, or bundler compatibility). Steps: (1) run `pnpm --filter @lyra-ds/styles pack` (or `pnpm pack` in the package dir) to produce the tarball; (2) assert the tarball's file list contains styles.css, tokens/, components/, compat-shadcn.css, README.md and EXCLUDES tests/ and handoff scratch; (3) create a throwaway temp fixture dir with a minimal package.json + a minimal Vite config (`tools/pack-smoke/fixture/vite.config.mjs`, or generated at runtime) and an entry CSS file `entry.css` that does `@import "@lyra-ds/styles";` + `@import "@lyra-ds/styles/styles.css";` + one token subpath `@import "@lyra-ds/styles/tokens/brand.css";`; (4) install the packed tarball into the fixture; (5) run a minimal `vite build` (pinned `vite@8.1.5`, invoked via the fixture's local install or `pnpm exec vite build`) that bundles `entry.css`; (6) read the emitted CSS from the fixture's `dist/` and ASSERT it contains a known `.lyra-*` class (e.g. `.lyra-btn`) AND a known token (e.g. `--brand` or a color token) — proving the root `@lyra-ds/styles` export, the literal `./styles.css` export, and the relative `@import` chain all resolve and bundle through a real consumer bundler; (7) clean up the temp dir; exit non-zero with a clear message on any failure (missing file in tarball, vite build error, or missing class/token in emitted CSS). Add a root script `"pack-smoke": "node tools/pack-smoke/pack-smoke.mjs"`. Then run each gate locally to confirm green before the PR: `pnpm exec publint packages/styles` (lockfile devDep pinned `publint@0.3.21`, NOT dlx) reports no errors and confirms the exports map (incl. `./styles.css`) + `sideEffects: ["**/*.css"]` are valid (STY-07); `pnpm run parity` reports 209 tokens + 248 classes + no-CDN (STY-06); `pnpm --filter @lyra-ds/styles run lint:css` is clean; `pnpm --filter @lyra-ds/styles run test` passes in chromium (STY-03/STY-04, after `pnpm exec playwright install chromium` locally); `node tools/pack-smoke/pack-smoke.mjs` passes. Record any publint suggestion that is intentionally accepted (e.g. no `types`/`main` on a CSS-only package) in the SUMMARY.</action>
  <verify>
    <automated>pnpm exec publint packages/styles && pnpm run parity && pnpm --filter @lyra-ds/styles run lint:css && pnpm exec playwright install chromium --with-deps && pnpm --filter @lyra-ds/styles run test && node tools/pack-smoke/pack-smoke.mjs</automated>
  </verify>
  <done>publint (pinned 0.3.21) is green on packages/styles (valid exports incl. `./styles.css`, sideEffects protected); parity reports 209/248 + no-CDN; stylelint clean; Browser Mode test green (run after the Chromium install, and now included in the automated verify command); the packed-artifact smoke test runs a real `vite@8.1.5` consumer build importing the root entry + `./styles.css` + a token subpath and asserts a `.lyra-*` class + a token in the emitted CSS. All gates pass, all via `pnpm exec`/local scripts (no floating downloads).</done>
  <acceptance_criteria>
    - `pnpm exec publint packages/styles` exits 0 (no errors), invoked via the pinned `publint@0.3.21` lockfile devDep (not floating "latest") — STY-07; addresses review concern: publint pin
    - `pnpm run parity` exits 0 and reports 209 tokens + 248 classes + no-CDN — STY-06
    - `pnpm --filter @lyra-ds/styles run lint:css` exits 0
    - the automated `<verify>` command runs the Browser Mode test (`pnpm --filter @lyra-ds/styles run test`) AFTER `pnpm exec playwright install chromium --with-deps`, and it exits 0 in chromium — STY-03/STY-04; addresses review concern: verify omitted Browser Mode
    - `node tools/pack-smoke/pack-smoke.mjs` exits 0: the tarball contains the allowlisted files (and excludes tests/), AND a real `vite@8.1.5` build of a consumer entry that `@import`s `@lyra-ds/styles` + `@lyra-ds/styles/styles.css` + a `./tokens/*` subpath emits bundled CSS containing a known `.lyra-*` class AND a known token — proving the root export bundles through a real consumer bundler — STY-01/STY-02; addresses review concern: pack smoke never imported CSS
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → required-status-check ruleset | job names are ruleset contexts; renaming/adding a job orphans the Phase-1 branch-protection contract |
| npm registry → CI runtime | publint/playwright pulled at build; invoke pinned lockfile devDeps via `pnpm exec` rather than floating `dlx`/`npx` remotes |
| packed tarball → consumer install | `files` allowlist + relative CSS imports can pass in-workspace yet break post-publish; the pack-smoke test exercises a real install |

## STRIDE Threat Register (ASVS L1 — packaging/CI integrity)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SE | Tampering | package.json sideEffects | high | mitigate | publint step (STY-07) fails the build if the exports/sideEffects surface is broken |
| T-02-JOBS | Tampering | ci.yml frozen jobs | high | mitigate | Assert exactly 4 jobs remain; steps only; actionlint validates the workflow |
| T-02-SC | Tampering | publint/playwright install | medium | mitigate | legitimacy-cleared in 02-04; pinned/save-exact and invoked via `pnpm exec` rather than floating `dlx`/`npx` |
| T-02-ORDER | Denial | fresh CI runner | high | mitigate | Chromium install ordered BEFORE the root `pnpm run test` step so a clean runner cannot fail on a missing browser |
| T-02-PACK | Info-disclosure/Integrity | packed tarball surface | medium | mitigate | pack-smoke test asserts the tarball file list + resolves the entry and a token subpath from a real install (STY-01/STY-02) |
| T-02-PARITY | Tampering | copy-verify drift | high | mitigate | parity step (STY-06) runs on every PR and fails on any token/class drift, relocation, or external `url()` |
</threat_model>

<verification>
- ci.yml: 4 frozen jobs intact; stylelint (lint), Chromium-install→test→parity (test, in order), publint + pack-smoke (build) added as steps.
- Chromium install ordered BEFORE root `pnpm run test`; tools invoked via `pnpm exec` (no dlx/npx).
- publint green (pinned 0.3.21, STY-07), parity 209/248 + no-CDN green (STY-06), stylelint clean, Browser Mode test green (STY-03/STY-04, and included in the automated verify command), pack-smoke green via a real vite@8.1.5 consumer build asserting a `.lyra-*` class + a token in the emitted CSS (STY-01/STY-02).
- actionlint passes on the edited workflow.
</verification>

<success_criteria>
- STY-06 parity runs in CI on every PR.
- STY-07 publint green with `sideEffects: ["**/*.css"]`.
- Chromium install precedes tests; no floating tool downloads.
- Packed-artifact smoke test runs a real vite consumer build (root entry + `./styles.css` + token subpath) and asserts a `.lyra-*` class + a token in the emitted CSS (STY-01/STY-02).
- All gates green; four job names unchanged.
</success_criteria>

## Artifacts this phase produces (this plan's slice)

- `.github/workflows/ci.yml` — filled hooks: stylelint step (lint job); playwright-install → vitest → parity steps (test job, in that order); publint + pack-smoke steps (build job)
- `tools/pack-smoke/pack-smoke.mjs` — `pnpm pack` + install-into-fixture + REAL `vite@8.1.5` consumer build asserting a `.lyra-*` class + a token in the emitted CSS (proves the root `@lyra-ds/styles`, `./styles.css`, and `./tokens/*` imports bundle)
- `tools/pack-smoke/fixture/` — minimal Vite consumer fixture (vite config + `entry.css` importing the root entry, `./styles.css`, and a token subpath)
- root `package.json` — pinned `publint@0.3.21` + `vite@8.1.5` devDependencies + `pack-smoke` script

<output>
Create `.planning/phases/02-styles-package/02-06-SUMMARY.md` when done.
</output>
