---
phase: 02-styles-package
plan: 06
type: execute
wave: 4
depends_on: [02-03, 02-04, 02-05]
files_modified:
  - .github/workflows/ci.yml
  - package.json
autonomous: true
requirements: [STY-06, STY-07]
user_setup: []
must_haves:
  truths:
    - "The three pre-placed CI hooks are filled as STEPS inside the four FROZEN jobs (never new jobs): stylelint in `lint`, token/class parity + Browser Mode test in `test`, publint in `build` (02-PATTERNS.md CI section)."
    - "publint passes green against packages/styles, confirming a valid exports map and that `\"sideEffects\": [\"**/*.css\"]` protects the CSS from consumer tree-shaking (STY-07)."
    - "The parity script runs in CI on every PR and fails the build on any token/class drift (STY-06)."
    - "The Browser Mode test runs in CI after `npx playwright install chromium --with-deps`, gating STY-03/STY-04 on every PR."
    - { statement: "import-safety: publint-green exports map + sideEffects:['**/*.css'] + the parity import-order assertion together prove the CSS import survives consumer tree-shaking and compat stays out of the entry — this CI wiring is the evidence the backstop item required.", verification: covered }
  prohibitions:
    - "MUST NOT add or rename CI jobs — the four job names (lint, typecheck, test, build) are required-status-check contexts; new gates land only as STEPS (02-PATTERNS.md FROZEN CONTRACT)."
    - "MUST NOT wire publint against a `\"sideEffects\": false` package (would defeat STY-07)."
  artifacts:
    - .github/workflows/ci.yml
  key_links:
    - "ci.yml steps → invoke `pnpm run lint:css` / `pnpm run parity` / `pnpm --filter run test` / `pnpm dlx publint` (pinned) inside the four frozen jobs; the phase's live quality gates."
---

<objective>
Wire all four Phase-2 quality gates into the existing CI workflow as STEPS inside the four frozen jobs, filling the pre-placed hooks: stylelint (lint), parity + Browser Mode test (test), publint (build). Install/pin publint. Confirm every gate runs green on the current package, closing STY-06 (parity in CI) and STY-07 (publint green with `sideEffects: ["**/*.css"]`).

Purpose: The gates authored in plans 02-04/02-05 only guard the phase once CI runs them on every PR. This is the integration step that makes the phase's guarantees continuous.
Output: updated .github/workflows/ci.yml, publint devDep pinned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-styles-package/02-CONTEXT.md
@.planning/phases/02-styles-package/02-PATTERNS.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire stylelint, parity, publint, and Browser Mode test into the four frozen CI jobs</name>
  <read_first>
    - .github/workflows/ci.yml (the FROZEN header lines 1-8; the three hook comments at the lint job "future hook (Phase 2): stylelint", the test job "future hook (Phase 2, STY-06): token/class parity script", the build job "future hooks (Phases 2-4, OSS-03): publint …"; the existing step style `- run: pnpm run <script>`)
    - 02-PATTERNS.md ".github/workflows/ci.yml" section (add STEPS never jobs; wire as pnpm scripts) + "CI gate wiring convention"
    - packages/styles/package.json scripts (`lint:css`, `test`) + root package.json script (`parity`)
  </read_first>
  <action>Edit .github/workflows/ci.yml IN PLACE, adding steps to the existing four jobs only (no new/renamed jobs). In the `lint` job at the stylelint hook: add `- run: pnpm --filter @lyra-ds/styles run lint:css`. In the `test` job at the parity hook: add `- run: pnpm run parity`, and add the Browser Mode test — a `- run: npx playwright install chromium --with-deps` step followed by `- run: pnpm --filter @lyra-ds/styles run test`. In the `build` job at the publint hook: add `- run: pnpm dlx publint packages/styles` (or a pinned root devDep `publint` invoked via `pnpm --filter @lyra-ds/styles exec publint`). Add `publint` to root devDependencies (save-exact, legitimacy-cleared in 02-04). Keep the frozen job names and the actionlint step intact; place each new step after the existing `pnpm run <job>` step.</action>
  <verify>
    <automated>grep -q 'lint:css' .github/workflows/ci.yml && grep -q 'run parity' .github/workflows/ci.yml && grep -q 'publint' .github/workflows/ci.yml && grep -q 'playwright install chromium' .github/workflows/ci.yml && test $(grep -cE '^  (lint|typecheck|test|build):' .github/workflows/ci.yml) -eq 4</automated>
  </verify>
  <done>ci.yml still declares exactly the four jobs lint/typecheck/test/build; stylelint is a step in lint, parity + playwright-install + vitest are steps in test, publint is a step in build; publint pinned in root devDependencies.</done>
  <acceptance_criteria>
    - exactly 4 top-level jobs remain (`grep -cE '^  (lint|typecheck|test|build):'` equals 4); no new job names
    - ci.yml contains a `lint:css`, a `parity`, a `playwright install chromium`, a `run test`, and a `publint` invocation
    - `actionlint` passes on the edited workflow (run the checksum-verified binary already in the lint job, or `pnpm dlx` it locally)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Prove all gates run green locally (publint + parity + stylelint + test)</name>
  <read_first>
    - packages/styles/package.json (exports/sideEffects/files — the publint target) + .planning/research/PITFALLS.md publint expectations
    - tools/parity/parity.mjs (02-04) + packages/styles/.stylelintrc.json (02-04) + packages/styles/vitest.config.ts (02-05)
  </read_first>
  <action>Run each gate locally to confirm green before the PR: `pnpm dlx publint packages/styles` (or the pinned devDep) reports no errors and confirms the exports map + `sideEffects: ["**/*.css"]` are valid (STY-07); `pnpm run parity` reports 209 tokens + 248 classes (STY-06); `pnpm --filter @lyra-ds/styles run lint:css` is clean; `pnpm --filter @lyra-ds/styles run test` passes in chromium (STY-03/STY-04). Record any publint suggestion that is intentionally accepted (e.g. no `types`/`main` on a CSS-only package) in the SUMMARY.</action>
  <verify>
    <automated>pnpm dlx publint packages/styles && pnpm run parity && pnpm --filter @lyra-ds/styles run lint:css</automated>
  </verify>
  <done>publint is green on packages/styles (valid exports, sideEffects protected); parity reports 209/248; stylelint clean; Browser Mode test green. All four gates pass.</done>
  <acceptance_criteria>
    - `pnpm dlx publint packages/styles` exits 0 (no errors) — STY-07
    - `pnpm run parity` exits 0 and reports 209 tokens + 248 classes — STY-06
    - `pnpm --filter @lyra-ds/styles run lint:css` exits 0
    - `pnpm --filter @lyra-ds/styles run test` exits 0 in chromium — STY-03/STY-04
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → required-status-check ruleset | job names are ruleset contexts; renaming/adding a job orphans the Phase-1 branch-protection contract |
| npm registry → CI runtime | publint pulled at build; pin it or invoke a pinned devDep rather than a floating remote |

## STRIDE Threat Register (ASVS L1 — packaging/CI integrity)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SE | Tampering | package.json sideEffects | high | mitigate | publint step (STY-07) fails the build if the exports/sideEffects surface is broken |
| T-02-JOBS | Tampering | ci.yml frozen jobs | high | mitigate | Assert exactly 4 jobs remain; steps only; actionlint validates the workflow |
| T-02-SC | Tampering | publint install | medium | mitigate | publint legitimacy-cleared in 02-04; pinned/save-exact rather than floating `dlx` |
| T-02-PARITY | Tampering | copy-verify drift | high | mitigate | parity step (STY-06) runs on every PR and fails on any token/class drift |
</threat_model>

<verification>
- ci.yml: 4 frozen jobs intact; stylelint/parity/test/publint added as steps.
- publint green (STY-07), parity 209/248 green (STY-06), stylelint clean, Browser Mode test green (STY-03/STY-04).
- actionlint passes on the edited workflow.
</verification>

<success_criteria>
- STY-06 parity runs in CI on every PR.
- STY-07 publint green with `sideEffects: ["**/*.css"]`.
- All four gates green; four job names unchanged.
</success_criteria>

## Artifacts this phase produces (this plan's slice)

- `.github/workflows/ci.yml` — filled hooks: stylelint step (lint job), parity + playwright-install + vitest steps (test job), publint step (build job)
- root `package.json` — pinned `publint` devDependency

<output>
Create `.planning/phases/02-styles-package/02-06-SUMMARY.md` when done.
</output>
