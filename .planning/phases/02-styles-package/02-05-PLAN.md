---
phase: 02-styles-package
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - packages/styles/vitest.config.ts
  - packages/styles/tests/brand-theme.test.ts
  - packages/styles/tests/fixtures/brand-theme.html
  - package.json
autonomous: true
requirements: [STY-03, STY-04]
user_setup: []
must_haves:
  truths:
    - "A minimal Vitest Browser Mode harness (Vitest 4 + @vitest/browser-playwright, chromium) renders the package CSS in a real browser — the only vehicle able to resolve `color-mix` and `[data-theme]`/`[data-brand]` cascade (jsdom renders no CSS) (D-04, UI-SPEC line 136)."
    - "The test fixture is loaded via a CONCRETE, specified mechanism (addresses review concern: 'load the fixture' is not a mechanism): the test itself injects the fixture DOM + a `<link rel=stylesheet>`/`@import` of the built entry `styles.css` into `document.head`/`document.body` at setup (explicit DOM/CSS injection), OR the vitest browser config sets `browser.testerHtmlPath` to the fixture HTML. The chosen mechanism is stated in the config/test and actually renders the probe elements with the package CSS applied."
    - "Setting `data-theme=\"dark\"` on the fixture root switches semantic tokens with no rebuild — asserted by reading a REAL longhand property (e.g. `background-color`) on a probe element bound to the token via `getComputedStyle(el).backgroundColor`, NOT by reading the raw custom-property string (STY-03)."
    - "The saturated `acme` teal brand (`--brand: #0D9488`) under `[data-brand]` resolves the color-mix-derived accent group (`--accent-hover/active/soft/soft-text/focus-ring`) correctly in BOTH light and dark; each derived var is asserted via a dedicated probe element that binds it to a real longhand (`background-color`/`color`/`border-color`/`box-shadow`) so `color-mix()` actually resolves to channels (custom properties preserve the unresolved token stream) — addresses review concern: color-mix test won't resolve via getPropertyValue (STY-04, D-04)."
    - "Color-mix assertions are ORDERED per the handoff formulas, not merely 'teal family / not indigo' (addresses review concern: assertions may not prove each formula): in LIGHT (brand.css:16-28) each derived value differs from raw Acme in the specified direction — `--accent-hover` (mix black 12%) and `--accent-active` (mix black 22%) are DARKER than `--accent` (=raw brand), with active darker than hover; in DARK (brand.css:31-40) each derived value is LIGHTER than raw Acme — `--accent` (white 14%), `--accent-hover` (white 26%), `--accent-active` (white 38%), `--accent-soft-text` (white 45%) increase in lightness in that order. The test asserts these ordered channel relationships (e.g. perceived luminance monotonic in the stated direction), tolerant of sub-pixel color-mix rounding."
    - "The chevron masks render (addresses review concern: malformed data URI must be caught by a test): a probe on a `.lyra-*` element that uses the chevron `mask`/`-webkit-mask` (e.g. the select dropdown indicator / breadcrumb separator) has a non-empty computed `mask-image`/`-webkit-mask-image` (the `data:` URI resolved, not the empty string) AND non-zero rendered box size — so a truncated/blank glyph from a malformed `data:` URI FAILS the test."
    - { statement: "long-text robustness: an overlong label in a fixed-width `.lyra-*` text element truncates or wraps (text-overflow/white-space/word-break) without expanding its container past its box — pure-CSS, asserted by ONE concrete long-text probe in this phase.", verification: backstop }
    - { statement: "broader overflow robustness (nav/feedback/data tables/file lists scrolling or clipping per the handoff CSS) is NOT asserted in Phase 2 — those surfaces need rendered component content and are deferred to Phase 3/4 React component tests. Phase 2 verifies only the single long-text probe above; the broader claim is explicitly out of scope here (honest backstop framing) — addresses review concern: overflow claimed but not tested.", verification: deferred }
  prohibitions:
    - "MUST NOT validate brand/theme in jsdom (renders no CSS, no color-mix — false negatives/positives)."
    - "MUST NOT ship any test/fixture file inside the package `files` allowlist (tests are dev-only)."
  artifacts:
    - packages/styles/vitest.config.ts
    - packages/styles/tests/brand-theme.test.ts
    - packages/styles/tests/fixtures/brand-theme.html
  key_links:
    - "brand-theme fixture → imports styles.css entry (02-03) → exercises brand.css color-mix (02-01) under [data-brand]/[data-theme]; wired into CI test job in plan 02-06."
---

<objective>
Stand up the minimal Vitest Browser Mode harness (chromium via `@vitest/browser-playwright`) needed to validate the two runtime-behavioral contracts of a pure-CSS package: dark theming (STY-03) and the white-label brand color-mix derivation (STY-04). Author the `acme` teal fixture test asserting the derived accent group in light AND dark, plus backstop visual checks for long-text/overflow robustness. This is a deliberately minimal harness that Phase 3 extends for React component testing.

Purpose: STY-04's acceptance (D-04) is locked to Browser Mode validation — `color-mix` only resolves in a real browser. This is the phase's only runtime test.
Output: vitest.config.ts, the brand/theme test + fixture, vitest/playwright devDeps (approved in 02-04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-styles-package/02-CONTEXT.md
@.planning/phases/02-styles-package/02-UI-SPEC.md
@.planning/phases/02-styles-package/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stand up the minimal Vitest Browser Mode (chromium) harness</name>
  <read_first>
    - .claude/CLAUDE.md "Testing layout" + Recommended Stack rows (Vitest 4.1.10, @vitest/browser-playwright 4.1.10, playwright 1.61.1; Browser Mode required because jsdom renders no CSS)
    - packages/styles/package.json (the `test` script authored in 02-03 = `vitest run`) + root package.json
    - 02-UI-SPEC.md White-Label Brand Contract "Validation contract (D-04)" (line 136)
  </read_first>
  <action>Add `vitest@4.1.10`, `@vitest/browser-playwright@4.1.10`, `playwright@1.61.1` to root devDependencies (save-exact; legitimacy-cleared in plan 02-04's blocking checkpoint — this plan now `depends_on` 02-04 so it runs only AFTER that approval). Create packages/styles/vitest.config.ts configuring Browser Mode with the playwright provider and chromium instance, headless, no React (pure CSS/DOM). SPECIFY THE FIXTURE LOADING MECHANISM CONCRETELY (addresses review concern: a standalone HTML file is not auto-loaded): choose ONE and implement it — either (a) set `browser.testerHtmlPath` (or `browser.instances[].testerHtmlPath`) in this config to `tests/fixtures/brand-theme.html` so Browser Mode serves that page as the test HTML, OR (b) leave the config with the default tester page and have the test file do explicit setup — inject the fixture DOM into `document.body` and load the built entry CSS by appending a `<link rel="stylesheet" href="/styles.css">` (served from the package root) or an `@import` style tag into `document.head`, awaiting stylesheet load before asserting. State which mechanism is used in a comment so the executor does not leave "load the fixture" underspecified. Ensure the package `test` script (`vitest run`, authored in 02-03) resolves this config. Document that CI must run the lockfile-installed `pnpm exec playwright install chromium --with-deps` BEFORE any test step that triggers Browser Mode (wired in 02-06 — the install precedes the root test command).</action>
  <verify>
    <automated>test -f packages/styles/vitest.config.ts && node -e "const p=require('./package.json'); for(const d of ['vitest','@vitest/browser-playwright','playwright']) if(!(p.devDependencies&&p.devDependencies[d]))throw new Error('missing '+d)" && grep -qi 'browser' packages/styles/vitest.config.ts</automated>
  </verify>
  <done>vitest.config.ts configures Browser Mode + chromium; the three devDeps are pinned in root; the package `test` script targets this config.</done>
  <acceptance_criteria>
    - `packages/styles/vitest.config.ts` enables Browser Mode with the playwright chromium provider
    - the fixture loading mechanism is explicit (addresses review concern): EITHER `browser.testerHtmlPath` points at `tests/fixtures/brand-theme.html` in the config, OR the test file injects the fixture DOM + loads the entry CSS into `document.head` and awaits stylesheet load — the chosen mechanism is present and commented, not left as "load the fixture"
    - root devDependencies pin vitest 4.1.10, @vitest/browser-playwright 4.1.10, playwright 1.61.1
    - `pnpm --filter @lyra-ds/styles run test` invokes vitest with this config (may require chromium install locally)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the acme brand + theme fixture test (light + dark)</name>
  <read_first>
    - packages/styles/tokens/brand.css (the color-mix derivation groups) + packages/styles/tokens/colors.css ([data-theme="dark"] overrides) + packages/styles/styles.css (entry)
    - packages/styles/components/buttons/buttons.css (`.lyra-btn--primary` uses `--accent`/`--on-accent` — a concrete element to read computed styles from)
    - 02-UI-SPEC.md White-Label Brand Contract derived-group tables (light + dark mixes)
  </read_first>
  <behavior>
    - Test: with no [data-theme]/[data-brand], a probe bound to `--accent` (via `background-color: var(--accent)`) has a computed `backgroundColor` equal to the default light accent (indigo-600 #5B5BD6, normalized to the browser's `rgb(...)` serialization).
    - Test: setting data-theme="dark" on the root changes that same probe's computed `backgroundColor` to the dark accent (indigo-500 #6E6ADE) — no rebuild, read from the LONGHAND, not the custom property (STY-03).
    - Test: under [data-brand="acme"] { --brand: #0D9488 } in LIGHT, the `--accent` probe's computed `backgroundColor` equals the teal brand; and per the brand.css:16-28 formulas the derived group is ORDERED — `--accent-hover` (black 12%) and `--accent-active` (black 22%) probes are DARKER (lower luminance) than `--accent`, with `--accent-active` darker than `--accent-hover`; `--accent-soft`/`--accent-soft-text`/`--focus-ring` resolve to concrete teal-family channels (not indigo) (STY-04).
    - Test: under [data-theme="dark"][data-brand="acme"], the same per-var probes resolve via the white-mix (lightened) dark formulas (brand.css:31-40) and are ORDERED — `--accent` (white 14%), `--accent-hover` (white 26%), `--accent-active` (white 38%), `--accent-soft-text` (white 45%) are each LIGHTER (higher luminance) than raw Acme, increasing in that order (STY-04 dark).
    - Test: a probe on a `.lyra-*` element that uses the chevron `mask`/`-webkit-mask` (e.g. `.lyra-select` dropdown indicator or a breadcrumb/nav separator) has a computed `mask-image`/`-webkit-mask-image` that is NON-empty (the `data:` URI resolved — not `none`/empty) and a non-zero rendered box — so a truncated/blank glyph from a malformed `data:` URI FAILS (addresses review concern: chevron render check).
    - Backstop: an overlong label inside a fixed-width `.lyra-*` text element truncates/wraps without expanding its container past its box (long-text robustness).
  </behavior>
  <action>Create packages/styles/tests/fixtures/brand-theme.html (a minimal fixture whose stylesheet is the built entry styles.css, loaded via the mechanism chosen in vitest.config.ts — testerHtmlPath or in-test injection). CRITICAL mechanic (addresses review concern): reading `getComputedStyle(el).getPropertyValue('--accent-hover')` returns the UNRESOLVED `color-mix(...)` token stream, so it cannot prove derivation. Instead the fixture MUST contain a set of dedicated probe elements, one per derived variable, each binding that variable to a REAL longhand property — e.g. a probe with `background-color: var(--accent)`, another `background-color: var(--accent-hover)`, `color: var(--accent-soft-text)`, `border-color: var(--focus-ring)`, `box-shadow: 0 0 0 1px var(--accent-active)` — plus a chevron-mask probe element (a `.lyra-select` or nav separator that carries the chevron `mask`) so the computed `mask-image` can be read. Create packages/styles/tests/brand-theme.test.ts: load the fixture (per the chosen mechanism), and across the four permutations (light-default, dark-default, light-acme, dark-acme), read the resolved LONGHAND off each probe (`getComputedStyle(probe).backgroundColor` / `.color` / `.borderColor` / `.boxShadow`), normalize the browser's color serialization (rgb/rgba forms), convert each to a perceived-luminance scalar, and assert the ORDERED relationships from the formulas (light: hover/active progressively DARKER than accent; dark: accent/hover/active/soft-text progressively LIGHTER than raw Acme) rather than exact float equality — resilient to sub-pixel color-mix rounding, but proving DIRECTION per brand.css:18/:33 (not merely "teal not indigo"). Assert the chevron-mask probe's computed `mask-image` (or `-webkit-mask-image`) is non-empty and its rendered box is non-zero. Include one long-text backstop assertion (a fixed-width `.lyra-*` element with overflowing text does not exceed its box). Do NOT claim broader nav/feedback/data/files overflow coverage — that is deferred to Phase 3 (documented in must_haves).</action>
  <verify>
    <automated>pnpm --filter @lyra-ds/styles run test</automated>
  </verify>
  <done>The Browser Mode test passes across all four theme×brand permutations, proving STY-03 (dark switch, no rebuild) and STY-04 (acme color-mix accent group in light and dark); the long-text backstop assertion passes.</done>
  <acceptance_criteria>
    - `pnpm --filter @lyra-ds/styles run test` exits 0 in chromium
    - assertions read RESOLVED longhand properties off probe elements (`getComputedStyle(probe).backgroundColor`/`.color`/`.borderColor`/`.boxShadow`), NOT `getPropertyValue('--accent-hover')` on the raw custom property (addresses review concern)
    - the test fails if brand.css color-mix derivation is removed (proves it is actually asserting resolved derivation, not just presence)
    - all four permutations (light/dark × default/acme) are asserted, each derived var (`--accent-hover/active/soft/soft-text/focus-ring`) via its own longhand probe
    - assertions verify ORDERED direction per the formulas, not just "teal not indigo" (addresses review concern): in light, `--accent-hover` and `--accent-active` are darker than `--accent` (active < hover in luminance); in dark, `--accent`/`--accent-hover`/`--accent-active`/`--accent-soft-text` are each lighter than raw Acme and increase in that order
    - a chevron-mask probe's computed `mask-image`/`-webkit-mask-image` is non-empty and its rendered box is non-zero (proves the `data:` chevron URI actually renders — a malformed/truncated URI fails) — addresses review concern: chevron render check
    - the long-text backstop element does not overflow its fixed-width box; broader nav/feedback/data/files overflow is explicitly NOT claimed here (deferred to Phase 3)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → dev/CI only | Vitest/playwright are dev-only; must never enter the published `files` allowlist |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | vitest/playwright installs | high | mitigate | Legitimacy-cleared in plan 02-04's blocking checkpoint; pinned + save-exact; dev-only, excluded from `files` |
| T-02-BRAND | Repudiation/Integrity | STY-04 color-mix contract | medium | mitigate | Real-browser assertion across 4 permutations catches a silently broken derivation that jsdom/parity cannot see |
</threat_model>

<verification>
- Vitest Browser Mode harness runs in chromium; fixture loaded via the explicit mechanism (testerHtmlPath or in-test injection).
- Brand + theme test green across the 4 permutations (STY-03, STY-04), with ORDERED color-mix direction asserted per brand.css:18/:33.
- Chevron-mask render probe asserts a non-empty computed `mask-image` + non-zero box (catches a malformed `data:` URI).
- Long-text backstop asserted; broader nav/feedback/data/files overflow explicitly deferred to Phase 3.
- CI wiring (playwright install + test step) in plan 02-06.
</verification>

<success_criteria>
- STY-03 verified: data-theme="dark" switches computed tokens with no rebuild.
- STY-04 verified: acme color-mix accent group resolves in light and dark, with ordered darkening/lightening direction.
- Chevron `data:` mask renders (non-empty computed mask-image, non-zero box).
- Long-text backstop exercised; broader overflow deferred to Phase 3.
</success_criteria>

## Artifacts this phase produces (this plan's slice)

- `packages/styles/vitest.config.ts` — minimal Browser Mode (chromium) config
- `packages/styles/tests/brand-theme.test.ts` — STY-03 + STY-04 (ordered darkening/lightening per brand.css:18/:33) + chevron-mask render probe + long-text backstop assertions (reads resolved longhands off probe elements, not raw custom-property strings)
- `packages/styles/tests/fixtures/brand-theme.html` — render fixture whose stylesheet is the entry (loaded via testerHtmlPath or in-test injection), with per-derived-var probe elements binding each token to a real longhand so `color-mix()` resolves, plus a chevron-mask probe element
- root `package.json` — vitest / @vitest/browser-playwright / playwright devDependencies

<output>
Create `.planning/phases/02-styles-package/02-05-SUMMARY.md` when done.
</output>
