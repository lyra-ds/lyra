---
phase: 02-styles-package
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - tools/parity/parity.mjs
  - tools/parity/fixtures/
  - packages/styles/.stylelintrc.json
  - package.json
autonomous: false
requirements: [STY-06]
user_setup: []
must_haves:
  truths:
    - "A plain Node parity script proves 209/209 handoff token declarations (base+brand+colors+effects+spacing+typography, including `[data-theme=\"dark\"]` re-declarations) are present in the package with identical values, normalizing whitespace only; fonts.css is special-cased (0 tokens, exempt) (STY-06, D-05)."
    - "The same script inventories the 248 unique `.lyra-*` class names from handoff/components/** and asserts the package's component CSS carries the identical set (STY-06, D-06)."
    - "Parity is placement-aware AND cascade-aware, not a blind multiset: declarations are compared keyed by `file + at-rule-ancestry + selector + property + occurrence`, where at-rule-ancestry is the ordered chain of enclosing `@media`/`@container`/keyframe blocks (source has `@media` at feedback.css:195 and `@container` at files.css:188). A declaration silently moved into `:root`, moved under/out of an `@media`/`@container` block, OR reordered among equal-specificity rules FAILS parity even though the token multiset is unchanged (addresses review concern: multiset misses at-rule ancestry + rule order)."
    - "The parity script parses CSS with a real tokenizer / brace-depth state machine (NOT naive regex or `split(';')`), so it correctly handles nested at-rules and quoted `data:` URIs that contain semicolons and braces (e.g. the `url('data:image/svg+xml;utf8,<svg …>')` masks and forms.css:204). Parser fixtures cover exactly those constructs: a nested `@media`, a nested `@container`, a `@keyframes` block, and a quoted `data:` URI with an interior `;`."
    - "A short, explicit intentional-divergence allowlist exempts exactly the known deltas — fonts.css's removed Google-Fonts `@import`, and the three `unpkg.com` chevron mask URLs in forms/display/navigation rewritten to local `data:` URIs — so the placement-aware diff passes on the faithful copy but still catches any UNlisted change. The verbatim-copied `lyra-fade-in` opacity keyframe (feedback.css:119) is NOT a divergence (unchanged copy) and needs no allowlist entry."
    - "The parity script includes a package-wide external-URL guard that rejects ANY `url()` whose target is not `data:` or a relative path: absolute schemes (`http://`, `https://`, and any other `scheme:`), protocol-relative `url(//cdn…)`, are all failed; only `data:` URIs and relative paths pass (the SVG `xmlns=\"http://…\"` namespace literal INSIDE a `data:` URI is part of the allowed data: target and is NOT a separate fetch), enforcing the no-runtime-CDN constraint (addresses review concern: guard missed protocol-relative + non-http schemes)."
    - "On drift the script exits non-zero and names the drifted token/class/declaration plus points at handoff/ as canonical (UI-SPEC line 148 error contract)."
    - "A stylelint config (extending pinned `stylelint-config-standard@40.0.0`) exists as the styles package's automated CSS quality gate (.claude/CLAUDE.md Recommended Stack)."
    - { statement: "parity determinism/adjacency edge (STY-06/STY-01): the 209-count and 248-class set are exact equality checks — a token present in both with identical value passes; a value that merely 'touches' but differs fails loudly rather than silently merging.", verification: covered }
  prohibitions:
    - "MUST NOT compare against a separate committed lockfile — compare directly against handoff/ so drift is caught at source (D-05)."
    - "MUST NOT weaken parity back to a pure token/class multiset that ignores selector/file/cascade placement."
    - "MUST NOT count fonts.css or compat-shadcn.css toward the 209 (fonts diverges/0 tokens; compat is opt-in, outside the entry)."
    - "MUST NOT install any package whose npm-registry legitimacy has not been confirmed (no RESEARCH.md audit exists → fallback [ASSUMED] → blocking human verify)."
  artifacts:
    - tools/parity/parity.mjs
    - tools/parity/fixtures/
    - packages/styles/.stylelintrc.json
  key_links:
    - "parity.mjs → reads handoff/tokens + handoff/components (canonical) vs packages/styles/** (candidate) with placement-aware declaration diff + external-URL guard; wired into CI test job in plan 02-06."
---

<objective>
Author the STY-06 parity validator (a plain Node script comparing the package's tokens + `.lyra-*` classes against the canonical `handoff/` with a placement-aware declaration diff and a package-wide external-URL guard) and the stylelint config that becomes the styles package's automated CSS quality gate. A blocking human legitimacy checkpoint clears the new Phase-2 devDependencies (no RESEARCH.md audit exists — incl. the now-pinned `stylelint-config-standard@40.0.0`) before install.

Purpose: STY-06 is the phase's correctness guarantee — it proves the copy-verify stayed faithful. Stylelint is the only other automated gate for a no-build CSS package.
Output: tools/parity/parity.mjs, packages/styles/.stylelintrc.json, devDeps added to root package.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-styles-package/02-CONTEXT.md
@.planning/phases/02-styles-package/02-UI-SPEC.md
@.planning/phases/02-styles-package/02-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — Phase 2 devDependencies</name>
  <what-built>No RESEARCH.md Package Legitimacy Audit exists for this phase (research was skipped), so the fallback treats new packages as [ASSUMED]. This checkpoint clears the Phase-2 dev-only tooling before any install. All are version-pinned in .claude/CLAUDE.md's Recommended Stack (npm-registry-verified, 2026-07-16). None ship in the published package (which has zero runtime deps).</what-built>
  <how-to-verify>
    Confirm each package on npmjs.com (name, publisher, that the pinned version exists and is not typosquatted):
    1. stylelint @ 17.14.0 — https://www.npmjs.com/package/stylelint
    2. stylelint-config-standard @ 40.0.0 — https://www.npmjs.com/package/stylelint-config-standard (peers stylelint ^17.0.0)
    3. vitest @ 4.1.10 — https://www.npmjs.com/package/vitest
    4. @vitest/browser-playwright @ 4.1.10 — https://www.npmjs.com/package/@vitest/browser-playwright
    5. playwright @ 1.61.1 — https://www.npmjs.com/package/playwright
    6. publint @ 0.3.21 (EXACT pin — addresses review concern: previously "latest stable") — https://www.npmjs.com/package/publint
    7. vite @ 8.1.5 (EXACT pin — the consumer-bundle engine for the 02-06 pack-smoke Vite build; addresses review concern: pack smoke must run a real consumer CSS build) — https://www.npmjs.com/package/vite
    (Items 3-5 are installed in plan 02-05; items 6-7 in plan 02-06, but all are approved here so those plans stay autonomous. If npmjs.com shows a newer patch for publint/vite at execution time, confirm the pinned version above still exists and is not deprecated; keep the pin exact — do NOT float to "latest". stylelint-config-standard, publint, and vite are pinned/approved here per the review concerns that they were previously unpinned/unlisted.)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installs, or name any package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author the token + class parity script (STY-06)</name>
  <read_first>
    - 02-PATTERNS.md "No Analog Found" row for tools/parity (canonical extraction commands, 209 target, 248-class inventory, error contract) + D-05/D-06
    - handoff/tokens/{base,brand,colors,effects,spacing,typography}.css (canonical token source — fonts.css EXCLUDED) and handoff/components/**/*.css (canonical class source)
    - packages/styles/tokens/** and packages/styles/components/** (the candidate copies from plans 02-01/02-02)
  </read_first>
  <action>Create tools/parity/parity.mjs (plain Node, ESM, zero deps). PARSER REQUIREMENT (addresses review concern: naive regex misparses nested at-rules + data: URIs): implement a real CSS tokenizer / brace-depth state machine — walk the source char-by-char tracking (a) brace nesting depth, (b) whether inside a string (`'`/`"`) so quoted `data:` URIs with interior `;`/`{`/`}` are treated as opaque, (c) the ordered stack of enclosing at-rule prelude blocks (`@media`, `@container`, `@keyframes`) and the current selector. Do NOT use `split(';')` or a flat regex to segment declarations. It runs the following checks. (A) Token check (reporting/summary): extract every `--name: value;` declaration from the six canonical handoff token files (exclude fonts.css and compat-shadcn.css), normalize interior whitespace only, confirm the canonical declaration count equals 209 (includes `[data-theme="dark"]` re-declarations) and each is present in packages/styles/tokens/** with an identical value. (B) Placement-aware + cascade-aware declaration diff (the fidelity guarantee — replaces the weak multiset): for each canonical source file (six token files + all handoff/components/**/*.css), parse declarations keyed by `file + at-rule-ancestry + selector + property + occurrence-index`, where `at-rule-ancestry` is the ordered chain of enclosing `@media`/`@container`/`@keyframes` preludes (empty for top-level rules). Also record and compare each rule's ORDER index within its file so reordering equal-specificity rules is detected. Assert the package copy at the same relative path carries the identical keyed declaration set, values, at-rule ancestry, AND relative order — so a declaration relocated to a different selector/file, moved under/out of an `@media`/`@container`, or reordered FAILS even if the raw token/class multiset is unchanged. Normalize only interior whitespace and strip comments before comparing. Apply a SMALL explicit intentional-divergence allowlist so the faithful copy passes: (1) fonts.css's removed Google-Fonts `@import` line; (2) the `mask`/`-webkit-mask` declarations in forms.css, display.css, navigation.css whose value changed from the `unpkg.com/lucide-static` chevron URL to the local `data:` URI. The verbatim `lyra-fade-in` opacity keyframe (feedback.css:119) is an UNCHANGED copy, so it matches identically and needs no allowlist entry. Any divergence NOT on the allowlist fails. (C) Class inventory (reporting/summary): extract the unique `.lyra-*` names from handoff/components/**/*.css (canonical set = 248) and assert packages/styles/components/** carries the identical set. (D) External-URL guard (broadened — addresses review concern): using the tokenizer, extract every `url(...)` target across every shipped `packages/styles/**/*.css`, strip surrounding quotes/whitespace, and FAIL unless the target is either a `data:` URI or a relative path. Specifically FAIL on: any absolute scheme (`https://`, `http://`, or any `scheme:` prefix other than `data:`) AND protocol-relative `url(//cdn…)`. The `xmlns="http://www.w3.org/2000/svg"` literal lives INSIDE a `data:` target (a single allowed `url()`), so it is not a separate `url()` and does not trip the guard. On ANY mismatch, print a message naming the drifted token/class/declaration and its handoff-vs-package values (e.g. `Decl mismatch forms.css @media(min-width:…) .lyra-select[property=color] #1: package=X handoff=Y — handoff/ is canonical`) or the offending CSS file+line for a non-`data:`/non-relative url, and exit non-zero; on full pass print `parity OK: 209 tokens, 248 classes, placement + at-rule ancestry + no-CDN verified` and exit 0. Add a root package.json script `"parity": "node tools/parity/parity.mjs"`. Also create parser fixtures under tools/parity/fixtures/ covering: a nested `@media`, a nested `@container`, a `@keyframes` block, and a quoted `data:image/svg+xml;utf8,…` URI containing an interior `;` — and a tiny self-check (assert the tokenizer segments each fixture into the expected declaration set) that runs as part of the script or a sibling `parity.fixtures.test`.</action>
  <verify>
    <automated>node tools/parity/parity.mjs && node tools/parity/parity.mjs | grep -q '209' && node tools/parity/parity.mjs | grep -qi 'ancestry' && node tools/parity/parity.mjs | grep -qi 'no-CDN' && test -d tools/parity/fixtures && node -e "if(!require('./package.json').scripts.parity)throw new Error('parity script missing')"</automated>
  </verify>
  <done>`node tools/parity/parity.mjs` exits 0 against the current copied CSS, reports 209 tokens + 248 classes + placement + at-rule ancestry + no-CDN verified; a deliberate value edit, a declaration moved to a different selector, OR a declaration moved under/out of an `@media`/`@container` block makes it exit non-zero with a naming message; injecting a `url("https://…")` OR a protocol-relative `url("//cdn…")` into any package CSS fails the guard; the tokenizer parses the data-URI-with-semicolon and nested-at-rule fixtures correctly; root `parity` script wired.</done>
  <acceptance_criteria>
    - `node tools/parity/parity.mjs` exits 0 and prints the 209/248 counts plus a placement + at-rule ancestry + no-CDN confirmation
    - temporarily changing one token value in packages/styles/tokens/ makes the script exit non-zero and name that token (revert after checking)
    - temporarily moving a `[data-theme="dark"]` declaration into a `:root`/other selector makes the script exit non-zero (placement-aware, not a blind multiset) — addresses review concern
    - temporarily moving a declaration under (or out of) an `@media`/`@container` block, OR reordering two equal-specificity rules, makes the script exit non-zero (at-rule-ancestry + order aware) — addresses review concern
    - injecting a `url("https://example.com/x.svg")` OR a protocol-relative `url("//cdn.example.com/x.svg")` into any `packages/styles/**/*.css` makes the script exit non-zero (broadened external-URL guard); the existing local `data:` SVG masks (with their `xmlns="http://…"` literal inside the data: target) do NOT trip it — addresses review concern
    - the parser is a tokenizer/state machine (not `split(';')`/flat regex): the fixtures under `tools/parity/fixtures/` (nested `@media`, nested `@container`, `@keyframes`, quoted `data:` URI with interior `;`) each segment into the expected declaration set
    - the intentional-divergence allowlist (fonts `@import`; the three chevron mask URLs) keeps the faithful copy green; the verbatim `lyra-fade-in` opacity keyframe matches identically without an allowlist entry
    - fonts.css and compat-shadcn.css contribute 0 to the 209 count
    - root package.json has a `parity` script
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author the stylelint config for the styles package</name>
  <read_first>
    - 02-PATTERNS.md "No Analog Found" row for `.stylelintrc*` (no lint config exists yet; stylelint 17.14.0 per .claude/CLAUDE.md)
    - packages/styles/**/*.css (the CSS this config lints) + root package.json lint conventions + repo .npmrc (save-exact=true)
    - .claude/CLAUDE.md Recommended Stack row for stylelint (version 17.14.0)
  </read_first>
  <action>Add `stylelint@17.14.0` and `stylelint-config-standard@40.0.0` (peers stylelint ^17.0.0 — the exact pin approved in Task 1's legitimacy checkpoint) to root devDependencies with pnpm (save-exact per repo .npmrc — both land as exact versions, no `^`). Create packages/styles/.stylelintrc.json extending `stylelint-config-standard`, tuned so the handoff's modern CSS (color-mix, custom properties, the intentional non-8 radius/spacing steps, `[data-theme]`/`[data-brand]` selectors, the inline `data:` SVG mask URIs) lints clean — do NOT introduce rules that would flag the locked token values. Confirm the `lint:css` script authored in plan 02-03 (`stylelint` over the package CSS) runs green.</action>
  <verify>
    <automated>pnpm --filter @lyra-ds/styles exec stylelint '**/*.css' && test -f packages/styles/.stylelintrc.json && node -e "const d=require('./package.json').devDependencies||{}; if(d.stylelint!=='17.14.0')throw new Error('stylelint pin='+d.stylelint); if(d['stylelint-config-standard']!=='40.0.0')throw new Error('config pin='+d['stylelint-config-standard'])"</automated>
  </verify>
  <done>stylelint 17.14.0 + stylelint-config-standard 40.0.0 installed at exact pins; .stylelintrc.json exists extending the standard config; running stylelint over packages/styles/**/*.css exits 0 with no errors on the copied handoff CSS.</done>
  <acceptance_criteria>
    - `packages/styles/.stylelintrc.json` exists and extends `stylelint-config-standard`
    - stylelint over `packages/styles/**/*.css` exits 0 (no violations on the locked handoff CSS, including the inline `data:` mask URIs)
    - stylelint version in root devDependencies is exactly 17.14.0 and stylelint-config-standard is exactly 40.0.0 (both save-exact, both legitimacy-approved in Task 1)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev toolchain | new devDependencies are pulled at install; a typosquat/compromised package could run install-time code |
| handoff/ (canonical) → package (candidate) | the parity script is the integrity check that the copy stayed faithful |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm devDep installs (stylelint et al.) | high | mitigate | Blocking human legitimacy checkpoint (no RESEARCH.md audit → [ASSUMED]); each package verified on npmjs.com; versions pinned + save-exact; none ship in the published package |
| T-02-PARITY | Tampering | copy-verify integrity | high | mitigate | parity.mjs does a placement-aware declaration diff (keyed by file+selector+property+occurrence) + token/class inventory against handoff/, with a small intentional-divergence allowlist, and fails CI (02-06) on any unlisted drift or relocation |
| T-02-ICON | Denial/Info-disclosure | shipped `.css` external `url()` | high | mitigate | parity.mjs external-URL guard fails on any `url(...http...)` in packages/styles/**/*.css (SVG `xmlns` data-URI literal excluded); runs in CI on every PR |
</threat_model>

<verification>
- Legitimacy checkpoint approved before installs (incl. stylelint-config-standard@40.0.0).
- parity.mjs green (209 tokens, 248 classes, placement-aware, no-CDN guard) and fails on injected value drift, selector/file relocation, OR an injected external `url()` (STY-06).
- stylelint config green on the package CSS.
- CI wiring of both gates happens in plan 02-06.
</verification>

<success_criteria>
- STY-06 parity script exists, passes on faithful copy, fails on drift with a naming message.
- stylelint gate green.
- Phase-2 devDeps legitimacy-cleared.
</success_criteria>

## Artifacts this phase produces (this plan's slice)

- `tools/parity/parity.mjs` — STY-06 token (209) + `.lyra-*` class (248) parity validator (zero-dep Node, tokenizer/state-machine parser) with placement- + cascade-aware declaration diff (file+at-rule-ancestry+selector+property+occurrence+order), an intentional-divergence allowlist (fonts `@import`, 3 chevron mask URLs), and a broadened package-wide external-URL guard (rejects absolute schemes + protocol-relative, allows `data:`/relative)
- `tools/parity/fixtures/` — parser fixtures (nested `@media`, nested `@container`, `@keyframes`, quoted `data:` URI with interior `;`) proving the tokenizer segments them correctly
- `packages/styles/.stylelintrc.json` — stylelint config extending pinned `stylelint-config-standard@40.0.0` (the package's automated CSS gate)
- root `package.json` — `parity` script + `stylelint@17.14.0` + `stylelint-config-standard@40.0.0` devDependencies (save-exact); `publint@0.3.21` + `vite@8.1.5` legitimacy-approved here (installed in 02-06)

<output>
Create `.planning/phases/02-styles-package/02-04-SUMMARY.md` when done.
</output>
