---
phase: 02-styles-package
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - tools/parity/parity.mjs
  - packages/styles/.stylelintrc.json
  - package.json
autonomous: false
requirements: [STY-06]
user_setup: []
must_haves:
  truths:
    - "A plain Node parity script proves 209/209 handoff token declarations (base+brand+colors+effects+spacing+typography, including `[data-theme=\"dark\"]` re-declarations) are present in the package with identical values, normalizing whitespace only; fonts.css is special-cased (0 tokens, exempt) (STY-06, D-05)."
    - "The same script inventories the 248 unique `.lyra-*` class names from handoff/components/** and asserts the package's component CSS carries the identical set (STY-06, D-06)."
    - "On drift the script exits non-zero and names the drifted token/class plus points at handoff/ as canonical (UI-SPEC line 148 error contract)."
    - "A stylelint config exists as the styles package's automated CSS quality gate (STACK.md)."
    - { statement: "parity determinism/adjacency edge (STY-06/STY-01): the 209-count and 248-class set are exact equality checks — a token present in both with identical value passes; a value that merely 'touches' but differs fails loudly rather than silently merging.", verification: covered }
  prohibitions:
    - "MUST NOT compare against a separate committed lockfile — compare directly against handoff/ so drift is caught at source (D-05)."
    - "MUST NOT count fonts.css or compat-shadcn.css toward the 209 (fonts diverges/0 tokens; compat is opt-in, outside the entry)."
    - "MUST NOT install any package whose npm-registry legitimacy has not been confirmed (no RESEARCH.md audit exists → fallback [ASSUMED] → blocking human verify)."
  artifacts:
    - tools/parity/parity.mjs
    - packages/styles/.stylelintrc.json
  key_links:
    - "parity.mjs → reads handoff/tokens + handoff/components (canonical) vs packages/styles/** (candidate); wired into CI test job in plan 02-06."
---

<objective>
Author the STY-06 parity validator (a plain Node script comparing the package's tokens + `.lyra-*` classes value-for-value against the canonical `handoff/`) and the stylelint config that becomes the styles package's automated CSS quality gate. A blocking human legitimacy checkpoint clears the new Phase-2 devDependencies (no RESEARCH.md audit exists) before install.

Purpose: STY-06 is the phase's correctness guarantee — it proves the copy-verify stayed faithful. Stylelint is the only other automated gate for a no-build CSS package.
Output: tools/parity/parity.mjs, packages/styles/.stylelintrc.json, devDeps added to root package.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-styles-package/02-CONTEXT.md
@.planning/phases/02-styles-package/02-UI-SPEC.md
@.planning/phases/02-styles-package/02-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint — Phase 2 devDependencies</name>
  <what-built>No RESEARCH.md Package Legitimacy Audit exists for this phase (research was skipped), so the fallback treats new packages as [ASSUMED]. This checkpoint clears the Phase-2 dev-only tooling before any install. All are version-pinned in CLAUDE.md's Recommended Stack (npm-registry-verified, 2026-07-16). None ship in the published package (which has zero runtime deps).</what-built>
  <how-to-verify>
    Confirm each package on npmjs.com (name, publisher, that the pinned version exists and is not typosquatted):
    1. stylelint @ 17.14.0 — https://www.npmjs.com/package/stylelint
    2. vitest @ 4.1.10 — https://www.npmjs.com/package/vitest
    3. @vitest/browser-playwright @ 4.1.10 — https://www.npmjs.com/package/@vitest/browser-playwright
    4. playwright @ 1.61.1 — https://www.npmjs.com/package/playwright
    5. publint (latest stable) — https://www.npmjs.com/package/publint
    (Items 2-4 are installed in plan 02-05 and item 5 in plan 02-06, but all are approved here so those plans stay autonomous.)
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with installs, or name any package to reject.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Author the token + class parity script (STY-06)</name>
  <read_first>
    - 02-PATTERNS.md "No Analog Found" row for tools/parity (canonical extraction commands, 209 target, 248-class inventory, error contract) + D-05/D-06
    - handoff/tokens/{base,brand,colors,effects,spacing,typography}.css (canonical token source — fonts.css EXCLUDED) and handoff/components/**/*.css (canonical class source)
    - packages/styles/tokens/** and packages/styles/components/** (the candidate copies from plans 02-01/02-02)
  </read_first>
  <action>Create tools/parity/parity.mjs (plain Node, ESM, zero deps). Token check: extract every `--name: value;` declaration from the six canonical handoff token files (exclude fonts.css and compat-shadcn.css), normalize interior whitespace only, and assert the identical multiset is present in packages/styles/tokens/** — total canonical declaration count MUST equal 209 (includes `[data-theme="dark"]` re-declarations). Class check: extract the unique `.lyra-*` names from handoff/components/**/*.css (canonical set = 248) and assert packages/styles/components/** carries the identical set. On ANY mismatch, print a message naming the drifted token/class and its handoff-vs-package values (e.g. `Token --accent value mismatch: package=X handoff=Y — handoff/ is canonical`) and exit non-zero; on full match print `parity OK: 209 tokens, 248 classes` and exit 0. Add a root package.json script `"parity": "node tools/parity/parity.mjs"`.</action>
  <verify>
    <automated>node tools/parity/parity.mjs && node tools/parity/parity.mjs | grep -q '209' && node -e "if(!require('./package.json').scripts.parity)throw new Error('parity script missing')"</automated>
  </verify>
  <done>`node tools/parity/parity.mjs` exits 0 against the current copied CSS, reports 209 tokens + 248 classes, and a deliberate value edit in any token file makes it exit non-zero with a naming message; root `parity` script wired.</done>
  <acceptance_criteria>
    - `node tools/parity/parity.mjs` exits 0 and prints the 209/248 counts
    - temporarily changing one token value in packages/styles/tokens/ makes the script exit non-zero and name that token (revert after checking)
    - fonts.css and compat-shadcn.css contribute 0 to the 209 count
    - root package.json has a `parity` script
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author the stylelint config for the styles package</name>
  <read_first>
    - 02-PATTERNS.md "No Analog Found" row for `.stylelintrc*` (no lint config exists yet; stylelint 17.14.0 per STACK.md)
    - packages/styles/**/*.css (the CSS this config lints) + root package.json lint conventions
    - CLAUDE.md Recommended Stack row for stylelint (version 17.14.0)
  </read_first>
  <action>Add `stylelint@17.14.0` (and a standard config such as `stylelint-config-standard`) to root devDependencies with pnpm (save-exact per repo .npmrc). Create packages/styles/.stylelintrc.json extending the standard config, tuned so the handoff's modern CSS (color-mix, custom properties, the intentional non-8 radius/spacing steps, `[data-theme]`/`[data-brand]` selectors) lints clean — do NOT introduce rules that would flag the locked token values. Confirm the `lint:css` script authored in plan 02-03 (`stylelint` over the package CSS) runs green.</action>
  <verify>
    <automated>pnpm --filter @lyra-ds/styles exec stylelint '**/*.css' && test -f packages/styles/.stylelintrc.json</automated>
  </verify>
  <done>stylelint installed at the pinned version; .stylelintrc.json exists; running stylelint over packages/styles/**/*.css exits 0 with no errors on the copied handoff CSS.</done>
  <acceptance_criteria>
    - `packages/styles/.stylelintrc.json` exists and extends a standard config
    - stylelint over `packages/styles/**/*.css` exits 0 (no violations on the locked handoff CSS)
    - stylelint version in root devDependencies is exactly 17.14.0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev toolchain | new devDependencies are pulled at install; a typosquat/compromised package could run install-time code |
| handoff/ (canonical) → package (candidate) | the parity script is the integrity check that the copy stayed faithful |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | npm devDep installs (stylelint et al.) | high | mitigate | Blocking human legitimacy checkpoint (no RESEARCH.md audit → [ASSUMED]); each package verified on npmjs.com; versions pinned + save-exact; none ship in the published package |
| T-02-PARITY | Tampering | copy-verify integrity | high | mitigate | parity.mjs diffs every token value + `.lyra-*` name against handoff/ and fails CI (02-06) on drift |
</threat_model>

<verification>
- Legitimacy checkpoint approved before installs.
- parity.mjs green (209 tokens, 248 classes) and fails on injected drift (STY-06).
- stylelint config green on the package CSS.
- CI wiring of both gates happens in plan 02-06.
</verification>

<success_criteria>
- STY-06 parity script exists, passes on faithful copy, fails on drift with a naming message.
- stylelint gate green.
- Phase-2 devDeps legitimacy-cleared.
</success_criteria>

## Artifacts this phase produces (this plan's slice)

- `tools/parity/parity.mjs` — STY-06 token (209) + `.lyra-*` class (248) parity validator (zero-dep Node)
- `packages/styles/.stylelintrc.json` — stylelint config (the package's automated CSS gate)
- root `package.json` — `parity` script + `stylelint` (+ standard config) devDependency

<output>
Create `.planning/phases/02-styles-package/02-04-SUMMARY.md` when done.
</output>
