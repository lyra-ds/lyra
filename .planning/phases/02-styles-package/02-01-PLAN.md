---
phase: 02-styles-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/styles/tokens/base.css
  - packages/styles/tokens/colors.css
  - packages/styles/tokens/typography.css
  - packages/styles/tokens/spacing.css
  - packages/styles/tokens/effects.css
  - packages/styles/tokens/brand.css
  - packages/styles/tokens/fonts.css
autonomous: true
requirements: [STY-03, STY-04]
must_haves:
  truths:
    - "Every semantic token has a [data-theme=\"dark\"] override in colors.css + effects.css, so setting data-theme=\"dark\" on <html> re-themes with no rebuild (STY-03, D-01)."
    - "brand.css ships the [data-brand] (light) and [data-theme=\"dark\"][data-brand] (dark) color-mix(in oklab, …) derivation groups verbatim, inert until [data-brand] is present (STY-04, D-01)."
    - "The dark-elevation contract is preserved verbatim: [data-theme=\"dark\"] zeroes --shadow-xs and --shadow-sm (none) and relies on borders, not shadow (UI-SPEC line 117)."
    - "With no data-theme and no data-brand attributes, the token set resolves to the baseline light Lyra appearance (STY-01 empty-input edge → light default)."
    - { statement: "concurrency edge (STY-02 probe) is N/A: static CSS token files have no runtime execution or shared mutable state — subpath reads are independent file loads with no interruption semantics.", verification: flagged-assumption }
  prohibitions:
    - "MUST NOT reintroduce a runtime CDN font @import (the handoff fonts.css Google Fonts @import is dropped — .claude/CLAUDE.md 'nenhuma dependência de CDN em runtime')."
    - "MUST NOT embed webfonts in the package (Plus Jakarta Sans + JetBrains Mono stay peer via @fontsource/*)."
    - "MUST NOT alter any token value — copy-verify only; the handoff bytes are the parity target (STY-06)."
  artifacts:
    - packages/styles/tokens/base.css
    - packages/styles/tokens/colors.css
    - packages/styles/tokens/typography.css
    - packages/styles/tokens/spacing.css
    - packages/styles/tokens/effects.css
    - packages/styles/tokens/brand.css
    - packages/styles/tokens/fonts.css
  key_links:
    - "colors.css [data-theme=\"dark\"] block → drives STY-03 theme switch for every consumer semantic token."
    - "brand.css color-mix derivation → the STY-04 white-label contract validated by Browser Mode (plan 02-05)."
---

<objective>
Copy the seven Lyra design-token CSS files from `handoff/tokens/` into `packages/styles/tokens/`, byte-faithfully (values are the STY-06 parity target), stripping the pt-BR explanatory comment prose (D-03) and prepending a minimal EN header banner. One file — `fonts.css` — intentionally diverges: it drops the forbidden Google Fonts CDN `@import` and ships as a peer-install-documenting stub.

Purpose: These tokens are the foundation of the whole `@lyra-ds/styles` package — dark theming (STY-03) and the white-label brand contract (STY-04) live entirely here.
Output: 7 token CSS files under `packages/styles/tokens/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-styles-package/02-CONTEXT.md
@.planning/phases/02-styles-package/02-UI-SPEC.md
@.planning/phases/02-styles-package/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy the five core value-token files (base, colors, typography, spacing, effects)</name>
  <read_first>
    - Source of truth (copy verbatim): handoff/tokens/base.css, handoff/tokens/colors.css, handoff/tokens/typography.css, handoff/tokens/spacing.css, handoff/tokens/effects.css
    - handoff/tokens/colors.css §[data-theme="dark"] (dark overrides, STY-03) and handoff/tokens/effects.css §[data-theme="dark"] (elevation rule, UI-SPEC line 117)
    - 02-PATTERNS.md "Token CSS files" section (banner pattern, dark-override pattern, structural-comment note) and 02-CONTEXT.md D-03
  </read_first>
  <action>Create packages/styles/tokens/{base,colors,typography,spacing,effects}.css as verbatim copies of the identically-named handoff/tokens/ files. Strip the pt-BR explanatory paragraph comment blocks (D-03); KEEP thin structural grouping labels (e.g. `/* ---- spacing scale ---- */`, `/* default */`). Prepend exactly one minimal EN header banner line per file in the form `/* @lyra-ds/styles — <file purpose> · MIT */`. Preserve every `--token: value;` declaration value-for-value (this is the STY-06 parity target), the `var()` token indirection, and the full `[data-theme="dark"]` override blocks including the elevation rule `--shadow-xs: none; --shadow-sm: none;` in dark. Do not reformat or reorder declarations.</action>
  <verify>
    <automated>test $(grep -c '^\s*--[a-zA-Z0-9-]*:' packages/styles/tokens/colors.css) -eq $(grep -c '^\s*--[a-zA-Z0-9-]*:' handoff/tokens/colors.css) && grep -q 'data-theme="dark"' packages/styles/tokens/colors.css && grep -q 'shadow-xs: *none' packages/styles/tokens/effects.css && head -1 packages/styles/tokens/spacing.css | grep -q '@lyra-ds/styles'</automated>
  </verify>
  <done>All five files exist under packages/styles/tokens/; each carries an EN banner as line 1; colors.css declaration count equals the handoff source and contains the [data-theme="dark"] block; effects.css preserves the dark elevation-zeroing rule; no pt-BR prose paragraph remains.</done>
  <acceptance_criteria>
    - `grep -c '^\s*--' packages/styles/tokens/colors.css` equals the same count over handoff/tokens/colors.css (109 declarations)
    - `packages/styles/tokens/effects.css` contains `--shadow-xs: none;` and `--shadow-sm: none;` inside a `[data-theme="dark"]` block
    - Line 1 of each of the five files matches `/* @lyra-ds/styles — … · MIT */`
    - No Google Fonts / CDN URL and no multi-line pt-BR comment paragraph present in any of the five files
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Copy brand.css verbatim and author the fonts.css peer stub (CDN divergence)</name>
  <read_first>
    - Source of truth: handoff/tokens/brand.css (lines 16-40 = the [data-brand] light + [data-theme="dark"][data-brand] dark color-mix derivation groups — copy verbatim)
    - handoff/tokens/fonts.css (the one file that MUST diverge — drop the line-5 Google Fonts `@import url(...)`)
    - 02-PATTERNS.md "brand.css" + "fonts.css" sections; 02-CONTEXT.md D-03a; 02-UI-SPEC.md "Fonts / no-CDN divergence" (line 41) and White-Label Brand Contract table
  </read_first>
  <action>Create packages/styles/tokens/brand.css by copying the two derivation blocks from handoff/tokens/brand.css verbatim: the `[data-brand]` group (`--accent: var(--brand)`, `--accent-hover/active/soft/soft-text` via `color-mix(in oklab, …)`, `--on-accent: var(--brand-contrast, #FFFFFF)`, `--focus-ring`, `--text-link`, `--border-accent`, `--radius-md: var(--brand-radius, 10px)`, `--font-sans`/`--font-display: var(--brand-font, …)`) and the `[data-theme="dark"][data-brand], [data-theme="dark"] [data-brand]` group (white-mix lightening: hover +26%, active +38%, soft 18%, soft-text +45%, focus-ring 30%, link +40%). Strip the pt-BR contract comment (handoff lines 1-14) — that documentation migrates to the README in plan 02-03 (D-03a). Prepend the EN banner. Then create packages/styles/tokens/fonts.css as a comment-only stub: EN banner + a short EN note that Plus Jakarta Sans and JetBrains Mono are PEER installs via `@fontsource/plus-jakarta-sans` and `@fontsource/jetbrains-mono`. The stub carries ZERO token declarations and MUST NOT contain any `@import url(...)`.</action>
  <verify>
    <automated>grep -q 'color-mix(in oklab' packages/styles/tokens/brand.css && grep -q 'data-theme="dark"' packages/styles/tokens/brand.css && ! grep -rq '@import url' packages/styles/tokens/fonts.css && test $(grep -c '^\s*--[a-zA-Z0-9-]*:' packages/styles/tokens/fonts.css) -eq 0 && grep -q '@fontsource' packages/styles/tokens/fonts.css</automated>
  </verify>
  <done>brand.css ships both color-mix derivation groups (light + dark) verbatim without the pt-BR contract comment; fonts.css is a token-free stub documenting the @fontsource peer install with NO CDN @import.</done>
  <acceptance_criteria>
    - `grep -c 'color-mix(in oklab' packages/styles/tokens/brand.css` is ≥ 10 (the derived accent group, both themes)
    - `packages/styles/tokens/fonts.css` contains zero `--token:` declarations and zero `@import url(` occurrences
    - `packages/styles/tokens/fonts.css` names `@fontsource/plus-jakarta-sans` and `@fontsource/jetbrains-mono`
    - brand.css contains no pt-BR paragraph comment; only the EN banner comment
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package → consumer runtime | shipped CSS loads in every consumer's browser; a stray CDN `@import` would create a third-party runtime dependency |

## STRIDE Threat Register (ASVS L1 — packaging/supply-chain framing)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-CDN | Denial/Info-disclosure | tokens/fonts.css | high | mitigate | Ship fonts.css as a token-free peer stub with NO `@import url(...)`; verify with `! grep '@import url'`; parity script (02-04) special-cases fonts.css so the divergence is intentional, not drift |
| T-02-VAL | Tampering | tokens/*.css values | medium | mitigate | Copy-verify only; the parity script (02-04, STY-06) diffs every value against handoff/ as canonical and fails on drift |
</threat_model>

<verification>
- All 7 token files exist under packages/styles/tokens/.
- colors.css + effects.css carry the [data-theme="dark"] overrides (STY-03) including the dark elevation-zeroing rule.
- brand.css carries the color-mix derivation groups for light and dark (STY-04).
- fonts.css has no CDN @import and no token declarations (no-CDN constraint).
- Definitive value-for-value parity is enforced later by the parity script (plan 02-04, STY-06).
</verification>

<success_criteria>
- packages/styles/tokens/{base,colors,typography,spacing,effects,brand,fonts}.css all present.
- Each shipped CSS file has an EN header banner and no pt-BR prose.
- STY-03 (dark override blocks) and STY-04 (brand color-mix groups) present verbatim.
- fonts.css is a peer stub (no CDN, no tokens).
</success_criteria>

## Artifacts this phase produces (this plan's slice)

- `packages/styles/tokens/base.css` — base/reset layer (references tokens; 0 token declarations)
- `packages/styles/tokens/colors.css` — color scales + semantic aliases + `[data-theme="dark"]` overrides (STY-03)
- `packages/styles/tokens/typography.css` — type scale, weights, line-heights, composed semantic styles
- `packages/styles/tokens/spacing.css` — 4px-grid spacing ramp, radii, control sizes, layout tokens
- `packages/styles/tokens/effects.css` — shadows/durations/easings + dark elevation rule
- `packages/styles/tokens/brand.css` — white-label `[data-brand]` color-mix contract (STY-04)
- `packages/styles/tokens/fonts.css` — peer-font stub (INTENTIONAL divergence: no CDN @import)

<output>
Create `.planning/phases/02-styles-package/02-01-SUMMARY.md` when done.
</output>
