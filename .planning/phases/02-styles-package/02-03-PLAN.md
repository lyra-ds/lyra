---
phase: 02-styles-package
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - packages/styles/styles.css
  - packages/styles/compat-shadcn.css
  - packages/styles/package.json
  - packages/styles/README.md
autonomous: true
requirements: [STY-01, STY-02, STY-05, STY-07]
must_haves:
  truths:
    - "A single `import '@lyra-ds/styles'` (or `@import`) resolves the `.` export to styles.css, which `@import`s the 7 tokens + 7 component files in the exact handoff order — one import styles all 209 tokens + 40 components (STY-01, D-01)."
    - "The literal subpath `import '@lyra-ds/styles/styles.css'` also resolves, via an explicit `\"./styles.css\": \"./styles.css\"` export key (required by PROJECT.md:19 + ROADMAP success-criterion #1 which name the `styles.css` entry literally; addresses review concern: missing `./styles.css` export)."
    - "Individual token files load via the `./tokens/*` subpath export (STY-02, D-02); no per-component subpaths are exposed (D-02 deferred)."
    - "compat-shadcn.css is reachable ONLY via its explicit `./compat-shadcn.css` export and is NEVER `@import`ed by styles.css — it stays outside the default entry (STY-05, D-02, locked)."
    - "package.json declares `\"sideEffects\": [\"**/*.css\"]` (never false) so consumer bundlers never tree-shake the CSS import away (STY-07)."
    - "With no data-theme/data-brand set, importing the entry yields the baseline light appearance (STY-01 empty-input edge)."
    - { statement: "exports-key ordering edge (STY-07 probe): the four export keys ('.', './styles.css', './tokens/*', './compat-shadcn.css') are non-overlapping — '.' and './styles.css' both resolve to the same target file, and the './tokens/*' wildcard does not match either the top-level compat entry or './styles.css' — so there is no ambiguous resolution order.", verification: covered }
    - { statement: "import-safety: sideEffects:['**/*.css'] + a publint-green exports map + a packed-artifact smoke test running a REAL vite consumer build (root `@lyra-ds/styles` + `./styles.css` + a token subpath, asserting a `.lyra-*` class + a token in the emitted CSS) (02-06) guard against consumers' bundlers dropping the CSS or a broken `files`/relative-import surface, and compat stays out of the entry — definitive evidence needs the publint gate + the vite pack-smoke build + import-order assertion in plan 02-06.", verification: backstop }
  prohibitions:
    - "MUST NOT set `\"sideEffects\": false` on this package (silently drops the CSS import in consumer prod builds — the classic broken-CSS-package bug)."
    - "MUST NOT `@import` compat-shadcn.css from styles.css (STY-05, locked)."
    - "MUST NOT add per-component CSS subpath exports (D-02, deferred to future)."
    - "MUST NOT include the `./compat-shadcn.css` entry in the `./tokens/*` wildcard."
  artifacts:
    - packages/styles/styles.css
    - packages/styles/compat-shadcn.css
    - packages/styles/package.json
    - packages/styles/README.md
  key_links:
    - "package.json `exports` map → resolves `.`, `./tokens/*`, `./compat-shadcn.css`; the single guard against a broken import surface (STY-01/STY-02/STY-05)."
    - "styles.css `@import` chain → pulls tokens (02-01) + components (02-02) in handoff order; omission of compat is the STY-05 assertion."
---

<objective>
Author the package's import surface and manifest: the entry `styles.css` (the verbatim 14-line handoff `@import` chain, minus compat), the relocated opt-in `compat-shadcn.css` subpath, the full `package.json` (exports map, `sideEffects: ["**/*.css"]`, files allowlist, publishability flip), and the `README.md` (canonical brand + fonts-peer docs, migrated from the stripped brand.css comments per D-03a).

Purpose: This is what makes STY-01 (single entry), STY-02 (`./tokens/*` subpath), STY-05 (compat opt-in), and STY-07 (`sideEffects`) TRUE.
Output: styles.css, compat-shadcn.css, package.json, README.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-styles-package/02-CONTEXT.md
@.planning/phases/02-styles-package/02-UI-SPEC.md
@.planning/phases/02-styles-package/02-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author entry styles.css and the opt-in compat-shadcn.css subpath</name>
  <read_first>
    - handoff/styles.css (the exact 14-line @import order to reproduce — already omits compat)
    - handoff/tokens/compat-shadcn.css (the shadcn→Lyra `var()` mapping under `:root, [data-theme="dark"]` to relocate)
    - 02-PATTERNS.md "styles.css" + "compat-shadcn.css" sections; 02-UI-SPEC.md entry-order block (lines 33-39) and Registry Safety section; D-02 (subpath naming discretion → choose `./compat-shadcn.css`)
  </read_first>
  <action>Create packages/styles/styles.css reproducing the handoff/styles.css `@import` chain verbatim in this exact order: tokens/fonts, tokens/colors, tokens/typography, tokens/spacing, tokens/effects, tokens/brand, tokens/base, then components/buttons, forms, display, navigation, feedback, files, data. Prepend the EN banner. It MUST NOT reference compat-shadcn.css. Then create packages/styles/compat-shadcn.css at the package root (chosen over `./compat/shadcn.css` for the cleaner exports entry, resolving D-02) by copying the shadcn→Lyra `var()` mapping from handoff/tokens/compat-shadcn.css verbatim under `:root, [data-theme="dark"]`; strip pt-BR prose, add the EN banner. This file is never imported by styles.css.</action>
  <verify>
    <automated>test $(grep -c '^@import' packages/styles/styles.css) -eq 14 && ! grep -q 'compat' packages/styles/styles.css && grep -q -- '--background: *var(--surface-page)' packages/styles/compat-shadcn.css && diff <(grep -oE '@import "[^"]+"' packages/styles/styles.css) <(grep -oE '@import "[^"]+"' handoff/styles.css)</automated>
  </verify>
  <done>styles.css contains exactly the 14 handoff @import lines in identical order and no compat reference; compat-shadcn.css exists at package root with the shadcn var-mapping.</done>
  <acceptance_criteria>
    - `grep -c '^@import' packages/styles/styles.css` equals 14 and the extracted @import list is byte-identical to handoff/styles.css
    - `grep 'compat' packages/styles/styles.css` returns nothing (STY-05 omission)
    - `packages/styles/compat-shadcn.css` maps at least `--background`, `--foreground`, `--primary`, `--destructive`, `--ring`, `--radius` to Lyra tokens via `var()`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author package.json manifest and README brand/fonts docs</name>
  <read_first>
    - packages/styles/package.json (current placeholder to replace) + root package.json (repo conventions: packageManager, engines, version semantics)
    - .planning/research/PITFALLS.md Pitfalls 1 & 2 (sideEffects, exports map, files allowlist, dual import/@import resolution)
    - handoff/tokens/brand.css lines 1-14 (pt-BR contract comment → migrate to README in EN, D-03a); 02-UI-SPEC.md White-Label Brand Contract table + Copywriting Contract (banner/README/fonts rows)
  </read_first>
  <action>Replace packages/styles/package.json: keep name `@lyra-ds/styles`; remove `"private": true` (flip publishable per Phase-1 lockstep, version stays lockstep at current monorepo version); add `"sideEffects": ["**/*.css"]` (EXACT value — never false); add an `"exports"` map with `"."` → `"./styles.css"`, `"./styles.css"` → `"./styles.css"` (the explicit literal-subpath export required by PROJECT.md:19 + ROADMAP criterion #1 — addresses review concern: a consumer doing `import '@lyra-ds/styles/styles.css'` would 404 under an exports map without it), `"./tokens/*"` → `"./tokens/*"` (STY-02, D-02), and `"./compat-shadcn.css"` → `"./compat-shadcn.css"` (STY-05) — NO per-component subpaths (the STACK.md draft's `./components/*` key stays OUT per locked decision D-02); add `"files"` allowlist `["styles.css","tokens","components","compat-shadcn.css","README.md"]`; add `"publishConfig": { "access": "public" }` (Claude's Discretion per PITFALLS provenance/access note); add package scripts `"lint:css"` (stylelint over the package CSS), `"test"` (`vitest run`) — the referenced config/tooling land in plans 02-04 and 02-05, so these strings may point at not-yet-created files at author time. Then create packages/styles/README.md documenting: the 4-token brand contract table (`--brand` required; `--brand-contrast` default #FFFFFF; `--brand-radius` default 10px; `--brand-font` default Plus Jakarta Sans stack), a copy-pasteable `[data-brand="acme"] { --brand: #0D9488; }` example, and the fonts peer-install note — all in English (EN governance, D-03a). The fonts note MUST give a consumer enough to actually LOAD the fonts (addresses review concern: package-install alone does not load font CSS): the install commands (`@fontsource/plus-jakarta-sans` + `@fontsource/jetbrains-mono`) AND the explicit per-weight CSS imports that mirror the dropped CDN request — Plus Jakarta Sans weights 400–800 (e.g. `import '@fontsource/plus-jakarta-sans/400.css'` … `/800.css`) and JetBrains Mono weights 400–600 (`/400.css` … `/600.css`).</action>
  <verify>
    <automated>node -e "const p=require('./packages/styles/package.json'); const a=JSON.stringify(p.sideEffects); if(a!=='[\"**/*.css\"]')throw new Error('sideEffects='+a); if(p.private)throw new Error('still private'); if(!p.exports['.']||!p.exports['./styles.css']||!p.exports['./tokens/*']||!p.exports['./compat-shadcn.css'])throw new Error('exports missing'); if(p.exports['./styles.css']!=='./styles.css')throw new Error('./styles.css export target wrong'); if(JSON.stringify(p.exports).includes('/components/'))throw new Error('per-component subpath leaked'); if(!p.files.includes('compat-shadcn.css'))throw new Error('files missing compat')" && grep -q 'data-brand="acme"' packages/styles/README.md && grep -q '@fontsource' packages/styles/README.md</automated>
  </verify>
  <done>package.json has `sideEffects: ["**/*.css"]`, no `private`, the four-key exports map (`.`, `./styles.css`, `./tokens/*`, `./compat-shadcn.css`; no per-component subpaths), the files allowlist including compat-shadcn.css; README documents the 4-token brand contract, the acme example, and the @fontsource peer install in English.</done>
  <acceptance_criteria>
    - `package.json` `sideEffects` deep-equals `["**/*.css"]` and `private` is absent/false
    - `exports` has exactly `.`, `./styles.css`, `./tokens/*`, `./compat-shadcn.css`; both `.` and `./styles.css` target `./styles.css`; contains no `/components/` key
    - `files` includes styles.css, tokens, components, compat-shadcn.css, README.md
    - README.md contains the `[data-brand="acme"]` block, the 4-token table, the two `@fontsource/*` package names, AND the explicit per-weight CSS import lines (Plus Jakarta Sans 400–800, JetBrains Mono 400–600) a consumer needs to actually load the fonts
    - the packed-artifact smoke test (authored in 02-06) runs a real vite consumer build importing the ROOT `@lyra-ds/styles`, the literal `@lyra-ds/styles/styles.css`, and a `./tokens/*` subpath from the installed tarball, and asserts a `.lyra-*` class + a token in the emitted CSS (proves the root export bundles, not just resolves) — addresses review concern: root-import coverage
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package manifest → consumer bundler | `sideEffects` + `exports` + `files` decide what ships and whether the CSS import survives tree-shaking |

## STRIDE Threat Register (ASVS L1 — packaging integrity)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SE | Tampering | package.json `sideEffects` | high | mitigate | Enforce exact `["**/*.css"]`; a Node assertion fails the task on any other value; publint gate (02-06) re-checks |
| T-02-FILES | Info-disclosure/Integrity | package.json `files` | high | mitigate | Explicit allowlist prevents leaking handoff assets/scratch files; verified by the pnpm-pack tarball inspection (02-06) |
| T-02-COMPAT | Tampering | styles.css / exports | medium | mitigate | Assert `grep compat` is empty in styles.css AND `./compat-shadcn.css` is an explicit standalone export (STY-05) |
| T-02-EXPORTS | Tampering | exports map | medium | mitigate | Assert `.` AND `./styles.css` both resolve (import + `@import`); no `/components/` subpath leaked; publint green + packed-artifact smoke test resolving both the entry and a token subpath (02-06) |
</threat_model>

<verification>
- styles.css = 14 handoff imports, compat omitted (STY-01, STY-05).
- package.json sideEffects exact, exports 4 keys (incl. `./styles.css`), files allowlist (STY-02, STY-05, STY-07).
- README carries brand contract + acme + fonts peer note (D-03a).
- publint-green confirmation happens in plan 02-06.
</verification>

<success_criteria>
- Single entry import styles all tokens + components (STY-01).
- `./tokens/*` subpath present, no per-component subpaths (STY-02).
- compat opt-in only, out of entry (STY-05).
- sideEffects `["**/*.css"]` set (STY-07).
</success_criteria>

## Artifacts this phase produces (this plan's slice)

- `packages/styles/styles.css` — aggregate entry (14 `@import`s, handoff order, compat excluded)
- `packages/styles/compat-shadcn.css` — opt-in shadcn→Lyra var mapping (explicit subpath)
- `packages/styles/package.json` — `exports` (`.`, `./styles.css`, `./tokens/*`, `./compat-shadcn.css`), `sideEffects: ["**/*.css"]`, `files` allowlist, `publishConfig`, `lint:css`/`test` scripts, publishability flip
- `packages/styles/README.md` — brand 4-token contract + `[data-brand="acme"]` example + @fontsource peer-install note

<output>
Create `.planning/phases/02-styles-package/02-03-SUMMARY.md` when done.
</output>
