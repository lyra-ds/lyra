---
phase: 02-styles-package
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/styles/components/buttons/buttons.css
  - packages/styles/components/forms/forms.css
  - packages/styles/components/display/display.css
  - packages/styles/components/navigation/navigation.css
  - packages/styles/components/feedback/feedback.css
  - packages/styles/components/files/files.css
  - packages/styles/components/data/data.css
autonomous: true
requirements: [STY-01]
must_haves:
  truths:
    - "All seven component category CSS files ship the handoff `.lyra-*` classes (248 unique across categories) referencing tokens via `var(--token)`, so a single CSS import styles all 40 components (STY-01, D-01)."
    - "Component styles reference semantic tokens only via `var()` (never hard-coded values), so `[data-theme=\"dark\"]` and `[data-brand]` re-skin the components with no rebuild (token-indirection contract, 02-PATTERNS.md)."
    - "Component files import in the handoff cascade order (buttons → forms → display → navigation → feedback → files → data); cross-component collisions resolve by that deterministic order (STY-01 adjacency edge)."
    - "NO shipped `.css` contains a runtime-fetched external `url(...)`: the handoff's three `unpkg.com/lucide-static` chevron mask URLs (forms.css chevron-down, display.css chevron-down, navigation.css chevron-right) are rewritten to inline `data:image/svg+xml` URIs with the local Lucide chevron path data, honoring the no-runtime-CDN constraint (.claude/CLAUDE.md; addresses review concern: runtime CDN leak in component CSS)."
    - { statement: "component inventory 40 vs 38 contracts: AvatarGroup (`.lyra-avatar-group`, display.css) and ToastStack (`.lyra-toast-stack`, feedback.css) are COMPOUND components whose CSS ships inside their parent category file — they have no standalone `.jsx`/`.d.ts` contract, so the 38 standalone contracts + these 2 compositions = the 40 named components; Phase 3 inherits them as compositions, not new exports (addresses review concern: 38-vs-40 inventory).", verification: covered }
    - { statement: "ordering-of-equal edge (STY-01 probe) is N/A as an algorithmic sort: CSS has no 'equal elements' to stably order — cascade precedence is governed by source/@import order, which is fixed by the handoff and asserted in plan 02-03.", verification: flagged-assumption }
  prohibitions:
    - "MUST NOT create `components/icons/` — icons carry no CSS (React/Lucide, Phase 3)."
    - "MUST NOT split a category into per-component files — each category ships ONE aggregate CSS file (verified layout)."
    - "MUST NOT alter any `.lyra-*` class name or token/value declaration — copy-verify only (STY-06/D-06 parity target); the ONLY intentional value divergences are the three `unpkg.com` chevron mask URLs rewritten to inline `data:` URIs (allowlisted in the 02-04 parity script)."
    - "MUST NOT ship any `url(\"http://…\")`/`url(\"https://…\")` runtime reference in any `.css`; the SVG namespace literal `xmlns=\"http://www.w3.org/2000/svg\"` inside a `data:` URI is NOT a fetch and is explicitly allowed (the guard targets `url(...http...)`, not bare `http`)."
  artifacts:
    - packages/styles/components/buttons/buttons.css
    - packages/styles/components/forms/forms.css
    - packages/styles/components/display/display.css
    - packages/styles/components/navigation/navigation.css
    - packages/styles/components/feedback/feedback.css
    - packages/styles/components/files/files.css
    - packages/styles/components/data/data.css
  key_links:
    - "Every `.lyra-*` rule → `var(--token)` references from plan 02-01's token layer; broken indirection would break theming/branding silently."
---

<objective>
Copy the seven aggregate component CSS files from `handoff/components/<category>/` into `packages/styles/components/<category>/`, byte-faithfully — the `.lyra-*` class set (248 unique classes, per-category counts below) is the STY-01 style surface and the D-06 class-inventory parity target. Strip pt-BR prose (D-03), add the EN banner. ONE intentional divergence: the three `unpkg.com/lucide-static` chevron mask URLs (forms, display, navigation) are rewritten to inline local `data:` SVG URIs to honor the no-runtime-CDN constraint (addresses review concern: runtime CDN leak).

Purpose: These files are the actual component appearance; with plan 02-01's tokens + the entry (plan 02-03), a single `styles.css` import styles all 40 components (STY-01).
Output: 7 component CSS files under `packages/styles/components/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-styles-package/02-CONTEXT.md
@.planning/phases/02-styles-package/02-UI-SPEC.md
@.planning/phases/02-styles-package/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy buttons, forms, display, data component CSS</name>
  <read_first>
    - Source of truth (copy verbatim): handoff/components/buttons/buttons.css, handoff/components/forms/forms.css, handoff/components/display/display.css, handoff/components/data/data.css
    - 02-PATTERNS.md "Component CSS files" section (class-authoring pattern, class inventory table) + D-03/D-06
  </read_first>
  <action>Create packages/styles/components/{buttons/buttons,forms/forms,display/display,data/data}.css as verbatim copies of the identically-pathed handoff files. Strip pt-BR explanatory comment paragraphs (D-03); keep thin structural grouping labels. Prepend one EN header banner line per file. Preserve every `.lyra-*` selector and every existing literal + `var(--token)` reference exactly — no reformatting, no value edits. ONE mandated divergence in forms.css and display.css: the source has a runtime CDN mask at forms.css:73-74 and display.css:162-163 — both `-webkit-mask` and `mask` set to `url("https://unpkg.com/lucide-static@0.469.0/icons/chevron-down.svg") no-repeat center / 100%`. Replace BOTH the unpkg URL occurrences in EACH of the two properties with an inline `url('data:image/svg+xml;utf8,<svg …>')` encoding the local Lucide `chevron-down` glyph (viewBox 0 0 24 24, fill none, a solid opaque stroke e.g. `#000`, stroke-width 2, round caps/joins, single `<path d="m6 9 6 6 6-6"/>`), keeping the surrounding `no-repeat center / 100%` mask shorthand intact. This matches the existing local inline-SVG precedent already in the handoff at forms.css:204 (the checkmark data URI) and packages the icon locally with zero new files. Expected unique-class counts: buttons 13, forms 29, display 34, data 16.</action>
  <verify>
    <automated>for c in buttons/buttons forms/forms display/display data/data; do test $(grep -oh '\.lyra-[a-zA-Z0-9_-]*' packages/styles/components/$c.css | sort -u | wc -l) -eq $(grep -oh '\.lyra-[a-zA-Z0-9_-]*' handoff/components/$c.css | sort -u | wc -l) || exit 1; done && head -1 packages/styles/components/buttons/buttons.css | grep -q '@lyra-ds/styles' && ! grep -rnE 'url\(\s*["'"'"']?https?://' packages/styles/components/forms/forms.css packages/styles/components/display/display.css && grep -q 'chevron\|m6 9 6 6 6-6\|data:image/svg' packages/styles/components/forms/forms.css</automated>
  </verify>
  <done>The four files exist; each file's unique `.lyra-*` class count equals its handoff source; each carries an EN banner line 1; no pt-BR prose paragraph remains; forms.css and display.css contain ZERO `url(...http...)` external references — the chevron mask is a local inline `data:` URI.</done>
  <acceptance_criteria>
    - unique `.lyra-*` count per file equals handoff (buttons 13, forms 29, display 34, data 16)
    - every existing literal and `var(--…)` reference is preserved exactly (no raw hex/px introduced beyond the source); the ONLY value change is the two chevron-down mask URLs → inline `data:` URI
    - `grep -rnE 'url\(\s*["'"'"']?https?://' packages/styles/components/forms/forms.css packages/styles/components/display/display.css` returns NOTHING (no runtime CDN url in the shipped CSS)
    - line 1 of each file matches the EN banner form
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Copy navigation, feedback, files component CSS</name>
  <read_first>
    - Source of truth (copy verbatim): handoff/components/navigation/navigation.css, handoff/components/feedback/feedback.css, handoff/components/files/files.css
    - 02-PATTERNS.md class-inventory table (navigation 72, feedback 40, files 44) + note that `icons/` has NO CSS
  </read_first>
  <action>Create packages/styles/components/{navigation/navigation,feedback/feedback,files/files}.css as verbatim copies of the identically-pathed handoff files. Strip pt-BR prose, keep structural labels, prepend the EN banner, preserve every `.lyra-*` selector and `var(--token)` reference. ONE mandated divergence in navigation.css: the source has a runtime CDN mask at navigation.css:81-82 — both `-webkit-mask` and `mask` set to `url("https://unpkg.com/lucide-static@0.469.0/icons/chevron-right.svg") no-repeat center / 100%`. Replace BOTH unpkg URL occurrences with an inline `url('data:image/svg+xml;utf8,<svg …>')` encoding the local Lucide `chevron-right` glyph (viewBox 0 0 24 24, fill none, solid opaque stroke e.g. `#000`, stroke-width 2, round caps/joins, single `<path d="m9 18 6-6-6-6"/>`), keeping `no-repeat center / 100%` intact. feedback.css and files.css have NO CDN url and copy verbatim. Do NOT create a components/icons/ directory (icons are React/Lucide, Phase 3). Expected unique-class counts: navigation 72, feedback 40, files 44. Note for Phase 3 handoff: `.lyra-avatar-group` (in display.css, Task 1) and `.lyra-toast-stack` (in feedback.css) ARE the CSS for the compound AvatarGroup/ToastStack components — CSS-covered here, no standalone contract needed.</action>
  <verify>
    <automated>for c in navigation/navigation feedback/feedback files/files; do test $(grep -oh '\.lyra-[a-zA-Z0-9_-]*' packages/styles/components/$c.css | sort -u | wc -l) -eq $(grep -oh '\.lyra-[a-zA-Z0-9_-]*' handoff/components/$c.css | sort -u | wc -l) || exit 1; done && test ! -d packages/styles/components/icons && test $(grep -roh '\.lyra-[a-zA-Z0-9_-]*' packages/styles/components/**/*.css | sort -u | wc -l) -eq 248 && ! grep -rnE 'url\(\s*["'"'"']?https?://' packages/styles --include='*.css' && grep -q '\.lyra-avatar-group' packages/styles/components/display/display.css && grep -q '\.lyra-toast-stack' packages/styles/components/feedback/feedback.css</automated>
  </verify>
  <done>The three files exist with class counts matching handoff; navigation.css chevron mask is a local inline `data:` URI (no CDN); no components/icons/ directory exists; the total unique `.lyra-*` inventory across all seven component files equals 248; a package-wide `url(...http...)` scan across all shipped `.css` returns ZERO matches; the `.lyra-avatar-group` / `.lyra-toast-stack` compound-component CSS is present.</done>
  <acceptance_criteria>
    - unique `.lyra-*` count per file equals handoff (navigation 72, feedback 40, files 44)
    - `packages/styles/components/icons/` does NOT exist
    - total deduped `.lyra-*` across `packages/styles/components/**` equals 248 (D-06 inventory target)
    - `grep -rnE 'url\(\s*["'"'"']?https?://' packages/styles --include='*.css'` returns NOTHING across the ENTIRE package (no runtime CDN in any shipped CSS) — the definitive guard is re-run in the 02-04 parity script and CI
    - `.lyra-avatar-group` (display.css) and `.lyra-toast-stack` (feedback.css) exist — the compound AvatarGroup/ToastStack are CSS-covered by their parent category (38 standalone contracts + 2 compositions = 40 named components)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package → consumer runtime | shipped component CSS is the public `.lyra-*` style API; class drift silently breaks the React package (Phase 3/4) parity contract |
| package CSS → third-party CDN | a verbatim copy would issue runtime `unpkg.com` fetches for chevron mask icons — a forbidden runtime CDN dependency and a supply-chain/availability risk |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-CLS | Tampering | components/**/*.css `.lyra-*` names | medium | mitigate | Copy-verify; parity script (02-04, D-06) inventories the 248 `.lyra-*` names against handoff/components/** and fails on any add/drop/rename |
| T-02-ICON | Denial/Info-disclosure | forms/display/navigation mask icons | high | mitigate | Rewrite the three `unpkg.com/lucide-static` chevron mask URLs to inline local `data:` SVG URIs; hard-fail guard `grep -rnE 'url\(\s*["'\'']?https?://' packages/styles --include='*.css'` must be empty (re-run in the 02-04 parity script + CI); the SVG `xmlns` namespace literal is explicitly not a fetch |
</threat_model>

<verification>
- All 7 component files present; total unique `.lyra-*` = 248.
- No `components/icons/` directory.
- ZERO runtime `url(...http...)` in any shipped `.css` (the three unpkg chevron masks rewritten to local `data:` URIs); re-verified by the 02-04 parity guard + CI.
- AvatarGroup/ToastStack compound CSS present (`.lyra-avatar-group`, `.lyra-toast-stack`); inventory reconciled (38 contracts + 2 compositions = 40).
- Definitive class-inventory parity enforced by plan 02-04 (STY-06/D-06).
</verification>

<success_criteria>
- packages/styles/components/{buttons,forms,display,navigation,feedback,files,data}/*.css present.
- Per-category and total `.lyra-*` counts match handoff (248 total).
- EN banners, no pt-BR prose, token indirection preserved.
</success_criteria>

## Artifacts this phase produces (this plan's slice)

- `packages/styles/components/buttons/buttons.css` — 13 `.lyra-*` classes
- `packages/styles/components/forms/forms.css` — 29 classes (chevron-down mask → local inline `data:` SVG)
- `packages/styles/components/display/display.css` — 34 classes (chevron-down mask → local inline `data:` SVG; includes `.lyra-avatar-group` compound CSS)
- `packages/styles/components/navigation/navigation.css` — 72 classes (chevron-right mask → local inline `data:` SVG)
- `packages/styles/components/feedback/feedback.css` — 40 classes (includes `.lyra-toast-stack` compound CSS)
- `packages/styles/components/files/files.css` — 44 classes
- `packages/styles/components/data/data.css` — 16 classes

<output>
Create `.planning/phases/02-styles-package/02-02-SUMMARY.md` when done.
</output>
