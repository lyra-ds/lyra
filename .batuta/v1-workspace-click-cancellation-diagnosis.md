# React workspace click cancellation — reproduced
At36fe361 compiled current source, root onClick is still forwarded through rest after child click defaults. Controller real-pointer9cases across Chromium/WebKit/Firefox all violate cancellation: prevented trigger click opens; prevented workspace click emits onChange('a') and closes; prevented Create emits onCreate and closes. Each root callback occurs once with root currentTarget, but selection/create effects have already occurred. The native artifact and effects are bound in .batuta/runs/v1-workspace-click-native/baseline-result.json. No page errors; owned browsers/server closed.
This is the click path of the existing architecture native-event consumer-first contract (04-component-architecture.md:251-275), distinct from completed Task21 keyboard default ownership. Repair after Task22 command markup settles. Preserve original click event/currentTarget and clicked workspace identity even if the consumer synchronously reorders data; cancellation prevents domain/default action, stopPropagation alone does not. No API/dependencies or state-veto event added. Alpine event semantics are separate.

Fresh Task22 artifact recheck at0ac89d0: post-command-baseline-result.json reproduces all9/9cancelled-click failures across3engines, artifact180c8395709634e95e1f50ae78dd9c6f68d1316cc74779cb43f5ec3c4f2889fd. No page errors; all owned resources closed. Command semantics did not repair original callback ordering.
