# Plan — explicit modal return focus

<!-- inputs: profile.md@sha256:c5fffda498a6 routing.md@sha256:99f96b92331f -->

**Goal:** Implement the approved returnFocusTo contract for React Dialog and Drawer and migrate their documented examples, preserving the incumbent and proving mouse restoration without focus preparation.
**Created:** 2026-09-08 · **Status:** in progress

The maintainer approved the contract and required Batuta planning/documentation.
This plan organizes that authorized implementation; it does not ask for the same
API approval again. Task 1 is verified; Task 2 is next. Each task is one verified
commit, executed sequentially through the project's Batuta routing.

## Tasks

<!-- prettier-ignore -->
- [x] 1. Implement shared return-focus ownership and integrate Dialog — frontend/high
      Scope: packages/react/src/internal/use-return-focus.ts, packages/react/src/internal/use-return-focus.browser.test.tsx, packages/react/src/dialog/dialog.tsx, packages/react/src/dialog/dialog.browser.test.tsx, packages/react/src/dialog/dialog.ssr.test.ts
      Accept: Dialog and owner Chromium regressions pass → pnpm --dir packages/react exec vitest run --project 'browser (chromium)' src/internal/use-return-focus.browser.test.tsx src/dialog/dialog.browser.test.tsx; The same cases pass in available WebKit → pnpm --dir packages/react exec vitest run --project 'browser (webkit)' src/internal/use-return-focus.browser.test.tsx src/dialog/dialog.browser.test.tsx; SSR never resolves focus or leaks the prop → pnpm --dir packages/react exec vitest run --project ssr src/dialog/dialog.ssr.test.ts; Trusted mouse regression is RED before repair and GREEN after with no pre-focus workaround; Eligibility, successor, diagnostic and once-per-close lifecycle cases from the approved contract have real behavioral proofs

- [ ] 2. Integrate Drawer with the same owner and fresh opening cycles — frontend/medium
      Depends on: 1
      Scope: packages/react/src/drawer/drawer.tsx, packages/react/src/drawer/drawer.browser.test.tsx, packages/react/src/drawer/drawer.ssr.test.ts
      Accept: Drawer and Dialog Chromium/SSR regressions pass → pnpm --dir packages/react exec vitest run --project 'browser (chromium)' --project ssr src/drawer/ src/dialog/; Both browser suites pass in available WebKit → pnpm --dir packages/react exec vitest run --project 'browser (webkit)' src/drawer/ src/dialog/; Drawer trusted mouse input restores declared targets and successors without test focus preparation; Rapid close/reopen captures a fresh opener and no stale restoration runs; Pointer-origin dismissal regression remains green

- [ ] 3. Migrate public examples and document the additive API — frontend/medium
      Depends on: 1, 2
      Scope: apps/docs/components/examples/dialog/basic.tsx, apps/docs/components/examples/dialog/dismissal.tsx, apps/docs/components/examples/drawer/basic.tsx, apps/docs/components/examples/drawer/without-footer.tsx, apps/docs/content/docs/en/components/dialog.mdx, apps/docs/content/docs/pt-BR/components/dialog.mdx, apps/docs/content/docs/en/components/drawer.mdx, apps/docs/content/docs/pt-BR/components/drawer.mdx, tools/docgen/output/props.json, tools/docgen/output/llms.txt, .changeset/explicit-modal-return-focus.md
      Accept: Current declarations build using existing resources → pnpm --filter @lyra-ds/react run build; API documentation is generated by its owner and matches current declarations → node tools/docgen/generate.mjs --check; React types pass → pnpm --filter @lyra-ds/react run typecheck; Four actual examples return focus on supported close paths under trusted Chromium/WebKit mouse input; Public docs explain a stable trigger and logical successor, optional compatibility and invalid-composition limits; Additive React API changeset follows current repository version policy without triggering publication

## Decisions and context

Active checkout: ../lyra-v1-stabilization, branch feat/v1-incumbent-stabilization.
Starting source: 1a60e6d plus the managed contract relocation/planning commit.
The authoritative task contract is .batuta/specs/2026-09-08-modal-return-focus-design.md.
Pass it verbatim in executor/reviewer briefs. The historical approved overlay
spec remains a requirement source; do not move or amend unrelated historical docs.

High routes to Codex gpt-5.6-terra with high reasoning; medium to Codex
gpt-5.6-terra at the configured medium lane. Research/review uses OpenCode
opencode/glm-5.3-flash. The controller runs verification outside executor sessions.
Prepare each closed-scope brief before dispatch; test first, inspect the diff,
run the appropriate proofs and independent review, then commit the atomic task.
A missing required proof stays pending; no declaring the entire plan complete
because implementation text exists. No new design-approval workflow is introduced.

Keep all planning, contracts and run coordination in .batuta/. Task 3 edits
existing product-facing API pages/examples and generated API reference, not
planning documentation. Regenerate tools/docgen/output through the existing
owner; never hand-edit generated declarations or docs to hide a failed build.
Use English prose according to the profile, including updated project documentation.

No dependency, lockfile, resource setting, Colima configuration/restart, Docker
operation, foreign-service cleanup, comparative research, workflow or release
ledger changes. Use pinned Node24.18.0/pnpm11.13.1 and existing installations.
Run bounded checks sequentially. If the declaration build or a browser is
unavailable with existing resources, retain the concrete failure and leave that
gate/task pending; do not add a resource workaround. Firefox and pinned Linux
release evidence remain required for eventual qualification, not inferred from
the two local engines. No push, PR, merge or publication is authorized.

**Task 1.** The shared owner stays internal and narrowly owns return-target
eligibility, capture and close-cycle consumption. Do not refactor trap tab order,
build a layer registry or add providers. Reject closing overlay/panel descendants,
unsafe/cross-document targets and body/html. Accept valid tabindex=-1 destinations.
Resolve the latest callback only after an accepted close commit, once per cycle;
handle initial-closed mount, ignored requests, changed resolver identities,
StrictMode replay and rapid reopen. Preserve the original diagnostic that shows
unprepared WebKit mouse failure. Use separate new evidence for the repaired API.

**Task 2.** Drawer must capture on accepted opening transitions, including reopen
within exit presence, rather than retaining its old mount-only assumption. Reuse
the Task 1 owner, preserving the existing panel API, classes, motion, scroll lock,
close paths and previous pointer-origin repair. If integration proves the owner
needs a change, re-brief that exact owner/test correction before widening scope;
do not duplicate the logic in Drawer to bypass ownership.

**Task 3.** Document and exercise returnFocusTo in the existing four Dialog/Drawer
examples. The public type remains () => HTMLElement | null and must not leak
onto the DOM. Consumers whose trigger can disappear/disable/hide must supply a
meaningful successor from current workflow state; a development diagnostic does
not satisfy the no-body requirement. Do not present omitted-prop unprepared mouse
usage as fixed. Public documentation and consumer migration are part of delivery,
while initial-focus policy, nested layers, other modals and Alpine remain outside
this slice and unqualified. No silent expansion into other examples/components.
