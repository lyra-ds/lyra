# Remaining React modal ownership — fresh native diagnosis

Guarded GLM report .batuta/scout/2026-09-09-v1-modal-remaining-owners.md identified candidate gaps. Controller now reproduced three distinct causes in current built artifacts: hidden first focus candidates in Dialog/Drawer/BottomSheet; background not inert in those three plus CommandPalette; and focus loss after application-controlled removal of the focused body input in the three content-bearing modal components.

Thirty native rows across3engines:0/30meet the relevant target contract, no probe/page errors. Initial-focus cases omit onClose so there is no earlier close button, render a hidden button before the next visible control and wait for the portaled child's effect/next-frame readiness before inspecting focus. Focus stays outside instead of reaching the next safe control. Inert cases show no inert ancestor on the background input and programmatic focus reaches it despite the modal being open. Removal cases reach the removable input through actual Tab, remove it via the fixture's React state update, and verify it is detached before observing focus outside the modal. CommandPalette only participates in the inert case; its private search input is not artificially removed.

Main raw .batuta/runs/v1-modal-ownership-native/{fixture.jsx,run.mjs,baseline-result.json,baseline.log}; exact four artifact SHA identities included. All owned resources closed. This establishes3root causes rather than30distinctbugs. Initial target API, inert/topmost owner and dynamic rescue require a concrete Batuta design and bounded subsequent slices; no newfoundation/dependency. Existing accepted returnFocusTo meaning remains application-owned, as .batuta/specs/2026-09-08-modal-return-focus-design.md already specifies; the scout's uncertainty about inventing a generic named-region fallback is resolved by that approved ownership split.
