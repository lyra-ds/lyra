Work only in the current task worktree. Read .batuta/alpine-dropdown-typeahead-brief.md; same approved scope and boundaries. One bounded verification retry, no product bug reproduced, no unrelated improvements.
Controller results: 11/11 cases per Chromium/Firefox/WebKit PASS; original dropdown.ts retaining new tests fails exactly matching+expiry cases in all3; removing only destroy clearTypeahead makes cleanup case FAIL. Those proofs are adequate and must remain.
Concrete failed criterion2 proof: packages/alpine/src/dropdown.browser.test.ts external-close button is outside .lyra-dropdown. userEvent.click sends mousedown first, so the existing outside listener invokes closeMenu and clears the prefix BEFORE external open=false. Controller temporarily removed ONLY clearTypeahead inside $watch('open') false branch; the entire expires-and-clears test still PASSED. Raw MAIN .batuta/runs/alpine-dropdown-typeahead-20260914/without-external-cleanup.json, exit0. Candidate source restored exactly. This does not prove the independent external-state closure required by the brief.
Fix only the existing test/fixture as necessary so actual external open=false then true occurs without invoking an ordinary outside/trigger/selection close path. Observe fresh typeahead behavior on reopening; removing watcher cleanup must make that assertion fail. No new test framework or giant suite; retain existing3 new cases. Keep exact cleanup proof and all original cases. Static checks only; controller runs browsers and mutations. No commit. Native events remain required for normal ASCII. Existing live-DOM synthetic controlled-clock expiry probe is accepted as explicitly labeled timer evidence, not native keyboard proof.
Report changed files and actual static results; do not claim unrun browser proofs. No new production changes unless directly required for reproduced behavior. Print completion and stop.
