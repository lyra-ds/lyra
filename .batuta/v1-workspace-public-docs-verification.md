# WorkspaceSwitcher public docs — controller verification

Verdict: approved. OpenCode/opencode/glm-5.3-flash low initial/retry, then Codex/gpt-5.6-terra medium wording completion. Independent GLM final3/3 DONE, unchanged guard, Batuta verifier PASS. Two low wording findings corrected; optional redundant callback sentence declined with rationale in review. Exactly two MDX pages changed; unchanged frontmatter, Example IDs, unrelated prose, StackApi/Blade structure and React example source. Newly written prose English in both locale paths per maintainer rule; unrelated existing pt-BR prose retained.
1. Source comparison confirms React selected entry, visible Workspaces label, actual options/creation command separation, roving/native Tab and unchanged callbacks. Alpine section explicitly identifies its selection-only scope, all4selected opening keys, exact arrow fallback, caller-owned IDs/label/selection/content and lyra:change data-id; no creation event/API or click-cancellation claim. Current public pages no longer call Create an option or claim first/last React opening.
2. Both actual HTML snippets are byte-identical, compile with the installed MDX compiler through current Fumadocs dependency resolution, and serve default-closed aria-expanded=false/display:none before initialization. Controller executed that exact snippet against the built Alpine plugin across3engines:18/18real-input cases PASS (5opening methods plus selecting last option in each engine), no page errors. Label/controls resolve, exactly2workspace options/no Create, selected opening, emitted nonempty activated data-id once, close/return and unchanged consumer-served selection verified. Owned browsers/server closed. Native result binds snippet and artifact SHA; mdx-result.json binds both page bytes.
3. Pinned Node24.18.0: MDX compile, Prettier, existing stack-section suite303/303 and native runner all exit0. No runtime/style/dependency/generated-output changes, no package size change or additional changeset. Full docs production build is a later final-candidate gate; this proves edited MDX and executable example, not a fresh whole-site export. V1 packed Linux/11React size overages remain pending.

Initial /tmp snippet extraction failed and its retry was blocked by the worker tool; no outside-scope product edit retained. Controller snippet comparison/compilation uses owned Batuta artifacts. The implementation retry corrected the initially mismatched server-expanded state and clarified React/Alpine/application ownership; it made no further outside-scope write attempt.

Main raw: v1-workspace-public-docs-checks/{results.json,mdx.log,format.log,stack-sections.log,native.log}. Worktree raw: v1-workspace-public-docs/{executor.log,result.json,retry.log,retry-result.json,initial-backup}; v1-workspace-docs-native/{check-mdx.mjs,mdx-result.json,snippet.html,fixture.js,run.mjs,result.json}. No remote/Colima/Docker/service action.

Final wording: both pages specify empty-create/entirely-empty focus fallbacks and explicitly scope onChange to React. Controller fresh MDX compilation/formatter PASS; executable snippet SHA unchanged, earlier18native/303stack proof still applies. Medium75s exit0, two scoped bullets only. Final bounded review findings saved verbatim.
