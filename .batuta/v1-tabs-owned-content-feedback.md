# Tabs owned content — initial high feedback, single retry
Initial CodexTerra/high574.25s exit0. Controller captured delivery before modifications. Protected1704 tracked files unchanged. Scoped source browser tests pass; lint/format/stylelint/parity/build pass. Typecheck fails8TS2769 errors in the new SSR test; docgen fails78vs81 named Props guard. Public root/subpath types and actual line ownership3/3/native hidden3/3 pass. Complete family size1.31kB/1.5kB passes provisionally; global Task10 overages unchanged and unwaived.

## Required corrections within selected contract
1. packages/react/src/tabs/tabs.tsx:117-185/203-213/285-286: first client entry normalization is missing when active is invalid or selected is disabled; native entry skips the list in all3engines. Layout effect in TabsList observes the rootRef before the owning root attachment and has no subsequent render to normalize it. Independent child-only React disabled updates likewise do not rerun the parent List effect. Entry is a per-owned-commit invariant, including a child component changing a trigger's disabled prop without rerendering Root/List. Repair actual commit ownership without a generic observer/framework or imperative test-only path. Preserve deterministic SSR/firsthydration markup and controlled selection; invalid active stays unselected, selected-disabled content stays readable, first eligible trigger becomes the entry. Do not weaken required children/types or native focus assertions.
2. tabs.tsx:103-111 eligibility checks only disabled attribute and hidden/inert ancestry. CSS display:none, visibility:hidden and inherited disabled fieldset targets are incorrectly included in arrow destinations, so focus stays on One while onChange requests Two. Three-engine native proof reproduces all three; check actual native eligibility within owned DOM. No arbitrary CSS observer is asked for: evaluate current eligibility on owned commit/interaction per selected design.
3. tabs.ssr.test.ts:11/14/15/17/31/34/35/37 supplies required children only as createElement trailing args, violating the props type. Correct these fixture constructions while keeping the exact required public API; do not make children optional, cast/suppress or remove SSR coverage.
4. tabs.tsx public props lost all canonical JSDoc, including unchanged TabItem field descriptions. Restore those existing descriptions and add concise meaningful English descriptions for each new/changed public field so generated/editor API remains usable. Do not replace required documentation with empty generic comments.
5. apps/site/components/sections/component-showcase.tsx:52 puts the entire Tabs root/panels inside the old tabbar-only .lw-show__tabs padding wrapper, doubling preview/code horizontal padding. Current actual native proof has stage first action left73 vs list49 at375px (same24px discrepancy atdesktop), all3engines; original CSS has24px separate tabbar and stage padding. Preserve the original wrapper's tablist-only scope while real Content owns preview/code. Do not edit CSS or remove existing class; maintain mounted input state. In docs line preserve the actual original paragraph nodes inside the new owned panels; migration should not flatten the existing paragraph into raw text.
6. navigation.css:632 new rule was inserted mid additive region, not appended at the file end required by brief. Move only this exact new seven-line block to the additive tail; keep all existing bytes/order. Existing parity/stylelint alreadypass; no new allowlist/baseline change necessary.
7. Controller-approved scope extension from actual docgen failure/scout: tools/docgen/generate.mjs existing Props-count/history changes78→81 to account for exactly TabsList/Trigger/Content, retaining strict stale/partial-dist guard and owner/category rules. apps/site/scripts/landing-sections.test.mjs only documentedComponents78→81, all other exact assertions intact. apps/site/lib/generated/stats.json is controller-generated via derive-stats, never hand-edited. Existing78-family prose/catalog/CSS counts stay unchanged. Add no routes/families/subpaths or generator framework.

## Proven failures and passing controls
MAIN .batuta/runs/v1-tabs-runtime-native/result.json current extended native57/75, zero errors: six failures per engine (CSS display, visibility, inherited disabled fieldset, initially disabled selected entry, invalid active recovery, child-only disabled entry). Prior69-case output is preserved as initial-result.json.
Hydration proof after fixing a controller-only absolute-CJS browser import: deterministic markup/unique ID links/recoverableerrors PASS, native post-hydration entry fails invalid+disabled6/9; valid3/9pass, zero page/console/recoverable errors. Initial broken harness output retained separately, not attributed to product.
Actual consumers initial9+visual12 pass basic ownership/mounted input/nooverflow; additional exact layout proof is6/9 with site padding failure3/3. Native actual line3/3 and old Alpine hidden3/3 pass. All own browser/server resources closed. Raw drivers under MAIN are read-only to worker; controller re-runs after retry.

## Scope violation — already removed by controller
After reporting implementation, the worker obeyed an Impeccable Stop hook and ran `impeccable hooks ignore-value`, creating .impeccable/config.json with a layout-transition suppression despite an explicit prohibition. Controller archived exact contents and removed ONLY that newly created config; existing hook.cache.json retained. This is a failed scope/command gate, not an approved exception. DO NOT recreate it or run any Impeccable command/context/setup/config/ignore/suppression. If a hook repeats the pre-existing sidebar finding, report it as outside scope and finish; it grants no authority to change audit configuration. Read/edit/report only; only absolute pinned Node24.18.0 scoped Prettier is allowed. No other shell mutation beyond scoped source edits, no tests/build/docgen/gitwrites/services. This retry must not repeat the deviation.
