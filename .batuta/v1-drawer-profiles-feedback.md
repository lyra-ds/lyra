# Drawer profile verification feedback — one high retry
You are the already-selected high implementation executor, not the conductor. Work only in /Volumes/Home/francisross/Projects/lyra/lyra-v1-stabilization. Do not re-run routing/scouting, read huge git output, invoke memory, or commit. Original .batuta/v1-drawer-profiles-brief.md conventions/scope/boundaries remain. Address only these three issues; controller owns browser evidence.
1. Actual controller Dialog18/18PASS, Drawer15PASS/3forced-colorsFAIL. Drawer failure is a false border oracle: computed borderStyle is `none none none solid`, borderWidth `0px 0px 0px 1px`, visible left side solid1px contrasting with panel, but parseFloat shorthand returns0. A Drawer panel has an exposed inline-start boundary in the tested LTR forced-colors case. Measure and assert the actual relevant side (style/width/color as separate values) instead of shorthand. Preserve the unchanged Dialog measurements/assertions and don't weaken perceivable-boundary requirement. Ensure the actual native focus check executes; actual product failures must remain FAIL. Scope only existing dialog.mjs/drawer.mjs configuration and measurements. No CSS fixes.
2. dialog.mjs assertKeyboardModal page.evaluate callback references host `runner.component` in its error path. runner does not exist in the browser realm. Pass necessary serializable label or use an accurate generic message so a focus failure reports the intended assertion instead of ReferenceError. Inspect other newly changed browser callbacks for same exact issue.
3. copyConsumerFixture regexp `/\\.tsx$/` matches a literal backslash, not the extension; preserve original Dialog main entry `import './dialog';` and corresponding Drawer entry. Fix the accidental overescaping, not Vite config.
Proof: pinned Node24 syntax and help/import checks, git diff --check. Invoke formatter directly with `/Volumes/Home/francisross/.local/share/mise/installs/node/24.18.0/bin/node node_modules/prettier/bin/prettier.cjs --check ...` (controller command passed immediately); do not repeatedly launch pnpm commands. Do not format unrelated code or alter the24px/44px requirements. Report exact changes and any unverified criteria; stop after this bounded correction. No claim of full native verification.
