packages/styles/components/navigation/navigation.css:666 · low · rule is unconditional, not hover-scoped as its comment narrates: metadata at rest also shifts #64748B→#475569 (light) on the card surface. Consistent with existing additive precedents (navigation.css:622,638,658) and required for deterministic rest coverage under pointer residue, but a maintainer reading the comment may assume hover-only. Tighten comment or none.
packages/react/src/workspace-switcher/workspace-switcher.browser.test.tsx:142 · low · keyboard-focus loop compares metadata against popoverBackground unconditionally; if a pointer residue hovered the focused option the effective surface would be sunken while the assertion checks card. No false-pass today (secondary clears both surfaces, axe also runs), but a future focus-surface regression with poor contrast vs card would evade this specific assertion until axe catches it.
packages/react/src/workspace-switcher/workspace-switcher.browser.test.tsx:127 · low · hover surface is used in the ratio but never asserted equal to the sunken token; a hover-background swap to any other ≥4.5 surface would pass silently. Contrast is the contract, so acceptable — noted for exactness.
