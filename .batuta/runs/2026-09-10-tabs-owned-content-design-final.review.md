# Tabs candidate — final technical review and controller selection

GLM5.3Flash346.17s,exit0,unchanged guard,3DONE; verifierPASS. Five precision findings, adjudicated before high implementation.

<<<FINDINGS
1. .batuta/specs/2026-09-10-tabs-owned-content-design.md §Alpine bindings/§Domain cancellation/event mapping MEDIUM: veto promise "List and trigger native consumer prevention runs before Lyra's default" is unsatisfiable if Alpine activation/keydown defaults stay at trigger level (bubble order: trigger consumer → Lyra trigger default → list consumer too late). Bindings paragraph pins no event handlers at all. Concrete: consumer puts `@click.prevent` on the `.lyra-tabs` list div to gate all activations; with packages/alpine/src/tabs.ts-style trigger-bound `['@click']` default, activation and both custom events still fire despite veto. Fix: pin Lyra's click/keydown defaults to the list binding (bubble phase, target→closest owned trigger, nested-root exclusion), matching React's pinned List-default ordering — or narrow the promise to trigger-level veto only.

2. .batuta/specs/2026-09-10-tabs-owned-content-design.md §Controlled values, invalid-value policy LOW: entry-normalization temporal scope ambiguous. "The first client commit sets the first eligible enabled trigger as the native Tab entry" reads one-shot; "if selected but disabled/hidden/inert… first other eligible trigger becomes the client entry" reads continuous. Concrete: valid active at load (entry = selected trigger); consumer later disables it or hides an ancestor → one-shot reading leaves stale tabindex=0 on a nonfocusable node, all others -1 → zero Tab stops → whole tablist keyboard-unreachable until pointer use. Fix: pin entry as a per-commit derived invariant (recomputed from active+eligibility each commit), not first-commit-only. Not the resolved initial disabled-selected case; strictly the later-mutation case.

3. .batuta/specs/2026-09-10-tabs-owned-content-design.md §Alpine init validation LOW: one-shot "unique paired values/IDs and a valid active target" plus "No broad observer" leaves post-init DOM/value changes unspecified. Concrete: sections injected by fetch or x-if after Alpine.start, or active referencing a not-yet-rendered value → validation fails → permanent silent ready=false fallback; no retry, no diagnostic (failed-init logger already declined — not requesting one). Fix: explicitly declare post-init content/active mutation unsupported for Alpine (parallel to the React async-content table row) or define a re-validation trigger on application writes.

4. .batuta/specs/2026-09-10-tabs-owned-content-design.md §Anatomy/§invalid-value policy LOW: unpaired TabsContent (value with no trigger) plus matching active → equality-derived visible panel with no accessible name; the emitted aria-labelledby value when no matching trigger exists is unspecified (empty string = dangling ARIA reference). Fix: specify deterministic omit-or-empty attribute so unsupported composition still has defined markup. No normalization/logger requested.

5. .batuta/specs/2026-09-10-tabs-owned-content-design.md §Domain cancellation/event mapping LOW: before-change veto vs native keydown default unpinned. Concrete: End-key navigation vetoed via `lyra:tabs-before-change`.preventDefault → Lyra focus/active/result effects abort, but if the keydown was not yet preventDefaulted the browser still scrolls (Home/End native default) → partial veto. Fix: pin that recognized navigation keys are claimed (preventDefault) once recognized, with before-veto aborting only Lyra's focus/active/result effects — or state native default proceeds deliberately.
FINDINGS>>>

Controller adjudication:
- Accepted1: Alpine default click/keydown belongs on the owning list after native consumer handling; trigger then list veto precedes any Lyra activation. Exact listener ordering requires native proof in the later Alpine cycle.
- Accepted2: normalize the entry on each React commit affecting this root/parts, plus each owned keyboard interaction. Arbitrary external CSS-only changes with no React commit/event are not an observed lifecycle channel; do not claim an observer that does not exist.
- Accepted3: Alpine's declared paired structure is complete before initialization and structurally static for that enhancement lifetime. New paired structures require reinitialization; no new refresh API/observer. Valid external active updates synchronize without user event echo; invalid active restores fallback until a valid input revalidates the existing structure. Failed structural setup requires correction and reinitialization.
- Declined4: collision-free prefix/value ID generation already defines the string. Clarify it is emitted even when its expected matching node is absent; dangling reference remains unsupported composition, with no empty/omitted normalization.
- Accepted5: after native consumer guards, recognized owned navigation keys claim native default before the custom veto event, so custom veto prevents Lyra effects without unwanted browser Home/End scroll.

Controller selected the revised contract for the bounded React implementation. This is technical selection within the user's already-authorized incumbent V1 work, not a new human approval or release qualification. Runtime review will receive the exact selected design and these dispositions.
