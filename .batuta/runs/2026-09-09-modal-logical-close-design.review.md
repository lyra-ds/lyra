# Modal logical-close technical design review

<<<FINDINGS
packages/react/src/command-palette/command-palette.tsx:247: MEDIUM: helper "scope includes guard siblings" is parentElement-relative in use-focus-trap.ts:128-129; in CP inline mode panel.parentElement is consumer app DOM, so naive wiring inerts application content — exactly what proposal forbids. Fix: define helper scope as the owned overlay node passed explicitly (root-rendered overlay div, command-palette.tsx:535-551), helper hard no-op when modal=false; portaled Dialog/Drawer/BottomSheet guards already sit inside the owned overlay.
packages/react/src/dialog/dialog.tsx:132: MEDIUM: gesture revocation — downOnOverlay/upOnOverlay provenance refs live in four different components (dialog.tsx:132, drawer.tsx:97, bottom-sheet.tsx:117, command-palette.tsx:420-421) and CP's pair is in ROOT, not panel. Reset must run on accepted open true→false in the owning component of each ref pair; CP reset must key on modal&&open only or inline gesture state is disturbed. Scenario: pointerdown old backdrop, flushSync close, reopen, pointerup/click on retained node — reset at close makes click handler at command-palette.tsx:544-550 see false provenance; fresh full gesture re-arms.
packages/react/src/command-palette/command-palette.tsx:295: LOW: aria-modal currently keys on mode only; inactive-retained CP keeps aria-modal=true. Fix: aria-modal/role-adjacent attrs key on modal&&open in CP, and same conditional in drawer.tsx:128, bottom-sheet.tsx:153-155, dialog.tsx:178; BottomSheet aria-label branch needs the same open gating. Explicit attr sits after {...restProps} spread (dialog.tsx:174) so conditional wins; no renderer-emitted boolean attr — callback-ref/node.inert only, SSR-safe for inline panel which renders server-side (command-palette.tsx:528 returns before mounted guard).
packages/react/src/internal/use-return-focus.ts:100: LOW: ordering invariant unstated in proposal: inactivation lands at commit (ref/layout effect, use-flip-placement.ts:19 isomorphic pattern), return focus runs after in owner passive effect at use-return-focus.ts:87-115. Safe only because return target is outside scope and :46 rejects [inert]-ancestor targets; browser drops focus from scope at inert-set (focusout path use-focus-trap.ts:177-184 is harmless). Fix: record invariant in helper — never extend scope to body/app host, never move return focus before commit, never "repair" the transient body-focus window.
packages/react/src/internal/use-focus-trap.ts:109: LOW: switching three owners to open-keyed trap activity leaves commit→passive-cleanup window where guards exist after scope already inert; guards are inside owned overlay so scope inert covers gap, but dispose stays effect-owned — do not dispose guards from ref callbacks (StrictMode ref/effect rehearsal must leave zero stale guards; exactly-one-scope requirement).
packages/react/src/internal/use-presence.ts:56: LOW: helper must not duplicate timers/observers for exit-finalization; EXIT_FALLBACK_MS and animationend path stay sole unmount owners — inert exit presentation still emits animationend, so "deactivation is not animation suppression" holds only if helper touches no animation/class state.
FINDINGS>>>

## Controller adjudication
Read-only guard unchanged; executor exit0; three DONE lines. All six observations concern implementation precision, not a replacement architecture.
1. Accepted: the helper must receive only an explicitly owned overlay; CP inline is outside its scope. Never infer app ownership from panel.parentElement.
2. Accepted: revoke provenance in each ref owner at accepted close; CP refs live in root, and inline behavior stays unchanged.
3. Accepted for aria-modal after consumer props; declined the unnecessary accessible-name removal. Inert already removes the whole inactive scope from AT; retain stable role/naming through the visual exit, only modal activity changes. No criterion requires deleting aria-label.
4. Accepted ordering clarification: scope inactivation at commit precedes current passive return-focus work. Do not move return-focus timing or add transient body-focus rescue.
5. Accepted: scope inert covers the commit-to-passive guard cleanup interval. Guard disposal stays in the existing focus-trap effect, never in a new ref callback.
6. Accepted: presence remains sole animation/fallback owner; no new timers/observers or animation suppression.
The revised exact design carries these constraints. Technical revision review follows; no implementation has started.
