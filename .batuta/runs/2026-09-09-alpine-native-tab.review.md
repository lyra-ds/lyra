<<<FINDINGS
packages/alpine/src/internal/focus-trap.ts:148: low: if Tab keyup is never delivered to panel (OS window switch mid-press), armed boundary stays tabIndex=0 with pending token past gesture end, breaking the inactive-between-gestures invariant until next panel keydown/focusout/keyup; self-heals on next gesture and no internal code path focuses a boundary, so no stale wrap observed — optional hardening: disarm on window blur/visibilitychange.
packages/alpine/src/internal/focus-trap.ts:100: info: focus landing on the non-pending boundary returns false without deactivating, leaving boundaries armed and focus stranded on the hidden span; unreachable via keyboard (only the pending boundary is ever armed, the other stays tabIndex=-1) — reachable only by external programmatic focus() onto the span; defensive gap only.
FINDINGS>>>

Controller: both optional observations declined for this bounded native-input repair. Window/OS focus-loss hardening requires a concrete reproducer and a separately scoped global-listener lifetime decision; the accepted design expressly keeps listeners panel-local. Native focusout/keyup and all24actual sequences deactivate the guards. Programmatic focus onto a private, inactive, aria-hidden nonpending guard is not an exposed consumer focus target; no supported input path reaches it. Do not infer general window/OS-switch or arbitrary internal-node mutation qualification from these tests. Final3/3DONE unchanged guard, no concrete blocking defect within scoped contract.
