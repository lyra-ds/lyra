packages/react/dist/tooltip.js sha256 == ac5c73fb…a17e4a claimed by final-result.json/actions-result.json; 42/42 contractPass (14/engine), 6/6 actions — byte-bound claims verified against this checkout · info
packages/react/src/tooltip/tooltip.tsx:97-120 · info — stale-target checks (isConnected/contains/scheduledTarget identity, no ReactElement identity use) correctly block replacement/removed-content revival; matches design removal-ownership clause
packages/react/src/tooltip/tooltip.tsx:263-270 · suggestion — consumer `onKeyDown` on Tooltip root now delivered; at HEAD e287941 it was silently overridden by `handleKeyDown` in JSX prop order. New capability, no regression path found; changelog accuracy note
packages/styles/components/feedback/feedback.css:604-615 · suggestion — `--bottom` open rule duplicates the top rule's identical transform; could collapse into one selector list. Nit; current form mirrors existing per-placement rule structure and parity policy
packages/styles/components/feedback/feedback.css:600-615 · info — all 20 added lines are end-additive; open transforms (translateX(-50%) translateY(0) / translateY(-50%) translateX(0)) exactly match existing per-placement :hover end-states (lines 251, 431-451); `pointer-events:auto` only under data-state='open', so raw CSS-only/Alpine tooltips (no data-state) and the closed escape-opacity handoff (line 453) are untouched; HEAD prefix byte-identical per diff hunks
packages/react/src/internal/tooltip-coordinator.ts:82-83 · suggestion — if `document.defaultView` is null when warm expiry should schedule (detached document edge), `warm` persists without a timer until the next ownership event; purely theoretical (detached docs host no live tooltips), distinct-document isolation unaffected
packages/react/src/internal/tooltip-coordinator.ts:56-66 · info — one-dismissal-per-event via `dismissedEvents` WeakSet plus native defaultPrevented guard; wrapper synthetic preventDefault and document-route native event cover both Escape routes; listener removed whenever visibleOwners empties (no listener without visible tooltip) — verified in code
package.json (size-limit tooltip entry 1.5 kB) unchanged in diff; no budget/baseline hash edits anywhere · info
Independent read-only approximation (esbuild minify + brotli q11 on checked-in dist) ≈ 2059B, consistent with controller's 1954B > 1500B; overall size gate FAILED is honestly reported, disposition correctly left pending Task10 — no waiver or concealment found
Added tests use exact fake-clock boundaries (499/500, 99/100, 299/300), no sleeps/skips/engine branches; act helper restores prior IS_REACT_ACT_ENVIRONMENT value; baseline-result.json shows original failing cold/combined/topmost/bubble cases per engine (pointerEvents 'none', at0 'open') · info
