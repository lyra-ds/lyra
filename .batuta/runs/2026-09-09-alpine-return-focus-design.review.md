# Initial review and adjudication

<<<FINDINGS
- dialog.ts:158-161, drawer.ts:148-151, bottom-sheet.ts:150-153, command-palette.ts:410-413 — medium: four restoreOpener bodies not identical; BottomSheet guards isConnected, other three call .focus() unconditionally incl. detached. Design says "eligible captured opener fallback" but never states the fallback passes the same eligibility list nor owns the narrowing of today's unconditional focus() for ineligible openers. Fix: state fallback passes identical list; prove no observable activeElement change.
- design §4 preventScroll — high (blocks TASK2): "Focus the selected target once with preventScroll:true" unspecified whether it covers the captured-opener fallback. Current no-option restore is plain .focus() (dialog.ts:159); preventScroll fallback changes scroll-into-view for eligible-but-scrolled openers, contradicting "Existing no-option captured-focus behavior remains for eligible keyboard-owned openers." Fix: reserve preventScroll:true for explicit resolver target, plain .focus() for fallback (or explicitly own the scroll edge).
- dialog.ts:122, drawer.ts:112, bottom-sheet.ts:114, command-palette.ts:369 — medium: design claims "preserve current accepted-opening capture semantics" while switching capture to root/panel ownerDocument. Current capture uses global document.activeElement; identical in single-document usage but newly defines cross-document behavior (top-document iframe-element opener rejected by same-ownerDocument rule). Fix: state cross-document capture is defined, not preserved.
- design §4 throwing resolver — medium: "remains an application error" does not specify propagate vs log. All four restore sites are last step of activateClose (dialog.ts:130-138, drawer.ts:120-128, bottom-sheet.ts:122-130, command-palette.ts:377-381), so propagation is contained, but contract must be explicit. Controller proof list covers only null/invalid resolver; add throwing-resolver scenario.
- design §2 reentrancy — medium: clear-before-resolve guarantees "reentrant application work cannot replay the old capture" but controller proof list has no matching scenario (current code is focus-then-null, dialog.ts:159-160). Add resolver-mutates-state/reopens-during-close proof with exact focus once-count.
- package.json:63 + .batuta/v1-alpine-native-tab-verification.md:7 — low: helper/warning bytes land on already-failing 21394/21200 gate; design keeps Task10 gate honestly, but dev-warning policy (React precedent warns when nothing eligible) is undecided and warning strings cost bytes. Decide before implementation.

Verified consistent: option interfaces match current fields exactly (dialog.ts:15-20, drawer.ts:15-18, bottom-sheet.ts:15-18, command-palette.ts:38-57); root exports and pass-through registrations unchanged (index.ts:95,97-102,106-107,169-191,323-330); inline boundary real (command-palette.ts:244-249, no watcher/capture/hotkey, !inline guards 444,454); once-only close via single $watch per open transition (dialog.ts:93-96 etc.); destroy nulls opener with no restore (dialog.ts:108 etc.); x-modelable ignored-close no-restore matches watcher-only firing; size claim 21394/21200 truthful FAIL retained; Task30 scope and Task31 deferral match v1-completion.md:144-145; tools/docgen/alpine.mjs exists; three engines match package.json:41; no broad parity claimed.
FINDINGS>>>

Controller: all six precision/compatibility findings accepted. Same eligibility applies to both candidate paths; retain plain focus() for eligible captured fallback and preventScroll:true only for explicit target; document defined ownerDocument capture, propagation into Alpine error handling, reentrant proof, and no added warning policy.

# Revised design review

<<<FINDINGS
All six adjudicated findings are incorporated in the revised design and verified truthful against the five scoped files:

- Fallback eligibility (was medium): design §3 now states both the explicit result and captured-opener fallback pass the identical complete eligibility list and explicitly owns the narrowing of today's unconditional focus. Anchors: unconditional `.focus()` at dialog.ts:159, drawer.ts:149, command-palette.ts:411; BottomSheet's `isConnected` guard (bottom-sheet.ts:151) is subsumed by the connected rule, so its detached-opener no-op is preserved, not narrowed. Truthful.
- Scroll (was high): preventScroll:true reserved for eligible explicit resolver target only; eligible captured fallback retains plain `focus()`, matching the current baseline (dialog.ts:159). Both scroll directions appear in the controller proof. Adjudication satisfied.
- ownerDocument capture (was medium): design §1 now declares root/panel ownerDocument capture as an intentional correction, not preservation of cross-document behavior. Capture sites confirmed: dialog.ts:122, drawer.ts:112, bottom-sheet.ts:114, command-palette.ts:369 (`document.activeElement`).
- Throwing resolver (was medium): propagation after cleanup is now explicit. Containment claim verified: restoreOpener is the last step in all four activateClose bodies (dialog.ts:130-138, drawer.ts:120-128, bottom-sheet.ts:122-130, command-palette.ts:377-381). Direct-helper regression requires thrown object preserved + opener cleared + no focus, consistent with clear-before-resolve ordering (§2).
- Reentrant proof (was medium): §2/§4 add the resolver-reopens-during-close case with exact once-counts and independent fresh capture. Current code is focus-then-null (dialog.ts:158-161), so clear-before-resolve genuinely closes the replay path the design claims.
- Warning policy (was low): decided — no dev warnings or warning strings when neither candidate is eligible; documentation and acceptance tests own the contract failure; Task10 size gate retained honestly (21394/21200 stated as current FAIL baseline, no release qualification claimed).

Verified consistent against the five files:

- Option interfaces match source fields exactly, including order and comment text (dialog.ts:15-20; drawer.ts:15-18; bottom-sheet.ts:15-18; command-palette.ts:38-57). CommandPalette item/group/hint types untouched (command-palette.ts:8-35).
- Pass-through registrations flow the new option with no code change: args[0] casts at index.ts:169-191 (dialog/drawer/bottomSheet) and index.ts:323-330 (commandPalette); option type exports unchanged at index.ts:95, 97-102, 106-107. No factory exports exist or are proposed — public surface remains default plugin + types (index.ts:61, 77, 110 are the only other value exports).
- Inline boundary real: command-palette init returns before watcher/presence/hotkey/initial-open (command-palette.ts:244-249), so inline mode never captures or resolves; Escape/pick guards (`!inline`) at 444/454 keep open watcher from firing. Design's inline no-capture/no-resolve claim is truthful.
- Once-only close and boundaries: single `$watch('open')` transition per component (dialog.ts:93-96, drawer.ts:83-86, bottom-sheet.ts:85-88, command-palette.ts:259-262); "request that leaves open=true does not restore" matches watcher-only firing; destroy nulls opener with no restore and no resolver call (dialog.ts:108, drawer.ts:98, bottom-sheet.ts:100, command-palette.ts:290). Design's destroy/initially-closed/rerender no-restore claims match.
- Scope and evidence: four owners + colocated tests + one private shared helper + one changeset is minimal and justified — the four restoreOpener bodies are the exact duplication extracted. Controller proof covers both scroll behaviors, throwing-resolver, reentrant reopen with once-counts, close/reopen, no-option keyboard regression, old-source negative regression, and the shared eligibility rejections (hidden/inert/aria-hidden/disabled-fieldset/foreign-document/closing-panel/body, tabindex=-1 successor). Closing-panel exclusion is implementable via existing panelElement() accessors (dialog.ts:111-113, drawer.ts:101-103, bottom-sheet.ts:103-105, command-palette.ts:352-354).
- No unsupported parity claim: design disclaims cross-stack parity, defers inert/topmost/child-transfer to Task31 and initial-focus policy to a separate owner, and keeps the docs/catalog as their own verified task.

No remaining blocking or medium findings.
FINDINGS>>>

Controller: all six precision findings accepted and resolved. Final3/3DONE unchanged guard; normalized Batuta verifier PASS. Technical proposal reviewed; no separate human sign-off is claimed. Execution continues within the user-authorized incumbent V1 completion scope.
