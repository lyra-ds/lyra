packages/react/src/dropdown/dropdown.browser.test.tsx:386 · low · No unprevented keyboard Enter/Space activation assertion (onSelect once + normal close): a regression adding preventDefault for key ' '/Enter in the menu path passes all suites while silently breaking keyboard activation — pointer activation and canceled keyboard activation are covered, the positive keyboard path is not.
packages/react/src/dropdown/dropdown.browser.test.tsx:455 · low · After-button tabIndex={0} is a measured WebKit sequential-nav precondition (implicit buttons are skipped) but carries no comment; future cleanup removing the attribute resurrects the body-target Tab failure in both this fixture and the standalone probe with no in-source cause hint.
