# CreateWorkspaceDialog design support review

<<<FINDINGS
design.md:36,47 | MEDIUM | Example scope ambiguity. Delivery bounds docs to "the two actual examples", both migrated to accepted-flow ("appends accepted workspace data, then returns matching accepted"), yet same section demands "cancellation/error examples use explicit controllable operations". Where cancel/error demonstrations live (inside the two examples, new files, or nowhere) is undecided; an implementer either silently drops the cancel/error docs demonstration or adds a third example outside the declared boundary, breaking "two generated React docs artifacts". Brief not self-sufficient on example content.
design.md:25,47 + VERSIONING.md:88-92 | MEDIUM | Changeset missing mandated migration note. Runtime task files "one breaking0.x minor changeset" but never requires the policy-mandated migration note ("Every breaking change ships with a migration note in the changelog"); MDX migration prose is deferred to a separate cycle. Changelog is generated from changesets, so this delivery's breaking release ships without the required what-changed/how-to-update note and no later cycle repairs it.
design.md:41 | LOW | Forced-destroy exit state unspecified. Controlled open=false "destroys the current form operation logically even if Dialog still renders an inert CSS exit", but the surviving exit render's data-state/aria-busy is undecided: keeping `canceling` leaves aria-busy="true" and disabled fields through the inert exit after the operation is invalidated; resetting to `editing` is unstated and untested (proof list covers reopen/reset, not destroy-exit attribute state). Stale settlement during exit lands on an unspecified state.
design.md:31,43 | LOW | Malformed-but-current result undecidable. `{operationId, status:'rejected'}` with empty/missing `error`, or unknown status string, is bounded only by "must never imply acceptance". No decided mapping (malformed→rejected with nonempty fallback message vs ignore-as-malformed) and no malformed-result case in the required-proof list; two implementers can ship different behavior for the same consumer bug.
design.md:28 | LOW | "read-only or disabled" while an operation is current is an unresolved test-visible choice: disabled removes fields from tab order and from the eligibility checks (packages/react/src/internal/use-initial-focus.ts:48 `:disabled` guard), readOnly keeps them focusable; keyboard/pointer proofs and axe outcomes differ per implementer. Brief should pin one.
design.md:33,38 | LOW | Never-settling consumer invariant ("leaves canceling; no timeout or fictional success") has no falsifiable proof entry: "closecancelingwait" asserts waiting, but nothing pins cancel→abort observed→`canceling` persists with aria-busy and no onClose across the settlement horizon, so the strongest no-fabrication guarantee ships unproven.
FINDINGS>>>

## Controller adjudication — 2026-09-10
Frozen ae3e6ce snapshot; exit0/unchanged guard/3DONE, but six accepted design clarifications must precede implementation. No Task26 worker dispatched.
1. Accepted: exactly the two existing live examples demonstrate synchronous accepted local workspace creation. Cancellation/rejection are exercised by controlled native/source fixtures and described in the subsequent MDX cycle; no new live example promised.
2. Accepted: the runtime breaking changeset itself must include what changed and how to migrate the plain/void callback to request/result, including matching ID, signal and explicit accepted/rejected/canceled acknowledgment. Later MDX is supplementary.
3. Accepted: forced controlledfalse invalidates/aborts then presents an inert exit in editing with aria-busy absent, preserving values only through that exit. Reopen resets values and errors. No pending ownership remains behind a busy attribute.
4. Accepted: missing/undefined/malformed current response, unknown status, or rejected response with missing/empty error maps to rejected with nonempty fallback. A different string operationId remains stale and ignored. Add exact malformed-result proof.
5. Accepted: pending name/slug fields are readOnly (not disabled), retaining their existing focusability while preventing edits. Duplicate submission remains disabled and logically guarded; cancel routes the handshake.
6. Accepted: prove no settlement is fabricated with a held consumer promise, observed abort, repeated close, and subsequent native frames/tasks retaining canceling/current ID/busy and zero onClose; release via matching canceled afterward in the fixture. Static review rejects timers/fictional result paths. A finite test does not prove infinite waiting alone.
