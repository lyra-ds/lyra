# Alpine BottomSheet — fresh native diagnosis

Current source tests10/10Chromium,9/10WebKit,10/10Firefox. The WebKit failure is exact opener restoration after implicit-button mouse opening. Both existing closed/open axe checks pass; no current axe filtering defect established. Main raw .batuta/runs/v1-alpine-bottom-sheet-baseline-checks/results.json and logs. No product edits.

Controller compiled native probe initially covered12cases with an explicitly tabbable trigger; all restored, but all4WebKit cases escaped the panel during8actual Tabs. The expanded24-case matrix independently varies explicit trigger tabIndex, focus-inside-click preparation and actual click/Enter. Chromium8/8 and Firefox8/8 retain containment/restoration. Every WebKit case (8/8) loses containment, independent of opener preparation. Restoration fails ONLY WebKit implicit trigger +unprepared click; explicit tabIndex0 or focus preparation inside the opening handler restores correctly. Keyboard opening restores too. This distinguishes an actual containment failure from native opener focus ownership.

Do not claim the ordinary test's boundary synthetic-key assertions prove native containment. The native probe records each real Tab destination before any recovery. After a containment failure ONLY, the probe explicitly focuses Middle solely to exercise the separate Escape-restoration path; that recovery is labelled per row and is not counted as successful containment. No page errors; owned browsers/server closed. Exact artifact SHA and row data live in .batuta/runs/v1-alpine-bottom-sheet-native/{baseline-result.json,implicit-baseline-result.json}; first12rows preserve the original explicit-stop fixture. Current fixture/run implement the24case matrix.

Next: guarded read-only scout of Alpine BottomSheet's shared focus/teardown owner and existing modal consumers, then a bounded design based on the proven native Tab escape. Reuse the current Alpine owner; no dependency/framework/Colima work. Return-focus pointer ownership and missing-opener successor policy require their own compatible caller/API decision. Do not weaken focus assertions or claim the unchanged runtime fixed. All P1/packed Linux evidence stays pending.

Broader Task25 current4modal-source comparison confirms six pre-existing WebKit captured-opener failures: BottomSheetEscape; CommandPaletteopen/reset/return; Dialogclosepaths/reopen; Drawerclosepaths/reopen. Exact same6failwitholdandnewhelper; newhelpercloses4additionalnativeTab/cancellation/resource regressions. Native24returnstays23/24, whilecontainment16/24→24/24 andallboundariescleanup. Repair captured-opener caller/explicit-successor semantics separately; do not label broadWebKit suitegreen.
