# React modal initial destination — controller verification
Approved as a bounded API/behavior slice. Independent GLM3/3DONE, unchanged guard, Batuta verifierPASS; no concrete defect returned. Task10 size failures and remaining V1 obligations stay pending. Commit recorded in the run trail. High CodexTerra367.62s initial +287.50s retry, then critical/controller numeric default-filter and pre1.0 changeset precision completion. No worktree reset, remote operation, service operation or dependency change.

## Criterion 1 — public destination and safe selection
All four current public props expose optional `initialFocusTo?: () => HTMLElement | null`; generated React props/LLM references add those four fields, existing root/subpath exports flow without index edits. CommandPalette documents ignored inline mode. The shared private module checks actual same-document HTMLElement, connectivity, rendered/visible/enabled including fieldset, hidden/inert/aria-hidden ancestry, programmatic focusability, own panel membership and closest modal ancestry. A failed explicit declaration selects the named panel directly; absence selects eligible native task control or numeric nonnegative tabindex candidate, then panel. Declared negative reading target remains valid. CP preserves search as default; its modal panel now has tabindex=-1 for the specified fallback and a bounded Escape handler so that fallback stays keyboard dismissible without duplicate input-close calls. This extends the previous input-only close path only to honor the new explicit panel target; shared return/trap/presence owners remain unchanged.

Native hidden-first original baseline0/9 becomes9/9. Initial delivery API fixture39/60 exposed9generic-negative defaults and12CPnonfocusable-panel cases (two root causes). High retry becomes60/60. Extra native whitespace fixture then0/9 with actual tabindex attribute " -2", reflected tabIndex=-2, focus on negative-region. Root cause was lexical CSS prefix filtering, fixed by numeric default eligibility. Lowest-layer numeric regression initially3fail/1pass under retry helper, then final fullhelper17/17 eachengine. The same original public-owner byte restoration makes4new option tests fail; restored final owners4/4 pass with exact SHA parity. CLI selection skipped unrelated tests; no skipped/exclusive source tests introduced.

## Criterion 2 — real opening lifetime and preserved behavior
Final compiled native results all PASS, Chromium/WebKit/Firefox:
- API60/60: four modal families × declared reading/panel, invalid outside -> panel, resolver identity rerender then current next opening, StrictMode and no-option defaults. Real mouse entry, exact native active nodes/call counts, accepted close -> explicit original trigger after real Escape (default no-header forms use their real Cancel button). No expected destination was manually focused.
- Lifecycle42/42: initially-open StrictMode and retained-exit reopen in eachfamily, same panel retained with fresh epoch/calls;12real exception contexts; CPpending-frame close and destroy6cases. Ref attachment queues a microtask controlled close/unmount before the actual frame, then two native frames establish no delayed resolver/search focus. No requestAnimationFrame/focus mocks.
- Exception subset:9effect-owner cases record actual panel focus then catch the same thrown Error in a React boundary;3CPcases record actual panel focus and the same uncaught browser Error exactlyonce, outside the React boundary. The harness does not preventDefault on global errors. Expected CPpageerrors remain in each row; no unexpected harness errors. Initial CPthrow3failed on the nonfocusable panel; final all12pass.
- Ignored close12/12: two real Escape requests leave controlled modal open and initial resolver count1; later accepted close returns to trigger without another initial call.
- Whitespace9/9 and originalhidden9/9 final. Cross-family Escape48/48 retained with current compiled owners, including prevented/native-canceled/nondismissible cases.

Colocated source tests/SSR passed144Chromium+SSR,130WebKit,130Firefox on highretry owners. Only the private numeric helper and additive changeset changed after that broad run; final helper17/17 eachengine and all above actual compiled native cases verify those final bytes. Colocated tests cover shared invalid-target branches, actual public option wiring, no DOM prop leakage, SSR resolver0, inline0 and CPpanel/input Escape counts. Detailed remaining lifecycle/exception scenarios are directly reproduced in controller native harnesses instead of copied into every colocated component test; no claim that the worker authored or ran that missing lifecycle coverage. Committed resolver ref updates happen in effect, so aborted renders do not replace it; completed flag is set before consumer invocation and only accepted false resets it. No validation-failure inference or background inert/dynamic ownership qualification is made.

## Criterion 3 — scope, static evidence and size boundary
Exact15product+2generated paths, zero outside product edits. Controller types, scopedESLint, scopedPrettier, fullReactbuild and ownerdocgen thencheck PASS on finalhelper; changeset-only formatter repeated after metadata correction. Final source SHA map binds17files. Four public runtime exports are exercised from current dist artifacts. No new dependency, styles, public helper export, validation token, configuration, shared trap/return/portal/presence change, package version command or publication.

Conductor-authored original design incorrectly requested additive minor on currentReact0.x. Existing VERSIONING.md:20–37 requires patch for compatible additions/fixes; scoped changeset now patch. This is an explicit precision correction to the design's metadata requirement, not executor noncompliance or a separate human approval. Future deliberate1.0 versioning remains separate.

The size gate fails and Task10 remains pending:12React entries plus1Alpine, with FileManager newly exceeding its cap. Current standalone limits: Drawer2530/2000B, BottomSheet2560/2000, CreateWorkspace4107/3200, Workspace8364/8250, FileManager9808/9500, TimePicker4817/4000, DatePicker5910/5000, DateRange approximately6/5kB, Tooltip1954/1500, CommandPalette10298/9500, Recurrence7636/7000, WeeklySchedule15044/14500; Alpine21949/21200 unchanged by this React task. No cap/hash waiver, no claim these are final packed Linux measurements. API correctness is boundedly verified; completeV1 release still blocked on remaining ownership/API contracts and final evidence/size resolution.

## Raw evidence index
All controller roots below are MAIN .batuta/runs/:
- v1-react-modal-initial-focus-checks, -retry-checks, -critical/checks (pinned command arrays/logs/results).
- v1-react-modal-initial-focus-initial (firstdelivery source copies/diff/SHA), -critical (retry copies, numericRED, finalsourceSHA, size rows), -negative (HEADownerRED and exact restoredGREEN).
- v1-react-modal-initial-focus-native/{initial-result,retry-result,final-result,spaces-retry-result,final-spaces-result}.json and runners/fixtures:60API and9numeric cases.
- v1-react-modal-initial-focus-lifecycle-native/{initial-result,final-result,ignored-result}.json plus runners/fixture:42lifecycle and12ignored. Original42fixture bytes preserved as fixture-42.jsx before the ignored-only branch extension; standard42modes unchanged.
- v1-modal-ownership-native/{baseline-result,initial-delivery-result,final-initial-current-result}.json: original/fullownership baseline untouched; only9initialcases qualify here.
- v1-cross-modal-escape-native/final-initial-focus-result.json:48cases; earlier original output preserved.
Every native runner closes its own browsers/server; no external network data, containers or foreign services.
