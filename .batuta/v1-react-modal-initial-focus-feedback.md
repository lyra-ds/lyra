# React initial focus — first verification feedback
Initial CodexTerra/high delivery exit0 in367.62s. Scope15product+2generated catalogs holds. Pinned controller Chromium/SSR131, WebKit117, Firefox117; types/lint/format/build/docgen/check PASS. Existing test coverage is insufficient to accept the complete brief. Native API/lifecycle/default fixture finished39/60: the9generic-negative default cases and12CPpanel declaration/fallback cases fail across allthreeengines. Remaining39cases pass. Raw initial-result.json and initial.log preserve exact compiled artifacts and traces. Owned resources closed. These are21cases for two concrete entry defects, not21root causes.

## Required bounded repair
1. internal/use-initial-focus.ts:9,79: default selector includes generic `[tabindex=-2]`; programmatic eligibility then selects it ahead of the actual field. Exact native scenario: no option, no header-close action, hidden button then generic div tabindex=-2 then task input. Default must skip the non-task negative-tabindex-only region; declared reading heading/panel remains accepted. Do not globally reject negative tabindex for explicit targets or remove valid native task controls. Add a regression that fails on this initial helper.
2. command-palette.tsx:285-294: modal panel has no tabIndex. A configured resolver returning the public panel ref or an invalid outside target falls back to panel.focus(), which cannot move focus. Native declared/invalid/rerender/StrictMode CP scenarios time out waiting for the exact named dialog. Make the modal panel an actual programmatic fallback (`tabIndex=-1` contract), preserve inline presentation. Current source also owns Escape only on combobox: a newly supported focused panel must retain an accepted Escape path. This bounded clarification of criterion2 is in the same owner/scope: Escape from the modal panel must be handled once/contained; the input's already handled event must not cause duplicate close. Retain cross-family cancellation behavior and existing default search behavior, no shared trap/return/presence changes.
3. Current source tests only add five happy-path option calls, two eligibility cases and four SSR assertions. They do not establish the explicit lifecycle/invalid/exception criteria from the original brief. Add focused meaningful coverage in the existing scoped files: shared invalid-target table (null/detached/outside/body/foreign doc/hidden/disabled incl fieldset/inert/aria-hidden/nonfocusable/nested modal -> panel), valid panel/reading/Cancel/default field cases; immediate-owner initiallyopen/StrictMode and resolver-identity changes/accepted false then retained-exit reopening; deferredCP pending-frame close/unmount cancellation and StrictMode valid reschedule, inline0 retained. A reusable fixture is acceptable within the scoped internal test, but exercise actual public owners for their wiring. Assert actual nodes/call counts, never merely existence or mocked focus. Do not mirror every shared-filter branch into every component.
4. Prove thrown resolver focuses the panel then propagates the same thrown value. Immediate owners use actual React effect/error boundary behavior; CP uses browser RAF error handling, not a React boundary. Avoid global error suppressions or mocked focus. If the browser runner cannot safely own an uncaught browser error, state the exact unverified branch for the controller's native error harness instead of introducing a test/config suppression. No new error-to-warning path.

## Size disposition
Size gate still fails. First delivery adds FileManager to overages (9808/9500B), now12React+1Alpine=13 total. Drawer2490/2000, BottomSheet2527/2000, CreateWorkspace4065/3200, CP10253/9500, TimePicker4776/4000, DatePicker5868/5000, DateRange5965/5000, Recurrence7602/7000, Weekly14992/14500; prior Workspace/Tooltip/Alpine remain. These are current standalone measurements, not final packed protocol. No cap/baseline changes or unrelated optimization permitted in this retry; controller records Task10 for final size work.

## Execution
One high retry, preserve all existing delivery bytes except necessary bounded repairs/tests. Read this feedback then the original .batuta/v1-react-modal-initial-focus-brief.md in full; its conventions, closed15product scope and exact reviewed design remain authoritative with the focused modal-panel Escape clarification above. No tests/build/package manager/git writes/config/services/WORK/.batuta writes. Only pinned scoped Prettier allowed; all validation belongs to controller. Report actually unverified branches honestly.
