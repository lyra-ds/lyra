You are the implementation worker already delegated by Batuta. One high-lane verification retry; original high brief and four-file scope unchanged. No further delegation, package managers, hooks/config/ignores, controller-harness edits, services or commits.
# Actual controller diagnosis
1. Native compiled replay17/18 PASS. WebKit forward Tab closes the menu but fails exact After focus at native/run.mjs:14:358. Chromium Tab and both ShiftTab pass. Your new runAfterConsumer (dropdown.tsx:169) defers all item defaults through queueMicrotask; the previous synchronous implementation reached After on both engines. The scheduling changed the menu removal/native focus ordering. Event defaults must be owned during the correct event propagation turn; delaying all defaults is not a sound substitute for ordered root consumption. Preserve actual root bubble currentTarget/phase and separate capture handlers, child cancellation, once-only callbacks. All17 other native paths must remain passing. No fake event forwarding/capture substitution/forced Tab restoration/sleeps/timing patches.
2. Browser Chromium11/13 and WebKit10/12: test:120 reads tabIndex immediately after raw .focus(), before React commits — the same proven fixture error in medium feedback. Await the runner's real committed-state boundary with exact assertions. Test:160 uses commands captured before Escape unmounted the menu, dispatches Unicode to a detached node, and expects old commands[2] (Add) instead of current Ångström. Requery the reopened live menu, focus the current Edit command, dispatch synthetic å to that current node, assert current Ångström. Preserve accents and real focus proof. No weakening.
3. Size2.04kB exceeds existing2kB by37B. Static build/types/lint/format/docgen PASS. Keep limit, no new dependencies or API. Simplify ownership rather than code golf. Synchronous root event ownership is the public semantic boundary; duplicate per-child deferred wrappers add work and complicate lifecycle.
4. Coverage remains materially incomplete despite the full brief. Add bounded tests proving: multi-character prefix actually changes focus; icon text has a DISTINCT prefix from its command label so exclusion is testable; no-match; meaningful fresh-reopen prefix reset; empty/changed collections retain exactly one0; mixed two-command SSR has one0/one-1; Ctrl/Alt/Meta/composition ignored; root callback currentTarget/phase and separate capture/child cancellation order; canceled navigation/Tab/keyboard activation; unmount cancels pending timer. Existing native controller replay independently covers ASCII,18 navigation/selection/cancellation paths. Do not replace existing tests with permissive assertions or claim synthetic Unicode as native.
The three prior attempts exposed ownership mistakes and malformed fixtures; investigate these exact causes before editing. Browser/native/build/size proofs are controller-owned and will rerun; your own tsc/lint/format checks remain available through pinned Node. Report honest pending proof.
