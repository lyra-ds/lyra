# Existing-library maintenance verification — 2026-09-09

Basea4686af. Codex gpt-5.6-terra/high, no implementation retry/escalation. Exactly16 entries for11 existing names across root/docs/site/evidence-tool manifests. Exact replacements are in the brief and controller manifest-proof.json. No public package manifest or dependency name added. pnpm11.13.1 generated pnpm-lock.yaml and installed --frozen-lockfile into the worktree's own real installation. Node24.18.0 throughout. Lock generation4.8s, install6.8s. Public React/Alpine/Styles importers unchanged; held Playwright1.62.1, Vite8.2.1, tsdown0.22.14, TS5.9.3, Changesets, lucide/Alpine remain unchanged. Fumadocs introduces its own updated build-tool peer contexts; root Vite/tsdown pins and public importer graphs remain held. The generated lock legitimately changes transitive closures; it was not edited by hand.

## Controller checks
PASS: security, release policy, V1 planning consistency, repository policy10/10; React/Alpine/docs/site builds; recursive types; full styles/react/docs/site lint; full React Chromium/SSR765/765 (same clean baseline count); evidence tool349/349; docs Node316/316 and Vitest36/36; site20/20; publint all3 published packages; scoped formatting and git diff --check; React/Alpine docgen; parity; client directive/no-CDN/Alpine type scans; Wrangler4.129.0 CLI startup.

All453 generated React/Alpine dist files are exactly byte-identical before/after this maintenance batch,7,964,384 total bytes including maps/declarations. No added public source files, runtime dependencies or public package manifest changes. This is not a claim of final versioned tarball qualification.

## Existing failures kept explicit
pnpm peers check exits1: eslint-plugin-jsx-a11y6.10.2 declares ^3||^4||^5||^6||^7||^8||^9, while baseline eslint was10.8.1 and updated is10.10.0. Same pre-existing range incompatibility, no new peer category; actual all3 ESLint suites pass. No override/suppression added.

Alpine size-limit passes21.12kB/21.2kB. React size-limit fails for9 entries. Controller restored all4 old manifests+old lock exactly fromHEAD, installed offline/frozen, measured, then restored the new files and offline/frozen installation and measured again. All measured sizes/limits and all9 failures are identical in both graphs. No budgets or baseline hashes changed. Excesses: Drawer245B, BottomSheet284B, CreateWorkspaceDialog653B, TimePicker522B, DatePicker653B, DateRangePicker729B, CommandPalette439B, RecurrenceSelector377B, WeeklyScheduleEditor265B. These are a separate incumbent source/size qualification issue, not a regression introduced by this batch. They block full release readiness and will be diagnosed in the next bounded task. No pass claim for the complete CI/build distribution gate.

Raw proof in main checkout .batuta/runs/v1-library-maintenance: manifest-proof.json, graph-proof.json, install-results.json, build-checks, validation-checks, distribution-checks, published-dist-{before,after,comparison}.json, size-comparison (all original/restored results and restoration hashes). Executor logs remain in stabilization .batuta/runs/v1-library-maintenance.

No changeset for private docs/development-only updates. No remote merge/publication/versioning/Colima or foreign-service operations. Full Linux/Firefox/packed qualification and all remaining V1 contracts remain open; no ledger status changed. Independent GLM5.3Flash review:3/3 DONE, unchanged status/diff/scoped-file guard, verifier PASS after lossless typographic normalization of the three explicit DONE headings. No product finding; informational managed-state observation accepted as controller-owned and not code scope. No retry/escalation.

The349 evidence tests include a real Wrangler pages functions build in stage-preview.test.mjs:94-100. No deploy was performed. A separate source diagnostic built React at9d214bf with the same held compiler/size tools: all71 original budgets pass. Current9 excesses arise in the incumbent stabilization source range, not this dependency graph. Raw .batuta/runs/v1-size-diagnosis; temporary checkout removed and borrowed installation preserved. This strengthens attribution, does not resolve the9 release-budget failures.
