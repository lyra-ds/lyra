# CommandPalette return-focus verification — one retry

Original scope/contract remain authoritative. Controller results: Chromium/SSR95 and WebKit84 pass; native explicit/successor48/48 pass; build, types, docs types/lint, actual built trigger example6/6, docgen and docgen --check all exit0. Initial native baseline still records four WebKit mouse failures; explicit/successor baseline RED28/48 failures. No runtime behavior failure was seen, but the delivery is incomplete:

1. packages/react/src/command-palette/command-palette.tsx:223 — React Hook useEffect missing dependency panelRef (react-hooks/exhaustive-deps). Add the complete dependency; do not suppress. Controller react-lint.log shows warning despite exit0.
2. Browser test file currently adds only four dismissal cases + ignored-close case. Missing explicit integration criteria: current successor after trigger removal; latest resolver across two fresh close/open cycles including rapid reopen; exact once-per-close with closed rerender suppression; permanently inline open toggles must not resolve/warn and must preserve input; invalid closing-panel/overlay result falls back to the prepared opener; no DOM forwarding. Add focused integration proof, reusing existing helpers/owner semantics rather than copying the full shared-owner suite. A coupled test can prove related cycle criteria. Keep all strict existing assertions and callback/filter/pointer tests. Remove the unused ignoreClose option you introduced on ExplicitReturnFocusHarness, since the actual ignored-close test uses its own harness.
3. Both API pages Return focus example (~40-53) never render the trigger or successor refs, and leave canReturnToTrigger/open/groups undefined. Provide a self-contained typed component example with actual ref attachments, current availability state/prop and a live tabindex=-1 successor. A removed trigger's null ref must fall back to that successor even if an availability flag remains true. Include imports. Keep snippet within this task.
4. Both pages say 'Lyra leaves focus unchanged' when no valid target exists. Correct this: no invalid focus call is made, but removal may leave the browser on body; that is an invalid invoking composition, not successful restoration. Document hidden/inert/aria-hidden ancestors and closing panel/overlay exclusion, including presence during exit. Do not claim every omitted mouse consumer is automatically repaired.
5. Controller Prettier --check exits1 for browser test and both MDX pages. Fix those formatting findings. For this retry ONLY you may run the pinned Node executable with the already-installed Prettier --write on your scoped edited source/test/MDX files. This is a formatting mutation, not proof; do not run pnpm/install/test/build/lint/docgen. Controller will rerun checks. Preserve controller-generated props.json/llms.txt and dependency links; do not regenerate or edit them.

All logs: .batuta/runs/v1-command-palette-focus/. No scope expansion, owner changes, public API change beyond the agreed prop, new architecture, commit or delegation. Report checks pending honestly.
