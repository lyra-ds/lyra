# Critical completion after the one high retry
Same exact scope and design/acceptance as v1-modal-isolation-brief.md and the final design. High initial601.24s and retry446.33s are preserved. Retry185 source tests each engine, SSR14/types/format/diff PASS; lint fails unused CommandPalettePanel overlayRef parameter. New coverage remains one direct isolation fixture, insufficient for the explicit lifecycle criteria. Controller owns critical completion, real RED/GREEN proofs and final review; no further implementation delegation. No scope expansion, dependency/public API/CSS/config/remote action.

First diagnosed residue: command-palette.tsx:201 destructures an unused overlayRef after registration switched to actual overlay state. Remove obsolete private threading, retaining the root's real overlayRef for its existing activity/return owners. Lint is the exact failing proof.

Native post-retry behavioral diagnosis follows against a rebuilt exact artifact set, before behavior fixes. Existing critical investigation method remains root-cause sentence, focused RED proof, source fix, focused GREEN and all relevant suites. Preserve all original evidence; previous outside-background native expectation changes only as explicitly documented in regressions/README.md.

Custom-host false positive diagnosed: native active(Parent) used contains(activeElement), which already passes while a nested DOM child owns focus. The fixture sampled before setChild(false) committed; runtime trace still had the child active entry. Tighten the wait to the nearest active modal ownership plus no inert ancestor. No product custom-host repair justified by that premature sample. Re-run corrected fixture before verdict. Also fixed fixture-only onOpen prop spreading to non-palette owners (unknown React event warning); onOpen is now supplied only to CommandPalette.

## Proven controller corrections
- Inert claims wrote an empty value over preexisting application values. Critical source proof failed, then passed when claims stopped rewriting preexisting attributes and released only their own unchanged empty attribute.
- Suspended owners retained backdrop pointer provenance. A native held-pointer sequence and a source event sequence both dismissed the parent after child release; the shared topmost transition now invokes each owner's existing revoker.
- A suspended palette's shortcut fell into onOpen because its guarded close condition was false. Source/native counters proved the callback; the handler now declines an already-open inactive owner before processing the shortcut.
- Nonfinal unmount focused a generic parent destination rather than the captured still-valid task action. Source exact activeElement failed; the registry now retains the opener and validates it using existing initial-focus eligibility before safe default/panel fallback.
These four source failures became9/9PASS. A separate focused RED proved that removing the active portal host elected a survivor without restoring focus; observer reconciliation now uses the same restoration path, then10/10PASS.
- Three additional source RED cases proved: independently ranked nested overlay could remain visually below a sibling because its ancestor stacking context stayed lower; a portalled sibling opener within the same React parent branch was rejected by DOM-only containment; changing the container of an already-open portal incorrectly counted as a new activation. The chosen correction groups branch ordering, promotes accepted ancestor priority when a child opens, captures registered parent-branch focus and stores activation order across inner portal remount/StrictMode. All13 direct/public ownership tests then PASS, including exact parent resumption and task-action restoration. No CSS/config/public API edits.
Raw critical-source-red/green/final, disconnected-red and branch-order-red/green logs preserve RED→GREEN. The original high/retry source snapshots remain immutable under initial-source/ and retry-source/. Full three-engine/source/compiled validation and final independent review follow; no release qualification inferred from these focused results.

Document ownership RED proved both wrong realm filtering and wrong scroll target: iframe backgroundInert=false, frameOverflow empty, outerOverflow hidden. The manager now checks elements against the owning document's HTMLElement constructor; modal scroll hooks use the actual panel's document (Dialog's claim moved into its portal child). One focused RED became14/14 direct/public ownership PASS. Existing bare internal scroll-hook usage retains its default document behavior. The independently bundled three custom-container owners receive compiled three-engine document proof.

Before this correction the full197-test source set passed each engine, SSR14/static/build/docgen/declaration/Alpine parity passed, and all45 compiled qualification rows passed functionally. Firefox's only console warnings were missing DOCTYPE in the new controller fixture; fixed the fixture's standards-mode declaration without suppressing warnings. Final gate results must be regenerated for the document correction.

A focused RED with a consumer CSS z-index base of9000 proved a retained inactive exit could cover the resumed active owner. The stacking floor now includes owned exit-overlay bases, places active branches strictly above that floor, and restores prior values when claims release. The full15-test ownership suite passed after this correction. No CSS file or numeric bundle limit changed.

Final inspection identified a palette state regression: initialization was coupled to topmost focus scheduling, so suspension/resumption called onReady and cleared an existing query. The focused native-input source test failed with billing becoming empty (palette-query-red.log). Separate effective-open initialization from active-layer focus scheduling; the same ownership and CommandPalette suites now pass46/46 (palette-query-green.log). The compiled shortcut scenario also asserts query preservation while suspended and after resumption. Final complete proof is regenerated on these bytes before review.

Final controller gate on the palette correction:199/199 source tests in each Chromium, WebKit and Firefox; SSR14/14, format/diff/types/scoped lint/build/docgen PASS. The same450 compiled React files passed342 native scenarios across eight runners, including mixed root/subpath entries, StrictMode, query preservation, iframe owning-document Tab/scroll, initial/return and dynamic recovery.153 public declarations/Alpine files match the protected baseline. No compiled fixture console/page errors. Source warning-path fixtures retain their expected warnings without suppression. Exact hashes and size disposition are generated in final-artifact-proof.json and size-delta.json before independent review.

# Final implementation review disposition

The first GLM call354.13s and exact-prompt retry353.35s returned no report, with1995-file guards unchanged. Exported session metadata proves finish=length,32000 reasoning tokens and zero output; no provider/tool configuration changed. Resuming the same review session delivered a valid report in23.44s: TASK1DONE/TASK2INCOMPLETE/TASK3DONE, unchanged1995-file guard. Findings preserved verbatim in resumed-review-findings.md.

1. Accept missing no-warning suspended sibling proof. Added a real controlled Drawer under a later Dialog: programmatic accepted close leaves exact activeElement intact, never calls returnFocusTo and never warns. Spy preserves the real console implementation. Disabling only the authority gate makes the resolver assertion fail (sibling-authority-negative.log); original bytes restored before final gates.
2. Decline alleged missing retained-reopen hit proof. The binding contract requires exact tests, not duplication of each compiled scenario at source level. qualification/run.mjs mode=reopen already tests actual retained exit, CSS9000 base, new activation, elementFromPoint hit target and inert=false in all three engines; its three exact rows are supplied in retained-reopen-review-proof.json. All checks pass on the review candidate; rerun on final corrected bytes. No assertion weakened.
3. Accept dead private fields. Removed unread topmostRef and reduced LayerEntry to the actual four shared identity/ref fields using Pick; parent/effective state remains in the provider where consumed. No behavioral/API change or unwaived size claim.
4. Accept intermediate focus defect, refining the review wording: the helper preferred the captured parent opener, not always its initial target. The new actual focusin sequence proves two focus events before the explicit successor on accepted close (review-focus-red.log). Registration cleanup now distinguishes the committed retained inactive panel via its owner-controlled aria-modal and leaves accepted-close focus to the existing return owner; missing/active panels retain destruction recovery. No render-time global mutation or new scheduling owner. Same exact sequence now passes, as do all17 direct/public ownership tests (review-focus-green.log).

Final complete source/SSR/static/build/hash/native gates and an independent delta review follow this correction. The already-produced342-scenario proof belongs to the pre-review-fix artifact manifest until regenerated; do not relabel it.

After the review correction, the complete final controller gate passes201/201 source tests in each engine, SSR14/14, types/lint/format/diff/build/docgen and153 protected declaration/Alpine files. All eight compiled runners pass342/342 with zero errors and a new450-file artifact manifest. All native artifact hashes match that manifest. The final delta review receives this regenerated proof plus the exact retained-reopen hit-test rows; no older result is relabeled.
