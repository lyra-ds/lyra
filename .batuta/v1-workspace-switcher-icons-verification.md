# WorkspaceSwitcher icon optimization — verified locally

Replaced the three private Icon calls with exact existing ChevronsUpDown, Check and Plus imports. All classes, dimensions, colors and decorative aria-hidden output are preserved. Global Icon/registry, Avatar, state, event handlers, public API and CSS are unchanged.

- Source18/18 each Chromium/WebKit/Firefox and SSR1/1; types/lint/format/diff/build/docgen PASS. Existing tests unchanged.
-18/18 actual compiled before-after cases:3engines ×2themes ×closed/open/focused-option. Exact SVG trees/attributes/colors/rectangles and PNG bytes match. Native ArrowDown/Enter selection and pointer creation produce exactly one callback each; no browser errors/warnings. Controller inspected the dark/open reference.
-153 protected declarations/Alpine files unchanged by direct hashes; all453 current compiled files match fresh packed artifacts. Actual tarballs retained, controlled temporary checkout cleaned. Final FileManager compiled bytes still match its independent accepted proof.
- Packed Size Limit8364→2889/8250B (-5475). Application-shell17000→16954B (-46); other scenarios unchanged versus accepted FileManager. Files/data stays11315B. Overall failed budgets12→11, unchanged caps and immutable baseline.

Codex gpt-5.6-terra/medium65.40s, no retry. Scoped formatter was permitted; all verification above was independently run by the controller. One bounded source file plus React patch changeset; no generic framework, resource change, dependency or public API change.

GLM36.24s3DONE/no actual findings,2012-file unchanged guard; verifierPASS. Clarified incidental review prose: FileManager's two opens are separate from Workspace's selection/creation assertions, and declaration equality is direct hash proof. No implementation correction required.

Combined approved icon changes now pass their own existing budgets. Ten React and one Alpine budget failures remain unwaived, as do Task39 final core/Alpine qualification and the anchored placement audit. Blade remains deferred; no remote/release action.

Raw main .batuta/runs/v1-icon-optimizations/: workspace-switcher/ proof/check/review, workspace-switcher-packed/ final combined tarballs and72entry/5scenario/4CSS measurements, native/ original/final image and SVG comparisons. This is bounded local verification, not final V1 qualification.
