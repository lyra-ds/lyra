# Native budget gate — one bounded correction round
Work only in this same isolated worktree. Read .batuta/native-bundle-budget-brief.md for original contract; scope/boundaries unchanged. Candidate9f3b89a was snapshotted. Fix implementation, no new framework/dependency or permission request, no commits. Controller has built React/Alpine through the real collector, so full focused measure.test.mjs now passes34/34; do not waive its build-dependent test.

Controller rejected delivery on concrete proofs:
1. Real collectBaseline→runBundleBaselineCli(--check-budgets) fails after63.94s: `bundle budget check: unsupported fixture source change` at budgets.mjs:166. installFixture computes sourceSha256 but environment() never emits it. Wire actual producer→validator (not merely test fixtures), preserving OLD --check/--write evidence shape; extra budget-only protocol fields must not alter historical exact reproduction projection or comparison semantics. Do not relax fixture validation.
2. installPackedArtifacts returns package properties tarball,sha256,version; historical order is version,tarball,sha256. compareBaseline uses JSON.stringify. Same exact historical values rebuilt in your order fail with `bundle baseline drift in: environment` and no leaf differences. Preserve original metadata serialization order, do NOT weaken compareBaseline. Make the archive checksum binding real across selection/extraction (capture/check before+after extraction at minimum), not solely an optional expectation exercised in a test. No second archive framework.
3. Public CLI discards checkBundleBudgets result and prints counts only; actual env/fixture/config/command/externals, candidate revision, artifact IDs, entry deltas/exception decisions and CSS/module drift must be retained in structured public output. Currently result itself drops fixture/brotli/command/candidate revision and all React/scenario module drift. Return a clear budget-only report through CLI, retaining source measurements/provenance as needed. Existing exports/API testing seams need no new unused checkBudgets injection (current tests do not use it); remove unnecessary override, actual policy must run.
4. Proven invalid-data acceptance: after adding the documented source fingerprint to the freshly collected candidate for this DIAGNOSTIC ONLY, comparator passes when deleting candidate.revision, setting schemaVersion999, deleting standalone[0].modules, setting modules[0].renderedBytes=-1, or deleting assets.javascript.files. Reject missing/malformed required identity and measurement structure using small existing helpers; preserve/validate module/file lists and nonnegative numeric values. Keep a compact parameterized regression, not a new schema framework.
5. Native portability: fixtureSourceSha256 hashes relative(REPO,file) paths with platform separators, so Windows backslashes produce a different fingerprint for identical fixtures. Canonicalize protocol labels (and checkout LF/CRLF text if needed) while retaining actual provenance, with a meaningful portable fingerprint proof. run() still directly spawnSync('pnpm',args); Windows pnpm.cmd shims cannot be launched that way. Keep shell-free argument execution through the actual current pnpm JS entry when available under `pnpm baseline:bundles ...` (npm_execpath/process.execPath), or an equally narrow robust native solution; do not add Docker/shell quoting/executable discovery framework. No Windows runtime PASS claimed on macOS.

Keep5 existing scenarios/72 entries/4 CSS, all budgets and exceptions unchanged. Existing canonical baseline/pointer/comparison files and production packages remain byte-identical. Update active docs only if result format changes need explanation. Run full `node --test tools/bundle-baseline/measure.test.mjs` and scoped Prettier/diff checks; controller owns full pnpm test and fresh measurement. New regressions must use actual exported logic, not mocked policy result; ensure the originally observed failures are detectable. No full suite/build/collector from executor. Report criterion1/2/3 DONE or INCOMPLETE honestly and list proofs. This is the single correction round; do not self-escalate or spawn agents.
