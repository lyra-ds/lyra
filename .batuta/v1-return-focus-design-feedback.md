Accepted major closing-subtree exclusion; added explicit DOM ownership exclusion and verification after panel removal. Declined a blanket late runtime refocus/retry: this would violate single-attempt close semantics and risk stealing focus after user navigation. Verification now checks the reported failure after actual exit; out-of-slice separately portaled layers remain unqualified. Accepted Drawer open-keyed capture clarification. Accepted example availability-state branch and corrected the JSX into a complete component-body example. No API expansion or runtime implementation.

Current contract: `.batuta/specs/2026-09-08-modal-return-focus-design.md`, approved
by the maintainer. Original paths and approval-pending wording below are retained
as historical drafting/review evidence; Batuta owns the active workflow.

Original findings:
docs/superpowers/specs/2026-09-08-modal-return-focus-design.md:79 · major · eligibility list omits exclusion of targets inside the closing modal's own subtree; panel stays mounted through exit (packages/react/src/dialog/dialog.tsx:257, packages/react/src/drawer/drawer.tsx:164), so an in-panel target passes connected/visible/focusable checks at resolution, takes focus, then unmounts and browser lands on body with no diagnostic — silent body fall forbidden by docs/superpowers/specs/2026-08-30-overlay-family-design.md:238 and contradicting this doc's no-successful-return-undetected claim at :105. Fix: add "MUST NOT be inside the closing modal subtree" to eligibility and recheck after panel removal
docs/superpowers/specs/2026-09-08-modal-return-focus-design.md:134 · minor · "use the existing component close/capture lifecycle" under-reserves Drawer: capture effect is mount-only (packages/react/src/drawer/drawer.tsx:75–81, deps [panelRef, captureOpener]) vs Dialog open-keyed (dialog.tsx:120–127), and restore nulls openerRef (drawer.tsx:169), so reopen inside the presence window gets no fresh capture and point 5's fresh-cycle rule cannot hold without an explicit capture re-key the brief must name
docs/superpowers/specs/2026-09-08-modal-return-focus-design.md:45 · minor · example resolver `triggerRef.current ?? successorRef.current` returns the trigger whenever mounted, so the doc's own disabled-trigger scenario (:51–53) never reaches the declared successor — Lyra rejects it, captured opener is the same disabled trigger, both ineligible produce a diagnosed failed cycle despite a live successor; example should demonstrate the state-conditional pick its prose mandates
