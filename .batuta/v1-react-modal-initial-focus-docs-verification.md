# React initial destination — public documentation verification
Approved. Independent GLM3/3DONE, unchanged guard, Batuta verifierPASS; no concrete defect returned. Medium CodexTerra341.53s initial +244.81s prose-only retry, no escalation. Exactly ten productpaths: two existing liveexamples and eight MDXpages. Runtime API was committed at1d5c8e5; no library/stylesheet/dependency/export/catalog change in this task.

1. Actual DialogBasic uses its existing Cancel button/ref as initialFocusTo while bothfooterbuttons preserve dismiss-only behavior and existing returnFocusTo. DrawerWithoutFooter uses a named h2 Activity details with tabIndex=-1/currenttypedref, preserving its read-only purpose/nofooter/returntarget. Existing CommandPaletteTrigger remains unchanged and enters search. Actual source-imported examples against current built publicroot: native9/9 across Chromium/WebKit/Firefox, exact activeCancel/heading/search and exact trigger after real Escape/exit. No prepared destination focus, mocks or fake domain operation. Baseline3/9 was the old examples' lack of the new declarations, not6librarybugs. The controller corrected its own h3-only test assumption to the observed valid named h2; no product change was made to satisfy that assumption.

2. Eight pages compile asMDX; each has its exact current JSXsnippet body typechecked against published declarations, alongside both actual examples. Existing Dialog/Drawer snippets intentionally omit import context; initial standalone extraction produced missing-symbol errors. The corrected controller harness prepends only their existing published React hooks/Button/component imports, matching earlier returnfocus validation, and leaves exact body bytes intact. Other snippets already carry imports. Current typesPASS. The retry places the complete new initial-focus sections before the complete return-focus explanations (six pages), corrects palette guidance to current search/namedpanel without inventing content slots, qualifies normal input/virtualfocus behavior and removes redundant warning/demo boilerplate. Initial invalid/null -> namedpanel, no-option task/searchdefault, eligibility/read-onlyonceperopening/no-rerender-replay/inlineignore/errorcontext/formvalidationownership are documented. Newlywritten prose is English in bothlocales. Explicit positive demo copy says bothfooteractions close the demo; no deletion-success claim.

3. Final owner MDX8/snippet+exampletypes/ESLint/Prettier/stack303 PASS, no skip/failure. Final source proof verifies tenpaths, all eight Alpine/Blade tails byte-identical to1d5c8e5, and bothliveexample files plus all8JSXsnippet bodies byte-identical across the prose-only retry. Therefore the existing native9proof remains bound; no redundant browser rerun for text-only changes. Currentfour compiled modalentry SHAvalues and publicindex SHAmatch the runtime/task baseline. No build/version/remote/service/Docker/Colima command was run; all native resources closed.

Raw MAIN .batuta/runs/v1-react-modal-initial-focus-docs-native includes baseline-result.json, initial-result.json (native9), current mdx-result.json/snippet wrappers/typesconfig/log, controller-fixture-diagnosis.md, scope.json and final-source-proof.json. Initial controller missing-import output remains -docs-checks/types.log; corrected types-context.log and -docs-final-checks/results.json allpass. Initialsource snapshots live under -docs-initial; exactworkerlogs/results remain WT .batuta/runs/v1-react-modal-initial-focus-docs and -retry.

Library size remains12React+1Alpineoverages at the preceding runtime commit, Task10 unresolved; no new library bytes in this docs task. No complete Next/deployed-page/packedLinux/AT/V1 qualification is inferred from exactMDX compilation, public type validation and actual imported example interaction.
