# Alpine Tabs — selected-contract baseline
No Alpine implementation yet. Selected API/fallback/event contract remains `.batuta/specs/2026-09-10-tabs-owned-content-design.md`; advisory scout `.batuta/scout/2026-09-10-tabs-alpine-implementation.md`.

Current compiled Alpine fixture with real headed panels/native fallback, before initialization, passes3/3 engines. Four selected-contract scenarios fail in each engine (12fail): successful enhancement leaves enhanced list hidden while hiding inactive content; owning-list native click veto changes active anyway; owning-list native key veto changes active/focus anyway; destroy leaves enhanced controls/hidden panels instead of restoring usable native fallback. All15observations preserve zero page/console errors and exact current Alpine artifact SHA under MAIN `.batuta/runs/v1-tabs-alpine-baseline/`.

This is a current-API baseline: the native fallback nav deliberately lacks the not-yet-existing fallback binding; manual enhancement exposes legacy list markup only in interaction/destroy controls. Do not claim it is the future complete binding fixture or JavaScript-disabled qualification. The first baseline key-entry harness assumed fallback links were skipped everywhere; Chromium/Firefox visit two links while macOS WebKit skips them. Corrected driver walks bounded real native Tab input until the first tab, proves the target, then exercises veto/destroy; initial mistaken harness outputs remain archived separately. Final nativeEntryTabs3/1/3, all key veto changes active toTwo despite defaultPrevented=true, all destroy cases start from verified focusedTwo.

Before freezing implementation: prove actual Alpine same-element listener ordering; list consumer prevention must precede Lyra default regardless registration order. A narrow listener on the owning root's bubble boundary may implement list-owned defaults after every list/trigger consumer, with nested ownership filtering and synchronous before/result events. If selected, record this implementation precision explicitly; no observer/global manager/asynchronous default/new dependency. Verify every referenced source/caller/generated-catalog scope before dispatch.

Actual event-order probe24/24 passed, zero errors, and selected the owned-root bubble boundary for list defaults. Current source browser suite passes in all3engines before implementation. Scope scout found the external Blade snapshot dependency; see `.batuta/scout/2026-09-10-tabs-alpine-scope.md`. It must remain unqualified and byte-identical during this bounded runtime slice.
