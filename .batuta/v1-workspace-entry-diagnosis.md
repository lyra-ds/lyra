# WorkspaceSwitcher selected entry — reproduced

Read-only GLM scout completed with unchanged main guard. Runtime React/Alpine files and Alpine tests are byte-identical to stabilization; React test differs only through the previously approved actual-dark contrast fixture. Source/anchor checks accepted. Verbatim report: scout/2026-09-09-v1-workspace-contract.md. The exact current normative paragraph was supplied inline because main's historical spec differs.

Controller replayed the existing built React and Alpine workspaces in3native engines (Chromium/WebKit/Firefox),5opening methods each, selected workspace Beta in the middle.18/30 rows pass: click/Enter/Space focus selected Beta in both stacks.12/30 fail the normative selected-entry assertion: ArrowDown focuses unselected Alpha in both stacks, ArrowUp focuses unselected Gamma in Alpine and the create command in React. No page/infrastructure errors; owned server/browsers closed. Raw worktree .batuta/runs/v1-workspace-entry-native/baseline-result.json records exact artifacts/input/result. This independently reproduces the source allegation; no source change was needed to obtain evidence.

The approved OF-COMPOSED paragraph requires opening to focus the selected option when available; current first/last-on-arrow fixture expectations do not supersede it. Two narrow stack-specific repairs follow: React trigger opening should reuse its existing selected-focus path; Alpine arrow opening should prefer its consumer-served selected option while retaining no-selection fallback. Post-open arrows/Home/End, effect callbacks, consumer-owned selected value and focus restoration stay untouched in those tasks.

Other scout proposals remain unqualified: React create button is currently an option, needs a separately bounded command/listbox containment design and native Tab accessibility proof; root option-key cancellation needs a concrete reproducer; Alpine change cancellation and automatic ARIA binding require exact event/served-markup compatibility checks. Do not invent an Alpine create API. A closed React aria-controls target is a source asymmetry, not a newly demonstrated accessibility failure. No P1/packed release qualification from this entry correction.
