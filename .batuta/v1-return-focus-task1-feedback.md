# Task 1 verification feedback
You are the implementation executor already dispatched by the Batuta conductor. Implement directly; do not dispatch another executor, run Batuta orchestration, review, commit or cleanup controller artifacts. Same five-file scope, approved contract and original brief remain authoritative. Controller owns verification and node_modules symlinks (do not remove them). Do not run pnpm checks inside sandbox; previous blank output was NOT successful proof. Report checks pending.

Controller trusted mouse proof passed all 12 cases, with baseline 9 failures preserved. However controller Chromium/SSR failed 4 of 43 tests and TypeScript failed. Files/logs under .batuta/runs/v1-return-focus-task1/ are read-only evidence.

Required corrections:
- dialog.ssr.test.ts:50 vi missing import; use-return-focus.ts:81 target has Element type without focus; :82 process has no types. Use existing repo development diagnostic convention without installing types, silencing casts or blanket suppression. Shared diagnostic must identify modal return focus, not always Dialog (Drawer will reuse).
- dialog.browser.test.ts:360 asserts optional panel attr is null AFTER panel removed, gets undefined. Assert real mounted DOM prop consumption before closing. :353 backdrop userEvent.click targets center under panel, so no close. Exercise actual exposed backdrop coordinates/origin using established tests; do not weaken pointer regression or fake close. Keep exact focus after real exit.
- Hook tests helper focusable() never marks cleanup attribute, leaking buttons between tests; unfocusable div leaks too. Clean every owned real DOM fixture, preserve foreign DOM.
- Contract coverage is incomplete: add real disabled-fieldset/hidden ancestor cases; connected iframe document target (current cross-document node is also disconnected and proves no cross-document rejection); closing overlay/panel AND descendant focus rejection with spies and valid fallback; invalid captured opener focus not called; ignored close requests and closed rerenders do not refocus/resolve; meaningful latest resolver, removed-trigger successor after closing ref commit, rapid reopen fresh opener; StrictMode real component lifecycle including omitted-prop valid opener.
- captureOpener currently overwrites capture on each effect replay. StrictMode can capture a control inside the modal on replay and lose the valid original opener. Prove and fix once-per-opening capture, allowing fresh capture after a real close/reopen. Parent root and portal child effect order matters; do not reset fresh capture in parent after child effect.
- Eligibility selector currently treats any [tabindex] and any summary as focusable and lets input[type=hidden][tabindex] through. Verify actual supported programmatic focusability, reject invalid tabindex and noninteractive summary cases rather than attempting invalid focus. Visibility collapse is also hidden.
- resolverRef.current assignment during render should not expose an uncommitted/suspended render callback to closing lifecycle; align current resolver with accepted commit. Keep callback evaluation after closing DOM/ref commit exactly once, no timeout or animation-driven refocus.

Test the behavior, never the mock. A failing test means fix the code, not the test. No test-only flags or branches in production code. Do not claim tests you did not run passed. Implement these corrections, report files and unverified checks, then end; conductor runs tests and independent reviewer. No further approval needed.
