# React logical close versus exit presence — native diagnosis
No implementation design chosen yet. Historical normative source docs/superpowers/specs/2026-08-30-overlay-family-design.md:163–169 requires logical close to stop interaction, dismissal, containment, isolation, scroll claims and AT exposure while presentation may remain; :200–203 requires aria-modal absent when inactive. This remains an incumbent obligation, not renewed comparative foundation work.

Current compiled React at1d5c8e5, docs/source checkoute13e300: native12/12fail (four modal families × Chromium/WebKit/Firefox), zero page/harness errors, allowned browsers/server closed. MAIN .batuta/runs/v1-modal-logical-close-native/{fixture.jsx,run.mjs,baseline-result.json,baseline.log} retains exact artifactSHA and state. It is one bounded lifetime defect with multiple observable consequences, not12independent bugs.

The fixture opens a named modal by real mouse, waits for actual entry, then commits controlledopen=false via flushSync and samples the still-connected exit panel. All12retain aria-modal=true, have no hidden/inert ancestor and accept a programmatic focus attempt into their closed input. Real Escape from that improperly focusable closed panel then makes one additional onClose request. Dialog/Drawer/BottomSheet retain2native focus guards; CommandPalette correctlyremoves its guards. All12already return to the explicit trigger and release their sole body scroll lock before the adversarial focus attempt, so no scroll/return-focus repair is inferred. The programmatic focus attempt is a test that a logically inactive scope rejects focus, not artificial preparation of an expected initial-focus destination. Natural Tab re-entry, nested claims and reopening still need preservation proof for the eventual repair.

Source owners: DialogPanel useFocusTrap(panelRef,true), aria-modal=true and event handlers; corresponding Drawer/BottomSheet owners duplicate that active-through-presence behavior. CP uses modal&&open for trap/scroll but aria-modal depends only on mode, and its input/panel/backdrop handlers remain callable while retainedclosed. Existing scroll owners already use open or !closing; preserve them and the shared lock implementation. Shared useFocusTrap listeners bind to each panel, not document (a controller hypothesis about document-wide listeners was disproved by source inspection).

Next bounded design must deactivate each own exit overlay/panel synchronously with logical state, preserve actual exit geometry, accepted-close returnfocus, rejected-close behavior, initiallyopen/StrictMode/SSR, inlineCP, and fresh reopening without stale inert/ARIA/guards or callbacks. Prefer one small private current-owner mechanism if needed; no React18-unsafe boolean attribute or new runtime dependency. Scope and effect/cleanup semantics must be technically reviewed before worker dispatch. This slice must not claim background inertness, topmost/sibling registry, dynamic focused-node rescue, parent-child transfer or fullV1qualification. Those remain Task31; no final registry/API architecture has been chosen here.

A second bounded native probe also fails12/12: real pointerdown on the old backdrop, accepted controlledclose then retained-node reopen, then pointerup/click without a new down. Allfourfamilies send an unwanted onClose request into the new cycle; same overlay/panel stays mounted. MAIN run-gesture.mjs/gesture-baseline-result.json/gesture-baseline.log, zero errors. The original close-only fixture is preserved as fixture-close.jsx; the extension only exposes a separate cycle function. This local pending-gesture revocation belongs to the same logical-lifetime slice; it does not establish a global stack design.

Task36 current implementation passes24/24primary compiled native close/retainedgesture cases, with zero errors after controller standards-mode HTML correction. The optional modal-inline-modal warning is pre-existing in a frozen ae3e6ce CP source baseline and current compiled code, WebKit only; retain as Task31 follow-up, not a new stalegesture finding or clean release proof. See v1-react-modal-logical-close-verification.md.
