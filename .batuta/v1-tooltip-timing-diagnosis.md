# Tooltip timing and ownership — reproduced

Guarded GLM scout completed read-only, unchanged HEAD/status/diff/source hashes. All cited source paths exist and match stabilization except the known Tooltip browser fixture tabIndex addition; controller read the current owner/test/placement/styles. Main's historical design differs, so the scout received the exact current normative excerpt inline. Report retained verbatim at scout/2026-09-09-v1-tooltip-timing.md.

Controller replayed built Tooltip754874b7f0756a05a41f305203030efe4d5b19b2425fa513a6f0b940d77a43af using pinned Playwright clock and real mouse/keyboard in Chromium151/WebKit26.5/Firefox153. All12 engine/scenario rows violate their narrow contract assertion: hover is already open at0ms (also499/500); leave closes immediately rather than retaining99ms; leaving hover closes while focus remains onA; focusA+hoverB Escape closes BOTH; moving onto actual bubble coordinates closes, computed pointer-events none and hit target HTML. No infrastructure/page errors; owned servers/browsers closed. Raw worktree .batuta/runs/v1-tooltip-timing-native/baseline-result.json and runner/fixture. An initial artifact import-depth error was corrected before the recorded12-case run; it produced no behavioral evidence.

Scout recommendations adjudicated: accept immediate state/event paths, absent shared timing and CSS visibility cause. Decline real-clock immediate assertions as deterministic500ms evidence; use the installed browser clock. Decline reducing content hoverability to100ms grace: pointer-over-content must retain ownership, and the actual pointer test is mandatory. Decline a realm-global singleton as per-document compliance; use root.ownerDocument and its window. Topmost-only Escape is explicit in the supplied contract, not optional. Per-instance pending-open timers may be coordinated by a shared document owner, rather than one shared500ms timer incorrectly coupling independent hovers. Static accessibility snapshots show generated text but do not establish duplicate screen-reader announcements; no assistive-technology qualification is claimed.

Proposed bounded implementation is specs/2026-09-09-tooltip-timing-design.md. It preserves the public API/current visual owner and adds only the specified timing/ownership behavior. Other overlay ownership, broad clipping/assistive-technology and packed/Linux release qualification remain separate.
