# Mobile representative-page measurement protocol

Frozen before observed performance results, 2026-09-11. Controller-owned critical diagnostic; fixture implementation delegated CodexTerra/medium. No product/cap/baseline changes. This is a bounded current-candidate laboratory validation, not field Core Web Vitals or comprehensive family/adapter conformance.

Candidate:6dacf7e exact already verified packed React0.5.0/Alpine0.6.0/Styles0.5.0 from v1-approved-budgets/approved/tarballs. Verify SHA256 and installed content. Existing frozen bundle-baseline consumer lock/peers/tools, fresh external install/store. Production Vite builds include framework peers and serve actual precompressed Brotli text quality11 JS/CSS/HTML, immutable recorded hashes. No development server/source alias. Plain loopback HTTP server, no external requests or downloaded fonts/images. No before/after latency claim: cap-only change already proved runtime byte identity, not a measured speed improvement.

Two related scheduling fixtures, not an adapter performance contest: React includes actual Drawer+DatePicker+TimePicker and native form/list; Alpine includes whole Lyra plugin and actual Drawer with native date/time fields.200deterministic unvirtualized rows, expansion to1000. These are chosen representative/stress datasets, not an already-approved family maximum. Preserve normal product motion; no focus/portal/observer shortcuts. Capture initial/open/expanded screenshots and layout width.

Pinned locally installed Playwright Chromium, exact version/executable/hash recorded.390x844 CSSpixels,DPR2,touch/mobile emulation, English locale, light scheme, normal motion; DevToolsCPU4x slowdown and network latency150ms/download200000bytes/s/upload93750bytes/s (1.6Mbps/750kbps). Synthetic throttling on host hardware, not a calibrated or physical phone. Each cold navigation uses fresh browser context and cache disabled. No foreign service/Colima/resource changes. Sequential only.

3warm-up navigations then30recorded cold navigations perpage. Record response/resource encoded/decoded/transfer bytes, navigation/load, FCP,LCP at quiet observation before input, layout shifts without recent input (lab cumulative sum), long tasks/count/max/total and DOM nodes. Initial collection starts from addInitScript before page code; finalize after ready/fonts and300ms quiet settling. This wait is an observation window, not substituted latency. Report median,p75,p95,worst and all30rows. LCP≤2500ms and lab layout-shift sum≤0.1 atp75 are diagnostic references from web.dev/vitals, not proof75%realusers pass. No observed samples discarded or winsorized.

3warm-up workflow cycles then30recorded cycles perpage. Real trusted click/keyboard actions verify: open Drawer; Tab from name to next field; type name then Save/commit+close; reopen and Escape/cancel+focus restoration; select alternating appointments2/3 (distinct state every cycle); expand200→1000 then reset outside measured operation. Each measured operation records event-to-ready-frame PROXY: trusted event timestamp through asserted relevant DOM state and one intervening animation-frame paint opportunity (two requestAnimationFrame callbacks). This is not exact presentation time, INP or animation-completion duration. Native PerformanceEventTiming is collected separately with durationThreshold16ms; durations are8ms-quantized and entries below threshold may be absent, never imputed as0. Record trusted event observed, starting DOM state, asserted end state, nodes, long tasks and matching EventTiming rows. A chosen diagnostic p95proxy≤200ms flags operations for investigation; it is not an approved family SLA or formal INP threshold. Report quantization and missing coverage.

Negative control before timed series: a separate disposable page injects a300ms synchronous capture-event blocker on Open editor; the proxy must exceed250ms and long-task observation must include≥250ms. Preserve the negative result separately, never mix it with recorded series. Fail closed if clocks, DOM proof, throttling setup, errors, sample counts, serving headers, packed hashes or controls fail. Keep all failing runs and corrections; no product fixes without diagnosis/scope.

References: https://web.dev/articles/vitals and https://developer.mozilla.org/en-US/docs/Web/API/PerformanceEventTiming (consulted2026-09-11). EventTiming observer is not full field INP aggregation. Report runtime concerns honestly; unrelated unchanged pages/families, mobile physical device, browser matrix, assistive technology and final V1 release/scenario/delta/baseline gates remain separate.

Supplementary coverage declared after the principal series, before observing these additional operations: same unchanged served assets/profile; actual React mobile DatePicker and TimePicker nested BottomSheets inside Drawer, selecting alternate dates12/13 and times09:30/10:00 each cycle. Verify parent inert and focus returned to picker triggers; verify Cancel preserves prior saved name.3warmup+30recorded workflows, same200ms p95 proxy reference and separate negative control. No reclassification/removal of principal samples. A separate one-cycle per-adapter functional Cancel/Escape probe additionally checks unsaved draft does not overwrite committed name. These checks do not turn the two pages into equivalent adapter workloads.

Execution record: .batuta/v1-mobile-validation.md. This protocol defines diagnostic scope, not a release-wide SLA.
