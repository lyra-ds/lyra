# Tooltip critical continuation after high retry

Codex Terra/high initial delivery failed scope (unauthorized Impeccable ignore, preserved then removed), three unflushed new fixture assertions and two native behavior regressions. High retry fixes those: controller Chromium/SSR13, WebKit12, Firefox12, all static/build/style/parity/docgen pass; no config/ignore remains.39/42 extended native rows pass. The remaining3failures are one cause across3engines: removing focusedA keeps its focusRef true, so the document coordinator sees phantom ownership and never starts300ms warm expiry; hoveringB at300ms opens immediately instead of waiting500ms. Exact raw retry-result.json under runs/v1-tooltip-timing-native. Focus/hover refs are only updated by input handlers; mutation observer closes but does not reconcile ownership. Unmount already unregisters the owner and is a separate passing path.

Continue critical/controller within existing approved scope. Add the focused-removal warm-expiry test and run RED before changing the observer. Reconcile ownership with actual remaining branch focus/hover when the tracked target disappears; cancel stale work, update document warmth, preserve any real remaining branch owner. Do not change general close to clear owners because Escape intentionally retains stationary ownership. Then run focused GREEN, owner suites and native42cases, independent GLM review and the required build/static/size evidence. Existing high attempts are preserved; no reset/removal of this shared stabilization branch. This continuation uses maintainer until-completion authorization. No new API/config/dependency or budget change.

Source-size budget remains a separate failing gate: retry Tooltip1924B vs1500B (+1039B versus original885B); nine earlier entry failures remain. Final measurement and explicit disposition must stay honest; no waiver implied by correcting behavior.

Controller fixture diagnosis: direct React act from the high retry emitted unsupported-act-environment errors; installed vitest-browser-react2.3.0 deliberately does not export its scoped act wrapper. Two synchronous flushSync attempts did not commit the warm mouseover update. Temporary coordinator tracing proved isWarm=true and opened() ran while the immediate DOM assertion still read closed; source instrumentation was restored exactly. The final test helper uses React act with its intended IS_REACT_ACT_ENVIRONMENT flag scoped to the call, restoring the prior value in finally, following the installed renderer's own mechanism. This is test-only React API setup, not a tool/host/Colima configuration change or console-warning suppression. No package/config file changes. Browser-clock native replay remains the independent timing oracle; no runtime clock or assertion boundary was relaxed.
