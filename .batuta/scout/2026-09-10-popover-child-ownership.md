# Popover child ownership scout disposition

Current base2935cda/product879b315. Controller verified Popover, Slot, controllable state, placement and Dialog source anchors plus four real internal consumers. Main .batuta/runs/v1-popover-child-layers/ preserves the exact compiled source5PASS/native baseline, hashes and raw advisory output.

GLM initial545.11s returned no report (finish=length); same-session low-variant continuation115.45s delivered advisory output;1996-file guards unchanged. The invocation variant is supported by the existing model; no configuration file changed. Source causes accepted after controller checks. Document-capture Escape, global Popover stack and modal registry read declined: they preempt consumer preventDefault and are unnecessary for the proven React child paths. Selected root-bubble/inside-event ownership and accepted-close focus contract is in specs/2026-09-10-popover-child-ownership-design.md. Native DOM listener order is not used as a correctness condition. CalendarView's PopoverState is a search false positive, not a current Popover import.

Advisory output is not an implementation approval. Exact selected design receives a separate technical review; product owner remains unchanged at this point.
