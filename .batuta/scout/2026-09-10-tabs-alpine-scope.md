# Alpine Tabs scope scout

OpenCode/GLM5.3Flash50.25s, exit0, unchanged MAIN status/diff guard. Report preserved before controller verification. Searches returned excluded generated output despite the brief; no writes occurred.

<<<REPORT
## Answer
(1) In-repo consumers of `lyraTabs` / Tabs x-bind markup, outside `packages/alpine/src/tabs.ts` and `tabs.browser.test.ts`:
- `packages/alpine/src/index.ts` — registers the `lyraTabs` Alpine.data binding (index.ts:193-195), imports `lyraTabs`/`LyraTabsOptions` (index.ts:9-10), re-exports `LyraTabsOptions` (index.ts:130). Adding `LyraTabsChangeDetail` export lands here.
- `packages/alpine/README.md:60` — bindings table listing `list`, `tab`, `panel` and `active` state; needs row updates for ready/fallback bindings and new events.
- Blade adapter — `tools/blade-api/api.json:2226-2228`: `<lyra:tabs>` renders `x-data="lyraTabs(...)"` with `x-bind="list"`, `x-bind="tab"`, `x-bind="panel"`. It is the only non-Alpine consumer of the Tabs binding contract; new event names and bindings flow into its rendered HTML snapshot.
- No other source consumers: MDX tabs docs (en/pt-BR) contain only hand-written static HTML with `lyra-tabs`/`lyra-tab` classes, no x-bind; styles (`packages/styles/components/navigation/navigation.css`) and parity (`tools/parity/parity.mjs:500`, `baseline.json:544-545`) reference CSS classes only, not the binding; `apps/docs/.../plain-html.mdx:68` is class-name only. React package (`packages/react/src/tabs`) uses `lyra-tabs` classes only — CSS coupling, not binding.

(2) Owning generator, commands, tests, outputs:
- Generator: `tools/docgen/alpine.mjs` (`pnpm docgen:alpine`, package.json:23); React docs via `tools/docgen/generate.mjs` (`pnpm docgen`, package.json:22). Outputs affected: `tools/docgen/output/alpine-llms.txt:191-193` (`### lyraTabs` section, mount snippet) and `tools/docgen/output/alpine-props.json:734-738` (`binding: lyraTabs`, `optionsType: LyraTabsOptions`). A new exported `LyraTabsChangeDetail` and `ready`/`fallback` options change these two outputs; `apps/docs` `<StackApi stack="alpine" />` in both tabs.mdx tails renders from `alpine-props.json`, so docs pages update via regeneration with no MDX edit required.
- Type gate: `pnpm dist-scan:alpine-types` (`tools/dist-scan/alpine-types.mjs`, package.json:25) — named for scanning Alpine exported types; new public export likely must pass it (not opened; unknown exact assertion).
- Commands/tests: `pnpm --filter @lyra-ds/alpine run build` (tsdown), `run typecheck`, `run test:browser` — root `test:browsers` chain (package.json:12) and root `test` chain (package.json:14, includes alpine build). Regenerate docs after build: `pnpm docgen:alpine` (and `pnpm docgen` if React side shared). Blade docs: `pnpm docgen:blade` (package.json:24) regenerates `blade-llms.txt` and `tools/blade-api/api.json`.

## Files
- packages/alpine/src/index.ts:9,10,130,193-195
- packages/alpine/README.md:60
- tools/blade-api/api.json:2226-2228
- tools/docgen/output/alpine-llms.txt:191-193
- tools/docgen/output/alpine-props.json:734-738
- tools/docgen/output/blade-llms.txt:1018-1027
- package.json:12,14,22-25
- apps/docs/content/docs/en/components/tabs.mdx:53-98
- apps/docs/content/docs/pt-BR/components/tabs.mdx:53-98
- packages/styles/components/navigation/navigation.css:3,42-56 (class-only, no change expected)

## Evidence
- rg `lyraTabs|LyraTabs|lyra:tabs|lyra-tabs` over packages/apps/tools (excl. node_modules/dist/.batuta/.planning/docs-superpowers/generated/lock/git): hits only in files listed above; no app/evidence usage of x-data="lyraTabs" beyond browser test and api.json.
- rg `tabs` in tools/docgen + tools/dist-scan: Tabs entries exist only in alpine-props.json, alpine-llms.txt, blade-llms.txt, and React-side props.json/llms.txt (React irrelevant). No tabs entry found in tools/dist-scan source grep, so its exact tabs assertion unverified.
- package.json scripts: docgen:alpine, docgen:blade, dist-scan:alpine-types, test:browsers filters @lyra-ds/alpine test:browser; root test includes alpine build + browser matrix tests.

## Uncertain
- tools/blade-api/api.json: not confirmed whether committed snapshot or regenerated by `docgen:blade`; its provenance and which command refreshes it unverified (budget exhausted).
- tools/dist-scan/alpine-types.mjs internals unread; whether it enumerates exported types (so `LyraTabsChangeDetail` must be whitelisted) is unknown.
- Blade component source dir (owning `.blade.php` for `<lyra:tabs>`) not located within searched dirs; only its api.json/llms outputs seen. Whether Blade must expose the two new `lyra:tabs-*` events was out of scope.
- packages/alpine/package.json scripts not read; assumed standard build/typecheck/test:browser names from root script filters.
REPORT>>>

Controller corrections after reading actual owners:
- `tools/docgen/alpine.mjs` derives registrations and only each registration's Options interface (plus declared stores). `LyraTabsChangeDetail` is not an Options interface, and ready/fallback are outputs/bindings, not options. Adding these does not automatically change catalogs or StackApi. No generator expansion selected; document outputs/events manually in the later MDX slice. Run existing owner/check, never hand-edit outputs.
- `tools/dist-scan/alpine-types.mjs` scans all exported source Lyra interfaces against emitted declarations; it has no fixed Tabs whitelist to update.
- `tools/blade-api/api.json` is an imported external snapshot, not an output of docgen:blade. Do not edit/rebaseline it. Its actual Tabs HTML lacks required fallback/enhanced marker/section IDs and uses pre-enhanced roles/hidden panels. This is a real downstream compatibility dependency; the future corrected Alpine cannot be declared compatible with this existing Blade snapshot. Record migration/release qualification as pending, preserve snapshot bytes, resolve through the owning Blade source workflow later.
- README's third column is controllable state, so ready must not be placed there. Add fallback to named bindings, keep active, and explain ready is adapter-owned output separately. No other in-repo runtime consumer was found outside the source test and external Blade snapshot.
