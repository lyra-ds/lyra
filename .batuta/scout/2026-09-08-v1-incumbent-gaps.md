<<<REPORT
## Answer

Checkout verified: clean `9d214bf` on origin/main. Read-only audit; no files touched, no Docker/Colima/dependency actions.

**(1) Multi-candidate comparison still required by current text; checker assertions that bind a retention decision**

Paragraphs that still mandate the comparison as written:
- `2026-08-30-overlay-family-design.md` Decision summary :31-35 ("later evaluation MUST measure"; incumbent/Radix/Base UI/Zag "remain unevaluated alternatives") and **Foundation evaluation gate** :665-672 ("MUST run the incumbent Lyra implementation, Radix, Base UI, and the active Zag direction against the same accepted fixtures… MUST NOT preselect a winner"), plus ADR rejection conditions :690-695.
- `2026-08-30-lyra-v1-deliberate-release-design.md` §5.2 :110-115 ("compares the incumbent… It may retain the incumbent"), §10 step 3 :254, §12 :298-305 (evidence-harness plan + approved decision record precede any production migration plan).
- `baselines/lyra-v1/README.md` :55-70 and :128-131 ("must compare the incumbent, Radix, Base UI, and the active Zag direction before any candidate dependency or production overlay migration is authorized").
- `tools/overlay-foundation-evaluation/README.md` :27 (fixed candidate order), :159-161 (wave evidence is local diagnostic, non-authoritative — never a selection).

Checker assertions hard-coding the multi-candidate language (they conflict only if the spec is edited to drop or soften the gate):
- `tools/v1-release/check.mjs:129-134` + loop `:405-409` — spec must name each of `incumbent Lyra implementation`, `Radix`, `Base UI`, `active Zag direction` verbatim.
- `tools/v1-release/check.test.mjs:613-625` — test "requires every named overlay foundation candidate".
- `tools/v1-release/check.mjs:136-142` — the five approval confirmations must remain verbatim (`[x]`) in an approved spec, including "before any candidate evaluation, dependency change, evidence-harness plan, or runtime migration begins" (`:141`).
- Demotion guard: wholesale `not-authored` overlay demotion is rejected (`check.test.mjs:1108-1131`); overlay `notApplicable` is always rejected (`check.mjs:577-581`).

Retention itself conflicts with **no** machine assertion: the ADR template (`overlay-foundation-adr.md:2-4`) explicitly covers "retains"; its Candidates table disposition is "Continue or reject" (:36-39); lifecycle pair `approved:implementing` is valid (`check.mjs:171-178`). What still binds after recording retention: every `v1-interactive` cell with evidence for qualification (`check.mjs:427-494`), `planning` release rejects any `qualified` claim (`:612-617`), `releaseStatus` stays `planning` until the ledger schema hardens evidence binding (`:610`, design §12). So: smaller route = record retention via an ADR using the existing template (fill contract/AT/SSR/migration tables; bundle/CSS/runtime tables remain template-required structure checked only for headings by `tools/phase0/check-artifacts.mjs:6-27`), keep the spec text untouched, move ledger overlays to `approved:implementing`, and fix the P1 gaps against the incumbent. No weakening of the 23-cell exit gate is possible without failing the checker.

**(2) P1 gaps — nine overlays + Tabs + DataTable** (tags: [S]=direct source observation, [H]=historical spec allegation, [R]=unverified runtime)

Shared modal gaps (hit Dialog/Drawer/BottomSheet/CommandPalette/CreateWorkspaceDialog): no `inert` background isolation [S — no inert code anywhere in `packages/react/src`]; no layer registry, topmost-Escape rule, or nested restoration [S — each panel handles its own Escape]; initial focus is "first focusable" only, no declared-target/validation/destructive ordering [S]; restoration has no logical successor when opener unmounts [S].

1. **Dialog** — capture/initial-focus correct incl. WR-03 reopen [S dialog.tsx:120-127]; zero-tabbable fallback present [S use-focus-trap.ts:71-75]. Tests: `dialog.browser.test.tsx` (29, incl WR-02 :352, WR-03 :287), `dialog.ssr.test.ts`.
2. **Drawer** — backdrop click has NO mousedown-origin guard: drag panel→backdrop dismisses [S drawer.tsx:97-101 vs dialog.tsx:160-168; H :447 "diverge… dismissal"]; initial-focus effect keyed on mount only, so reopen-during-exit does not re-enter focus [S :75-81 vs dialog.tsx:120]. No `closeOnEsc`/`closeOnOverlayClick` props. Tests: `drawer.browser.test.tsx` (5 only).
3. **BottomSheet** — inherits shared modal gaps; known-gap row cites Alpine focus-trap baseline failure [H overlay-spec :87, :497; `03-interaction-accessibility.md:450-453`]; current `alpine/src/internal/focus-trap.ts` reads correct [S], actual failure mode and retained failing evidence location unknown [R]. No footer part. Tests: `bottom-sheet.browser.test.tsx` (11).
4. **Popover** — in-flow inside anchor `span`, no portal/layer record [S popover.tsx:118-143; H :88]; every open popover closes on one Escape, no topmost rule, unconditional `preventDefault` [S :82-88]; outside dismissal `mousedown`-only — no full pointer-sequence, cancellation, context-menu, or child-layer rules [S :77-95]; class-flip only, no shift/bounded-scroll fallback when neither side fits [S use-flip-placement.ts:67-69]. Tests: `popover.browser.test.tsx` (4).
5. **Dropdown** — menuitem buttons are plain tab stops; no roving `tabindex="0"` model [S dropdown.tsx:209-224]; no printable-typeahead with 500ms buffer [S absent]; item model has no disabled/submenu/check/radio (spec requires approved revision before claiming; docs must not claim them) [S :8-22; H :371-372]; uncontrolled open only [S :55]; `mousedown`-only outside close [S :90-100]. Tests: `dropdown.browser.test.tsx` (7).
6. **Tooltip** — no 500ms initial delay, no shared coordinator, no warm 0ms, no 300ms grace, no 100ms pointer grace — show/hide immediate [S tooltip.tsx:63-64,144-159 vs spec :376-396]; visible text lives in CSS `data-tip` while semantics live in hidden `role="tooltip"` span — split output [S :142,163-166; H :90]; no coarse-pointer policy [S]. Tests: `tooltip.browser.test.tsx` (6).
7. **CommandPalette** — reimplements Portal/focus-trap/presence/scroll-lock wiring instead of composing Dialog → parallel owners where OF-COMPOSED + spec :480-483 require one owner [S command-palette.tsx:16-19,222-223]; backdrop lacks pointer-origin guard [S :497-501]. Tests: `command-palette.browser.test.tsx` (13).
8. **WorkspaceSwitcher** — create action rendered as `role="option"` [S :223-238]; option buttons all tab stops, no roving model or typeahead [S :188-219]; in-flow popup, no portal [S :177-241]; `mousedown`-only outside close [S :97-104]. Alpine: trigger binds `aria-haspopup`/`aria-expanded` but no `aria-controls`; served `aria-selected` never updated; `lyra:change` then close, cancellation uninspected [S alpine/src/workspace-switcher.ts:204-217,230-240; H spec :92,:129]. Tests: `workspace-switcher.browser.test.tsx` (4).
9. **CreateWorkspaceDialog** — the approved OF-COMPOSED creation state machine is entirely absent: no `operationId`/`AbortController`, no `submitting/canceling/error/accepted` states, no `data-state`, no `aria-busy`, no result/stale-result handling; submit is synchronous and always closes [S create-workspace-dialog.tsx:55-60 vs spec :431-471]. Alpine unsupported (documented). Tests: `create-workspace-dialog.browser.test.tsx` (2).
10. **Tabs** — empty `role="tabpanel"` divs owned by the control; real content not owned [S tabs.tsx:95-104; H P1 finding `03-interaction-accessibility.md:444`]; IDs derived from collection index (`${tabsId}-tab-${index}`), not stable identity [S :46-47]; controlled-only, automatic activation only, no orientation/RTL mirroring, no disabled tabs [S :18-27,53-56]. Governing spec **not-authored** (`program.json:219`) — any contract change needs a new approved spec. Tests: `tabs.browser.test.tsx` (3).
11. **DataTable** — `onRowClick` on `<tr>` is pointer-only activation with no keyboard/focusable semantic row control; selection and activation conflated [S data-table.tsx:319-323; H P1 finding :446; design §5.4 :146-149]; loading/empty exist, no error state [S :286-309]. Governing spec is the implemented Data-and-Files one via the FileUpload-wave exception (`program.json:236-242`, `check.test.mjs:733-747`). Tests: `data-table.browser.test.tsx` (6).

Cross-cutting: "axe helpers filter known contrast pairs" [H :448] — both current helpers contain no filtering [S `react/src/internal/test-axe.ts`, `alpine/src/internal/test-axe.ts`]; whether outputs pass unfiltered in the pinned matrix is [R].

No component needs a rewrite: all eleven have real implementations plus browser+SSR regression files; gaps are bounded per-contract behaviors on top of existing shared internals.

**(3) Reuse vs unknowns, bounded first proof**

Reusable today: `packages/react/src/internal/*` (portal, presence, focus-trap, scroll-lock, controllable-state, flip-placement, tooltip-placement) and `packages/alpine/src/internal/*` with `internal.browser.test.tsx` as the regression net; the Dialog WR-02 pointer-origin pattern (dialog.tsx:137-168) is a copyable in-repo template; the ledger/checkers as-is; the evaluation harness's repository-owned `contracts/`, `fixtures/`, `candidates/incumbent.mjs`, and `evidence/` identity checks can later supply incumbent-retention evidence without running any external-candidate wave — none of it authorizes anything (harness README :159-161). Alpine registrations (8) and program.json currentContract paths stay authoritative.

Unknowns before first fix: actual Alpine BottomSheet focus-trap failure mode and where its retained failing evidence lives; nested-modal/inert behavior in real engines (no tests exist); axe status under the unfiltered helpers; whether the decision owner must still complete the spec-mandated four-candidate run before an ADR can be "accepted" (template permits retention, spec prose mandates the run — an operator ruling or spec revision is needed; not resolvable from this checkout); content of experimental branches/external evidence (out of scope).

Bounded first proof (recommended): **Drawer backdrop pointer-origin guard** — owner `packages/react/src/drawer/drawer.tsx:97-101` (add the `downOnOverlay` mousedown-origin ref exactly as dialog.tsx:137-168), regression case in `drawer.browser.test.tsx` mirroring `dialog.browser.test.tsx:352`, plus the reopen-during-exit focus re-entry fix at drawer.tsx:75-81 (key the effect on `open` like Dialog). Covered by already-approved text (shared rule 7, spec :170-175; known-gap row :86); no spec change, no dependency, no ledger-status claim, diff of ~20 lines + 2 tests. Then, in ascending size: CommandPalette backdrop guard; Dropdown roving tabindex; Tooltip coordinator delays; CreateWorkspaceDialog state machine (largest, still single-owner-file).
REPORT>>>
