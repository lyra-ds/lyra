# React initial focus public docs — one medium prose retry
Read this feedback then .batuta/v1-react-modal-initial-focus-docs-brief.md in full. Same ten-file ceiling, narrowed here to eight MDX React sections; both delivered liveexamples must remain byte-identical. No changes to Alpine/Blade tails, runtime, styles, package managers/config/services/git writes. No delegation. Controller owns all checks; only original pinned scoped Prettier is allowed.

## Current proof
Medium initial delivery341.53s exit0. Exact ten-path scope; all eight Alpine/Blade tails unchanged. MDX8, examplelint, format and stack303 pass. Actual DialogBasic Cancel, DrawerWithoutFooter h2 Activity details, and unchanged CommandPaletteTrigger search native9/9 across threeengines, accepted Escape returns to exact trigger. The named h2 is correct; do not change it to match the controller's discarded h3 assumption. Initial controller standalone snippet typecheck omitted the existing import-elided Dialog/Drawer context; adding only their established published imports produces PASS for all8exact bodies and both actual examples. These were controller fixture errors, not product type failures, and are already resolved.

## Required prose corrections
1. Six Dialog/Drawer/BottomSheet MDX pages split the existing React return-focus explanation: a new `## React initial focus` heading appears before the still-return-specific paragraph `After an accepted close...`. Current English anchors Dialog105/126, Drawer96/116, BottomSheet97/116, mirrored in pt-BR. Under that new heading, `the resolver` misleadingly appears to describe initialFocusTo as resolving afterclose/capturedopener fallback. Move the complete new initial-focus section before the existing `## React return focus` heading (or an equivalent complete section boundary), preserving the original return-specific text and snippet together. This is a section-ownership defect, not a runtime failure.
2. CommandPalette en94/pt-BRmirror recommends Cancel, readingheading and arbitraryformfield despite this component offering fixed search/commands instead of a general content slot. Explain the supported normal default (search) and a current eligible node already present, such as the named palette panel. Do not invent extra components/slots/examples or narrow the runtime resolver contract. The existing Accessibility bullet en125 `DOM focus stays in the input` must be qualified for normal search interaction, because declared panel focus is now supported. Preserve actual input/virtual-option keyboard semantics.
3. Remove the new boilerplate `it does not swallow errors or promise a warning` in all eight pages. The preceding affirmative description of exact error propagation/context already carries the contract. In Dialog bothlocales replace `does not fabricate deletion state` with plain user-facing copy, e.g. both footer actions close the demo dialog. Keep the honest demo boundary and existing action behavior. No general rewrite or unrelated translation pass.

## Verification on return
Controller reruns exact MDX/snippettypes/format/stack for the edited pages; liveexamples/native9 may be reused only after their actual file hashes are proven unchanged. Independent read-only review then checks all three original criteria. Preserve all current source bytes outside the described new prose/heading placement. No new tests or release action. Report exact files and unrun controller checks honestly.
