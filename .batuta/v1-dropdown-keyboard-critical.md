# Critical continuation — Dropdown native Tab diagnosis
The maintainer explicitly authorized continuing until completion. Medium initial/retry and high initial/retry did not satisfy the bounded task. Continue with controller-owned diagnosis and independent review, preserving all failed scoped snapshots; do not destroy the shared stabilization worktree or unrelated completed work. No blanket reset. The original three acceptance criteria and four-file product scope remain authoritative.

High retry: Chromium/SSR18/20; WebKit16/19. Two malformed typeahead scenarios retain incompatible prefix sequences; WebKit forward Tab still reaches body in both source and compiled proof. Native17/18, static checks pass, size2.04kB/2kB (+45B). The earlier queueMicrotask cause was a hypothesis: synchronous root ownership did NOT resolve Tab, so it is not the established root cause. Isolate React menu removal versus native focus navigation before editing the owner. Do not add forced Tab focus, timers, framework, dependencies, event fabrication or budget changes. Repair fixture sequences without weakening any assertion; prove original-source regression RED and full fixed suites/native/size before independent review.

## Established Tab cause
The isolated current-React/pinned-browser matrix reproduced WebKit BODY even with no menu closure, and for capture/target/root/microtask/blur closure. The After button had no explicit tabIndex; this platform excludes that implicit button from sequential keyboard navigation. Changing ONLY the fixture After button to tabIndex0 makes all12 two-engine/mode combinations reach After, with no runtime focus override. The existing native modal Tab critical trail already recorded this platform precondition. Reviewing retained initial native outputs confirms WebKit Tab also failed then; previous feedback incorrectly said that path had passed before event-phase checks. Those prior claims are superseded by the actual raw failures. Fix both source-test and standalone fixture preconditions. Product Tab closure remains synchronous and unchanged.

Typeahead fixture causes: after a,p the retained prefix is ap, so a asks for apa; after the fresh-reopen r proof, Unicode å asks for rå. Start each independent search cycle by real Escape/reopen and requery live elements. Preserve exact expected targets and accent handling. Explicitly prove icon exclusion by searching after Apply and wrapping past the icon-bearing Edit command. Strengthen unmount proof to the actual500ms timer identity.

For the measured45B overage, remove redundant command focus querying/clamping/state writes: both callers already own the exact commands array and valid matching/navigation indices, and existing onFocus owns roving state. Verify all focus/collection regressions after this bounded simplification; no budgets change.

## Post-review consumer update regression
Independent GLM review returned3/3 DONE with two low observations and unchanged guard. Controller independently reproduced a blocking identity regression against the exact reviewed public build: click Beta while root onClick flushSync-reverses items selects Alpha and closes. Root onClick currently calls the consumer BEFORE deriving the selected item from the now-reordered DOM, while reading the previous render's items array. Capture the clicked logical command before notifying the consumer, then honor cancellation before invoking it. This is a correction to the newly centralized event owner, not an API expansion. Add a real React flushSync reorder regression and run it RED before changing source; retain exact native proof and re-review the final diff.

Review observation1: accept portable source tests for positive Enter/Space activation; decline its claim that all existing proof would miss Space, since compiled native space-activation already covers it. Observation2: accept one concise fixture comment for explicit After tabIndex. Both are in the existing task scope. Prior review is not final approval due the reproduced identity bug.
