# Alpine Tabs panel-container teardown — 2026-09-10
Observed actual current MDX snippets with compiled87cd246 runtime:84/90native scenarios PASS,6/6destroy-panel FAIL across both locales and Chromium/WebKit/Firefox, zero JavaScript errors. Existing runtime native63 cases covered descendants (input/button), not the role=tabpanel section itself. New failing scenario focuses the actual enhanced section then destroys Alpine; resetNativeContent removes tabindex, native browser focus leaves the section.

Selected bounded correction: a focused owned enhanced panel container is an enhancement-owned focus destination just like its trigger. On destroy, move that lost destination to the panel's matching eligible native fallback link, else first eligible fallback link. Preserve native descendant/outside focus; never target nested ownership or detached roots. Restore ordinary section markup without residual tabindex/role. No generic manager, observer, callback, new API or arbitrary focus restoration.

This refines the old broad "live panel focus" wording: native descendants retain focus; a section container losing enhancement-only focusability returns to its own fallback link. Current docs/changeset must state this precisely. This is a newly discovered bounded repair, not a redispatch of the completed runtime retry. Invalid-active transition focus and broader Task27/size/Blade/final qualification are not implied by this destroy-only proof.
