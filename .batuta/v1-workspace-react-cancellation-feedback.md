# Task21 feedback — one medium retry
Read original .batuta/v1-workspace-react-cancellation-brief.md. Product scope is valid and controller scoped Chromium/SSR11, WebKit10, Firefox10, types/build/docgen pass. Repair only the new test fixture; retain runtime source unchanged unless a concrete discovered defect demands a report.
1. workspace-switcher.browser.test.tsx:265-281: new selected.focus()/create.focus() calls violate the brief's expected-option.focus boundary. Establish the selected option through existing actual opening and assert exact initial focus, then retain it through canceled keys. Reach Create through an actual noncanceled supported navigation key, assert exact focus before testing canceled activation. Adjust callback accounting for any legitimate setup keys; do not weaken before/default/callback/focus assertions. Trigger.focus() precondition is allowed. No new test-only production branch.
2. Controller eslint fails browser.test.tsx:305:7 jsx-a11y/no-static-element-interactions for the new ancestor div onKeyDown. Use an appropriate semantic fixture container without adding a suppression/ignore/config or assigning false interactive semantics to a container. Preserve real ancestor propagation assertion.
3. Controller Prettier --check fails only browser.test.tsx. Match the existing formatting. Controller owns rerunning formatter/checks; worker must not run validation/package managers/configs/hooks or git writes.
No source skips/only, sleeps/permissive assertions. Scope unchanged. Report actual changed lines and pending proof. Do not modify WORK/.batuta or other files.
