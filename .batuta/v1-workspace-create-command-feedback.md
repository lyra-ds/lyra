# Task22 — one high retry
Read original .batuta/v1-workspace-create-command-brief.md and exact .batuta/specs/2026-09-09-workspace-command-design.md. Scope remains the same4files; original boundaries/test laws apply. No configs/ignores/hooks/dependencies/git writes/validation; scoped pinned Prettier edit remains authorized.
Controller scope/static/build/docgen checks pass, but functional proof fails. Original implementation is preserved under runs/v1-workspace-create-command/initial-backup. Repair the actual causes, no expected-destination.focus/sleeps/platform branches/permissive assertions.
1. Native command Tab stop is missing: Create source has no explicit tabIndex attribute (its JS tabIndex property nevertheless reads0, so that test does not prove keyboard eligibility). Native9matrix is8/9: WebKit Tab skips Create. Native extended51/57: WebKit fails Tab roundtrip, backward trigger exit, no-create backward, Enter/Space creation reachability, and empty-none pointer opening (focus BODY). Trigger is also only implicit. The design requires native stops/destinations in all engines; preserve actual native Tab, no forced external-focus/Tab preventDefault/blur-close hack.
2. Newly added source tests fail all engines at browser.test.tsx ~210: options[1].focus followed by immediate state-derived tabindex assertion is unflushed React work and violates direct expected-option-focus boundary. Use actual opening/navigation with exact initial/result focus and roving array assertions. The reorder fixture likewise directly calls originalOptions[1].focus; use real keyboard preparation before the consumer rerender, retaining exact identity/roving assertions. Do not weaken results or add sleeps.
3. Task21 cancellation regression now uses setup End then Tab, but its exact expected event-key list omits the legitimate added Tab. Chromium/Firefox catch13actual vs12expected. Keep exact once-per-key/order accounting including setup; preserve cancellation and callbacks assertions.
4. Source SSR's new indexOf(listbox)<indexOf(create) also passes the old invalid nested-create structure. Replace that added check with a meaningful SSR semantic/structure regression (e.g. exact workspace option count and command beyond listbox closing boundary) without new dependencies/browser globals. Existing no-browser SSR proof remains.
5. Empty-create backward native probe3/3fails: Shift+Tab leaves the only command but popup stays open. The design now explicitly spells out the natural no-workspace fallback: native backward exit to trigger closes. The original option/command+workspace backward behavior stays as designed. Original current artifact failure is runs/v1-workspace-command-native/initial-empty-backward-result.json; the first57case extended result remains initial-extended-result.json. A final60case native matrix will include this fallback.
Scoped initial tests: Chromium/SSR14/16, WebKit10/15, Firefox13/15. types/eslint/format/build/docgen pass. Current WorkspaceSwitcher8321B against8250B; overall11React entries fail size. Report actual bounded size, do not change budgets/hashes or broaden optimization. Controller runs final proofs and independent review.
Report exact changes and uncertainty; no claimed PASS for unrun checks. Preserve managed WORK/.batuta except the controller-written design/feedback already present; do not edit them.
