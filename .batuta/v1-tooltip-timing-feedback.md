# Tooltip high retry — specific controller failures
Read the original v1-tooltip-timing-brief.md and exact design again; all scope/boundaries persist. This is the ONE high retry. Fix directly; no further delegation/commits/package managers/services/hooks/config/ignores.

Initial delivery is rejected for scope and concrete behavior/test failures. Controller independently ran scoped suites: Chromium/SSR8pass/3fail, WebKit7pass/3fail, Firefox7pass/3fail. Types/lint/format/stylelint/parity/build/docgen pass. Built native replay33/33 basic clock/ownership/grace/isolation/cleanup cases pass, but two additional real regressions fail in ALL3 engines (39total/33pass). Raw evidence available in .batuta/runs/v1-tooltip-timing-native/initial-extended-result.json, runner and fixture; source check logs in main are not accessible/needed because exact evidence follows.

1. OUT OF SCOPE: after your first completion, an Impeccable Stop hook prompted triage and you created .impeccable/config.json suppressing layout-transition on feedback.css. This explicitly violates the brief, even though the Progress finding is pre-existing. Controller preserved and removed ONLY your newly created config/cache. NEVER recreate any settings/ignores, even if a hook asks. Do not run Impeccable, change existing Progress/CSS handoff or edit tooling to unblock completion. Report the pre-existing out-of-scope finding honestly if the hook blocks; it is not authority to change unrelated behavior/configuration. No permission to triage it by suppression.

2. THREE NEW TESTS FAIL in every engine: browser.test.tsx:34 (at500ms closed vsopen), :66 (warm same-turn closed vsopen), :118 (second focus closed vsopen). Existing8/7 tests pass. Native installed-browser clock independently proves intended basic states, so investigate React commit flushing in tests: dispatchEvent + Promise.resolve does not guarantee a React render commit. Use an appropriate existing React act boundary around synchronous events/timer advances, keeping exact clock boundaries and immediate/no-extra-timer assertions. Do not replace them with sleeps, broaden expectations, change runtime clock to satisfy a fixture, skip tests, or claim fake promises are a flush. The warm test name says document isolation but renders only one document: rename precisely or actually add meaningful second-document coverage.

3. FOCUSED ESCAPE CONTAINMENT REGRESSION. Native fixture main handler does if(e.key==='Escape'&&!e.defaultPrevented) parentEscapes++. FocusA opens; real Escape closes Tooltip BUT parentEscapes===1 in Chromium/WebKit/Firefox. Current root calls coordinator.dismissTop(event.nativeEvent), which only native-prevents; the React SyntheticEvent's defaultPrevented remains false for React ancestors. Preserve consumer-first root/child behavior and mark the React event handled ONLY when an actual Tooltip dismissal happened, so parent code cannot also treat it as uncancelled Escape. Do not swallow unrelated keys/Escape when no Tooltip is visible. Add the exact real React ancestor regression.

4. REPLACEMENT FOCUS REGRESSION. FocusA opens; parent removes the actual button while keeping Tooltip mounted; then renders a fresh button in the same place; focus the NEW button. Tooltip remains closed in all3engines because targetRef still points to the disconnected old element. Refresh the current target at a genuinely fresh input cycle while preserving pending stale-open cancellation; do not use ReactElement identity or remount the entire coordinator on ordinary renders. Add a meaningful removal/replacement/fresh-focus regression. Existing pending-remove and tip-replacement native proofs already pass and must remain.

5. Lifetime/readability review: attachRoot currently writes rootNode state through a fresh ref callback every render. Stabilize it if needed to avoid needless detach/attach/ref-callback churn; keep forwarded-ref semantics. Remove only demonstrably unused newly introduced private contract pieces if doing so improves this code; no speculative optimization. Current Tooltip size1852B vsbaseline885B exceeds the unchanged1500B limit by352B. Report it honestly; DO NOT raise budgets, change baseline hashes or strip required behavior. Overall measured budget disposition remains controller Task10 after behavior is complete.

Preserve native bubble hoverability and public pseudo-element/description/placement/API. Do not claim whole Tooltip/P1/packed qualification. Static commands alone are not browser evidence. End with exact changed paths, tests actually run, and any standing limitation; do not alter configuration in response to a completion hook.
