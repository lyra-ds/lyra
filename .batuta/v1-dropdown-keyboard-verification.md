# Dropdown keyboard verification — approved
Base c5cfaae. Source SHA256 f1b206e8478623fd631348f04d5c2599cfe5da89e9f35fac52b42735b4310742; compiled public dropdown.js SHA256 6a86d6f4323b65202024179126294b5181a4d88cf64e17b3376eaa902e5ca17f. Node24.18.0, pnpm11.13.1, Vitest4.1.10, Playwright1.62.1; local macOS Chromium151.0.7922.34, WebKit26.5 and Firefox153.0. No publication or P1 ledger qualification.

## Criterion 1 — PASS
One command owns tabindex0; direct focus and keyboard navigation update it. Mixed-item SSR renders one0/one-1 deterministically; empty/replaced collections retain one valid owner. Final scoped Chromium/SSR23/23, WebKit22/22 and Firefox22/22 PASS. Built public-entry replay27/27 across three engines proves wrapping/Home/End/trigger first-last, exact Tab→After and ShiftTab→trigger, zero imperative trigger.focus calls on Tab, and Escape return. The external After fixture is explicitly tabIndex0 to define a keyboard stop consistently across platforms.

## Criterion 2 — PASS
Browser tests prove a→Archive/p→Apply, repeated a cycling, wrapping past a distinct decorative icon prefix, no-match prefix retention, fresh real close/reopen cycles,500ms expiry, ignored Ctrl/Alt/Meta/composition and teardown of the actual500ms timer identity. Unicode å→Ångström uses a standard synthetic KeyboardEvent on a live re-queried menu, with real DOM focus asserted; it is not claimed as trusted Unicode input. Isolated pinned Playwright probe proved keyboard.type(å) emits no keydown and press(å) rejects the key on Chromium/WebKit. Native ASCII replay includes uppercase and multi-character input.

Original-source mutation against c5cfaae, retaining final meaningful tests, went RED:7 focused browser failures cover missing roving, collection ownership, typeahead/expiry/timer and cancellation.1 selected test passes;12 unmatched tests are excluded by the explicit test-name filter, not skipped in source. Original/fixed SHA and exact restoration recorded. No product test skips, permissive assertion replacements or fake successful focus were introduced.

## Criterion 3 — PASS
Consumer root bubbling callbacks run once before corresponding Lyra defaults, retaining real currentTarget/eventPhase for root click and menu key events. Separate capture callbacks and child/native/Slot preventDefault remain observable; canceled navigation/Tab/Escape/Enter/pointer/keyboard-generated click preserves menu and selection. Compiled27/27 and source cancellation tests PASS. Trigger key callback retains its existing trigger-owned passthrough convention; no API/variant/manifest/dependency/style change.

Full React Chromium/SSR776/776 across153files and full Firefox679/679 passed before the final clicked-command identity correction described below; final affected three-engine suites pass after that correction. Build, types, scoped ESLint, Prettier, docgen--check and diff hygiene PASS. Dropdown size-limit reports2kB (PASS) against its unchanged2kB limit. Whole React size-limit still exits1 for the SAME9 independent focus-source overages documented in v1-focus-size-diagnosis.md; this task does not waive or change those budgets. Public exports and generated API unchanged; four-file product scope only, with a React patch changeset.

## Diagnosis and routing history
Medium/Codex gpt-5.6-terra initial+retry failed: manual forwarding changed currentTarget, then capture substitution changed eventPhase and overwrote capture handlers; malformed timing/Unicode fixtures and excess size also blocked approval. High/same model with explicit high reasoning initial+retry retained malformed prefix/fixture sequences and an over-budget implementation. Critical/controller continuation under the maintainer's explicit until-completion instruction isolated the causes and finished within the same scope; all prior owned snapshots retained. No blanket reset or destruction of the shared worktree.

The earlier claim that WebKit Tab had passed before callback assertions was WRONG. Retained initial native outputs show the same exact-target failure. An isolated12-case matrix proved WebKit skipped the implicit After button even when the menu never closed; explicit fixture tabIndex0 makes all12 modes/engines reach After. This is a fixture precondition, not a runtime scheduling defect. No Tab delay or forced focus was added; root defaults remain synchronous. Both earlier queueMicrotask attribution and old incomplete verification prose are superseded by this measured diagnosis. Detailed proof: v1-dropdown-keyboard-critical.md and tab-order-probe/*.json.

Critical controller repaired stale/different-prefix test sequences, live Unicode targets and exact timer teardown; replaced an unsupported test findLastIndex with equivalent findIndex under existing TS target. Bounded source simplification removed redundant command-array queries/clamps/roving writes and unreachable negative pending-focus handling, and stores the case-normalized prefix once. Current onFocus remains the roving owner. Intermediate size2.04→2.01→1.99kB; final selection identity repair still passes at displayed2kB, with all scoped proofs rerun. No budget increase or code golf.

The first broad Vitest invocation omitted project selection and stopped at the missing Firefox executable; it is not a suite failure attributed to Dropdown. Controller installed only the already-pinned Playwright Firefox1538 browser cache, then verified scoped Firefox19/native9. This is verification tooling, no package/lock/Colima configuration change. Required exact packed Linux/React18-19 release evidence remains pending; these macOS source/built-entry checks do not substitute for it.

## Raw controller evidence
Main .batuta/runs/v1-dropdown-keyboard-candidate-checks (final full Chromium/SSR and static/native/size); v1-dropdown-keyboard-firefox-checks; v1-dropdown-keyboard-original-source (RED and restoration); v1-firefox-qualification (pinned browser install).
Worktree .batuta/runs/v1-dropdown-keyboard/native/result.json and firefox-result.json bind built SHA and27scenarios, with owned server/browser cleanup. tab-order-probe/ holds isolated implicit/explicit button matrices. high/medium snapshots and verification-history.md retain failed attempts. Independent read-only review is next; source and managed state will remain fixed during review.

## Final selection identity correction and review disposition
First independent GLM review returned TASK1/2/3 DONE with unchanged status/diff/file guards and two low observations. Its findings are preserved verbatim in .batuta/runs/2026-09-09-dropdown-keyboard.review.md. Accepted portable positive Enter/Space source assertions; declined only the claim that all earlier proofs missed Space, because native space-activation already covered it. Accepted the concise explicit After-tabIndex fixture comment. Both edits are verified in final23/22/22 scoped suites.

Controller independently reproduced an additional blocking bug before approving that review: built click Beta + consumer root onClick flushSync-reorder selected Alpha. The new real React source regression also went RED. Root selection used the reordered live DOM index against the prior render's items. The final handler captures the clicked logical command before calling the consumer, then still checks preventDefault before selection/close. Source regression GREEN, all native27 GREEN, and identical built probe now yields selected=[beta], open=false, asserted by controller. Main .batuta/runs/dropdown-reorder-probe preserves red-result.json, source-red.log and final result.json. No action was approved from the earlier review alone. Final independent review follows.

Final exact-byte static/native/scoped proof: main .batuta/runs/v1-dropdown-keyboard-selection-checks and v1-dropdown-keyboard-selection-extra. Current final full Chromium/SSR rerun may be recorded afterward; do not infer its count from the earlier776-test baseline.

Broader fresh WebKit source baseline:673/679, six failures across DatePicker, DateRangePicker, TimePicker, Tooltip and two FileUpload cases; all Dropdown cases pass. These separate failures are not attributed to Dropdown or waived, and need bounded diagnosis next. Full Firefox source baseline679/679 passed. Neither source baseline is packed Linux release qualification.

## Final approval
Exact final source completed the full Chromium/SSR suite:779/779 across153files, main .batuta/runs/v1-dropdown-keyboard-final-full. Final GLM review returned TASK1/2/3 DONE, findings none, unchanged HEAD/status/diff/scoped-file hashes. Core verifier criteria3 PASS. All four scoped product files are approved; source did not change after their final proofs. Final review findings preserved verbatim in .batuta/runs/2026-09-09-dropdown-keyboard-final.review.md. Whole React size-limit and broader WebKit/P1 release blockers remain explicitly pending.
