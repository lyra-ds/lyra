# Task 2 feedback — complete the real reopen proof
Controller Chromium+SSR70/70 passes. WebKit fails only `captures a fresh opener and resolves once per accepted close during a rapid reopen` (webkit.log): it uses unprepared mouse with resolver returning null, which is intentionally not qualified in WebKit by the approved contract. This test must exercise valid captured-opener/keyboard semantics, while preserving the separate real unprepared mouse explicit-target tests.
The current rapid test can also wait for the old overlay to disappear before Playwright clicks the covered second trigger. It does not assert panel identity; thus Chromium green may only prove remount, missing the actual Task 2 regression. Make the accepted close/reopen occur deterministically within the same exiting panel's presence; prove the panel DOM identity survives, focus enters it on reopen, a fresh second opener is captured, and resolver is called exactly twice for two accepted closes. Do not use a sleep, relax the contract, or add any production branch. Test fixtures can expose controlled transitions for this lifecycle proof. Use real DOM and valid focused keyboard opener semantics. The old Drawer mount-only capture must fail this new proof.
Same three-file scope and full original approved contract. The component integration appears correct; do not modify the shared owner or unrelated tests. Conductor will format and run all static/browser checks and review. No checks, pnpm, nested delegation, cleanup, commit or another approval in your session. Implement directly and end with honest unverified report.
