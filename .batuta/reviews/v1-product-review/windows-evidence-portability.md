# FileUpload evidence test portability — 2026-09-17

Windows run35158953780 exposed16 failures across four existing FileUpload evidence test files. The bounded repair changes fixtures and process invocation only: normalize workflow CRLF before semantic mutations; compare native lock/audit path components; use literal root containment instead of a path-built regex; retain hostile Markdown metadata while giving the materialized file a Windows-valid name; execute the real Wrangler JS entry, native/JS pnpm entry and Git-for-Windows POSIX shell without dropping assertions. The100ms lock bound and production ingestion/publication algorithm remain unchanged.

The earlier broad baseline LF rule also forced binary files through Git's text filter. The policy now selects JSON/Markdown only. Controller compared all48canonical members to Git blobs and performed a real core.autocrlf=true checkout: every byte matches, including PNG/WebM/ZIP. No binary needed rewriting; apparent binary diffs came from clean-filter interpretation. No immutable artifact, workflow, dependency, budget or library runtime changes.

Validation: complete pnpm test PASS, including349FileUpload evidence tests; actualCRLF workflow with original test produces5failures/75passes, corrected test80/80PASS, source restored exactly. Existing negative policy, lock takeover, release-audit and hostile rendering checks remain enforced. Scoped formatting and diff checks PASS. NativeWindows qualification awaits the follow-upCI, not inferred from macOS.

Codex/Terra medium plus one corrective retry; first attempt's direct.cmd launch rejected before integration. Fable5.1 through Cursor approved the bounded plan after read-only inspection; initial silent invocation was terminated and the documented readonly adapter retry completed. GLM independent final review found no concrete defect and marked three criteria DONE; tracked status unchanged. The earlier scout's copy-fallback and blind-timeout suggestions were rejected. Raw MAIN .batuta/runs/pr223-followup retains all logs, receipts, negative outputs and48-member hashes.
