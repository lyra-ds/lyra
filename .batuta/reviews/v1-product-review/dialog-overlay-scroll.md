# Dialog overlay scrolling and popup visibility — 2026-09-17

This supersedes the layout choice in [the initial Dialog repair](dialog-content-scroll.md). Greptile identified a real composition regression before PR230 merged: making every Dialog body a scroll box clipped its absolute Dropdown. Controller reproduced it on Styles45d9: the sixth command lay at y566–599 while the body ended at414; native hit testing failed. A locator click still succeeded because it automatically scrolled the body, so click success alone was insufficient evidence. The original archive, screenshot and reports are preserved.

Product revision `50325fc0d1b5b9733a206822e5a586518cd7dbad` moves native vertical scrolling to the existing overlay and uses safe centering. Short dialogs retain their natural placement; tall dialogs start at the reachable padded edge and scroll as a whole. The body and panel no longer form clipping boundaries. Header/footer are reachable by scrolling or keyboard; they are not claimed to remain fixed. This is two shared CSS declarations, existing test-file changes and the Styles patch changeset, without React/Alpine markup, API, dependency or parity-policy changes.

## Controller evidence

- All325 Styles cases pass across the existing Chromium/Firefox/WebKit projects, including9focused Dialog cases. Restoring the previous clipping CSS yields2failures/7passes; exact corrected bytes were restored. Stylelint, parity, formatting and diff checks pass.
- Fresh Styles archive `f88b5ada8c154d45e824e243984a4c9bb880c465b57744ca507d9d253dec42bd` binds23extracted files. React `ae3cf0039dca3eea026de16b148a1e07572bcd15d66be90b1214cf2b7b987b54` and Alpine `09c6351db2f7ad35dfe44e68c91e782d4f71b3e84aa0c00fa0d02a0ed8f1c0b3` remain unchanged. Production proof rejects workspace source imports.
- The same public short Dialog/Dropdown now passes native hit testing at the unchanged menu geometry, then selects command6. The new permanent public Styles-markup assertion directly protects that hit test.
- Four public native coarse-touch scroll cases pass: Dialog overlay221px, Drawer221px, BottomSheet221px and DataTable220px. Trusted touchmove, preserved page offset and close/selection outcomes remain required. The Dialog probe targets the actual new scroll owner; the predecessor probe and evidence remain intact.
- Actual Chromium page zoom100/200/400% passes native wheel scrolling, native Tab to body/footer actions, complete focused-action visibility and closing without moving the background page. Browser zoom is verified; CSS zoom remains1. The controller inspected the400% compositor image with the full3px focus ring. The isolated browser profile is removed.

Codex/gpt-5.6-terra medium implemented the bounded review correction without retries. Independent OpenCode/GLM review returns3DONE and no blocker. Its conditional safe-centering support concern is resolved by the completed three-engine suite. Its hypothetical bottom-padding cosmetic concern is not a demonstrated defect; current footer reachability passes. The menu fixture is deliberately bounded to its viewport and does not qualify arbitrary anchored placement.

The full native bundle budget gate passes at50325fc:72standalone entries,5scenarios and4CSS entries, with no limit change. Its Styles archive hash matches the fresh packed proof. Updated PR integration remains pending. The earlier CI belongs to e7884584 and cannot qualify this revision. These proofs do not close general anchored scrolling, complete composed-media mapping or exact versioned1.0candidate acceptance. The merge monitor was stopped upon the P1 finding; publication/Version Packages remain separate.
