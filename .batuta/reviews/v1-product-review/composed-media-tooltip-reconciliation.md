# Composed/media and Tooltip requirement reconciliation — 2026-09-18

Read-only controller adjudication at merged main `650b326a20b0087652ca084a3a3cf5fd4f06794b` (PR #231). Its tree is identical to the CI-qualified head `2a14cb7d`. Packed identities for this tree come from the PR #231 official budget gate: React `9d07d9ddbfbefc4841de0da490c085d51cf4a333f8ecdd3df88a59c3148687cc2`, Alpine `324c2adc2bc25c77ad34d857baa3aef195017d01f31e8018e5b4ed50c15998a8`, Styles `f88b5ada8c154d45e824e243984a4c9bb880c465b57744ca507d9d253dec42bd`. The review follow-up commit changed one Alpine test file only. No product, validator, program ledger, baseline pointer or release status changes. This record supersedes the PR230 status paragraph of MAIN `.batuta/runs/pr223-followup/remaining-acceptance-adjudication.md`; it does not create a task queue or a test per ledger cell.

## Identity method

PR #223 was squash-merged as `41a82e3`. The evidence revisions cited by earlier reports (`e9b6cd6`, `d6cf5cc`, `7b7fd96`, `0fcea80`, `9207dac`, `3ea446a`, `5ac8922`, `50325fc`) exist locally but are not ancestors of main. Reuse is therefore decided by owning-file bytes (`git diff <revision> HEAD -- <owner>`) and by archive SHA-256, never by branch ancestry. A report whose owners are byte-identical at HEAD remains applicable at owner level; its archive-level binding still needs the fresh candidate archives, because sibling owners changed the package bytes.

## Owner identity review

| Owner group | Files (SHA-256 prefix at HEAD) | Last product change | Reports whose base predates that change |
| --- | --- | --- | --- |
| Tooltip | `packages/react/src/tooltip/tooltip.tsx` (9a53576a), `internal/tooltip-coordinator.ts`, `internal/use-tooltip-placement.ts`, `packages/alpine/src/tooltip.ts` (9c61c282), `alpine/src/internal/tooltip-placement.ts`; `.lyra-tooltip` rules in `feedback.css` | `41a82e3` (PR #223). No tooltip file or `.lyra-tooltip` rule changed since `e9b6cd6`. | None. `tooltip-native-tap.md` (React 658d9fa/Alpine de29688/Styles 74fd16f) reuses at owner level. |
| Popover / Dropdown / WorkspaceSwitcher placement | `react/src/popover/popover.tsx` (601b496c), `react/src/dropdown/dropdown.tsx` (df9a2247), `react/src/workspace-switcher/workspace-switcher.tsx` (035ee1f5), `react/src/internal/use-flip-placement.ts` (a45dd581), `alpine/src/popover.ts` (62a434fd), `alpine/src/dropdown.ts` (92ca0797), `alpine/src/workspace-switcher.ts` (34d62bbf), `alpine/src/internal/flip-placement.ts` (9feba85b) | `650b326` (PR #231) | `dropdown-native-tap.md` (d6cf5cc), `popover-native-tab.md` (513418b), `popover-rtl-alignment.md` (72 packed cases), `dropdown-rtl-and-current-screening.md` (24 packed cases), `current-media-and-zoom.md` (7b7fd96). Their packed observations are historical for these owners. |
| CommandPalette | `react/src/command-palette/command-palette.tsx` (a00fc522), `alpine/src/command-palette.ts` (fd69c3a8) | `41a82e3` (PR #223); Alpine last `77d1626` | None at owner level. Screening at `7b7fd96` used the same React/Alpine bytes; only shared CSS moved (see below). |
| CreateWorkspaceDialog | `react/src/create-workspace-dialog/create-workspace-dialog.tsx` (a89874df) | `577e408` (PR #229) | `current-media-and-zoom.md` screening predates the Promise repair. `workspace-native-promise.md` (React ae3cf003) covers the repaired owner; React bytes since then changed only in the anchored owners above and the size-limit manifest. |
| Shared Styles used by composed/anchored | `feedback.css` (3ee569d9), `navigation.css` (49436ea5), `primitives.css` (dd7a8012), `buttons.css`, `forms.css`, `data.css` | PR #228 (coarse/hybrid targets), PR #230 (Dialog overlay scrolling) | All packed media reports on Styles `e45ff57`, `74fd16f`, `6185b27`, `3888d2d`, `45d9ef7`. Class rules changed since `e9b6cd6`: `.lyra-btn`, `.lyra-input`, `.lyra-menu__item`, `.lyra-cmdk`, `.lyra-cmdk__search`, `.lyra-cmdk__item`, `.lyra-cmdk__item--active`, `.lyra-table__sortbtn`, `.lyra-table__check`, `.lyra-checkbox`, `.lyra-drawer__close`, `.lyra-dialog-overlay`, `.lyra-popover`/`--align-*`, `.lyra-menu`/`--start`/`--end`, `.lyra-tab`, `.lyra-tabs--pills`, `.lyra-wscreate__slug-input`. No `.lyra-tooltip` change. |
| FileUpload | `react/src/file-upload/*`, `files.css` unchanged since `3ea446a`; shared `buttons.css`/`forms.css`/`primitives.css` gained coarse-pointer rules only | PR #228 | `file-upload-current-runtime.md` measured React `f8d99e`/Styles `e45ff57`. The numerical protocol binds exact artifacts, so it is a mandatory refresh, not a reuse. |

## OF-ANCHORED — constrained placement and native scrolling (specification lines 291–295, 593–597)

| Requirement | Current evidence at HEAD | Disposition |
| --- | --- | --- |
| Fit preferred side, flip when necessary | React Popover 529/711, Dropdown 565/625/635, WorkspaceSwitcher 589/626; Alpine Popover 262/279, Dropdown 142, WorkspaceSwitcher 261 (test-file line anchors). 474 focused source cases pass in Chromium/Firefox/WebKit at the PR #231 revision. | Proved at source level for the three engines. |
| Bounded scroll region when neither side fits, remain reachable | React Popover 529, Dropdown 580, WorkspaceSwitcher 604; Alpine Popover 293, Dropdown 165/214 plus the PR #231 follow-up End-key case. Six packed native touch/keyboard/selection cases (React and Alpine × Dropdown/Popover/WorkspaceSwitcher) and the selected-last WorkspaceSwitcher hit test at 390×844 on the current packed bytes. | Proved for the vertical axis on current owners and archives (`anchored-popup-scroll.evidence.json`). |
| Shift along the chosen side within the usable visual viewport | Both placement owners measure the visual viewport and select `align: start` or `end` when the popup width exceeds the room on the default side. There is no continuous horizontal shift and no horizontal bounded region; an anchor with insufficient room on both inline sides can still overflow. Source RTL/LTR alignment cases (React Popover 104–121/165–166, Dropdown 62–69; Alpine Popover 192–241, Dropdown 237–244) prove the logical edge selection. | **Open applicability decision.** The implemented discrete alignment flip is not literally the specified shift. No defect was reproduced at 390 px with the public fixtures. Either accept the discrete flip as the V1 placement contract (record the deviation in the migration guide) or open one bounded product task for inline-axis constraint. Not decided here. |
| Updates on open, resize, visual-viewport change, ancestor scroll; stop on close/teardown | Captured window scroll, window resize, `visualViewport` resize/scroll listeners and, for bounded owners, a `ResizeObserver` on anchor and popup; cleanup restores inline geometry. React Popover 711 (scroll re-measure), Dropdown 483/Popover 665 (unmount cleanup); Alpine Dropdown 165/340/439 (restore geometry, destroy). | Proved at source level. Content resize is observed only for bounded owners; non-bounded Combobox keeps its prior lifecycle by design. |
| Single semantic trigger, stable relationships, nested child, outside pointer origin, Escape, connected/removed trigger, portal context, LTR/RTL | React Dropdown 102/128, WorkspaceSwitcher 193; Popover 57/683, 365/412 (portalled Dialog child, ordinary portal), 78/196/489/612/634; Alpine Popover 88/148/170, Dropdown 263/276/416/439, WorkspaceSwitcher 86/179/204/221. | Mapped to existing source scenarios; no new tests requested. |
| Native pointer selection and outside-tap dismissal (Dropdown) | `dropdown-native-tap.md` observed React/Alpine open, exactly-once selection, focus restoration and outside-tap dismissal on owners that PR #231 has since changed (`dropdown.tsx`, `dropdown.ts`, both placement internals). | Historical. Re-observe once at candidate time with the retained script in MAIN `.batuta/runs/dropdown-native-tap-20260915/`; no new producer. |
| Native sequential Tab (Popover) | `popover-native-tab.md` on React 658d9fa; owners changed in PR #231 only in placement measurement, not key handling. WebKit plain-Tab qualification remains open per that report. | Reuse for key ownership with the explicit owner note; WebKit plain-Tab stays open as already recorded. |

## OF-COMPOSED — reused lower-level scenarios and domain outcomes (lines 603–611)

| Contract item | Current evidence at HEAD | Disposition |
| --- | --- | --- |
| CommandPalette reuses OF-MODAL in overlay mode; inline mode acquires nothing modal | React 325 (portal, trap, scroll lock, opener restoration), 744 (inert retained scope), 632 (backdrop gesture), 273/136/609 (inline is not a dialog, no returnFocusTo); Alpine 213/283/399/462. `command-palette-forced-state.md` adds forced-color active indication with Styles regressions. | Mapped; owners unchanged since screening. |
| CommandPalette combobox/listbox model | React 280/312/726/733; Alpine 292/311/377/349. | Mapped. |
| WorkspaceSwitcher reuses OF-ANCHORED with listbox selection, focuses selected option | React 152/337/604, 193/312; Alpine 109/137/160/179/238. PR #231 packed workspace cases on current bytes. | Mapped; current. |
| CreateWorkspaceDialog composes Dialog, state machine, single AbortController, terminal results | React 45/80/103/153/183/202/223/266/315; SSR case; `workspace-native-promise.md` negative proof on the current owner. No Alpine counterpart exists or is claimed. | Mapped; Alpine cells are not applicable for this component and must be recorded as such, not as PASS. |
| Media/direction/coarse for composed components | `current-media-and-zoom.md` screening: 195 interactions and 66 axe observations over 11 P1 fixtures × 6 profiles × 3 engines on React `f8d99e`/Alpine `09c635`/Styles `e45ff57`. The coarse-pointer profile ran 10 of 11 fixtures (Tooltip skipped). Since then only shared CSS changed for these components: coarse-only minimum sizes on `.lyra-cmdk__item`, `.lyra-cmdk__search`, `.lyra-menu__item`, `.lyra-wscreate__slug-input`, `.lyra-btn`, `.lyra-input`, plus the Dialog overlay scroll owner. Current Styles regressions cover those rules: `coarse-targets.test.ts` (five pointer contexts, 10 owners), `active-hover.test.ts` (forced colors), `popover.test.ts` (reduced motion), `dialog.test.ts` (overlay scrolling), all passing in the 325-case Styles suite at `50325fc` = Styles `f88b5ada`. | Axe light/dark, forced-colors, reduced-motion and RTL screening reuses at owner level for CommandPalette and WorkspaceSwitcher (React/Alpine bytes unchanged or changed only in placement measurement); CreateWorkspaceDialog reuse is limited to the unchanged rendering path, the Promise repair did not change markup. Coarse-pointer relies on the current Styles regressions and the PR #228 packed 92-target observation; the composed fixtures were not re-screened on `f88b5ada`. One bounded rerun of the retained `p1-media.mjs` runner against the candidate archives is the smallest closing action at candidate time; it is a rerun of an existing script, not a new producer. |

## OF-TOOLTIP — coarse pointer and essential information (lines 405–409)

| Requirement | Current evidence at HEAD | Disposition |
| --- | --- | --- |
| Tap retains native action, no hover emulation or long press | `tooltip-native-tap.md`: six adapter/engine cases, trusted touch events, exactly-once native submit, interception control. Tooltip owners and `.lyra-tooltip` rules are byte-identical at HEAD. | Reuse at owner level; rebind to candidate archives by hash. |
| Stable `aria-describedby`, hidden `role="tooltip"` matching visible text, no name substitution | React 313 (target wiring, axe light/dark), 397; Alpine 68/166. Source: React merges the consumer `aria-describedby` and renders `<span role="tooltip" hidden>`; Alpine preserves an existing `aria-describedby`. | Proved at source level. |
| Essential name, instruction, error or operation must exist visibly or through an independent disclosure | This is a consumer-content obligation. Lyra's scope is (a) the component contract above, (b) the published guidance and (c) first-party content. Guidance: `apps/docs/content/docs/en/components/tooltip.mdx` and the pt-BR page state that a tooltip is never the only place a control's name exists, that icon-only controls take `IconButton` `label`, and that touch users may never see a tooltip. First-party content: the only Tooltip consumers in `apps/` are the two docs examples; `Copy share link` has a visible label and `IconButton label="Archive"`/`"Duplicate"` carry names independently of the tip; the Alpine/plain-HTML sample uses visible button text. No component in `packages/react` or `packages/alpine` composes Tooltip internally. | **Disposition: satisfied within Lyra's scope** (component contract + documented rule + first-party content audit). Arbitrary third-party content cannot be proved by Lyra and is not a ledger cell. The `coarse-pointer` cell for `OF-TOOLTIP` maps to the native tap proof plus this audit; the screening's skipped coarse Tooltip fixture is explained by the no-hover contract and is not a gap. No new gate, test or documentation change is required. |
| Timing, warmth, Escape, ownership, trigger removal, teardown | React 30/65/130/175/203/231/271/338/360/382; Alpine 81/103/120/137/156. Timing remains a functional contract, not a performance budget (September 15 decision). | Proved at source level. |

## Cell disposition for the anchored and composed components

| `v1-interactive` cell | Popover, Dropdown, WorkspaceSwitcher | Tooltip | CommandPalette, CreateWorkspaceDialog |
| --- | --- | --- | --- |
| chromium / firefox / webkit | Current: PR #231 474 focused cases; Styles 325 cases | Current source suites (last full three-engine run in PR #223 and the packed Alpine 945-case run at Alpine `09c635`, now superseded by `324c2adc`) | Current source suites; Alpine packed run predates `324c2adc` |
| react-18 / react-19 / ssr / hydration | `.batuta/v1-p1-react-compat-verification.md`: 11 named P1 SSR and hydration cases per React version on React `f8d99e`. React runtime bytes changed in PR #229/#231. | Same report; Tooltip owners unchanged | Same report; CreateWorkspaceDialog owner changed |
| keyboard-focus | Current source suites (see mappings above); native Tab report reused with owner note | Current | Current |
| axe-light / axe-dark | Source per-theme axe cases current; packed screening historical | Source current; packed screening historical | Source current; packed screening reusable at owner level |
| forced-colors / reduced-motion / rtl | Source RTL cases current; forced/reduced only through screening and Styles regressions | Screening only (Styles rules unchanged) | Screening + `command-palette-forced-state.md` |
| coarse-pointer | PR #231 native scroll cases current; Dropdown tap historical; targets via PR #228 | Native tap current at owner level; content audit above | Targets via PR #228 and Styles regressions; composed screening historical for CSS |
| bundle-standalone / bundle-composition | PR #231 official gate: 72 entries, 5 scenarios, 4 CSS entries on the HEAD archives | Same | Same |
| packed-esm / packed-cjs / packed-types / consumer-* | `final-local-qualification.md` on React `f8d99e`/Alpine `09c635`/Styles `e45ff57`; all three archives have since changed | Same | Same |

"Current" means the evidence was produced on the HEAD owner bytes; "historical" means owners or archives changed afterwards and the record is retained as evidence of the earlier revision only.

## Refresh obligations at candidate time

These are reruns of existing commands and retained scripts on the exact candidate archives, bounded by the identity table above. None is a new test, producer or CI job.

1. Pack the candidate and bind React/Alpine/Styles SHA-256; run `pnpm baseline:bundles --check-budgets` (72/5/4) on them.
2. Rerun `pnpm test:react-compat` (React 18.3.1 and 19.2.8, 11 named P1 SSR/hydration cases each), `pnpm pack-smoke`, `pnpm smoke`, publint/attw/distribution scans, and the packed Alpine public browser run retained in MAIN `.batuta/runs/alpine-packed-runtime-20260914/`.
3. Rerun the FileUpload numerical producer (six operations, 100 items, 20 active attempts, at least 30 samples) on the candidate React/Styles pair; the protocol binds exact artifacts.
4. Rerun the retained `p1-media.mjs` screening (6 profiles × 3 engines × 11 fixtures) and the Dropdown native tap script on the candidate archives.
5. Record explicitly: manual AT `deferred-by-release-profile`; Overlay/Tabs/DataTable numerical runtime deferred under the September 15 decision (non-PASS); WebKit plain-Tab Popover qualification open; Alpine cells for CreateWorkspaceDialog not applicable.

## Decisions for the maintainer

1. **Inline-axis placement.** Accept the discrete start/end alignment flip as the V1 OF-ANCHORED placement contract, documenting the deviation from "shift along that side", or authorize one bounded product task for an inline bounded region. This reconciliation makes no product change.
2. No other decision is required by this record. The candidate acceptance proposal in MAIN `.batuta/runs/pr223-followup/candidate-acceptance-proposal.md` can proceed under the refresh obligations above; its prerequisites paragraph is superseded by the completed PR #228–#231 merges.

## Limits

Controller-only reconciliation; no OpenCode/GLM scout was launched because the requirement mapping depends on file-identity comparisons already available locally. No browser test, build, package install, packing or remote action ran for this record. Test-file line numbers are anchors at HEAD and will drift. The audit of first-party Tooltip content covers `apps/docs`, `apps/site`, `packages/react/src` and `packages/alpine/src` by search for `Tooltip`, `lyraTooltip` and `lyra-tooltip`; generated docgen output and starters in external repositories were not inspected. Nothing here qualifies V1, promotes a baseline, merges Version Packages, versions or publishes.
