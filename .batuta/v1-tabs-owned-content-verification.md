# Tabs React owned content — controller verification
Status: React runtime controller verification and independent review PASS; local commitd1ad2ad. Existing global Task10 overages and remaining Task27/V1 work are not qualified.

## Routing and scope
Selected critical design followed GLM301.61s and346.17s guarded technical reviews, with all findings adjudicated under .batuta/runs/. CodexTerra/high574.25s initial +314.48s single retry; critical controller completed one reproduced owned-commit focus repair, restored the original exact dark rest/hover contrast test, clarified controlled-value changeset wording and corrected a native-ID SSR fixture. Initial17allowed paths,16tracked product changes; generated site stats is an existing ignored build output and will not be forced into Git. The packaging follow-up extends the scope below.

Initial worker violated the explicit command/scope boundary by creating .impeccable/config.json through an ignore-value command after an unrelated Stop-hook sidebar finding. Exact config is archived in MAIN .batuta/runs/v1-tabs-owned-content-initial/unauthorized-impeccable-config.json; controller removed only that new config. Retry did not recreate it or run Impeccable config commands. Automatically generated untracked hook cache was archived/removed after execution. No Colima/Docker/resource/service/remote action occurred. No new dependency, foundation, class, stylesheet or generic manager.

## Actual behavior and compatibility
One controlled Tabs root and one TabsList per root, named TabsTrigger/TabsContent plus four Props at root/existing subpath; unchanged safe TabItem type. Real mounted panels own consumer nodes, with stable encoded value IDs and useId fallback. Required values/children are preserved; invalid input does not silently select a different value. Current eligible DOM order, RTL, native prevention, ignored controlled requests, nested isolation, child-only entry and removal lifetimes are covered. No uncontrolled/namespace/legacy placeholder mode. Consumer class/style/native props/ref targets remain.
Exactly three current consumers migrated: docs line/pills and site showcase. Paragraph nodes preserved; site tabbar padding still wraps only the list, preview/code own actual panels and preserve input data across switches. Seven-line hidden rule appended to current navigation CSS additive tail; original CSS prefix byte-identical, no parity baseline/algorithm update. Styles patch and React breaking0.xminor changesets identify native-hidden fix, explicit project SemVer exception, earliest0.6/1.0removal, manual content/value migration and Styles0.5.1-or-later-containing-release floor. No version command.
Docgen required existing exported-Props guard78→81 and one downstream documentedComponents expectation; strict count/extraction/category checks retained. The81interfaces do not add CSS families/entries/routes; existing78-family prose unchanged. Outputs regenerated via docgen and derive-stats.

## Controller evidence
- Baseline actual line: empty named panels with Project summary outside, RED3/3, zero pageerrors. Current actual line ownership GREEN3/3. MAIN .batuta/runs/v1-tabs-owned-content-native/ keeps baseline and current outputs.
- Baseline current Alpine/native CSS: hidden flex panel21px and hidden tab39px visibly laid out, RED3/3/zero pageerrors. Corrected scoped CSS GREEN3/3 with unchanged Alpine compiled artifact. MAIN .batuta/runs/v1-tabs-hidden-native/.
- Initial scoped source9/7/7passed but typecheck had8SSR-required-children errors; docgen78vs81failed. Native extended57/75/zeroerrors identified initial/child-only entry plus CSS/inherited-disabled eligibility. A controller CJS-browser-import harness error is preserved separately and not attributed to product; corrected hydration harness reproduced6entry failures with valid3pass and zero errors. Initial actual consumers basic9 +visual12passed, then exact site padding proof failed3/3.
- Retry source12/10/10/types/lint/format/stylelint/parity/build/docgen81/check/derive-stats/docs stack303/docs inventory/sitebuild/site20 PASS. Native75/78 failed only descendant-only removal of the focused unselected paired tab; hydration9 and actual consumers/layout9 passed. No pageerrors.
- Critical source regression RED on exact retry source (focus stayed body at first child-only removal), then GREEN after using the existing per-owned-commit normalization point for the same local focus repair. Clear old tracking before focus() so synchronous capture retains the next destination; repeated removal ends on surviving list fallback. Source full Chromium+SSR14/WebKit12/Firefox12 PASS including restored exact contrast. Types/lint/format/stylelint/parity/build/docgen81/check/derive-stats/docs stack303/docs inventory/sitebuild/site20 PASS. The final native-ID-only SSR fixture refinement passed2SSR tests; an initial invocation from MAIN selected no matching project and is retained as controller-command error, followed by the correct package-cwd run.
- Current compiled native78/78, hydration9/9 (deterministic server output, collision-free whitespace/percent/Unicode IDs, no recoverable errors, actual native entry), actual line3/3, old Alpine hidden3/3, actual three consumers9/9 incl preserved input and original padding; zero page/console errors. ESM+CJS root/subpath4/4 actual SSR renders and public type-positive/negative fixture PASS. MAIN .batuta/runs/v1-tabs-critical-native-checks/, v1-tabs-runtime-native/, v1-tabs-critical/.
- Negative proofs: initial high source under final relevant source tests is4RED (CSS eligibility, first entry, child-only entry, repeated child-only removal); exact restoration4GREEN. Original CSS3RED and exact restoredCSS3GREEN; source/CSS byte/SHA parity verified in MAIN .batuta/runs/v1-tabs-negative/result.json. Negative-driver outputs do not replace baseline/current positives.
- Visual:12captures at375/1280 light/dark after the site layout repair; six representative captures manually inspected across all three consumers, both widths/themes. Current consumer/style sources match those captures; the later critical runtime delta changes only removed-focus handling, with current compiled native consumer9 reaffirmed. No new appearance change or overflow; shared default39px tab targets remain existing styling, with mobile site44px override, not falsely called a new WCAG defect. Broad zoom/forcedcolors/manualAT and final-artifact ledger remain pending. No extra visual polish or configuration suppression.
- Complete family (all four named imports, not the tree-shaken root alone): initial1.31kB, retry1.49kB, final1.46kB against unchanged1.5kB cap; old complete data-only component543B. Values from size-limit console are rounded; no exact byte delta invented. All existing13globalTask10 overages persist, with no cap/hash waiver. Site stats is owner-generated and ignored, not a new distributed artifact.

## Criteria
1. Public compound ownership/export/native types/current consumers: controller PASS, final independent review pending.
2. Controlled native interaction, SSR/entry and paired-removal lifetime: controller PASS, final independent review pending.
3. CSS/compatibility/inventory/verification/scope: controller PASS after removed initial scope violation, final independent review pending. GlobalTask10 and final packedLinux/React18+19/manualAT/23cells remain explicitly unqualified. Task27 React public MDX and Alpine runtime/fallback/docs follow as separate cycles.

## Final corrections verified — independent follow-up review pending
The first final GLM review299.58s had three DONE statuses with unchanged guard and one accepted callback-ref identity finding. Separately, real mixed root/subpath SSR failed4/4; these failures superseded the earlier separate-entry export verdict. See `.batuta/runs/2026-09-10-tabs-owned-content.review.md`.

Critical controller added a callback-ref regression: RED after controlled active change with the previous owner, GREEN after making the root callback stable on the supplied ref and moving root normalization to the isomorphic layout effect. Existing descendant-commit normalization/focus repair stays intact; no render-time latest-ref mutation. Final source15Chromium+SSR/13WebKit/13Firefox, types/lint/format PASS.

Root-only packaging now forwards to the existing same-format tabs sibling instead of duplicating the context. The two-file scope extension is `.batuta/v1-tabs-packaging-critical-brief.md`: tsdown config plus a real packed-consumer mixed SSR regression in existing smoke stage7. No dependency, new chunk, entry or public symbol. Initial research214.79s was stopped for excluded dependency traversal (unchanged guard); report-only retry29.29s valid. Advisory source counts/other-context suggestions were corrected/declined.

Actual config diagnosis: callback return values replace defaults, so preserve `...options`; initial omission failed build. Next build preserved defaults but still emitted extensionless ./tabs: paths receives an absolute source module id. An isolated root-build probe logged that exact id and both formats/cjsDts; mapping the fileURLToPath-resolved source ID fixes it. Probe setup syntax/cwd errors are retained separately, no product changes/config suppression from those diagnostics. No dependency version change or heuristic generated-file rewrite. ESM root now imports ./tabs.js; CJS root requires ./tabs.cjs; matching declarations verified by attw.

Permanent real tarball-install smoke failed at mixed TabsList provider with old build, then passed after correction: four ESM/CJS/both-direction SSR renders, real content/roles/visible selection; existing Vite types/build/isolation/CDN and Next App Router packed-consumer builds all PASS. Separate direct mixed SSR4/4, publint, attw node16, all150 client directives, CDN scan PASS. Same450artifact inventory; only10root/Tabs artifacts changed, all440others byte-identical. Final protected1700tracked files unchanged; no Impeccable config.

Final built native78/78, hydration9/9, publictypes, actual line3/3, unchanged Alpine hidden3/3 and actualconsumers9/9 PASS with zero unexpected browser/recoverable errors. All4Tabs exports measure1.45/1.5kB (rounded size-limit console). The global size command still exits1 for the existing13other overages; none waived. Prior source/CSS negatives and visual captures remain valid for unchanged consumers/styles, supplemented by final built checks. Existing mixed-import regression is now permanent in packed smoke, not solely controller evidence.

Final overall scope19paths/18tracked product paths; generated site stats stays ignored. Controller criteria1/2/3 PASS; final independent follow-up review pending before commit. React MDX, Alpine migration and all remaining V1 qualification stay pending.

## Final independent verdict

GLM159.11s exit0, unchanged HEAD/status/diff/scoped-file guard,3actualDONE statuses, empty findings block with no concrete findings; Batuta verifier3/3PASS. Final React-runtime criteria1/2/3 PASS. See `.batuta/runs/2026-09-10-tabs-owned-content-final.review.md`. Local commitd1ad2ad; fullTask27 still requires React MDX and Alpine slices.
