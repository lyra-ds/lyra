# Alpine return documentation — one medium retry
Original eight-file scope and brief remain. Read only this feedback and each affected Alpine StackPanel, not repeated unrelated React/Blade examples. Edit directly, no further orchestration/delegation/config/git writes. No validation; exact pinned scopedformatter allowed.

Controller current MDX8/8, format and stack303/303 PASS. Exact compiled-snippet native48:36PASS (alltrigger/removed/disabled cases across4families×3engines),12FAIL for newly exercised hidden-trigger scenario; zero page/probe errors. These are an unhandled sample composition state, not a runtime-helper regression: the helper correctly refuses hidden targets but the sample resolver still returns the hidden trigger. Keep initialscope unchanged.

1. All8current resolvers test actualbutton and !trigger.disabled. Include the trigger's own hidden state before selecting it, then choose existing named successor. Keep implementation concise; no duplicate generic eligibility engine. Update the adjoining “absent or disabled” sentence to cover hidden and clearly say other application availability changes (including hidden/inert ancestry or a no-longer-meaningful trigger) need the composition to choose its meaningful successor from current state. The example covers its named simple states, not arbitrary automatic discovery. Do not claim a non-null reference alone is safe. Preserve current defaultclosed style, activebindings and labels.
2. Both Dialog Alpine examples label a footer action “Delete” but wire it only to x-bind=close. That invents a fake destructive success action contrary to originalbrief. Make these Alpine examples a neutral Project details workflow with Open project details, a normalprimary trigger, a matching title and informationalbody, and a truthful Done/Close footer wired to close. Preserve successor project-overview and exact realbinding semantics. Updating the trigger ID accordingly is allowed. Leave the existing React destructive example entirely unchanged. Other3families alreadyneutral; no unrelated edits.

Proof afterretry: controller compiles8exact MDX and checksformat/stack303; native same48cases mustallpass using actual snippets, direct hidden/disabled state, unprepared mouse opening/realEscape, exacttarget afterexit. Do not runthese tools; report pendingcontrollerverification. Only execution allowed: /Volumes/Home/francisross/.local/share/mise/installs/node/24.18.0/bin/node node_modules/prettier/bin/prettier.cjs --write plusONLY eight scopedMDXpaths. No package managers/hooks/ignores/services/Colima/Docker/remote or managedstate edits. Stop/report if source contradicts requirements or outside scope needed; do not broaden.
