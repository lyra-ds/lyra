# Critical verification completion after the one high retry
Same product scope and selected revised design; no further implementation delegation. High initial353.55s and retry319.16s preserved. Runtime retry removes the proven premature microtask and scopes the established delegated-wrapper lint exception; controller does not claim the worker ran tests.

Retry first Chromium38/39, sole new portal test failure. Screenshot proves its button at fixed900/560 is outside the runner's narrow visible viewport, while the Popup remains open. This is a fixture failure, not a remaining ownership defect. Position relative to viewport bottom/right, require full viewport bounds and exact elementFromPoint hit before real userEvent.click. That exact test then passes (critical-portal-green.log,22/22). No forced click or relaxed assertion.

Controller test audit found the alleged replacement only changed button text; React reused the same DOM node. Added explicit distinct keys and assertions that the old node disconnects and the new node differs. The three ineligible replacement cases now prove the same real disconnection. Added current-trigger removal with exact zero focus attempts; no new runtime change. The initially-open child Dialog fixture now provides the existing returnFocusTo to its real parent trigger, removing its legitimate missing-opener warning through a correct consumer fixture, without console suppression. All23Popover cases then pass Chromium with no warnings (critical-source-green.log).

Native fixture corrections/limits retained in fixture-disposition.md. The plain React portal source/native pair is needed to verify event identity independently of modal inert protection. Capture microtask timing correction is already in the selected design and retry brief, backed by uninstrumented and instrumented native3FAIL proofs. No public API/dependency/CSS/Alpine/Blade/build-config edits. Final whole-source/static/build/hash/native/negative/review gates follow these exact bytes.

Final WebKit three failures were the same documented native pointer-focus assumptions: explicit tabIndex0 on the two clicked outside destinations and fused trigger preserves exact focus assertions across engines. Final40/40 per engine, SSR5, types/lint/format/build/docgen and153protected/fourmodal parity PASS. All51 compiled native cases pass with zero errors. Original owner2935cda makes14of23 current Popover tests fail; restoring exact final source bytes restores23PASS.
