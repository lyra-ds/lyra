# React workspace keyboard cancellation — controller verification

Verdict: approved. Independent OpenCode GLM3/3 DONE with unchanged HEAD/status/diff/scoped SHA guard; one informational cast observation declined with native-target rationale in the review trail. Codex gpt-5.6-terra medium initial and one retry, then high static-fixture completion. Product runtime unchanged after initial implementation. Medium attempts passed11/10/10 but failed ancestor-fixture JSX accessibility lint and formatting; original direct expected-option focus preparation was also rejected. Retry uses actual opening/End and exact focus assertions. High changes only wrapper semantics to presentation and applies pinned Prettier; no ignore/suppression/config change.

1. Current scoped Chromium/SSR11, WebKit10, Firefox10 pass; types/eslint/format all exit0 on final tests. Compiled native42/42 across3engines pass: option navigation/Escape/Tab/Enter/Space prevention; all4trigger key preventions; stopPropagation-only preserves navigation and blocks ancestor; already-prevented descendant event stays canceled. Callback receives original root currentTarget exactly once, before focus/default changes. Source SHA256be4e90f48f26c103da676b5a09b6b019f261e97c5fa6bcaa5becf4b28c3f9cbd is unchanged from build/native proof through final high fixture. Original18-row native baseline is0/18 with no page errors; its earlier text-oracle draft is not used as evidence. Exact DOM-node oracle is retained.
2. Original HEAD source restored under current cancellation test is RED on incorrect root callback/currentTarget; exact source restoration GREEN. Existing selected entry, create activation, close/restoration, contrast/axe, flip and SSR tests retained. New cancellation fixture adds no source skips/only, permissive result or expected-option.focus; no runtime testing branch. Native proof uses local compiled artifacts and actual input, not source mocks. Owned browsers/server closed.
3. Three scoped product files only; public API/classes/exports/dependencies unchanged. Build/docgen passed on exact current runtime; final type/format/lint passes. WorkspaceSwitcher8.21kB within unchanged8.25kB. Overall React size gate still fails the10previously recorded unrelated entries; no budget/hash update. This bounded proof does not qualify packed Linux or any full P1 ledger cell. No remote/publication or Colima/Docker/service action.

Main raw: v1-workspace-react-cancellation-initial-checks, final-checks (failed medium retry), high-checks (final), regression/{red.log,green.log,result.json,final.tsx}. Worktree raw: v1-workspace-react-cancellation/{executor.log,retry.log,high.log,*result.json,initial-backup,retry-backup}; v1-workspace-cancel-native/{baseline-result.json,final-result.json,activation-final-result.json,fixture.jsx,run.mjs}. Final42 rows are activation-final-result.json; previous36 rows preserved in final-result.json. No full React rerun required for this isolated event fix after the preceding full Tooltip matrix; relevant scope/native/static checks are complete.
