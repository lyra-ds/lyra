# CreateWorkspaceDialog public lifecycle docs — controller verification

Task37 after runtime6f450d2. CodexTerra/medium260.61s initial +168.7s single prose/migration retry; then high130.12s canceled-close wording completion; no high retry. Exactly two current MDX pages in scope. Protected frontmatter, complete static HTML panel/tail and two existing Example ids unchanged; no claim that HTML is an Alpine runtime. All other tracked product hashes and453React/Alpine distribution hashes unchanged.

Controller initial and final MDX compile, complete current snippets public typecheck, scopedformat and docs stack303tests PASS. Extraction honors MDX code indentation and removes only controller-owned stale extracted files before final extraction. The old explicitly labeled unsupported legacy block was not treated as current runnable code; the final migration uses a diff fence and points to the complete consumer, without an ambient placeholder or duplicate handler.

Exact current WorkspaceCreator snippet in each locale was extracted without changes (only MDX indentation removed), imported into a native Vite fixture and supplied application persistence.24/24cases =2locales×3engines×4scenarios: held thenaccepted; rejected with errorfocus/valuepreservation thenretry; abort causing canceled acknowledgment; successful persistence after an abort request retaining accepted outcome. Native realinput/Escape, fresh calls/data/unaborted signal, pending busy, abort reason/canceling observation, committed persistence count, controlled closure and actual trigger returnfocus. Zero page/console errors. Final snippetSHA matches the exact code used for those24cases in both locales; the retry changed prose and removed the redundant migration snippet only. Existing real live TSXexamples unchanged, runtime is already Task26qualified within its stated boundaries.

Direct prose review confirms blank-field validation instead of disabled-until-name, currentrequest data/id/signal, matching explicitterminal outcomes, rejection preservingvalues/retry, pendingcancelhandshake vs idleclose, accepted-aftercancel stillrequestsonClose, forcedclose/unmountinvalidateslatework, reset-onreopen, and existingDialog returnFocusTo eligibility/fallback. Newly updated React prose English in both locales; existing localized frontmatter/HTMLtail preserved. Aborting does not pretend database rollback; application persistence owns its outcome.

Raw MAIN .batuta/runs/v1-create-workspace-docs-{initial,checks,final-checks,native} and native final-snippet-parity.json. Raw workerlogs in WT v1-create-workspace-docs and v1-create-workspace-docs-retry. Initial GLM100.04s3DONE with two duplicated low canceled-close wording findings accepted. High completion adds only explicit onClose after the matching canceled acknowledgment in both pages; exact before.replace(old,new) equality proves no other text/code changed. Pinned controller MDX/snippettypes/stack303/format rerun PASS; current snippetSHA remains identical to native24. High executor invoked global Node26 for a scoped Prettier command despite the pin; it reported both files unchanged, and controller audited the exact two-clause diff/protected453hashes and reran pinned Node24.18.0 formatting/types. No library, installation, config or service changes occurred; executor formatting is not used as verification evidence. Final corrected-wording GLM review36.75s3DONE/no findings/unchangedguard/verifierPASS. No source/runtime/dependency/config/Colima/Docker/services/remote/release change. This does not qualify complete Next/sitechrome/Linux/manualAT or resolve Task10size.
