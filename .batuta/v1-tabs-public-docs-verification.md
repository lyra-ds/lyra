# Tabs React public documentation — controller verification
Status: controller criteria1/2/3 PASS; independent review47.49s3DONE/no findings/unchanged guard/verifierPASS; local commit pending. Task27 Alpine and full V1 remain unqualified.

Route: Codex gpt-5.6-terra/medium376.85s initial plus88.84s one bounded retry; no escalation. Initial source scope and MDX2/snippettypes/stack303/native6 passed; missing Styles floor, hydration clause and formatting were concrete feedback. Retry adds those clauses, clarifies eligibility and uses pinned Node24.18.0 Prettier. Initial executor read/diff commands only; retry additionally pinned formatter and diff check, no service/config/hook/ignore/package/build mutation. No Impeccable config.

Scope: only en/pt-BR components/tabs.mdx. Controller snapshots confirm1716other tracked product paths and453public React/Alpine artifact hashes unchanged. Frontmatter, both live Example IDs, full Alpine/Blade tail preserved byte-for-byte. All new prose is English; current existing adapter sections remain knowingly unchanged for their later migration.

Final controller: both MDX compile; exactly one complete exported ProjectTabs TSX per locale extracted from actual current MDX, built root/subpath public declarations typecheck; stack303 PASS; scoped Prettier and git diff --check PASS. Native6/6 =2actual locale snippets×3engines, with trusted native Tab/ShiftTab, arrows/wrap/Home/End, pointer activation, actual panel content/visibility/ARIA pairing and unchanged mounted panel nodes across switches; zero page/console errors. Raw snippets and SHA, driver, result under MAIN .batuta/runs/v1-tabs-public-docs-native/. Current check outputs .batuta/runs/v1-tabs-public-docs-final-checks/.

Criteria1: four named controlled parts/native refs/props/owned attributes, exact one-list/unique pairs/ID contract, eligible entry/RTL/native prevention/requests/no echo, invalid active and selected-disabled behavior, mounted state and SSR/hydration boundary all documented.
Criteria2: one complete usable current TSX example with actual Project summary, clearly historical migration diff, no legacy/uncontrolled alias; preserve safe TabItem data type, manual value/content decisions and old ref/class/label move. Explicit unsafe coexistence/project pre1SemVer exception, earliest React0.6.0 or stable1.0.0 removal and Styles0.5.1-or-later-containing-fix floor; no publication performed.
Criteria3: exactly two MDX files, frontmatter/Example IDs/Alpine/Blade retained, no new widgets/live examples/runtime/generated files/dependencies; formatter and contract checks pass. Runtime source-size13overages, remaining Alpine/DataTable/modal/Linux/React18+19/AT qualification are unaffected, not waived.
