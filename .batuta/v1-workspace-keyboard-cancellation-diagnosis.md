# React workspace keyboard cancellation — reproduced
At3c000ab React source and the artifact built by Task19 retain option-local defaults before root onKeyDown. The scoped GLM scout identified this path. The controller compiled native fixture prepares selected Beta via actual Enter, retains the exact selected DOM node, sets consumer prevention and presses ArrowDown/ArrowUp/Home/End/Escape/Tab. All18engine/key cases violate pre-default cancellation: four keys move focus before the callback; Escape and Tab close despite prevention. No page errors; owned browsers/server closed. Raw .batuta/runs/v1-workspace-cancel-native/baseline-result.json binds artifact SHA.

The initial text oracle incorrectly expected Beta without Avatar's rendered initial. It is preserved in initial-text-oracle-result.json and is not the accepted proof. The final baseline captures exact DOM-node identity for both callback-time and resulting focus;18/18fail independently of label text. Fixture/run.mjs retain that exact oracle.

Architecture source docs/superpowers/specs/lyra-v1/04-component-architecture.md:251-275 defines consumer first, defaultPrevented cancellation, original event and at-most-once delivery; stopPropagation alone is not cancellation. Trigger also incorrectly calls the root callback with button currentTarget through a cast. Task21 preserves API, current markup and create behavior while repairing this existing event contract. Creation semantics/click ordering and Alpine event/API design remain separate.
