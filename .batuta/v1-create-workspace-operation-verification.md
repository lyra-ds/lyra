# CreateWorkspaceDialog operation lifecycle — controller verification

Commit 6f450d2.

Status: implementation and bounded controller gates verified; independent GLM review162.94s,3DONE/no concrete findings/unchanged guard/verifier PASS. Runtime belongs to Task26; public MDX explanation follows separately. No stable release qualification claimed.

## Routing and scope
High Codex gpt-5.6-terra408.24s initial,339.27s single retry, then critical/controller test-only completion. Initial failures: unnamed form, null guards, Chromium pending focus loss, cross-realm Promise rejection, passive commit/destroy invalidation, duplicate example keys and missing durable source coverage. Retry corrected runtime; source fixtures then failed because they clicked a disabled submit and a background button covered by the modal, plus iframe Window typing. Critical fixed only that test file: native form requestSubmit duplicate proof, genuine external React transition, and typed foreign window constructor access. No forced clicks, prepared focus, warning suppression, skips or product test flags.
Nine changed product files in the ten-file scope; unchanged SSR file. Existing Dialog/Input/Button/styles and all other tracked source hashes match initial3bf23ef. No dependencies/config/Colima/Docker/services/remote/version commands. Generated props/llms are controller-generated after a successful current build.

## Controller proof
1. Public request/result/form/validation/terminal semantics: current source12/12Chromium+SSR,11/11WebKit,11/11Firefox; React types, scoped ESLint/Prettier PASS. Native81/81 uses compiled current public module, actual input and Escape: fresh copied request/data/signal, submitting-before-notify, invalid field focus, sync/async terminal results, undefined/throw/rejection/malformed fallback, duplicate guard, readonly pending, cancel handshake/order/single abort/reason, accepted/rejected after cancellation, held promise/no fabricated settlement, stale IDs, reset/retry, actual trigger return focus, StrictMode and cross-realm native Promise. Zero unexpected page errors. Three deliberately thrown onClose errors are observed in original error context, not swallowed or mapped to creation rejection.
2. Lifetime: separate native6/6 verifies parent layout-effect settlement after startTransition controlled close or destruction. Zero extra onClose, exactly one owned abort and correct reason; exiting forced closed form stays editing/nonbusy. Source negative proof replaces only layout cleanup with passive effects:1/1 lifetime test RED. Initial high source under final tests:3/3 named form/pending keyboard/cross-realm regressions RED. Finally all4/4 selected tests GREEN, source bytes restored by SHA. CLI selection exclusions are not committed skips.
3. Actual examples and public package contract: two existing examples accept real local data and render unique internal operation identities; repeated identical workspace names twice per example in all3engines gives6/6cases/12creates, correct reset and trigger returnfocus, zero console/page errors. Built root and existing subpath type imports and actual example typecheck PASS, including intentional type rejection of legacy void handler and malformed rejected result. Breaking0.x minor changeset explains request/data/signal/matching acknowledgment and controlled close request. Build/docgen/check PASS. Final source-only fixture repair did not change runtime or generated outputs.

## Visual and size limits
Actual two imported live examples,375/1280light/dark:8/8 no horizontal document/dialog overflow, all8 final screenshots inspected with existing tokens/styles, focused name, readable title/URL/footer and no clipping. First capture happened during entry animation; it is preserved as initial-* evidence. One bounded confirmation waits for real document animations to finish, never disables motion. This is the actual example mounted in an isolated Vite page, not full Next/site chrome or manual AT certification.
Impeccable context permits existing-surface audit without new context artifacts. Detector on exactly runtime+twoexamples reports[]. No style/config change. Local audit scores: accessibility3/4 (named form/focus/error/keyboard and existing axe source test; full AA/AT not claimed), performance2/4 (existing bundle cap fails), responsive3/4 (measured contained375/1280; existing footer targets40px high; close target44px), theming3/4 (both existing themes inspected, no exhaustive contrast/forcedcolor test), implementation integrity4/4 (same composed DS owners, explicit real local effects, no detector findings). Provisional15/20; one existing P2 touch-target issue and one P1 release-budget issue, no new visual repair justified in the closed runtime scope. Touch target sizes are retained in visual-result.json and stay in final shared-control qualification; this is the Impeccable44px guideline, not by itself proof of WCAG AA failure. Apply a bounded adaptation only under that shared-control scope if required, followed by one polish pass.
CreateWorkspaceDialog standalone now approximately5.12kB vs previous4194B, cap3200B. size-limit exit1 is retained, not waived. Same12React+1Alpine overage entries; no cap/hash change. Exact final packed before/after and disposition remain Task10; current native/visual correctness is not a size PASS.

## Evidence locations
MAIN .batuta/runs/v1-create-workspace-operation-{initial,checks,retry-checks,critical,critical-checks,negative,retry-native-checks}; native folder v1-create-workspace-operation-native holds initial/pre-retry/current results, fixtures, compiled artifact SHA, actual example source SHA,8screenshots and detector output. Critical/final-shas.json and final.diff bind source/type/example/generated outputs. Raw executor logs retained in WT v1-create-workspace-operation and v1-create-workspace-operation-retry.
Final Linux matrix, complete React18/19 packed SSR/hydration/consumer/security, global modal qualification/manual AT, size resolution and public MDX remain pending. No remote or release action taken.
