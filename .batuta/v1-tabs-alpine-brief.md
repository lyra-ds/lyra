You are the Alpine implementation executor already delegated by Batuta. Implement only this bounded slice; read/edit/report, controller owns validation. Do not orchestrate/delegate/start another workflow or ask for a new design interview.
# Alpine Tabs readable fallback and native lifecycle — high
## Goal
Implement the selected Alpine Tabs contract on the incumbent adapter: usable native server fallback, honest successful enhancement, consumer-first native events, controlled/modelable active inputs and teardown restoration. Preserve current classes and option/binding names, add only ready/fallback output and the selected event detail type.
## Context
HEAD49c8447 on feat/v1-incumbent-stabilization. React Tabs runtime d1ad2ad and docs49c8447 verified; shared native-hidden CSS correction already committed. Read `.batuta/specs/2026-09-10-tabs-owned-content-design.md` in full, especially Alpine section and final listener-boundary precision; that is the selected contract, not a request to redesign. Read `.batuta/v1-tabs-alpine-diagnosis.md` and `.batuta/scout/2026-09-10-tabs-alpine-scope.md` for corrected source/generator/downstream findings.
Existing tabs.ts has required active, list/tab/panel, ID helpers and automatic key activation, but no ready/fallback/events/destroy, nested or eligible/RTL filtering; invalid active silently chooses first. Source current three-engine suite passes while actual compiled selected-contract baseline fails12/12 relevant cases: valid enhancement keeps static-hidden list hidden, list click/key veto is late, destroy leaves controls/hidden panels. Three pre-init native-content controls pass, zero pageerrors. Raw controller source before snapshot and baseline under MAIN .batuta/runs/v1-tabs-alpine-baseline/ (do not run or edit).
Actual Alpine listener probe24/24 proves physical list x-bind defaults run before list consumers, even when their @click/@keydown directive appears before x-bind. A root bubble listener sees both those and later native addEventListener vetoes. Therefore selected list-owned defaults run on native listeners on the owning root, resolve the one owned list/trigger, exclude nested roots, and are removed on destroy. No delayed default, event manager or new binding for this implementation detail.
Required native server markup is the exact design example: marked root/data; visible labelled fallback nav with links to every real headed section; enhanced marked lyra-tabs list statically hidden; native paired buttons; real section[id] panels initially readable with no enhanced roles/hidden state. Fallback binding is new output. Supplied section IDs support native fragment links before JavaScript; missing root/trigger IDs may be generated value-stably and collision-free. Preserve supplied IDs and consumer content. No fabricated panels or alternate legacy mode.
The current imported Blade snapshot uses old markup and is a downstream migration dependency. Do not edit it or claim compatibility; this runtime slice remains unqualified for old Blade. Full public Alpine MDX migration follows separately. Catalog generator reads registrations and Options interfaces only: ready/fallback/detail are NOT new options; no generator expansion or hand-editing.
## Conventions
Pinned Node24.18.0, pnpm11.13.1, TypeScript5.9.3, current Alpine3.15.12. Existing Alpine object/ThisType/magic conventions and native DOM APIs; no React/other state library or external foundation. CSS-first: use existing class names and committed Styles correction; no CSS changes. English JSDoc/new prose. Keep helpers local to Tabs, derive state where possible, no speculative generic validator/observer/focus owner. Typed public detail with JSDoc; no any/suppression/test-only branch.
Only read/edit/report. If formatting is needed, only run absolute `/Volumes/Home/francisross/.local/share/mise/installs/node/24.18.0/bin/node` with repository `node_modules/prettier/bin/prettier.cjs` and the explicitly scoped source/README/changeset paths. No tests/build/install/package-manager/git writes/services/config or commands modifying hooks. A Stop hook finding is outside scope: do not create .impeccable/config.json, run context/ignore commands, or suppress anything. Do not change Colima in any way.
Tests exercise actual DOM/Alpine and meaningful state/event/visibility/ARIA/focus, not locator existence. Preserve existing class/server-class/modelable/axe behavior in the new required markup. Use the existing Alpine nextTick synchronization; synchronize real conditions, no arbitrary sleeps or fake dispatch-proof shortcuts. Direct focus preparation is not proof of native Tab entry; controller owns trusted native/browser proof. Never count an unrun test as passed.
## Acceptance criteria
1. Public ownership/enhancement: keep `lyraTabs({active:string})`, exported LyraTabsOptions, modelable active, list/tab/panel; add adapter-owned ready=false, fallback binding and exported `LyraTabsChangeDetail {value:string;previousValue:string}`. Validate required one-root/list/fallback/unique paired native values and IDs/valid active before successful setup. Fail observably ready=false with all native content/fallback usable, controls hidden, no half-applied roles/state/logger. On success actual DOM before a visible frame has fallback hidden, named list visible, linked tabs/panels/active-inactive data-state and appropriate selected/hidden/roving state. Preserve supplied native IDs; generated missing root/trigger IDs value-stable. Ordinary initial sections have IDs for their fallback links. Missing/failed/no initialization keeps native readability. Current active value must match a pair; invalid external active restores fallback, valid input revalidates existing structure without events. Pairs structurally static during an enhancement lifetime; correction/replacement after failed structural setup requires reinitialization, no refresh API/observer. Current owned interaction/active input re-evaluates eligibility; do not invent an external CSS observer. Selected-disabled content stays readable, with an eligible alternative keyboard entry; all ineligible means no interactive tab stop.
2. Native event/lifetime: root-bubble list-owned defaults run only after trigger/list consumer prevention and only for owned eligible native button targets; disabled/hidden/inert/inherited-disabled/CSS-hidden excluded, nested roots isolated, current DOM order/effective RTL wrap/Home/End, native Enter/Space click. Recognized navigation prevents native default after consumer guards and before custom veto. For accepted native activation dispatch exactly one synchronous bubbling/composed/cancelable lyra:tabs-before-change with {value,previousValue}; veto means no Lyra focus/active/result. Then applicable navigation focus, active assignment, synchronous bubbling/composed NONcancelable lyra:tabs-change with same payload. Observer sees committed active; reactive DOM uses normal Alpine flush, not falsely already-flushed. External input changes produce no user events. No queued result events or callback exception swallowing. Destroy retires owned pending init work/listeners, restores unenhanced attributes/readable panels/native fallback without stale re-hiding; if a live root's focused enhanced button will be hidden, focus its matching native fallback link, else first eligible fallback link. Preserve live panel/outside focus, do not focus detached nodes or mutate a retired owner after callbacks destroy/remove it. Fresh reinit has its own lifetime.
3. Scope/verification/migration: meaningful source regressions cover ready/failure/event/native prevention/nesting/RTL/disabled/modelable/destroy including init-then-immediate-destroy; preserve old meaningful tests with fixture migration, no deleted coverage. Export detail at package root; README bindings row adds fallback but controllable state stays active; explain ready read-only/output plus required native fallback briefly (not full new docs). Add Alpine minor changeset for the unsafe markup migration with project pre1SemVer exception, earliest0.6.0/stable1.0.0 correction and Styles earliest0.5.1-or-later-containing-hidden-fix floor. Manual producer/template migration required; no publication, version command or claim old Blade now works. Controller build/types/all3source/native/current examples/catalog/typescan/format/negative proofs/review. Existing21.2kB whole-Alpine cap stays unchanged; baseline21949B already exceeds it, final size must be reported honestly under Task10. No dependency/schema/CI/baseline/Blade/React/CSS changes.
## Boundaries
No new library/foundation/public option/refresh/uncontrolled/legacy-compatibility mode, namespace/class/export-map change, generic manager/observer/logger, deep consumer content inspection or runtime CSS. No documentation files outside README/changeset in this runtime cycle. No modification of imported Blade API/fixtures/lockfiles/package manifests/generators/type scanners/budgets or unrelated components. No Colima/Docker/memory/CPU/VM/service/remote/release/commit/worktree action. Do not run or edit controller raw fixtures/logs. If the exact selected contract cannot be implemented within this scope, report a concrete discrepancy; do not silently weaken it.
## Scope
- packages/alpine/src/tabs.ts
- packages/alpine/src/tabs.browser.test.ts
- packages/alpine/src/index.ts (new detail type export only)
- packages/alpine/README.md (Tabs bindings row and short output/fallback/compatibility note only; ordinary formatter table alignment allowed)
- .changeset/alpine-tabs-fallback.md
Controller-only generated outputs if the existing owning generator legitimately changes them:
- tools/docgen/output/alpine-props.json
- tools/docgen/output/alpine-llms.txt
Do not change anything outside this list; if the task requires it, stop and report.
## Expected evidence
Changed paths, criterion coverage, new/regression test names and any actual allowed commands with output; explicitly list all unrun validation. Controller will rerun exact source/compiled/consumer/fallback and negative checks, not trust an executor success claim. Preserve source behavioral coverage and failure signals.
For each criterion n, print an isolated `BATUTA-PROGRESS <n> START` before implementing it and `BATUTA-PROGRESS <n> DONE` only if its proof actually passed; otherwise report verification pending. Plain text, no tool needed.
## Stop conditions
The code shape contradicts this brief; two occurrences of the same unexpected command failure; or required edits exceed scope/boundaries. Do not restart hooks/services or ask for another architecture interview. Report exact limitation and leave controller-owned decisions to Batuta.
