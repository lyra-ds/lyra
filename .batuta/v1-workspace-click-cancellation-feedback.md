# Click cancellation — single medium retry
Controller current Chromium/SSR19,WebKit18,Firefox18,types/lint/format/build/docgen PASS. Runtime reviewed as matching scope. Keep runtime and changeset bytes exactly unchanged. The two new workspace.focus()/create.focus() calls in the cancellation test bypass native access despite the brief. After the canceled Create pointer click, use real Shift+Tab to the current roving workspace and assert exact document.activeElement===workspace, then Enter; use real Tab to Create and assert exact activeElement===create, then Space. Do not add sleeps, weaken7-event count or expectations, change other tests/source/config. Controller owns validation. Scoped pinned Prettier on browser test only allowed. This is already delegated implementation; no further delegation.
