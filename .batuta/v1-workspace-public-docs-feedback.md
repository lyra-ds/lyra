# Workspace public docs — one low retry
Original .batuta/v1-workspace-public-docs-brief.md remains in force; only2MDX files. Current diff otherwise matches the requested semantics and bindings. Correct these concrete points:
1. The Alpine snippet uses lyraWorkspaceSwitcher() (defaultOpen=false) but serves aria-expanded=true and an initially visible listbox. Serve the closed default consistently before initialization: aria-expanded=false and initial display:none on the existing popover (the existing binding owns/removes display on open). No new state/binding/CSS and no defaultOpen demo workaround. Keep both snippets byte-identical.
2. Explicitly label the common creation/keyboard accessibility description as React, because Alpine's no-selection ArrowUp fallback differs and Alpine has no Create. In the Alpine paragraph, state all4opening keys prefer served selected when available, then exact first/last no-selection fallback. Clarify that the binding emits lyra:change {id} and does NOT rewrite served aria-selected or visible current-workspace content; application code owns those updates. The sample can remain selection-only with fixed initial served state and no app handler.
3. Do not attempt /tmp extraction or other outside-scope writes again. The initial external-directory operation was blocked, not user task cancellation; controller will compare/compile/execute the snippets from its own Batuta artifacts. Only read/edit the scoped MDX and run the authorized pinned formatter on them. No tests, package managers, config/hooks/ignores, git writes or further delegation.
Preserve all unrelated prose/frontmatter/Example IDs/stack blocks and current correct content. Newly updated prose English in both paths, per user rule. Report exact edits and actual commands, with native proof pending controller.
