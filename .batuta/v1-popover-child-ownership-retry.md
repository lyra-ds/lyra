# One high retry — Popover child ownership

Continue the original v1-popover-child-ownership-brief.md and exact revised design. Same Goal, Context, Conventions, three criteria, Boundaries, Scope and stop rules. This is the one permitted high retry; controller owns tests/build/review/commit. Do not delegate or edit managed documents. Initial high353.55s completed three product files; snapshot/logs retained under main .batuta/runs/v1-popover-child-layers/initial-source and executor.log. Source/static/native proof below is controller evidence, not an executor claim.

## Exact failures and corrections
1. Production popover.tsx capture cleanup: queueMicrotask deletes the WeakSet membership before the document native listener sees the event. The original contract's end-of-dispatch assumption was wrong and is now explicitly superseded. Real createPortal child action outside the Popover DOM, with no modal/inert cover: all3engines parentClose1/action0, expected parentClose0/action1; no page errors. Capture trace in every engine is capture:child-task, microtask:child-task, document:child-task. Remove the capture-time cleanup microtask. Keep instance WeakSet identity until document bubble checks/deletes it; stopped events only retain weak keys, so no timer/global registry/strong-memory leak. Add a real native userEvent click source regression with an unobscured ordinary React portal button. No modal helper/import/role scan needed.
2. Typecheck fails popover.browser.test.tsx:134 TS2315 KeyboardEvent is not generic. Import the React KeyboardEvent type explicitly rather than resolving DOM KeyboardEvent or weakening types.
3. Lint fails static root delegation at popover.tsx:241 and test ancestor:161. Keep the production wrapper noninteractive and preserve markup. Existing exact precedent: Dropdown.tsx:248–251 and WorkspaceSwitcher.tsx:186–187 document the consumer-first delegated wrapper and narrowly disable jsx-a11y/no-static-element-interactions at that JSX root. Use that justified pattern, not a fake interactive role or broad suppression; report the deliberate rule exception. The test ancestor can use role=presentation as existing workspace-switcher.browser.test.tsx:470 does.
4. Five WebKit source failures are explicit native pointer-focus fixture assumptions: existing outside-close and trigger-Escape, new programmatic-close, delayed-close, and stopped-inside/next-outside. Controller native WebKit proved plain button click→BODY, button tabIndex=0 click→that button, input click→that input. Establish explicit tabIndex=0 only for the tested clickable focus targets (or use an appropriate natural input); keep all exact focus/presence/callback assertions and original five test behaviors. Do not change browser preference/product focus to fake pointer focus. The delayed-focus test must also include a path that moves focus with focus() then accepts by controlled rerender, without an outside mousedown that clears the intent and masks the newer-focus guard. Programmatic-close proof should isolate a controlled prop change rather than relying on a document outside click to close first.

## Complete the binding coverage
- Existing accepted-successor test claims once but only checks activeElement; use a call-through focus spy (never replace its implementation) or equivalent actual proof for exactly one attempt. Add zero-attempt proof when focus already belongs to the trigger.
- In nested ignored case, explicitly assert focus remains on the child action.
- Add current-trigger replacement, disconnected/ineligible trigger (disabled/hidden/inert), fresh reopen and unmount/stale ownership proof in this same owner suite. Keep data/state/callback assertions exact; no test-only runtime branches or arbitrary sleeps.
- Reuse the actual portal button test to prove delivery of its click action, not just surviving markup. Its pointer coordinates must be visibly outside the parent popup so CSS occlusion cannot be mistaken for event loss.

## Controller evidence and retained boundaries
Initial Chromium32/32 (Popover+four direct consumers); WebKit27/32 with only the5focus-fixture failures; focused FirefoxPopover16/16 and SSR1/1 PASS. Types and lint fail as above; build PASS. All153public-declaration/Alpine hashes and four modal ESM files unchanged. Original24compiled scenarios now all pass. Owning-document fixture3/3 passes after giving its clicked button the explicit native tab stop; the original artifact failed all3 waiting for owning-document outside dismissal. Mixed family proof is separately controller-owned. No full V1/size/placement qualification. No tests/build/package manager/git writes from worker; scoped formatter/diff only. Report actual commands and unverified claims.
