# Existing tooling/docs dependency maintenance — high/maintenance — Codex gpt-5.6-terra
You are the implementation worker already delegated by Batuta. Edit directly in the existing stabilization worktree. Do not delegate, orchestrate, create worktrees, invoke package managers, install, or commit. Read only this brief and listed manifests. Controller owns lock generation, installation and all build/runtime verification; executor network/sandbox is not the verification environment.
## Goal
Apply the selected11 existing tooling/docs dependency versions from Dependabot PR221, preserving the published library dependency baseline and current native evidence/compiler pins.
## Context
PR221 head ef0dee1b01deee506f318c59ccd610b05765dff1 contains17 updates. The controller reviewed the PR and CI logs, repaired the obsolete experiment freeze, and selected this smaller batch. The clean pre-update React Chromium/SSR baseline is765/765 after the separately verified WorkspaceSwitcher contrast correction. Only existing dependency entries may change. Exact replacements wherever these names already occur within Scope:
@types/react-dom19.2.4→19.2.7; eslint10.8.1→10.10.0; publint0.3.23→0.3.24; stylelint17.14.1→17.15.0; typescript-eslint8.66.0→8.69.0; vitest-browser-react2.2.0→2.3.0; wrangler4.120.0→4.129.0; fumadocs-core16.14.2→16.15.7; fumadocs-mdx15.2.2→15.4.0; next16.3.0→16.3.4; next-intl4.13.5→4.14.2.
Manifests: root, apps/docs, apps/site, tools/file-upload-evidence. Root @types/react-dom and app/tool copies must agree. Do not add any dependency name or change section. Hold Playwright1.62.1, Vite8.2.1, tsdown0.22.14, lucide-react1.30.0, alpinejs/@alpinejs/csp3.15.12 and all Changesets packages. Do not alter public package manifests/versions. All planning docs belong in .batuta; controller manages them.
## Conventions

- Follow the existing code style of the files you touch — naming, formatting,
  import order.
- Change only what the brief asks. Every changed line must trace directly
  back to the brief.
- Clean up only your own mess: remove imports/variables/functions that YOUR
  change made unused. Leave pre-existing dead code alone — mention it in your
  output instead of deleting it.
- Keep functions small and names descriptive; prefer clarity over cleverness.
- Comments only for constraints the code cannot express — never to narrate what
  a line does.
- If the brief references tests, make them deterministic: no real network, no
  time-dependent assertions.

Never:

- Reformat code you were not asked to change.
- Add a dependency the brief does not explicitly allow; no lockfile changes
  except from an allowed dependency.
- Drive-by refactors or "improvements" outside the brief's scope.
- Touch CI config, license, or anything listed under the brief's Boundaries.
- Silence a signal instead of fixing its source (casts, empty catch blocks,
  sleeps, copy-paste to dodge the real fix) — the method line says how to
  mark an unavoidable workaround.


Project exact versions, Node24.18.0 and pnpm11.13.1. Preserve JSON layout/key order. No new tests for these reversible metadata changes; use existing compatibility gates. No new changeset for private docs/development-only dependency maintenance. No Superpowers workflow or docs.
Work test-first from acceptance criteria. Investigate failures instead of silencing signals. Do not weaken checks or fix unrelated product code to make a dependency update pass; controller will assess compatibility and retain prior versions when a migration is out of scope.
## Acceptance criteria
1. Exactly the selected11 existing dependency names change to their supplied exact versions consistently in the four manifests; no runtime package or held version changes. Proof: controller JSON before/after comparison and diff review.
2. Lockfile is generated by exact pinned pnpm11.13.1 and installs with --frozen-lockfile. No hand edits to lockfile. This is a controller-owned generated artifact step after your manifest delivery; report it pending rather than trying to install in executor sandbox.
3. Existing builds/types/lints/security/policy and affected native/runtime tests pass against the selected installed graph; no gate weakening or unmeasured bundle hash refresh. Controller proof after installation and independent GLM review. All remaining V1/packed Linux qualification stays open.
## Boundaries
No other manifest, public package source, tests, workspace config, security override, workflow, fixture pin or baseline change. No dependencies added. Do not invoke pnpm/npm/npx/corepack or networking tools. No services/Docker/Colima/remote writes/commits. No hook settings, ignore entries or suppression configuration; leave unrelated observations standing. Do not edit WORK/.batuta or generated lockfile yourself.
## Scope
package.json
apps/docs/package.json
apps/site/package.json
tools/file-upload-evidence/package.json
pnpm-lock.yaml — generated only by controller, not hand edited
Do not change anything outside this list; if the task requires it, stop and report.
## Expected evidence
Changed names/counts per manifest; real diff --check/JSON validation output. Lock/install/build/tests remain controller-owned pending; do not claim done. No need to read raw PR logs or history.
## Stop conditions
Source contradicts supplied versions; same unexpected command fails twice; wider scope required. Report instead of broadening a migration.
For each criterion n, print isolated BATUTA-PROGRESS n START before first edit and BATUTA-PROGRESS n DONE only when its proof actually passes locally.
