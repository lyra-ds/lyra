You are the implementation worker already delegated by Batuta. One verification retry for .batuta/v1-dropdown-keyboard-brief.md; same4-file product scope, no configs/limits/deps/services/commits.
Controller findings:
1. Chromium/SSR12/14, WebKit11/13: test:118 reads tabindex immediately after raw element.focus(), before React commits. Use the real runner's act/poll boundary while retaining exact [-1,0] assertion; do not change product timing to fit a premature assertion.
2. Test:184 userEvent.keyboard('å') produces no keydown. Controller pinned Playwright probe on both engines: keyboard.type('å') emits0 keydown events; keyboard.press('å') reports Unknown key. Prove Unicode event-key matching with a standard dispatched KeyboardEvent in the browser (only input delivery; real focus/selection remains asserted), and identify that delivery as synthetic. Trusted ASCII navigation/typeahead is independently covered by controller replay. Keep the Unicode assertion, no filtering or replacing it with ASCII. Fake clock is permitted only for timer boundaries.
3. Real public regression in notifyRootKeyDown/notifyRootClick: manually calling the root callback from item/trigger handlers changes its actual event.currentTarget from the root span to a button/trigger. Native controller replay retains root currentTarget assertions. Preserve real root onClick currentTarget for every click and root onKeyDown for non-trigger descendants, exactly once, before Lyra defaults. Do not patch/fabricate SyntheticEvent properties or use casts as a fix. Preserve Slot child cancellation and normal propagation. Existing two WeakSets and duplicate forwarding owners add complexity while failing this contract.
4. Actual rebuilt Dropdown is2.06kB Brotli,62B above the existing2kB limit. Do not raise the budget. Simplify state/event ownership while preserving behavior. In particular, the effect that writes activeCommandIndex from commandItemCount is derived-state duplication: the rendered rovingCommandIndex already clamps it; the brief's conventions prefer deriving state. Do not replace readable code with code golf. Nine OTHER baseline focus-size failures remain separate; only Dropdown must fit here.
5. Finish missing acceptance coverage: empty→commands→empty/reordered collections; exact Tab and ShiftTab adjacent target; no modifier/composition typeahead; canceled navigation/Tab/Enter and pointer/keyboard-generated click; root currentTarget at callback time; multi-character match that actually changes focus (current a,a,p sequence stays Apply even if p matching is broken); actual rendered rich label and decorative icon exclusion. SSR has only one command, so its tabindex0 check does not prove roving across mixed items: cover2commands+separator with exactly one0 and one-1, deterministic across render. Unmount with pending timer must not do stale work. Keep tests bounded and behavioral.
Controller full build/types/lint/format/docgen passed.18 native scenarios use built public entry; behavioral paths run but root-currentTarget assertion exposes the callback regression. Current initial evidence: main .batuta/runs/v1-dropdown-keyboard-checks; native harness and initial result copied under worktree .batuta/runs/v1-dropdown-keyboard/native-initial.json. You need only these exact findings; no raw-log exploration or controller-harness edits. Controller reruns browsers/build/native/size. End with honest verified/pending results. No further delegation, pnpm/npm/npx/corepack, hooks/ignores/settings or commits.