# Tabs real content ownership — current baseline, 2026-09-10

Task27 diagnosis only; no implementation/API choice made. Current committed6f450d2 retains original Tabs source. Existing public TabsLine example renders Project summary after a Tabs control whose selected Overview tab names an empty focusable role=tabpanel. Its Activity/Settings panels are also empty. Controller ran the actual imported example with current compiled public React/styles in Chromium, WebKit and Firefox: all3 fail the required owned-content assertion, with zero page errors. The application text is visible outside all tab panels. Raw source/artifact hashes, fixture, driver and exact errors: MAIN .batuta/runs/v1-tabs-owned-content-native/{baseline-result.json,baseline.log}. Each owned browser/server closed normally. This is a reproduced semantic gap, not an allegation from source alone.

The established P1 interaction contract requires real compound trigger/content ownership. Architecture04 migration examples explicitly leave final signatures/markup to a selection-family design and require native server fallback before successful Alpine enhancement. Existing family design was not authored; no final compound API has been chosen. The current public surface is Tabs/TabsProps/TabItem with controlled items/active/onChange and horizontal automatic activation. Current3 consumers are two docs examples and the site component showcase. Docgen reads built declarations; controller regeneration owns outputs. The no-new-foundation/dependency and no-Colima rules remain absolute.

Next: a bounded Batuta selection-family design choosing exact React parts/ref/value/native-prop/state/event signatures, current-class/ARIA/value ownership, dynamic focus and activation behavior, supported Alpine/server fallback, compatibility/removal and concrete migration before implementation. Preserve existing automatic horizontal keyboard behavior and native button semantics. Do not invent grids, a generic collection framework, extra visual variants or retain empty fake panels as compatibility. Task37 documentation remains the current active implementation cycle; this diagnosis is independent preparation only.

Fresh hidden baseline: current Alpine dist SHA8378a723069d2b94bdf3cafe7a7132719f3ef4d83908bfc8727454a1c8eeb6da and existing navigation CSS leave hidden flex content visible in Chromium/WebKit/Firefox: panel21px, tab39px, zero page errors. MAIN .batuta/runs/v1-tabs-hidden-native/baseline-result.json binds the actual measurements. Selected candidate adds one existing-family scoped display:none!important rule; no global reset, new class or parity-baseline update. Task37 is now committed8987a05; Task27 design is active.
