---
'@lyra-ds/alpine': minor
---

`lyraDropdown` — APG menu button porting the React Dropdown state machine: named `trigger`/`menu`/`item` bindings, `defaultOpen`/`align` seeding, `open` controllable via `x-modelable`, roving focus, flip placement (`lyra-menu--up`), document-level outside-click, and `type="button"` protection inside forms. Shared internals `flip-placement` and `test-axe` land for the next wave-1 components.
