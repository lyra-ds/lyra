---
'@lyra-ds/react': minor
---

PageHeader can now use an `h2` or `h3` title for a titled section, and Shell can render its main
content as a `div` when it is nested or embedded in a page that already provides the main landmark.
