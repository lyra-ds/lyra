---
'@lyra-ds/alpine': minor
---

Add `lyraTimeInput`, an Alpine.js port of the React time-input state machine with served-markup bindings, controllable `selected` state, tolerant time parsing, clamped stepping, ARIA spinbutton support, and interaction-only change events.
