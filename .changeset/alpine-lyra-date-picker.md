---
'@lyra-ds/alpine': minor
---

Add `lyraDatePicker`, an Alpine.js thin coordinator that composes the shipped Calendar with Popover on desktop and BottomSheet on mobile, supports controllable date and open state, normalizes serialized date models, and formats the consumer-served trigger.
