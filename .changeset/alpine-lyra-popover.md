---
'@lyra-ds/alpine': minor
---

`lyraPopover` — React Popover state-machine port with named `trigger`/`panel` bindings, modelable `open` state, generated dialog ARIA wiring, keyboard and outside-click dismissal, and trigger-anchored flip placement.
