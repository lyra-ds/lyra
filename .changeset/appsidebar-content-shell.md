---
'@lyra-ds/styles': patch
---

Let a content-scroll `Shell` rail follow an `AppSidebar`'s own width (expanded and icon rail) instead of clipping it to the page-model track, and let a `WorkspaceSwitcher` placed in the sidebar's brand slot fill that column instead of overflowing it.
