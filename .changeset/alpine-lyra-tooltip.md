---
'@lyra-ds/alpine': minor
---

`lyraTooltip` — React Tooltip state-machine port with named `root`/`target`/`bubble` bindings, always-closed non-modelable state, generated tooltip ARIA wiring, hover/focus and document Escape dismissal, and visual-viewport-aware pseudo-element placement flipping.
