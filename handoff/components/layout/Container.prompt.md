# Container

Centraliza conteúdo em `--container-max` com `--content-gutter` lateral. Existe para que páginas não hardcodem `max-width: 1200px` — o token muda por brand e todos os layouts acompanham.
