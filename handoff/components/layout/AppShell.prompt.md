# AppShell

O frame que AppSidebar sempre pediu: sidebar `flex: none`, main `min-width: 0` (evita overflow de tabelas), topbar de 56px com borda e só o conteúdo rola — sidebar e topbar ficam fixos. Slots em vez de composição livre para que todo app Lyra tenha a mesma anatomia.
