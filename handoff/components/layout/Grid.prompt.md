# Grid

Duas modalidades e nada mais: `columns` (número ou template) para grids deliberados, `minItem` para o padrão responsivo `auto-fit/minmax` que ninguém lembra de cor. `minmax(min(Npx, 100%), 1fr)` evita overflow em containers mais estreitos que o item mínimo.
