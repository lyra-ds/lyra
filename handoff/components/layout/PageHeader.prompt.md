# PageHeader

Padroniza o topo de toda página de app: eyebrow accent em caps, h1 com `--h1-font`, descrição muted limitada a 640px e ações à direita com wrap. `children` recebe Tabs/filtros para que a linha secundária fique dentro do mesmo ritmo vertical.
