# RadioGroup

Radio já existia; faltava o grupo: name automático (useId), valor único controlado ou não, label/hint/erro no padrão dos campos e opções com descrição secundária. `direction="row"` para escolhas curtas.
