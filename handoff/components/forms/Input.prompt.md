Campo de texto padrão; aceita label, hint, erro e ícone à esquerda sem markup extra.

```jsx
<Input label="E-mail" placeholder="voce@exemplo.dev" hint="Nunca compartilharemos." />
<Input label="Busca" iconLeft={<Icon name="search" size={16} />} placeholder="Buscar…" />
<Input label="Senha" type="password" error="Mínimo de 8 caracteres." />
```
