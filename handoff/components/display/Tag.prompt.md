Tag neutra com borda para rótulos, categorias e filtros aplicados (Badge é para status; Tag é para coisas).

```jsx
<Tag>design-tokens</Tag>
<Tag onRemove={() => removeFilter("react")}>react</Tag>
```
