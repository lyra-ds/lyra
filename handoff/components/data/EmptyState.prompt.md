Estado vazio que orienta o próximo passo — nunca deixe uma lista vazia sem ação.

```jsx
<EmptyState
  icon={<Icon name="folder-plus" size={24} />}
  title="Nenhum projeto ainda"
  description="Crie seu primeiro projeto para começar a usar o Lyra."
  action={<Button iconLeft={<Icon name="plus" size={16} />}>Criar projeto</Button>}
/>
```
