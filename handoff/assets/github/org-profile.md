# Lyra — GitHub org profile

## Avatar
- `org-avatar-1024.png` — upload this one (GitHub recommends ≥ 500px square)
- `org-avatar-512.png` — fallback / smaller use
- `org-avatar.svg` — vector master

## Org name
Lyra Design System (handle suggestion: `lyra-ds`)

## Short description (160-char bio field)
> Open source, CSS-first design system. Semantic tokens, white-label theming, thin React wrappers — Vue, Svelte & Web Components on the same CSS core.

## Alternative (shorter)
> CSS-first, white-label design system. One CSS core, thin adapters for React, Vue, Svelte and Web Components.

## Org profile README (profile/README.md)

```md
<p align="center"><img src="https://raw.githubusercontent.com/lyra-ds/.github/main/assets/lyra-mark.svg" width="72" alt="Lyra"></p>
<h1 align="center">Lyra Design System</h1>
<p align="center">Open source, CSS-first, white-label design system for SaaS products.</p>

**Lyra** keeps every visual decision in CSS custom properties and `.lyra-*` classes — framework adapters are thin wrappers on top of the same core. Rebrand an entire app by setting four tokens.

- 🎨 **209 semantic tokens** — light/dark built in, no manual dark styles
- 🏷️ **White-label** — brands define `--brand`, `--brand-contrast`, `--brand-radius`, `--brand-font`; everything else is derived via `color-mix`
- ⚛️ **`@lyra-ds/react`** — 40 components as thin wrappers; Vue, Svelte & Web Components adapters on the roadmap (Zag.js)
- 🤝 **shadcn interop** — opt-in compat layer maps `--background`, `--primary`, `--ring`… to Lyra tokens
- 🤖 **LLM-first** — `llms.txt` generated from the real `.d.ts` sources, so agents write correct Lyra code

| Package | Description |
|---|---|
| `@lyra-ds/styles` | Tokens + component CSS, zero JS |
| `@lyra-ds/react` | React 18+ components |

Docs & registry: coming soon.
```

Note: emoji bullets above are optional — remove if you prefer the stricter Lyra voice.
