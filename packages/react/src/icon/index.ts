// SCAFFOLD (Phase 3, plan 03-01): buildable placeholder for the `icon` tsup entry.
// Replaced by the Icon pilot plan (wave 3). Kept as a valid empty module so the 5-entry
// tsup build stays green from wave 1 (no intentionally-red CI window). Named exports only (D-12).
export {};
