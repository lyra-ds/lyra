# Read-only design handoff prototype bundle — never formatted
handoff/
# GSD-generated planning + agent docs — formatting them would break required CI checks on planning PRs
.planning/
.claude/
# Superpowers task briefs and reports are operational agent artifacts, not source files.
.superpowers/sdd/
# Handoff-verbatim CSS — stylelint owns CSS formatting, not prettier (locked decision: byte-faithful fidelity)
packages/styles/**/*.css
# Parser edge-case fixtures — intentional CSS formatting must be preserved for the parity self-check
tools/parity/fixtures/
# Extraction-rule fixtures — intentional formatting preserved for the icon-registry --self-check
tools/icon-registry/fixtures/
# GENERATED registry — its byte-exact format is owned by `generate.mjs --check`; prettier --write
# would strip identifier-safe key quotes and permanently break the drift guard (D-02)
packages/react/src/icon/icon-registry.ts
# Generated llms reference — its byte-exact format is owned by docgen --check.
tools/docgen/output/llms.txt
# Blade API snapshot — copied byte-for-byte from the lyra-ds/blade release asset;
# reformatting would break parity with the published artifact (blade-api check owns it).
tools/blade-api/api.json
# Batuta task worktrees — a second checkout of this repo under .batuta/worktrees/, so every file
# in it is a duplicate that prettier would scan (and report, including the handoff and fixtures this
# file deliberately excludes above — the exclusions are relative to the repo root, not to a nested
# copy). Git ignores the directory via .git/info/exclude, which prettier does not read, so it needs
# its own entry here. Never committed, so CI never sees it; without this line `pnpm run lint` fails
# locally for anyone with a worktree open.
.batuta/worktrees/

# Generated / non-source files
# Modal evaluation manifest — copied byte-for-byte from the immutable evaluated input.
tools/overlay-foundation-evaluation/candidates.json
pnpm-lock.yaml
CHANGELOG.md
.changeset/*.md
node_modules/
dist/
apps/docs/.next/
apps/docs/.source/
apps/docs/out/
apps/docs/public/llms.txt

# Next.js generated ambient types
apps/*/next-env.d.ts
