# Read-only design handoff prototype bundle — never formatted
handoff/
# GSD-generated planning + agent docs — formatting them would break required CI checks on planning PRs
.planning/
.claude/
# Handoff-verbatim CSS — stylelint owns CSS formatting, not prettier (locked decision: byte-faithful fidelity)
packages/styles/**/*.css
# Parser edge-case fixtures — intentional CSS formatting must be preserved for the parity self-check
tools/parity/fixtures/
# Extraction-rule fixtures — intentional formatting preserved for the icon-registry --self-check
tools/icon-registry/fixtures/
# GENERATED registry — its byte-exact format is owned by `generate.mjs --check`; prettier --write
# would strip identifier-safe key quotes and permanently break the drift guard (D-02)
packages/react/src/icon/icon-registry.ts
# Generated / non-source files
pnpm-lock.yaml
CHANGELOG.md
.changeset/*.md
node_modules/
dist/
